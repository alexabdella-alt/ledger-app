# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev       # Start Vite dev server (http://localhost:5173)
npm run build     # Production build
npm run preview   # Preview production build locally
npm test          # Run the Vitest suite (tests/ directory)
npm run test:watch
```

No linter is configured. Always run `npm run build` before committing — it's the de-facto type/syntax check.

---

## 1. Project overview & tech stack

**Shadow CFO** is a single-page AI accounting/bookkeeping app for small-business owners: drop in a document (invoice, receipt, bank statement, contract) and the AI extracts it, codes it to the right GL account, books a balanced double-entry journal entry, and answers CFO-level questions in plain English.

- **Frontend:** React 18 + Vite. No routing library — views are switched via a `view` state string in the `ERP` component.
- **Backend:** Supabase (Postgres + Auth + Storage + Edge Functions). Postgres Row Level Security is the tenant-isolation boundary.
- **AI:** Anthropic Claude (Haiku classifier + Sonnet brain), proxied through a Supabase Edge Function so the API key never reaches the client.
- **Charts/tests:** dependency-free inline SVG (no recharts); Vitest for unit tests.
- **Styling:** all inline JS style objects. Dark auth screens; light app shell. Inter font, DM Mono for numbers. Accents: indigo `#4F46E5`, green `#039855`, amber `#DC6803`, red `#D92D20`.

The entire authenticated app lives in **`src/App.jsx`** (~4000+ lines). Views were progressively extracted into `src/components/views/*` and shared logic into `src/lib/*`, but `ERP` in `App.jsx` still holds nearly all state and is the source of truth via `ERPContext`/`useERP()` (~300 keys).

### Component hierarchy
```
AppWrapper          — auth state machine (session → companies → view routing)
  AuthScreen        — email/password login, signup, password reset
  CompanySetup      — first-run company creation (seeds COA, bank account, subscription)
  ERP               — the entire application once authenticated (holds all state, provides ERPContext)
    CompanySwitcher, DashboardView, BooksView, ReportsView, TaxView, ...
    ClarificationFlow, TransactionDetailPanel, ChatRichOutput, DocumentPreviewModal
```

---

## 2. Database schema (Supabase Postgres)

All business tables carry a `company_id uuid` and are tenant-isolated by RLS (§3). Key tables:

| Table | Purpose / key columns |
|---|---|
| `companies` | Tenant root. `id`, `name`, fiscal/tax metadata. Keyed by `id` (not `company_id`). |
| `company_users` | Membership/roles. `company_id`, `user_id`, `role`, `accepted_at`. The RLS source of truth. |
| `accounts` | Chart of accounts. `company_id`, `code`, `name`, `category`, `account_type`, `system_role`, `active`, `is_system`. Unique `(company_id, code)`. |
| `journal_entries` | Double-entry header. `company_id`, `entry_date`, `description`, `source` (CHECK-constrained), `status` (`posted`/`voided`), `deleted_at`, `deleted_by`, `posted_at`, `created_by`. |
| `journal_entry_lines` | Entry lines. `journal_entry_id`, `account_id`, `debit`, `credit`, `company_id`. Debits must equal credits. |
| `bank_accounts` | Bank/credit accounts. `company_id`, `name`, `type`, `gl_code`, `last4`, `institution`. |
| `contacts` | Vendors & customers. `company_id`, `name` (unique per company), `type`, `gl_code`, 1099 fields, `payment_terms`, `tags`. |
| `vendor_rules` | Auto-coding rules. vendor → `gl_code`/`gl_name`/`project`. |
| `contracts` | Contracts/leases (ASC 842). counterparty, terms, generated entries. |
| `recurring_transactions` | Recurring expenses. vendor, amount, `frequency`, `next_date`, `gl_code`, `active`. |
| `ar_invoices` / `ar_invoice_lines` | Outgoing (sent) customer invoices. |
| `subscriptions` | App subscription/plan per company. |
| `audit_log` | Immutable activity log. `action`, `detail`, `before`/`after` jsonb, `performed_by`. Written by `logAudit`/`logAI`. |
| `documents` | Uploaded files. `name`, `mime_type`, `document_type`, `storage_path` (Storage bucket `documents`), `file_size_bytes`, `linked_invoice_id`, `tags`. |
| `reconciliations` | Bank-match sessions. The app reads/writes a **denormalized** record (`account_id`, `statement_balance`, `books_balance`, `difference`, jsonb `matched_transactions`/`unmatched_*`); the normalized `005` columns (`bank_account_id`, `statement_date`, `statement_ending_balance`) and the `reconciliation_items` table are **intentionally unused dead schema** — see §11. |
| `tax_settings` | Per `(company_id, tax_year)`: estimated payments, filed deadlines, work-from-home flag. |
| `chat_messages` | Persistent AI chat. `role`, `content`, `actions_taken` jsonb (string array OR `{actions, rich}` for inline charts). |
| `upload_log` | One row per uploaded file; processing status + result. |
| `rate_limit` | Per-user hourly counters (`user_id`, `bucket`, `hour_bucket`, `count`). RLS-on, no policies — service role only. |
| `client_ai_profile` | Per-company learned AI profile. `business_type`, `common_vendors`, `spending_patterns`, `custom_rules`, `ai_notes`. One row per company. |
| `unknown_documents` | Uploaded docs the AI couldn't classify. |

### Key RPCs (SECURITY DEFINER)
- `is_company_member(cid)` / `is_company_admin(cid)` — membership checks used by every policy.
- `create_company(...)` — atomic company + owner-membership creation (direct INSERT on `companies` is blocked).
- `seed_company_accounts(company_id)` — seeds the default COA (membership-checked).
- `post_journal_entry(...)` — atomic, balanced journal-entry write (the single DB write path for entries).
- `bump_rate_limit(user, bucket)` — atomic increment-then-check counter (service role only).
- `security_check(...)` — data-isolation self-test used by the Security dashboard.

---

## 3. Multi-tenancy & RLS

Isolation is enforced **at the database**, not the client. Every tenant table has four policies (`select`/`insert`/`update`/`delete`) of the form `using (public.is_company_member(company_id))`. `is_company_member` is `SECURITY DEFINER` so it can read `company_users` with RLS bypassed (avoids recursion and is fast). Migration `001` applies these in bulk to the core tables; later migrations add the same shape per new table.

- The **anon key is public by design** — RLS is the real boundary. Never rely on a client-side `.eq("company_id", …)` filter for security; it's for correctness/perf only.
- **Platform-admin bypass (Option A):** `is_company_member` returns true for platform admins (`PLATFORM_ADMIN_EMAILS`) so Support Mode works through the normal client. Support-mode actions are attributed to `Platform Admin - <email>` in the audit log.
- **UI-layer complement:** switching companies calls `resetCompanyState()` so no previous company's data bleeds into the next while the new data loads.

> When adding a company-scoped table, you MUST add a migration enabling RLS + the four `is_company_member(company_id)` policies. (This audit found `accounts` was missing it — fixed in `023`.)

---

## 4. Chart of accounts & the `system_role` scheme

Account **numbering convention** (helpers in `src/lib/gl.js`):
- `1xxx` Assets · `2xxx` Liabilities · `3xxx` Equity · `4xxx` Revenue · `5xxx`/`6xxx`/`7xxx`/`8xxx` Expenses.
- `glIsRevenue`, `glIsExpense`, `glIsBalSheet`, `glPLType` classify by the first digit.

**Authoritative expense accounts** (default codes — users may rename/renumber):
`6000` Salaries & Wages · `6100` Rent & Occupancy · `6200` Utilities · `6300` Marketing & Advertising · `6400` Travel & Entertainment (meals 50%) · `6500` Technology & Software · `6600` Office Supplies · `6700` Insurance · `6800` Professional Services · `6900` Depreciation & Amortization · `7100` Miscellaneous · `8000` Interest Expense.

**Why roles, not hardcoded codes:** users can renumber/rename accounts, so code never hardcodes `"6100"`. Each default account has a stable `system_role` (e.g. `rent_occupancy`, `travel_entertainment`). Resolve via `getAccountByRole(role)?.code` (or `rc(role)`/`rn(role)` inside `ERP`). The deduction tracker, recurring detection, and AI all go through roles so totals follow the company's actual accounts. The role↔code map lives in `DEFAULT_CHART_OF_ACCOUNTS` (`src/lib/constants.js`); `useAccounts` builds the live `byRole` index.

---

## 5. AI architecture

Two-stage pipeline, both calls through the **`ai-proxy`** Edge Function (`supabase/functions/ai-proxy/index.ts`):

1. **Intent classifier** (`classifyIntent`, `src/lib/ai.js`) — Haiku (`AI_MODEL_FAST`), ~150 tokens. Classifies the message (`ledger`/`contacts`/`rules`/`general`) to decide how much context to load.
2. **CFO brain** (`runAIBrain`) — Sonnet (`AI_MODEL`), up to 4000 tokens. Receives a system prompt with: today's date + current year, the COA, vendor rules, contacts, up to 80 ledger rows, a **live financial snapshot** (`buildFinancials`), the **learned client profile** (`client_ai_profile`), and a **sandbox statement**. Returns `{ reply, actions[] }`.

**ai-proxy** authenticates the caller's Supabase JWT (service-role client), enforces rate limits (**60 AI/hr**, **20 uploads/hr** per user via `bump_rate_limit`; `x-rate-kind: upload` header tags uploads), then forwards the unchanged Anthropic Messages payload to `https://api.anthropic.com/v1/messages` with the server-only `ANTHROPIC_API_KEY`. The client never holds an Anthropic key.

**Action sandbox** (`src/lib/aiCapabilities.js`): the AI may only emit whitelisted action types (`recode`, `void_invoice`, `delete_invoice`, `reverse_entry`, `add_rule`, `add_account`, `add_recurring`, `add_contact`, `navigate`, `render_chart`, `export_csv`, `render_summary`, …). The action loop refuses + logs (`ai_action_refused`) anything else, caps bulk deletes at 3, and every data-changing action is audit-logged.

**Reply parsing** (`runAIBrain` tail): the reply is scrubbed brace-balanced (`scrubLeakedActionJson`) so action JSON never leaks into the chat bubble; actions are recovered (`extractActionObjects`) even from malformed JSON. Inline charts/CSV/summaries render via `ChatRichOutput` and persist in `chat_messages.actions_taken`.

---

## 6. Migration conventions

> **★★ `074` + `075` APPLIED & VERIFIED, `ai-proxy` v16 → v17 ACTIVE (2026-08-26/27). O113a IS LIVE.** Ledger now: through `061`, plus `063`, `064`, `066`, `068`, `069`, `070`, `071`, `072`, `073`, **`074`, `075`**. Applied in the required order — **migration before deploy**, because the reverse calls a function that does not exist and fails every AI request.
>   · **`074` — VERIFIED BEHAVIOURALLY, WITH THE NUMBER THAT DECIDES IT.** Block (b) `4_after_refused = 10` — *"the fix: refused and NOT charged"*. **Under `021` that reads 11, and the difference IS the fix.** Block (c) `2_ai_after = 0` — *"ai NOT charged for a call upload refused"*, closing the second instance of the same bug (a call refused by `upload` used to leave an `ai` charge behind). **The limiter was watched to refuse WITHOUT charging**, which is the standard `071` set and the only thing that separates this from a fix on paper.
>   · **`075` — APPLIED & VERIFIED 2026-08-27. `anon_exec=false · authd_exec=false · svc_exec=true` → PASS.** Proved with **`has_function_privilege`, which is Postgres's OWN resolver** — the code path the executor consults — rather than a regex over `proacl`, which silently misses a grant arriving through PUBLIC or role inheritance. **One remaining shade of doubt, recorded not glossed:** this is the executor's DECISION, not its REFUSAL — we still never watched an `anon` call raise, and `075` being applied means the failing state can no longer be observed. *(The earlier partial record, kept because the sequence is the useful part:)*
>   · **`075` — APPLIED, AND VERIFIED ONLY IN PART. SAY SO.** Catalog read confirms **`bump_rate_limit` acl went `{postgres,anon,authenticated,service_role}` → `{postgres=X/postgres,service_role=X/postgres}`** — `anon` and `authenticated` revoked, `service_role` preserved so the proxy and `074`'s rollback path still work. `consume_rate_limit` matches. **That is VERIFY (a) only. Blocks (b) and (c) WERE NOT RUN** — the operator's paste came through as an unfilled placeholder, and nothing is recorded from it. **So the ACL change is evidenced and the BEHAVIOURAL refusal is not:** we never watched `anon` actually be refused, and the chance to observe the hole in its failing state is now gone, because `075` is applied. *(Recorded as owed, not as done — a before/after on `proacl` is a strong catalog fact and it is still not the same claim as "the call raises for an unauthenticated caller".)*
>   · **`ai-proxy` v17** (`2026-08-27 03:08:05Z`, both assets). **This proves the bundle shipped and NOTHING about the new limiter path under load** — the first real drive is its test. The old `bump_rate_limit` is now called by nothing; it survives only as `074`'s rollback path.
>
> **★ MIGRATIONS APPLIED — the live state.** **Applied & verified live: through `061`, PLUS `063`** (`060` on 2026-08-08, `061` on 2026-08-09, **`063` on 2026-08-17**; all verified via catalog queries, not an HTTP 201). **Pending: `062`** (unknown_documents policy dedup — queued, unwritten).
>
> **★ MIGRATION PASS 2026-08-25 — `066`, `071`, `072`, `073` ALL APPLIED & VERIFIED.** Ledger now: **through `061`, plus `063`, `064`, `066`, `068`, `069`, `070`, `071`, `072`, `073`.** *(`062`, `065`, `067` unused — `062` still owed; `065` released, the vendor backfill cannot be SQL; `067` freed when the flag queue moved to `072`'s shape.)*
>   · **`066`** — 11 directory rows, **1 prefix / 10 exact**, RLS on, 4 policies correct (SELECT on `auth.uid() is not null`; INSERT/UPDATE/DELETE on `is_platform_admin()`). **VERIFY (b) came back a FINDING, not a pass → `O112`.**
>   · **`071`** — constraint present and **tighter than specced**: it validates months `01`–`12`, not merely two digits.
>   · **`072`** — table created, all 16 columns including both `descriptor_display` and `resolver_input`.
>   · **`073`** — origin now **external 36 · runtime 6 · seed 530**, exactly as predicted. What remains `runtime` is only genuine materialisation: `2101` × 3 (the payroll path) and Franklin Ave's `3400`/`6520`/`6530`. **`origin` is now precisely right, and the O108 detector means what it says again.**
>   · *A correction worth keeping: `073` was challenged as moving 22 rows. It moves **33** — it has TWO update clauses, and the reconstructed preview implemented only the ≥5 batch rule. The proposed generalisation (same-code-across-many-companies-same-day) would have been actively WRONG: that is precisely the `2101` signature, the one population established as genuinely runtime.*
>
> **`070` APPLIED 2026-08-25 — RECORDED AS *APPLIED AND UNDERSTOOD*, NOT AS CLEAN.** `accounts.origin` exists; the O108 detector (`system_role is null and origin <> 'external'`) returns **0 rows — restored**. Distribution: **external 36 (4 companies) · runtime 39 (11) · seed 497 (11)**. But `runtime 39` is **not** the "handful" the file predicted, and the diagnostic showed the `created_at` window mislabelled **35 of the 39**:
>   · **`7150` × 11 (2026-08-17)** — `063`'s OWN BACKFILL. Seed by definition, mislabelled by construction.
>   · **22 codes × 1 company (c787582f, 2026-06-07)** — a seed batch. **Mechanism corrected from the first reading:** these are NOT "the chart grew" — all 22 are in the `000`-era seed function already, so the chart had them. **That company was behind the seed and `seed_company_accounts` was re-run for it.** Same conclusion, different cause, and the cause is the useful part: re-running the seed is a thing that happens here.
>   · **`2101` × 3 (2026-06-21)** — **GENUINELY RUNTIME.** The payroll path needed Payroll Taxes Payable, the company lacked it, `ensureAccount` created it. One row per company, idiosyncratic: exactly what the label should mean.
>   · **`3400`/`6520`/`6530` × Franklin Ave (2026-07-22)** — the O108 materialisations. **Staying `runtime`, against the operator's leaning**, because `origin` records HOW A ROW GOT HERE and that stays true after `068` blessed them. Marking them `seed` would erase the only durable evidence in the database that the materialisation path ever fired on a real client's chart. "Runtime-created and now canonical" is exactly `origin='runtime' AND system_role IS NOT NULL`.
>   **CORRECTION OWED: migration `073`** relabels the 33 mislabelled rows by SHAPE — *seed operations are batches, materialisations are singletons* (≥5 accounts for one company on one day) — rather than by naming dates that would be wrong next time. Until it applies, `origin` is **directionally right and precisely wrong**, and any query over it must say so.
>
> **`064` APPLIED 2026-08-23.** Ledger now: applied & verified through `061`, plus `063`, `064`, `068`, `069`, `070`. *(`062` still owed. `065` RELEASED — see §11: the vendor backfill cannot be SQL. `066`/`067` still reserved for the directory and the flag queue; `070` written, ▶ HOLD.)*
>
> **`064` verification, 2026-08-23** — 15 columns ✓ · RLS on, 3 policies, no DELETE policy ✓ · CHECKs carry the Q1/Q3 rules (`tier` four-valued; `demotion_reason` limited to `mapping_correction`/`dormancy`, with amount behaviour correctly ABSENT) ✓ · 0 rows ✓. **Two operator questions answered: (a) the unique key is a unique INDEX (`vendor_state_company_entity_idx`), not a table constraint, which is why it did not appear in `pg_constraint` — confirm with `pg_indexes`; it still backs `ON CONFLICT (company_id, entity_key)`. (b) `first_seen`/`last_seen`/`demoted_at` are TEXT 'YYYY-MM' deliberately** — Postgres has no month type, `date` would force a spurious day, and `monthsBetween` is pure string/integer arithmetic with no `Date` and no timezone, which is exactly the O86 UTC-date-key class avoided by construction. **GAP FLAGGED: the format is guarded in JS (`isYm`) and NOT in the schema** — a `check (first_seen is null or first_seen ~ '^\d{4}-\d{2}$')` on the three columns would close it, and is proposed rather than slipped in.
>
> **`068` + `069` APPLIED 2026-08-17.** Ledger now: applied & verified through `061`, plus `063`, `068`, `069`.
>
> **`068` verification** — (a) roles set on all three codes ✓ · (c) all six seed checks true ✓ · (d) Franklin Ave lines untouched (`6520`=3, `6530`=2) ✓ · **(a) surprise: `3400` exists on TWO companies, not one** — a second company's on-demand OBE materialisation, now correctly roled by the backfill; benign, and the identity of that company is still owed. **(b) FAILED ITS EXPECTATION AND THAT IS THE VALUABLE PART: expected 0 role-less accounts, returned 36** — none on Franklin Ave, all on three other companies, in a foreign numbering scheme. **DESIGNATED `O110`.**
>
> **`069` verification — PARTIAL, and it is NOT closed.** (a) `anon` revoked, `authenticated` kept ✓ · (b) body intact ✓ · **(c) NOT RUN.** The behavioural check — create a company end to end and confirm it seeds — is the only one that can prove the revoke didn't break onboarding, and a catalog read cannot stand in for it. **`069` is applied but NOT fully verified; the open item is recorded on ROADMAP `O108`.** *(The file's expected count said 56, from before `068`; corrected to 59 — a stale expected value turns a check into a rubber stamp, and 56 would now read as a pass while meaning `068` did not take.)*
>
> **`063` APPLIED OUT OF ORDER, deliberately.** `062` is queued-but-unwritten and is not a prerequisite of `063`; the chain is monotonic, not contiguous (§6), and waiting on an unwritten file would have blocked calibration for no gain. **`062` remains owed.**
>
> **`063` verification, 2026-08-17** — (a) **11 companies / 11 with 7150 / 11 with the role** · (b) all five true: gained `uncategorized_expense`, kept `lease_liability_current`, `marketing_advertising`, `common_stock`, and no `account_type` · (c) **0 lines booked to 7150** (C200 wires no caller, as intended) · (d) ACL `{postgres=X,anon=X,authenticated=X,service_role=X}` — preserved by create-or-replace, **and see `O108` finding 2: `anon` holds EXECUTE, which the repo believes was revoked.**
>
> **RESERVED — `063`–`067` are the O88 CALIBRATION chain (2026-08-16), in this order:** `063` Uncategorized Expense (COA seed + backfill) · `064` `vendor_state` · `065` `vendor_state` historical backfill · `066` `universal_vendor_directory` · `067` `calibration_flags`. **Applied in commit order, not written all at once.** Do not mint `063+` for anything else without renumbering the chain. *(`062` stays reserved for the queued `unknown_documents` policy dedup.)*
>
> **RULE: every migration application gets its date recorded on this line, in the same docs commit that closes the session.** The repo records migration FILES; nothing in it records their APPLICATION, and there is no `supabase_migrations.schema_migrations` table to ask (see below — deliberately). So a file's presence proves only that it was written. Without this line every fresh session re-derives the answer, guesses wrong, and re-raises a migration as "still pending" that went live days ago. *(Cost already paid: `060` was flagged unapplied twice after it was live.)*

- Files live in `supabase/migrations/NNN_description.sql`, applied in numeric order. Wrap in `begin; … commit;`.
- **Idempotent**: `create table if not exists`, `drop policy if exists` before `create policy`, `add column if not exists`.
- Some numbers are intentionally absent from the repo (e.g. 016/017/020/024/025 were applied directly during development) — numbering is monotonic, not necessarily contiguous. New migrations take the next free number (the tree currently runs through `061` plus a dated `20260605_*` file, so the next free number is `062`). **Before choosing a number, `ls supabase/migrations/` and confirm it's unused** — a duplicate prefix silently breaks things (a duplicate `051` once caused a migration to be skipped; it was renumbered to `053`).
- Migrations are **not auto-run**; the user applies them in the Supabase SQL editor. Code that depends on a new table must degrade gracefully (try/catch, "table may not exist yet") so the app keeps working pre-migration.
- **NEVER run `supabase db push` (or `supabase migration up`) against this project.** There is **no `supabase_migrations.schema_migrations` tracking table** — migrations have always been applied manually (SQL editor / Management API), so the CLI has no record of what's live and would try to re-run the ENTIRE chain, including `000_baseline_schema.sql` (bare `CREATE TABLE`, unsafe on a live DB — see below). Migrations are managed manually by design; do not create a tracking table or introduce `db push`.
- **Apply AND verify in the SAME task, with the verification output in the report.** When you (or the user) apply a migration to production, immediately query the live DDL to confirm each object the migration creates/changes actually exists, and paste that query output into your report. The `053_signoff_reviewer_and_override.sql` report is the template: for each element (new columns, functions, policies, any data remap) run a read-only check (`information_schema.columns`, `pg_proc`, `pg_policies`, row counts) and show the result — never report a migration as "applied" on the strength of an `HTTP 201` alone. (Applying via the Supabase Management API SQL endpoint with the CLI's access token is the working mechanism; it runs raw SQL and does not touch any tracking table.)
- **★★ VERIFICATION SQL IS ONE STANDALONE STATEMENT PER CHECK, EACH RETURNING A SINGLE VERDICT ROW (operator's standing preference, 2026-08-27).** Paste-ready, no editing. **The Supabase SQL editor displays ONLY THE LAST STATEMENT'S RESULT**, so a multi-statement block hides every verdict but the final one — silently, and in the direction of looking fine.
  - **COST ALREADY PAID — THREE CHECKS LOST IN ONE CHAIN (`074`/`075`):** *(1)* the convention of writing VERIFY blocks as `--` comments means stripping the prefix un-comments the PROSE too, producing SQL that dies on line one; *(2)* last-statement-wins hid three of four SELECTs in `074` block (b), leaving only a raw count with no verdict; *(3)* it hid `075`'s `anon` privilege check **entirely — even after that block was restructured to return a result set** — because the file was pasted whole. **A check whose result the operator cannot see is a check that was not run.**
  - **THE RULES:** compute the verdict IN SQL (`case when … then 'PASS - …' else 'FAIL - …' end as verdict`) rather than making the reader interpret raw values · **never report a verdict via `RAISE NOTICE`/`RAISE WARNING`** — the editor does not reliably surface either · **if a check genuinely needs a transaction with a rollback, SAY SO and give it as ONE self-contained block**, never as loose statements, because the rollback is what makes it safe and a statement pasted alone commits.
  - **FOR PRIVILEGE CHECKS, USE `has_function_privilege(role, 'schema.fn(args)', 'EXECUTE')`, NOT A REGEX OVER `proacl`.** It is **Postgres's own resolver** — the code path the executor consults — so it accounts for PUBLIC grants and role inheritance, which a `proacl LIKE '%anon=%'` match silently misses. **An ACL can read clean and still grant EXECUTE through PUBLIC.** *(This is why `075`'s catalog check was weaker than it looked; the single-statement replacement is `supabase/verify/075_anon_denied.sql`.)*
  - **RUNNABLE COMPANIONS LIVE IN `supabase/verify/`, NEVER IN `supabase/migrations/`** — a file sharing a migration's numeric prefix is how a duplicate `051` once caused a migration to be SKIPPED. Nothing in `supabase/verify/` is ever applied as a migration.
  - **When handing SQL over via `pbcopy`, verify the clipboard matches the file** (`shasum`), so a truncated copy is caught before it is pasted into production.
- Show the SQL and get confirmation before writing code that depends on a new table.
- **★★ THE CHAIN DOES NOT REPRODUCE LIVE FUNCTIONS — ANY `create or replace` STARTS FROM `pg_get_functiondef` (recorded 2026-08-17, O108).** The repo holds **five** definitions of `seed_company_accounts` (`000`,`008`,`009`,`038`,`044`) and the highest-numbered, **`044`, was never applied and could not be** — it inserts an `account_type` column that does not exist on `public.accounts` (confirmed live: 0 rows in `information_schema.columns`). So the reproducibility claim below is **TRUE FOR TABLES, FALSE FOR FUNCTIONS**: replaying the chain would install a function body that has never been live.
  - **THE RULE: `create or replace` of a live function is a COPY operation, never a reconstruction.** Pull `select pg_get_functiondef(oid) from pg_proc where proname = '…'`, paste it verbatim, make the smallest possible edit, and **prove the diff mechanically** — write both versions to files and `diff -u` them, don't eyeball it. Cost already paid: `063` was first written from `009` with its middle reconstructed from `constants.js`; it would have **dropped 17 accounts and repointed 11 codes** (`2400` Lease Liability → Notes Payable, `6300` Marketing → Repairs, `3000` Common Stock → Owner Equity) plus role renames that break `getAccountByRole` for every new company. It was caught only because the operator asked for a diff against live instead of accepting the file.
  - **Corollary: the function's ACL is not in the repo either.** `pg_get_functiondef` does not emit grants, and `create or replace` PRESERVES them — so never re-assert a grant you have not read. `009`/`044` both revoke `anon`; live still carries `anon=X` (O108 finding 2, → `069`).
- **`000_baseline_schema.sql` is the schema baseline** (a schema-only `pg_dump` of live). The chain is now reproducible: empty DB → `000` → `001…036` recreates the live schema (see §11). `000` uses bare `CREATE TABLE` (no `IF NOT EXISTS`) — **never run it against the existing production database; it is for an EMPTY database only.** Migrations `001…036` are idempotent (create-or-replace functions; if-not-exists tables/columns/indexes; drop-if-exists+create policies/triggers; drop+add constraints), so they re-apply cleanly on top of `000`.

### The OPEN LEDGER is a living section, not a printout

> **★★ EVERY SESSION-CLOSING `docs·` COMMIT UPDATES `ROADMAP.md` → "OPEN LEDGER — every tracked, unshipped item" (recorded 2026-08-11).** Items added, items checked off, gates changed, counts refreshed. **The orphan sweep (§11) runs WITH it** — the sweep finds what has no home, the OPEN LEDGER *is* the home, and running either alone is how both drifted. **A closing commit that does NOT touch the OPEN LEDGER must say why in the commit message** ("no tracked item changed" is a valid reason; silence is not).
>
> **WHY THIS RULE EXISTS.** The census was *printed into a commit message* on 2026-08-09 (`3f49afd`) and was stale within two sessions — because a printout cannot be wrong, only old, and nothing about it invites correction. The 2026-08-11 sweep then found **five launch-blocking Tier 1 items marked open that had already shipped** (C194, C195, C197, and C198 in full), **a live security ladder with no roadmap rows at all** (six gates, parked inside an item marked DONE), and **duplicate `O90`/`O91` ids** created one session earlier. All three are one failure: state recorded somewhere nobody re-reads.
>
> **The OPEN LEDGER is written FOR THE OPERATOR** — plain language, no jargon, readable on a phone. If a line needs the codebase to parse it, it is written wrong. Detail stays in §11 and the tiers; the ledger is the index a human actually reads. **Completion is as visible as backlog** (the RECENTLY SHIPPED list) — a backlog that only grows reads as failure regardless of the work done.
>
> **Before minting an `O###` id, `grep -n "^| O" ROADMAP.md` and confirm it is unused across ALL of Section 2** — the tables are separate, the id space is not. Taking the next number off the end of one table is exactly how `O90`/`O91` collided.

### Production actions — who triggers them

> **★★ PRODUCTION ACTIONS RUN ONLY ON EXPLICIT OPERATOR AUTHORIZATION (recorded 2026-08-11).** Deploys, data writes, migration applications and pushes are the **operator's** to trigger, not the assistant's to infer. **A command pasted back into the conversation is a QUESTION, not a green light.** Cost already paid: the operator said *"print only — I'll run it"*, then pasted the deploy command back; it was read as authorization and the `ai-proxy` deploy ran. No harm — the deploy was wanted, and it is idempotent — but the authorization was **inferred**, and checking the live state first ("has this already run?") is evidence-gathering, not consent. **When authorization is ambiguous, ASK.** One turn of friction against an unrequested production change is not a close call. This sits beside the migration rules deliberately: same class, same discipline.

### Edge-function deploys (`ai-proxy`)

> **★ THE WORKING COMMAND — pass the project ref EXPLICITLY.**
> ```bash
> supabase functions deploy ai-proxy --project-ref hhhuvoycumjzcjbawwff
> ```
> **The bare `supabase functions deploy ai-proxy` FAILS** with `Cannot find project ref. Have you run supabase link?`. There is **no `supabase/config.toml`** in this repo, and `supabase/.temp/linked-project.json` — which *does* hold the ref, and which looks like proof of a link — is **not what the CLI keys on**. Recorded 2026-08-11 after that exact assumption cost a round-trip. *(A one-time `supabase link --project-ref hhhuvoycumjzcjbawwff` would write `config.toml` and make the bare form work — but `config.toml` is **not** gitignored, so it becomes a TRACKED file. **Decision DEFERRED**; until it's made, always pass the flag.)*

- **The function name as Supabase knows it is `ai-proxy`** — the directory name under `supabase/functions/`, matching `AI_PROXY_URL` (`src/lib/constants.js`): `https://hhhuvoycumjzcjbawwff.supabase.co/functions/v1/ai-proxy`.
- **`aiProfiles.js` is never deployed on its own.** `index.ts` is the entrypoint and imports it, so one command bundles the whole directory; the CLI output names both assets, and seeing both is part of the check.
- **NEVER pass `--no-verify-jwt`.** The client calls with `getAuthHeaders()` and the function does its own per-user rate limiting (migration `021`). JWT verification staying ON is what keeps the proxy closed.
- **Project secrets survive a deploy.** `ANTHROPIC_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` are project-level — never re-set them as part of one.
- `WARNING: Docker is not running` is **benign** for a remote deploy (Docker is only needed for local `supabase functions serve`).
- **VERIFY LIKE A MIGRATION — and know what the verification proves.** `supabase functions list --project-ref hhhuvoycumjzcjbawwff`, confirm `VERSION` incremented and note the UTC timestamp. That proves **the bundle shipped**. It does **not** prove a **prompt** change works: a profile edit has no client-visible surface, and the client-side fallbacks mean a model that ignores the new instruction looks *exactly* like the old behaviour. Prompt changes are only closed by a live drive (see ·3c (ii) in §11).

---

## 7. Key architectural decisions (and why)

- **Soft delete** (`deleted_at`/`deleted_by`) instead of hard delete — recoverable, preserves the audit trail. All read queries filter `deleted_at IS NULL`; client logic also excludes `status === "voided"`/`"deleted"`.
- **Atomic journal writes via RPC** (`post_journal_entry`) — guarantees balanced, all-or-nothing double-entry writes. `persistJournalEntry`/`bookToDb` is the single write path; `source` is normalized to the CHECK-allowed set there.
- **`system_role` indirection** — see §4. Survives user renumbering.
- **Server-side AI key + rate limiting** — the edge function is the only place the Anthropic key lives; limits prevent abuse/runaway cost.
- **Adaptive `client_ai_profile`** — the AI gets smarter per client (vendor→GL memory, spending patterns) without retraining.
- **State reset on company switch** — UI-layer isolation complementing RLS.
- **Inline SVG charts** — zero new dependencies, full control, no bundle bloat.

---

## 8. Patterns to follow

- **New tenant table** → migration with RLS + the four `is_company_member(company_id)` policies; map columns back to the app shape in `loadAllData`; add to `resetCompanyState`.
- **New booking path** → go through `bookToDb`/`persistJournalEntry` (never insert journal entries directly). It handles `source` normalization, doc relinking, profile learning, and recurring detection.
- **GL lookups** → `getAccountByRole`/`rc`/`rn`, never a hardcoded code string.
- **New AI capability** → add the action to `AI_ALLOWED_ACTIONS`, document it in the prompt, add a handler in the action loop, and `logAI` it.
- **Mutations** → call `logAudit`/`logAI` so it lands in `audit_log`.
- **User input destined for HTML/CSV** → escape it (`SendInvoiceView` `esc`); use the `insights.js` CSV helpers for exports.
- **Dates** → `fmtDate` (`src/lib/format.js`); store `YYYY-MM-DD`.

---

## 9. Anti-patterns to avoid (mistakes already fixed)

- ❌ Hardcoding GL codes (`"6100"`). ✅ Use roles.
- ❌ Inserting journal entries outside `post_journal_entry`. ✅ Single RPC path.
- ❌ Trusting client-side `.eq("company_id")` as security. ✅ RLS is the boundary.
- ❌ Letting AI action JSON render in the chat reply. ✅ Brace-balanced scrub + separate `rich` array.
- ❌ Auto-navigating when rendering a chart. ✅ Only the "View full report" button navigates.
- ❌ Date filters that include the prior year for "this year"/deductions. ✅ Inject current year; filter `startsWith(year)`, exclude voided/deleted.
- ❌ Description/keyword matching for deductions or duplicates. ✅ GL-code + role for deductions; vendor-normalize + amount/date windows for duplicates.
- ❌ Leaving company-scoped state set across a company switch. ✅ `resetCompanyState`.
- ❌ Un-escaped user input in generated HTML (`document.write`). ✅ `esc()`.
- ❌ **Deriving matchability / openness / settledness from a denormalized status FLAG (`type`, `payment_status`, `matched`).** ✅ **Derive truth from the live GL entries.** This was the single root cause behind ~10 rounds of bank-matching bugs (O73): a `type` string that drifted from `"revenue"/"expense"` starved the candidate set AND gated a `type`-based early-return that skipped the deterministic matcher entirely; a stale `payment_status="collected"/"paid"` (from a prior clearing later reversed) both excluded genuinely-open invoices and made the clearing post silently skip (`isTransition` false). The flags are caches that go stale; the journal entries are the source of truth. **Openness = "no LIVE clearing JE links this bill"** (`matchableOpenItems`, keyed on `import_metadata.payment_for`), **side = the A/R/A/P OFFSET code on the entry's leg** (`autoMatchBankLines`, not `type`), **already-cleared = a real posted payment JE exists** (not the flag). When a flag and the GL disagree, the GL wins — and the flag should be treated as a display hint to re-derive, never a gate. (Mirrors §12's "cash on hand is GL-derived" and "amount owed = `glAccountBalance(AP)`, not a flag sum.")
- ❌ **Composing the sentence that describes an action ALONGSIDE the action, instead of deriving it FROM the result.** ✅ **A USER-FACING STRING ABOUT WHAT THE SYSTEM DID MUST BE GENERATED FROM THE RECORDED OUTCOME (recorded 2026-08-27, O115/O114/O128).** A description assembled in parallel with the work **can diverge from it** — and when it does, **the books are right and the user is misinformed**, which is the worst combination available: nothing is broken, so nothing gets fixed.
  - **THREE INSTANCES IN ONE WEEK, ALL THE SAME SHAPE:** *(1)* *"Lone Star $1,344.85 **as a client meal**"* while the ledger said **5000 COGS** (`O115`) · *(2)* **`MULTIPLE_CANDIDATES` invented as a reason** when the real cause was a failed write — a claim about the LEDGER that was false, and it made a whole drive undiagnosable · *(3)* **"✓ 0 invoices booked · $0.00 total"** for the most valuable outcome the feature produces (`O128`).
  - **★ THE STRUCTURAL SIGNATURE, and it is greppable: THE STRING READS THE INTENT, NOT THE RESULT.** Every instance takes the **pre-write invoice object** and re-computes from it, rather than reading **the entry as recorded**. `invoiceResult` (`App.jsx:4273`) carries **two of the three in twelve lines** — `invoiceCount: highConfidence.length` is blind to `attached` (3), and `bookedAs: plainCategoryPhrase(firstBooked)` re-derives a category the account already states (1). `describeBooking` (`clarify.js:176`) is a third carrier of the same call.
  - **THE CHECK: FOR EACH CLAUSE, NAME THE FIELD OF THE OUTCOME IT READS.** If a clause is computed from the **input**, it is a parallel description and **will** diverge — the only question is when. *"Booked as X"* must read the booked account. *"N booked"* must count what was written. *"Because Y"* must read the recorded reason.
  - **★★ THIRD RULE IN ONE FAMILY, AND THEY ARE A LADDER:** **name the reader** — the write does not land · **an invisible action will be repeated** — the effect lands but is not seen · **describe from the record** — the effect lands, is seen, and is described wrongly. **Each is harder to detect than the last**, because each one looks more like success.
- ❌ **Shipping an action whose effect the user cannot see.** ✅ **AN ACTION WHOSE EFFECT IS INVISIBLE WILL BE REPEATED (recorded 2026-08-27, O123/O124).** Not *may* be — **will**. A user who cannot tell that a thing happened has exactly one move left, and it is to do it again. **Cost already paid: three reversals against one invoice, −937.00 on one vendor**, from a user clicking a Void button whose effect was real, correct, and completely invisible on screen.
  - **THE FOUR INSTANCES, all live, all the same shape:** *(1)* **void leaves no visible marker** — the original stays listed and looks identical to a live entry (correct GAAP, §12 #14; the missing MARKER is the defect) · *(2)* **the Void control stays enabled** on an already-reversed entry, so the UI invites the repeat it should refuse · *(3)* **`softDeleteInvoices` says "Deleted" without checking whether the write landed**, so a blocked or failed delete is indistinguishable from a successful one · *(4)* **the SAFE action is four steps deep behind a filter label while the dangerous one is on the landing screen** — so invisibility of the *option* pushes the user onto the destructive path.
  - **★ IT IS THE USER-FACING TWIN OF "NAME THE READER".** That rule catches a write whose effect is invisible **to the system**; this one catches a write whose effect is invisible **to the person**. Same defect, two layers, and **the second is worse because a human responds to silence by retrying** — where an unread field merely sits there, an unseen action compounds. *(C195(7) is the same shape a third time: a guard whose input is always empty is indistinguishable from a clean queue.)*
  - **THE CHECK: after this action succeeds, what CHANGES ON SCREEN? Name the pixel.** If the answer is "a toast that disappears", it is invisible — **a transient confirmation is not a state change.** And **if the action is now impossible, DISABLE the control**; refusing on click is strictly worse than not offering, because it teaches the user that clicking is how you find out.
- ❌ **Shipping a limit, gate or threshold whose PURPOSE nobody can state.** ✅ **NAME THE THREAT, THE WAY YOU NAME THE READER (recorded 2026-08-26, O97).** *Operator: **"a limit nobody can explain the purpose of is the same class as a field nobody reads."*** Both are machinery that runs forever while nobody can say what it accomplishes — and **an unexplained limit is worse than an unread field, because it does not merely fail to help, it actively refuses work.**
  - **THE CASE: `AI_LIMIT` / `UPLOAD_LIMIT`.** Migration `021`'s header states **what** they are and never **why**. `ai-proxy`'s header does the same. The purpose is stated **exactly once in the whole repo**, in §7: *"limits prevent abuse/runaway cost"* — **two different threats, joined by a slash, implying two different mechanisms, neither of which the current design is shaped for.**
  - **THREE CANDIDATE THREATS, AND THE UNIT ONLY FITS ONE:**
    1. **RUNAWAY COST** → the unit should be **tokens or dollars, not requests.** Today a 20-token `classify-document` and a 4,000-token `extract-invoices-batch` **cost the same against the budget** — a 200× miscalibration. As a cost control it caps the wrong quantity.
    2. **UPSTREAM PROVIDER LIMITS** → Anthropic's are **org-wide and per-minute**. A **per-user, per-hour** cap does not defend them at all: ten users at 59/hour each can spike hundreds of requests into one minute while every one of them is "under". **Wrong scope and wrong window.**
    3. **ABUSE — a compromised or malicious authenticated account burning the key** → **per-user-per-hour is exactly right.** Bounded blast radius per account, and the account is the thing you can revoke.
  - **★ SO THE LIMIT IS WELL-SHAPED FOR (3) AND BADLY SHAPED FOR (1) AND (2) — AND IT IS CURRENTLY BEING ENFORCED AGAINST A LEGITIMATE FIRST-TIME ONBOARDING, WHICH IS NONE OF THE THREE.** That is the product working as designed, refused by a mechanism built for someone attacking it. **Naming the threat is what makes that visible**; while the purpose stayed a slash in one sentence, the only available move was to argue about the number.
  - **THE CHECK, and it is the same shape as "does this actually route?": *what specifically goes wrong if this limit is absent, and is the quantity I am counting the quantity that goes wrong?*** If the threat is cost, count cost. If it is upstream, match upstream's scope and window. If it is abuse, per-actor is right — **and then a legitimate bulk workload needs a different door, not a bigger number.**
- ❌ **Writing a field to route something, without naming the reader that will act on it.** ✅ **FOR EVERY FIELD WE WRITE IN ORDER TO ROUTE SOMETHING, NAME THE READER — AND CHECK THE VALUE DOESN'T DEFEAT IT (recorded 2026-08-26, O114).** A write that succeeds and a routing that happens are two different events, and **the write's success is not evidence of the routing.** There are two shapes and the second is nastier:
  1. **NO READER EXISTS.** The field is written faithfully forever and nothing consults it. **`import_metadata` (O87/·3a)** — `post_journal_entry` cherry-picks six named scalars out of `p_meta` and never writes `import_metadata` at all, so the payroll auto-post gate read it, found nothing, and **failed closed for an entire release** while a green suite reported health. Same mechanism silently disabled the O59 sales-tax control and `signedPeriod`'s `original_date`.
  2. **★★ A READER EXISTS AND THE WRITTEN VALUE GUARANTEES IT SAYS NO. "A VALUE THAT SATISFIES THE WRITE AND DEFEATS THE READ IT WAS WRITTEN FOR."** **The duplicate card's "Not sure" branch (O114)** stamps `approval_status: "flagged"` and `duplicate_flag: true` — read by a detail panel and an unrelated AP screener, by nothing in the review queue — **and books at `confidence: 100`, which is precisely the value that makes `shouldFlagForReview` return `none`.** So the option that *looks* like "send this to a human" **books the expense and then guarantees no human sees it.** Worse than an inert path: an inert path does nothing, this one acts and hides.
  - **★★ THE CHECK IS A QUESTION, AND THE QUESTION IS THE RULE — NOT A PARAPHRASE OF IT. ASK IT OUT LOUD, AT WRITE TIME:**
    > ### *"Does this actually route?"*
    > *Who reads this field, at what `file:line`, and what would they do with the exact value I am about to write?*
    - **If the answer is a `file:line`, it routes. If it is a table name, a status string, or a hopeful adjective, it does not.**
    - **Where the reader is a THRESHOLD, write the value that TRIPS it.** `confidence: 100` on a row you want reviewed is a request to be ignored.
  - **★★ AND WHY THIS CANNOT BE DELEGATED TO THE SUITE: A TEST ASSERTS A VALUE WAS WRITTEN. ONLY TRACING THE READER PROVES THE WRITE DOES ANYTHING.** Both instances above sat behind green suites — `import_metadata` for an entire release, `confidence: 100` for longer — and **neither was found by a test. Both were found by a human asking the question. Twice this month.** A test that asserts `approval_status === "flagged"` passes forever on a field nobody reads; it is verifying the writer against itself, which is the same shape as ·3a's extractor and fixture agreeing while neither agreed with the database.
  - **CHEAPEST DETECTION, in order: (1) `grep` for the reader before shipping the writer** — one command, and it answers the question definitively; **(2) prefer asserting the mechanism FIRES over asserting nothing is outstanding** (C195(7) — a guard whose input is always empty is indistinguishable from a clean queue); **(3) where the field is a routing decision, assert a POSITIVE COUNT downstream**, not an absence upstream.
- ❌ **Counting a human's answer as evidence for a question they were not asked.** ✅ **AN ATTESTATION IS SCOPED TO THE QUESTION THAT WAS ASKED (recorded 2026-08-26, O114).** A click is evidence about **the thing the human was looking at**, and nothing else. Promoting it to evidence about an adjacent fact is the machine laundering its own guess through a human's hand — the guess acquires a person's authority without a person ever having considered it. **TWO INSTANCES, WHICH IS WHY IT IS A RULE AND NOT A PATCH:**
  1. **Amendment B** (`docs/CALIBRATION_SPEC_O88_AMENDMENT_B.md`, signed 2026-08-25) — **signing a month is not examining a vendor.** A sign-off attests the period as it stood; it does not attest that each vendor's account was inspected. Backfill graduation therefore requires an EXPLICIT attestation, and on current data that graduates **0 of 13**.
  2. **O114** (`docs/INVOICE_PAYMENT_SPEC_O114.md`) — **answering a duplicate/ambiguity card is not attesting a mapping.** The question is *"are these two documents the same purchase?"* — document identity. The human need not have seen the account, and attaching does not change it. Without the exclusion, `attestationStrengthFor` grades the resolution EXPLICIT, Amendment B's bar is met, and **a vendor graduates to KNOWN on PAPERWORK VOLUME** with `attested_account_id` set to whatever the machine booked — routing around the amendment within one release of it being signed.
  - **THE MECHANISM, so the third instance is caught rather than rediscovered: grade on the EXCEPTION KIND, never on the bare fact that a human clicked.** `NON_ATTESTING_EXCEPTIONS` (`vendorBackfill.js`) is a SET for that reason, and a test asserts its **exact contents** — widening it silently is how the bar would disappear. **A RECODE always attests** (the human looked at the account and changed it); when a resolution both matches and recodes, that is **two facts**, and only the recode touches the familiarity clock.
  - **AND THE COROLLARY THAT MAKES IT ACTIONABLE:** before recording any human action as evidence, write down the question they were actually shown. If the fact you want to record is not an answer to that question, it is not attested — it is inferred, and it must be graded IMPLICIT and say so.
- ❌ **A row-targeted `.update()` without `.select()`, and an empty `catch {}` around a write.** ✅ **Row-targeted mutations go through `checkedRowUpdate` (`checkedWrite.js`) — ZERO ROWS IS A FAILURE.** PostgREST reports **no error** for an update that matched nothing, so without `.select()` (returning=representation) a broken write is indistinguishable from a successful one; an empty `catch {}` then swallows what little signal remains. This is the **root-cause class of two live O84 bugs** — C191 (planner exceptions carried a non-DB id → every `.eq("id", …)` matched zero rows → five lines silently stayed 'pending') and C189 (the payroll entry never got `payment_status`, invisible until an `ap_tie` control total failed by $4,306). Both would have been **one loud console line** under checked writes. Doctrine: **(1)** row-targeted mutations go through `checkedRowUpdate` / `checkedIdsUpdate` (assert no error AND rows-affected ≥ 1; loud `console.error` + a counted failure record); **(2)** **ids reach DB calls ONLY through `lineDbId` / `resolveLineDbId`** — never a raw parse-time id (grep-enforceable); **(3)** **empty `catch {}` around a write is forbidden** — log it or count it; **(4)** every new write path stamps the hygiene fields (`payment_status`, `bank_account_id`, `meta.kind`).
- ❌ **A write path that creates a CASH-SETTLED entry but omits `payment_status` `'paid'`.** ✅ **Every write path that posts a cash-settled entry must stamp `payment_status: 'paid'`** (via `meta` → `post_journal_entry` → `flattenJournalEntries`). The sub-ledger heuristics `apUnpaid`/`arUnpaid` (`reports.js`) TRUST that field — `apUnpaid = isExp && payment_status !== 'paid'` — so a path that omits it lets the entry's **expense legs leak into the open-bills universe**, failing the `ap_tie` control total (Review shows "1 figure that should match but don't", Home downgrades) **while the GL itself is perfectly correct**. C189 (O84): PayrollView's post path posted a correct 4-leg payroll entry with no `payment_status`, so Salaries + Payroll Tax Exp leaked and `ap_tie` failed by exactly gross+employer ($4,306). Root cause is **write-path hygiene divergence** — the bank-line payroll path stamped it (`buildBankLineEntry`: `payment_status: "paid"`), the PayrollView path didn't. The heuristic is correct; the writer was wrong. (Payroll's withholding LIABILITY is tracked by its own GL — Payroll Taxes Payable — not the bills list, so stamping the whole entry paid is right.)
- ❌ **Keying bank re-upload dedup on TEXT or GL code (vendor / description / gl_code).** ✅ **`bankTxnKey` = `date | abs(amount) | direction` only** (`openingBalanceProposal.js`). O83 production: re-uploading a statement double-booked 14 of 20 lines because the key mixed the CLEANED vendor (booked side: `"Toast POS"`) with the RAW memo (re-parse side: `"TOAST POS DEPOSIT 011326"`), and the GL even re-categorized run-to-run ($15 fee `8000`→`7100`) — so text/GL keys are asymmetric and never collide. **Known trade-off (accepted):** two genuinely-distinct same-day/same-amount/same-direction lines collide; failure mode is a real new line arriving FLAGGED already-booked (defaulted UNCHECKED in review — visible, one click to re-check), never a silent double-post. Safer than the text key that shipped the bug.

---

## 10. Roadmap summary (delivered)

Core: universal upload → AI extract/code → balanced journal entries; Books (transactions, payables, AR); Reports (P&L, Balance Sheet, Cash Flow, cash vs accrual); Tax Center (estimates, deadlines, deduction tracker, 1099s); bank import + reconciliation; recurring; contracts/ASC 842 leases; QBO import; payroll import.

Data safety: soft delete + undo, bulk-delete protection, audit log for all AI actions, export-all, data-isolation Security dashboard, platform admin panel + Support Mode, upload log, per-user rate limiting.

AI: conversational clarification flow with free-text booking, adaptive `client_ai_profile`, world-class CFO prompt with live financial snapshot + proactive insights, action sandbox + capability doc, inline charts/CSV/summaries, smart duplicate detection, recurring-expense suggestions.

Security/quality: OWASP Top-10 audit (this pass) — `accounts` RLS (`023`), invoice-print XSS hardening; expanded docs; Vitest suite (`tests/`).

---

## 11. Known dead schema & future considerations

A full `information_schema` audit against the live database (the schema-drift pass) surfaced schema the app does not use. Do **not** "clean these up" reactively — each is documented and intentional:

**ORPHAN AUDIT (process).** Every roadmap-touching docs commit ends by re-running the orphan check — read §11 end to end and list any finding or backlog item with **no commit ref, no C### designation, and no Tier/roadmap line** — and stating the result. A finding with nowhere to go is a finding that will be lost.

> **2026-08-08 run — five items stamped.** The check caught three bullets carrying no anchor at all (bulk soft-delete/void UI · trust-panel Documents glyph · Home Activity feed jargon) and two anchored only by cross-reference from OTHER bullets (business-type COA · confidence calibration). **All five were already on the roadmap** — TIER 2 and TIER 1 #6/#7 respectively — so nothing had actually been lost; the divergence was one-directional, the roadmap knowing what §11 didn't say. **Lesson: "is it scheduled?" must be answerable from the item itself, not by grepping for a sibling that happens to mention it.** Same failure shape the security cluster's Tier 1 designation would have had — §11 calling it launch-blocking while the TIER 1 list omitted it (fixed in the same pass, now ROADMAP §0 TIER 1 #11).

- **Backlog — bulk-selection soft-delete / void UI in Books (not built). DESIGNATED: ROADMAP §0 TIER 2 "bulk soft-delete/void UI".** `softDeleteInvoices` (plural, batch + single Undo toast) exists in `ERP` but is wired to **no component** — the UI exposes only per-entry delete/void. Justified by the O83 duplicate incident: remediating the 14 double-booked entries required scripted DB access (soft-delete + audit rows via the Management API) because there's no in-app way to select and remove a batch. Build a multi-select + bulk soft-delete/void surface in Books (route through the existing `softDeleteInvoices`/`voidInvoiceWithUndo` so audit + Undo are preserved) so batch remediation is self-serve.

- **★ NORTH STAR — bank statements are HANDLED, not OPERATED (not built; sequence after launch-gate).** *(Supersedes and absorbs the former "bank-import verdict" backlog item and the universal-drop-zone note.)* Bank Import and Reconcile are bookkeeper-workbench surfaces inherited from the QBO mental model; **Shadow's client persona has no bookkeeper role**, so a client dropped into raw line-lists + checkboxes + "Needs review" / "Select all" is being handed a job that isn't theirs (O83 live-drive: the operator nearly re-booked 20 duplicates via Select-All three separate times, despite writing the fixture himself). **Target architecture:** uploading a statement from ANY surface — including the home drop zone (whose promise "your AI controller handles the rest" today delivers a multi-screen manual flow) — triggers an **automatic pipeline with zero required interaction on the clean path**: parse → dedupe against books (idempotency guard, `markAlreadyBooked`) → book new lines → match booked lines (A/P matcher) → derive opening/ending balances from the statement → complete a **verified** reconciliation. The client sees **outcomes, not tasks**: "January matched to your bank ✓" on the trust panel + plain-language confirmation of anything auto-booked. **Exceptions** (unmatched lines, balance discrepancies, low-confidence categorizations) route to the **CPA Review queue**; the client is only ever asked plain-language questions the CPA can't resolve. **Bank Import and Reconcile demote to read-only history / exception views for the CPA — never a required client workflow.** Nearly all the machinery already exists post-O83 (idempotent re-parse, deterministic A/P auto-matching, statement-derived opening/ending balances, verified-balance completion + honest gate records that don't false-pass); the **remaining work is orchestration + the exception queue** (and the `bank_account_id` linkage below, which lets the pipeline persist/source statement lines instead of re-parsing). Interim mitigation already shipped: C174 truthful already-booked labeling in Bank Import (so the current manual surface at least can't mislead into re-booking).

  **★ NORTH STAR PHASE 2 — IA COLLAPSE (not built; sequence AFTER Phase 1 pipeline automation).** Once the clean-path pipeline runs end-to-end (parse→book→match→reconcile automatically, Phase 1 above), collapse the navigation to match the role model. **Client-facing nav = Home (drop zone + trust panel + business health), Reports, and a read-only Activity/Documents view — nothing else.** Everything **workbench-shaped** (Bank Import, Reconcile, Matching Engine, Payables-as-a-workflow, Payroll import) either **demotes to CPA-only surfaces behind the existing reviewer role**, or becomes **pipeline internals surfaced as OUTCOMES on Home** ("January matched to your bank ✓"), never as tabs the client operates. **Evidence (O83):** the operator *himself* repeatedly couldn't locate the Matching Engine (reachable only via a post-booking redirect), stalled on Bank Import's checkbox states, and had to hand-type a statement balance the parser had already extracted — the ten-sub-tab Books nav is **QBO's bookkeeper IA transplanted onto a product whose client persona has no bookkeeper**. **Sequencing is strict: pipeline automation FIRST (the tabs become UNNECESSARY), THEN IA collapse (the tabs become GONE)** — collapsing the nav before the pipeline can do the work would just hide functions the client still needs. **The reviewer role built for sign-off separation (C178/O83) is the SAME gate that declutters the client:** every surface gated to `isReviewer` disappears from the client's view for free — the separation-of-duties boundary and the IA-simplification boundary are one and the same. This is the UI expression of Phase 1's "outcomes, not tasks."

- **★ O84 PART 1 — COMPLETE (2026-08-04 live drive of the persisted-statement pipeline, C185–C187; remediated by C188–C191).** ✅ **RESULT: March 2026 completed live** — 22 lines: **13 auto-booked** + **5 human-resolved** (low-confidence exceptions, surfaced only after C191) + the **Atlas outstanding clear** (no double-book) + **2 A/P clears** + **2 payroll matches**; **auto-reconciliation completed at 27,762.31 / 27,762.31 / 0.00** against an **independent answer key**; **line idempotency held across 5 statement uploads**. **Part 2 (April dirty-path fixture: forced exceptions, attention flow, CPA resolution) is the remaining O84 scope, sequenced after C192–C193.**
  **RESOLVED this drive:** payroll parse died on trailing text → **C188** (`extractFirstJson`, + a 19-site sweep list for deliberate migration, **DESIGNATED: ROADMAP §2 Bugs `O99`**) · payroll `payment_status` `ap_tie` leak → **C189** (+ the §9 cash-settled-write anti-pattern) · planner dead-zone guarantee + review-screen teardown → **C190** (exhaustiveness invariant + conditional `bankStep`) · **planner-exception id seam — THE actual live five-pending-lines bug** (exceptions carried the parse-time local id, so the DB write matched zero rows AND the lines were filtered out of the review table: invisible in the DB *and* the UI) → **C191**, mutation-tested (3 of 5 new tests proven red pre-fix).
  Original six findings:
  1. **STATEMENT/DOCUMENT DEDUP (→ C193).** Re-uploading the same statement creates a NEW `bank_statements` row + a NEW `documents` row each time (verified live: **3× Feb rows**). Line-level idempotency HELD — **zero double-books** (`markAlreadyBooked` / `bankTxnKey`) — but the `bank_statements` table and the doc library accumulate duplicates. **Fix:** a **SHA-256 content hash at `storeDocument` time, unique per company** — identical bytes link to the existing document instead of inserting; `bank_statements` dedups on **account + period + content hash**, superseding the prior row.
  2. **QUEUE COPY SHOWS THE PRE-OVERRIDE AI LABEL.** The processing queue said "booked … as a client meal" while the entry correctly booked to **5000 COGS** via the learned vendor mapping. The displayed label must be the **FINAL post-mapping account**, not the raw AI guess — an owner-facing honesty bug. Live specimen of the confidence/reasoning-consistency item (Tier 1 #7) — **fold into that work**, not a new entry.
  3. **PAYROLL INGESTION FORMAT GAP.** The Home drop zone advertises PDF, but payroll accepts only `csv/xls/xlsx/iif/txt` — a **Gusto PDF summary** (the artifact an owner actually has) is rejected. Either **parse payroll PDFs through the extraction path** or **stop advertising what we reject**. **DESIGNATED: its own ROADMAP §0 TIER 2 line** ("payroll PDF ingestion through the extraction path") — it was previously mis-filed under the doc-library line, where ingestion work would have been lost.
  4. **DOC LIBRARY DATE = UPLOAD DATE.** Cards show the **Aug upload date for a Feb statement**. Same as the existing doc-library backlog item **(c)** (derived document date) — the date must be the derived statement/invoice date; **now confirmed live**, fold into that item.
  5. **DOCS SEARCH MISSING.** The library header says "stored and searchable" with **no search input**. Minimum: **filename + type + date-range**; content search later on extraction text. Folds into the Tier 2 doc-library work.
  6. **API CREDIT EXHAUSTION HAS NO GRACEFUL MODE.** The org ran out of Anthropic credits mid-drive; extraction-dependent features fail **without a clear in-app explanation**. Launch-week: **credit/balance monitoring, auto-reload guidance, and an explicit degraded-mode banner** ("document reading is paused") instead of silent failures.

  **Back-half findings (added after the drive's second session):**
  - **(a) OPENING-BALANCE BANNER IS OUTSTANDING-BLIND.** It warns on a bank-vs-books difference (**$275 live**) that the **reconciliation chain fully explains** — the Atlas uncleared check. It should consult `priorOutstandingCandidates` (C187) and render **"explained by N uncleared item(s) ✓"** instead of an alarm. **Small, high trust-value** — the same false-alarm class as the C179 comparand fix: frightening a client with a difference the system already understands.
  - **(b) TRANSITIONAL REVIEW FLASH.** On upload the **full parsed line list renders briefly**, then the pipeline's verdict replaces it — content appears, then vanishes. Replace with a **"handling this automatically…"** state that resolves *directly* into the outcome summary (never render a list the client isn't meant to operate). **Dissolves fully at IA collapse (North Star Phase 2); cheap interim fix.**
  - **(c) ★ SILENT WRITES — the root-cause CLASS of the night (→ C192, NEXT CODE COMMIT).** Supabase updates that match **zero rows still "succeed"** (no error), and empty `catch {}` blocks swallowed **two live bugs** this drive (C189's missing stamp and C191's id seam were both invisible for exactly this reason). **Fix:** a **checked-write helper** for row-targeted mutations — assert **no error AND rows-affected ≥ 1**, with a **loud console warning + a visible failure counter** on violation; **adopt at every `bank_statement_lines` / `journal_entries` mutation site**. Plus **§9 doctrine**: ids reach DB calls **only** through `lineDbId` (grep-enforceable), and **every new write path stamps the hygiene fields** (`payment_status`, `bank_account_id`, `meta.kind`).
  - **(d) STATEMENT/DOCUMENT CONTENT-HASH DEDUP (→ C193).** The live library now holds **3× March + 3× Feb copies**. Dedup per finding 1's spec: **SHA-256 at `storeDocument`** (unique per company; identical bytes link to the existing document) and **statement supersede on account + period + hash**.

- **★ O84 PART 2 — COMPLETE (2026-08-05, the April dirty-path drive). O84 IS NOW FULLY COMPLETE — 4 attested months.** ✅ **RESULT: April 2026 driven end-to-end and SIGNED OFF.** The pipeline correctly landed **'attention'** (**no false reconciliation** — the honest outcome for a dirty month). Of the 3 designed traps: the **mystery deposit** was **excepted** (routed to a human, as designed) and the **merchant fees** were **legitimately BEATEN** — booked to **6520 at 99 confidence with coherent reasoning**, meaning **the §11 miscategorization specimen no longer reproduces**. Manual reconcile completed at **33,064.41 / 32,714.41 / 0** with **check #1043 written to `outstanding_books`** — **THE CHAIN PRODUCED ITS FIRST ITEM** (May will consume it, exercising C187's clear path on a self-generated item). **Learning layer, precisely observed:** Lone Star (taught) → **booked at 87**; Bluebonnet (taught) → **excepted at 78** — i.e. the learned mapping informed the **SUGGESTION but not the SCORE**. **Line idempotency and ledger integrity held throughout; ZERO wrong entries across the whole of O84.**

  **PART 2 FINDINGS:**
  - **★★ FALSE ATTESTATION SUCCESS (→ C194, NEXT CODE COMMIT — before anything else).** `ReconView`'s completion renders **"Your books match your bank ✓" UNGATED**: a failed completion insert is merely `console.warn`'d and **the success screen renders anyway** — **reproduced live with NO row existing**. This is the **worst class of bug in this product** (a false green on the attestation surface itself). **Trigger isolated by A/B:** entering the balance **BEFORE** resolving the outstanding item made the session autosave fail; completion then **UPDATED a nonexistent row** — a **zero-row, UNCHECKED** write (ReconView sites were **deferred** in the C192 sweep — this is that debt coming due). **C194 scope:** root-cause the ordering failure; **gate the ✓ on a VERIFIED complete row** (checked write **+ re-select**); adopt `checkedRowUpdate`/`checkedIdsUpdate` at **all** ReconView mutation sites; **regression test pinning "completion write failure → NO success state, plain-language 'nothing was locked in'".**
  - **★ CONFIDENCE PRICES DESCRIPTOR CLARITY, NOT VENDOR FAMILIARITY (Tier 1 #7 spec input — the sharpest specimen yet).** A **never-seen** vendor auto-booked **$850 at 88** on mere *name plausibility*, while a **twice-taught** vendor scored **78 and excepted**. The score is reading **descriptor legibility**, not **what we actually know about this client's vendors**. **Requirement:** **novel vendors DAMPENED below the auto-book floor; taught vendors BOOSTED above it** — confidence must price familiarity. (Pairs with the onboarding vendor teach-in / entity-profiles item: teaching is worthless if it doesn't move the score.) **MAY EVIDENCE (O85) — the score WOBBLES:** Lone Star, taught and **booked @87 in April**, was **excepted below the floor in May** — with the correct taught account already pre-selected. The mapping is stable month-to-month; the CONFIDENCE isn't. So calibration must not merely nudge taught vendors up once — it must **stabilize them above the floor across periods**, or the same vendor keeps oscillating between auto-booked and hand-resolved for no reason the client can see.
  - **→ C195 — WORKBENCH HONESTY SWEEP** (the narration layer, batched): **(1)** exception cards **not refreshed after MANUAL booking** (**5 zombies live** — the C193 supersede fix's sibling on the manual path); **(2)** reconciliation completion **doesn't retire covered statements'** attention status/cards (**1 stale card persists post-sign-off**); **(3)** **anomaly pattern suppression** — an identical 4-card Bluebonnet dismissal **two months running** is alarm fatigue (see the §11 pattern-level duplicate-suppression item); **(4)** **manual JE entry is UNREACHABLE** — `AddView` is orphaned, so booking check #1043 required **direct SQL mirroring `post_journal_entry`** (**near-Tier-1**: a CPA cannot hand-enter a transaction); **(5)** **transitional parse flash** (now raised **4×**); **(6)** booking toast reported **0/0 after booking 5**; **(7)** **intake orphan should auto-resolve** on content-hash match to an already-recorded doc (C193's hash, applied to the intake ledger); **(8)** **outstanding-check copy must pass the "knows nothing" bar.** **STANDING DIRECTIVE (record once, applies everywhere): every owner-facing surface assumes ZERO accounting knowledge** — this generalizes the Cardinal Principle from "no jargon" to "no assumed concepts."

- **★ O85 — MAY DRIVE, IN FLIGHT (→ C196, honesty follow-ups).** The May statement consumes April's self-generated outstanding item (check #1043), exercising C187's clear path end-to-end for the first time. Findings so far:
  - **(a) BANK IMPORT'S OPENING BANNER IS STILL THE ORANGE ALARM.** C195 item 8 gave **Reconcile** the calm, chain-aware copy ("explained by 1 check ✓"), but **Bank Import's** banner was not touched — so the same fully-explained gap still reads as a warning on the surface the client actually lands on first. **Fix:** adopt `openingMismatchCopy` + `priorOutstandingCandidates` there too. (Same false-alarm class as C179; the fix already exists, it just isn't wired to both surfaces.)
  - **(b) POST-PIPELINE SUMMARY TILES NARRATE ONLY THE RESIDUE.** After a 21-line statement runs, the tiles read **"Total transactions 5"** — the count of what's LEFT, not what happened. The counters must describe the WHOLE statement: **"21 transactions · 16 handled automatically · 5 need your input."** **That sentence IS the product pitch** — the one moment the client sees the machine's whole contribution — and today it's hidden behind a number that makes the product look like it did less than nothing. Surface it prominently, not as a footnote.
  - **(c) ANOMALY DETECTORS FIRE ON PAYROLL/SYSTEM ENTRIES.** The large-charge and round-amount detectors flagged a **$4,000 payroll gross** as "may need to be capitalized" — nonsense for a payroll leg. **Fix:** exempt `source=payroll` (and system kinds generally) from `large_transaction` and `round_number`. Detection should never opine on entries whose shape it doesn't own.
  - **(d) RECONCILE NEEDS "ABANDON THIS MATCH".** An accidentally-started match session left an `open` reconciliation row that required **manual SQL** to clean up — there is no in-app way to discard a session you didn't mean to start. **Fix:** an explicit, confirm-guarded abandon action that deletes/retires the open row (routed through the C192 checked writes + an audit row), so no reconciliation ever needs a DBA.

- **★ O86 — JUNE DRIVE, COMPLETE (2026-08-06; → C198). MONTH SIX SIGNED OFF.** *(Outcome: **(a)(g)(i)(j)(k)(l) SHIPPED + live-verified** — see ★ O86·2 below; **(b)(c)(d)(e)(f) — ALL SHIPPED**: (b) → C198·3a `68151c5`; (c)(d)(e)(f) → C198·3b `30cf680`. **C198 IS COMPLETE.** *(Read "(b)(c)(d)(e)(f) remain" until the 2026-08-11 sweep — this section stamped (e) and (f) shipped elsewhere while this summary still asked for them.)*)* The June drive ran on the post-C197 client seat (Home + Reports), so for the first time the findings are about what the OWNER experiences rather than what the workbench exposes. ✅ **RESULT: June 2026 reconciled and signed off** — reconciliation **46,434.91 / 46,434.91 · difference 0 · `complete` · verified · `outstanding_books` []**, the `bank_statements` row reached **`complete`**, and the month holds **exactly two Gusto entries** (no payroll duplication). **Operator rubric:** 1 — No · 2 — No · 3 — **upload flow not clean** · 4 — No · 5 — **"Not yet, but closer."** The engine and the ledger were right again; **the drive was spent fighting the INTAKE surface**, which is precisely what C198 (a) exists to remove. Findings:
  - **(a) ★★ HEADLINE — DROP-ZONE AUTO-RUN (single-account).** The Home drop zone still only **stashes** a statement for the CPA ("your accountant will add these") — account binding was deferred at C186 (the offset account can't be read off the file) and never revisited, and C197 turned that deferral into the client's whole experience. **Fix:** a company with exactly **ONE bank account auto-binds**, so a drop on Home runs the **FULL pipeline** (parse → dedupe → book → match → derive balances → reconcile) and the **trust panel narrates the outcome**. This is the **missing half of "handled, not operated"** — the North Star's clean path has existed since C186 but nothing client-facing could reach it. **Related fragility:** the pending-import stash is **in-memory** — today's Act-1 statement stash was **lost before Bank Import could consume it**. Auto-run **replaces** the stash for the single-account case; the stash must be **persisted** for multi-account companies (where a human still picks the account).
  - **(b) PAYROLL AUTO-POST — PROMOTED (Tier 3 → C198-adjacent / Tier 1-adjacent).** A CPA clicking **"Post to Ledger"** on a standard biweekly register is **operating, not reviewing** — the same class of defect as (a), one surface over. **Fix:** **standard-shape registers auto-post** with an **undo window + audit row**; the debit/credit confirm card survives **only for non-standard shapes**. (Supersedes the Tier 3 "payroll auto-post" product idea below, which was scoped as post-launch polish before the client seat made it load-bearing.)
  - **(c) `firstUnsignedMonth` SPEC FIX (Act 0).** The C196 helper picks the earliest month **with activity** that isn't signed off; the correct rule is **the month AFTER the latest sign-off, independent of activity** — a quiet month is still the month to review next, and skipping it silently walks the reviewer past a period nobody attested. **Fix + test:** a **zero-activity unsigned month** case (activity list empty, prior month signed → returns the following month).
  - **(d) OWNER-SURFACE SELF-CONTRADICTION.** The trust panel says **"Nothing needs your attention"** while an **amber "5 unusual patterns"** box sits **below it on the same screen** — the panel and the box are both telling the truth about different things and the owner reads a contradiction. **Fix:** on the **owner seat**, LOW anomalies render as **at most one muted line (or nothing at all)**; **amber/warning framing is CPA-side only**. (The C197 seat boundary is the mechanism; this is the content decision it enables.)
  - **(e) BURN/RUNWAY CONTRADICTION — PROMOTED onto C198.** *"You're profitable **with ~2 months of runway** · Needs attention"* now renders on the **demo surface** a prospect sees, which moves it from a backlog honesty bug to a launch-facing one. Resolve per the original Tier 1 #9 spec (burn = **net cash change**; profitable/cash-positive → **no runway warning**, positive framing). See the §11 burn/runway backlog item for the blast-radius caveat — one shared figure across card, drill and AI snapshot.
  - **(f) ANOMALIES AUTO-EXPIRE ON SIGN-OFF — ✅ SHIPPED (C198·3b f1, `30cf680`; migration `060`).** Open **LOW/MEDIUM** anomalies whose period gets **signed off** should **auto-resolve** — *"period attested over this note"* — instead of lingering into later months. A reviewer who attested the month has, by that act, judged its low-severity notes acceptable; carrying them forward is the alarm-fatigue class again (cf. the pattern-level duplicate-suppression item). HIGH anomalies are deliberately excluded — those should **block** sign-off, not be retired by it.
  - **(g) SPREADSHEET MIS-ROUTE (the C100 caveat, suspected live).** The universal drop path classifies **ALL spreadsheets as bank statements** (`docType = "bank_statement"` for any unrecognized spreadsheet), so a **payroll CSV dropped on Home** never reaches payroll intake. **Verify against the live path and fix**, with a `fileDetect` test pinning payroll-CSV → payroll intake. Pairs with (a): auto-run is only safe if the router is right about what it's auto-running.
  - **(h) FUTURE (note, NOT C198) — Reconcile collapses to an exceptions-only surface.** Once confidence calibration lands (Tier 1 #7) and the auto-run pipeline of (a) is carrying the clean path, **Reconcile stops being a standing tab** and becomes an **exceptions view the CPA visits only when the pipeline couldn't finish** — the same demotion C197 gave the client, applied to the reviewer once the machine earns it. *(Carried on ROADMAP §0 TIER 3.)*
  - **★★ DESIGN DECISION — THE MACHINE COMPLETES THE RECONCILIATION (Alex, 2026-08-06; shipped C198·2).** *Verbatim:* **"when the pipeline completes and the balance ties, the MACHINE completes the reconciliation. Rationale: lower tiers have reduced/no CPA-review cadence; the pipeline must function without a CPA click. Reconciliation = arithmetic (machine-verifiable). Sign-off = judgment (human, always). C194's gate applies unchanged: a reconciliation row may only be created complete when the balance verifiably ties — auto-complete converts a proven tie into a record, never manufactures one."** Implemented as `shouldAutoCompleteReconciliation` (all lines settled AND balance ties AND no completed reconciliation already covers the period) re-asserting `canCompleteReconciliation` immediately before the insert, writing through the EXISTING `completePipelineReconciliation` (C186) — no second writer, no new column: the **auto-vs-manual marker lives in the audit log** (`auto: true` + a plain-language sentence). **No tie → no row, ever** (mutation-tested).
  - **(g) VERIFIED — ALREADY FIXED (O55), NOW PINNED (C198·2).** The xlsx mis-route was **not reproducible**: `detectFileType` has sniffed binary workbooks through the xlsx lib since O55, and it was **verified live 2026-08-06 against real Gusto and bank workbooks**. What was missing was a **test holding it there** — every prior `fileDetect` test feeds TEXT, so the entire binary branch (`arrayBuffer` → `XLSX.read` → `sheet_to_json` → `detectFromText`) was unpinned. **Deliverable: `tests/xlsxRouting.test.js`** builds REAL .xlsx bytes in memory with the repo's own xlsx lib and asserts through the real async detector — payroll register (named AND neutrally-named), bank export, QBO export, junk sheet, corrupt bytes. One-line detector improvement went with it: garbage that xlsx TOLERATES (rather than throwing) now returns an explicit "no readable rows" reason instead of an unknown carrying no reason at all; routing is unchanged.
  - **(i) SECOND HALF CLOSED (C198·2) — the INTAKE row advances too.** C198·1 narrowed and fixed the `bank_statements` status advance; the **intake ledger** had the same defect — a statement whose every line was in the books stayed `held_for_review` forever, so the completeness net kept describing finished work as parked. Now the intake row advances **HELD → RECORDED with its journal-entry linkage** (generalizing C196(5)'s payroll path) exactly when all lines are settled, and stays HELD when any line is open. **No backfill — May's dangling rows stay as evidence.**
  - **(i) NARROWED — statement status only advances on the RECONCILE-COMPLETION path.** The **first-pass pipeline** path fails to advance `bank_statements` rows past `held_for_review`/`attention`; the **reconcile-completion** path DOES advance them (proved live: the **June row reached `complete`** while the **May rows still dangle**). So this is not "status is broken" — it is one missing transition on one path. **Fix scope: the first-pass path ONLY** (don't touch the completion path that demonstrably works).
  - **(j) ★★ HEADLINE BUG (June drive) — RE-UPLOAD OF A NON-COMPLETE STATEMENT SILENTLY NO-OPS.** Re-uploading a statement with an **identical content hash** onto a **non-complete** statement is swallowed by the C193 dedupe: it does **not** re-run the pipeline and does **not** re-evaluate the reconciliation. The stale **'attention'** status then **lies to Review** — the screen reports a problem the books no longer have. **Worse, the surface then demands work the system doesn't need:** the **Reconcile tab starts EMPTY** and asks for a **third physical upload plus a hand-typed ending balance**, when the system already holds the **parsed statement** (`bank_statements.stated_ending_balance`) **and every booked line**. **Required behavior:** (1) **once all statement lines are booked, reconciliation is OFFERED directly** — no re-upload, no re-parse; (2) **dedupe on a NON-COMPLETE statement RE-EVALUATES** (re-run pipeline / re-derive status) instead of no-oping. Dedupe must stay a no-op only where the statement is genuinely **finished**. *(This is the same "don't make the client re-do what we already did" class as (a), on the CPA side.)*
  - **(k) REVIEW'S STATEMENT-LEVEL EXCEPTION CARD LINKS NOWHERE USEFUL.** The card's **"Open in Bank Import"** renders an **empty screen** when the lines are already booked — the link's target is line-level, but the exception is **statement-level**. **Fix:** a target that carries the context — **Reconcile**, or a **statement detail view** — so the reviewer lands on the thing the card is actually about. *(Sibling of the §11 "Review-queue anomaly cards lack inspection affordances" item — same defect shape, different card.)*
  - **(l) RECONCILE MUST PRE-FILL THE ENDING BALANCE from `bank_statements.stated_ending_balance`.** The value is **already persisted** (C185), yet the completion bar still makes the CPA **hand-type a number the system holds**. **Fix:** prefill from the stored statement, **field stays editable** — it remains the CPA's **independent check**, not a rubber stamp. *(Promotes the §11 "Reconcile's Bank Ending Balance field should PREFILL" backlog item onto C198, and now sources it from the PERSISTED statement rather than a re-parse.)*
  - **(m) NOTE ONLY — DISMISSAL-WITH-REASON AS A LEARNING SIGNAL (spec belongs to the calibration session, Tier 1 #7; NOT C198).** When a reviewer dismisses a card with a user-confirmed reason ("recurring charge"), that context should **suppress future cards of the same shape** AND **seed a recurring-rule suggestion** — the dismissal is real human knowledge and today it evaporates. **Hard constraint: it must NEVER silently alter booking behavior** — a dismissal explains a flag, it does not re-code an entry; any booking change stays an explicit, visible action. *(Pairs with the pattern-level duplicate-suppression item and `learnFromCorrection`.)*

  **Also recorded from the June drive:**
  - **STALENESS DETECTOR USES `now()` INSTEAD OF PERIOD CONTEXT (→ C198, with the (d)/(f) anomaly work).** "Vendor hasn't charged you in **51 / 52 / 59 days**" rendered **on the same screen as that vendor's current-month charge** — the detector measures against **wall-clock**, not the period the books are in. This fires wrongly for **ANY books trailing wall-clock, i.e. every real client** (bookkeeping is always behind). **Fix:** measure staleness against the **period context** (latest booked/attested period), never `now()`.
  - **ANOMALY CARDS DOUBLE-EMITTED ON STATEMENT RE-UPLOAD (→ C198, same batch).** A re-upload took the queue from **5 → 10** cards: the C193 dedupe covers **lines**, not **anomaly emission**. **Fix:** anomaly emission must be idempotent per (fingerprint, period) across re-runs — the fingerprint machinery exists (C178), it just isn't consulted on the re-upload path.
  - **MULTI-COMPANY v1 JUNK CENSUS (quarantined; cleanup deferred → ROADMAP §0 TIER 3).** The **fixture company `3a704760` (Franklin Ave Pizza LLC) is FULLY CLEAN — zero pre-July entries**, so no O83–O86 result is contaminated. Surviving v1/demo junk lives in **`c787582f` (Test Corp ONE — 16 entries incl. personal purchases; the Dell asset + Bob's contact were retired 2026-08-06)**, **`24d5e576` (Northwind Studio — 21)**, **`3a4a6b3c` (Shadow Test Co — 12)**, **`66fa0cc2` (1)**. All of it is **quarantined by company scoping** (RLS + `company_id`), so it cannot leak into a real tenant; cleanup is **deferred, not forgotten** — pair it with the Tier 2 demo/showcase-company work so the demo entity is built clean rather than cleaned up.

- **★ O86·2 — THE C198·2 VERIFICATION DAY, COMPLETE (2026-08-08). C198·2 IS CLOSED.** Shipped across **`7c1f180` (C198·2)**, **`3a49906` (C198·2b)** and **`c2dcbbc` (C198·2c)**; **(a)(g)(i)(j)(k)(l) are done and LIVE-VERIFIED**, leaving **(b)(c)(d)(e)(f)** as the remaining C198 scope.
  - **(a) DROP-ZONE AUTO-RUN + AUTO-RECONCILE — LIVE-VERIFIED.** An owner-seat drop of the June statement ran the whole pipeline unattended and rendered **"All 21 transactions from your statement are in your books, and this month was already checked against your bank ✓"** — a **machine-verified** sentence: `alreadyReconciled` is derived from **real coverage** (`reconciliationCoversStatement`), never inferred from `ready` (which is false for two different reasons), so the clause cannot appear where the coverage fact isn't actually known. Single-bank-account companies auto-bind; 0 or 2+ still route to a human, now with a **durable** stash.
  - **(g) XLSX ROUTING — PINNED.** Not reproducible (the O55 sniff works); the gap was that every prior `fileDetect` test fed TEXT, so the binary branch was unheld. `tests/xlsxRouting.test.js` builds **real .xlsx bytes** and asserts through the real async detector, including a **neutrally-named** payroll register (content, not filename) and corrupt bytes.
  - **(i) BOTH HALVES CLOSED.** `bank_statements` advance on the first-pass path (C198·1) **and** the intake row advances **HELD → RECORDED** with its journal-entry linkage (C198·2). No backfill — May's rows stay as evidence.
  - **(j)(k)(l) — LIVE-VERIFIED.** Re-upload re-evaluates instead of no-oping; the statement-level card targets Reconcile (and renders nothing once resolved); the session opens with the ending balance already in it, editable.
  - **QUEUE-LINE HONESTY (C198·2b).** The tile said **"Done"** beside the **old stash sentence** over work already booked: the (a1) branch stamped `done` **before** awaiting `handleBankFile`, and nothing wrote the outcome back. Now: **processing → await → done-with-outcome**, a throw stamps **"Needs a look"** and never `done`, the stash sentence is **structurally unreachable** for a `to:"pipeline"` result, and an in-flight run says **"Adding these to your books…"**.
  - **★ C195(7) HAD SHIPPED INERT — REVIVED (`c2dcbbc`).** The duplicate auto-resolve **never fired once**: `reconcileIntake` built its dropped rows without `content_hash`, so `reconcileDroppedDocs`' `hashes` array was **always empty** and the whole block was unreachable. **A block that never runs looks identical to a block with nothing to do** — which is why it went unnoticed from C195 to now. Fixed by carrying `content_hash` + `document_id` through, and **live-verified clearing a 2-day-old orphan on the next Review load**, with plain-language detail. *(Lesson worth generalizing: a guard whose input is always empty is indistinguishable from a clean queue — prefer asserting the mechanism FIRES, not merely that nothing is outstanding.)*
  - **NOTE — THE UPLOAD QUEUE IS IN-MEMORY. DESIGNATED: ROADMAP §2 Bugs `O97`** *(was "pursue later, not scheduled", which anchors nothing — it carries live data-loss evidence and now carries a row).* `uploadQueue` + `fileStoreRef` are React state/ref, so a **reload or navigation between enqueue and processing wipes an arrival** before it is ever processed. **Live evidence:** the **2026-08-06 20:11:42** orphan — the operator hard-refreshed between drops, and the intake row sat at `received` while the same bytes were dropped again 23s later. Mitigations that now exist: the revived auto-resolve **self-heals the already-recorded-bytes case**, and a genuinely lost file still surfaces through the **completeness net after 30 minutes**. The real fix is a **durable queue** (persist the pending work, not just the arrival record) — **not scheduled**.
  - **CALIBRATION FOOTNOTE — auto-reconcile DECLINES months with outstanding items.** The auto path computes its difference with a **hardcoded zero outstanding**, so any month carrying an uncleared item fails to tie and correctly falls back to the **offered CPA session**. That is deliberately conservative (never assert a tie the arithmetic didn't prove). **Whether the outstanding chain should ever feed the AUTO path is a calibration-session question** — it is a judgment about how much the machine may assume, not a bug.
  - **COPY NIT (→ C198·3).** Bank Import's re-upload toast says **"21 transactions imported"** when all 21 were **deduped** — it should say we **already have** them. Same class as the queue-line fix above, one surface over.

- **★ O87 — JULY DRIVE, COMPLETE (2026-08-09; → C198·3c). MONTH SEVEN SIGNED OFF — THE MAGIC PATH'S FIRST LIVE FIRE.** ✅ **RESULT: July 2026 reconciled and signed off — `54,880.41 / 54,880.41 · difference 0 · complete · verified · outstanding_books []`, AUTO-COMPLETED by the pipeline on exception booking.** This is the **first live auto-reconcile**: the machine converted a proven tie into a record with no CPA click, exactly as the C198·2 design decision specified. **17 of 21 lines auto-booked** — including **Lone Star, which auto-booked for the first time after flapping UPWARD past the floor following two sub-floor months** (calibration evidence, see (iv)). **C198·3b's f1 made its debut: 16 notes closed as `attested`/`2026-07` at sign-off.** Findings:
  - **(i) ★★ HEADLINE — THE PAYROLL AUTO-POST GATE IS STRUCTURALLY INERT (shipped C198·3a, never able to fire).** `payrollHistoryFromLedger` selects on `import_metadata.kind === 'payroll'`; **no payroll entry has ever carried `import_metadata`**, so history is **always empty**, so **condition 5 (within-norms) fails for every company, forever**. Both July registers correctly fell to the confirm card — the gate **failed CLOSED**, which is the one mercy: an inert gate that defaults to "ask a human" is a missed feature, not a wrong posting.
    - **★ ROOT CAUSE IS ONE LAYER DEEPER THAN "the poster doesn't stamp it" — and it is NOT payroll-specific.** The poster stamps `meta` correctly and `persistMultiLineEntry` passes it as `p_meta`. **`post_journal_entry` (migration `010`) then cherry-picks SIX named scalar keys out of `p_meta`** (`ai_reasoning`, `ai_confidence`, `approval_status`, `payment_status`, `payment_method`, `due_date`) **into their own columns and never writes `import_metadata` at all.** Every other key — `kind`, `gross`, `net`, `withholdings`, `employer_taxes`, `period` — is silently discarded at the RPC boundary. Since the RPC is the **single canonical write path** (§7), **every entry posted through it has `import_metadata = NULL`**; the only writers are the payment path's follow-up `.update()` (App.jsx:6087) and the QBO direct insert. **⚠ This means the fix "stamp it on post" DOES NOT WORK if stamped into `p_meta`** — it must be either a follow-up `checkedRowUpdate` (the shape the payment path already uses, **no migration**) or a change to `post_journal_entry` (**a migration**, which would move `·3c` off "migrations unchanged"). Decide which before writing code.
    - **★ BLAST RADIUS beyond payroll — other `import_metadata.kind` readers are suspect on the same evidence.** `matchPayrollBankLine` (`payroll.js`) keys on `kind==='payroll'`, so **O72's payroll bank-line reconciliation is inert by the same mechanism** — a payroll net-pay bank line cannot match its register. `depreciation.js` reads `{kind:'depreciation', asset_id, period}` and needs the same check. **NOT affected:** `isSystemPostedEntry` (C196(4) noise exemption) also tests `source`, which the RPC *does* write, so it still works; `apBackfill`/`settlementLink` read the payment path's kinds, which are stamped by that path's own update. **Audit every `import_metadata` reader as part of ·3c** — one null column has been quietly disabling features for as long as the RPC has existed.
    - **★★ WHY THE TESTS PASSED — the C195(7) lesson, in test form.** `tests/payrollAutoPostGate.test.js` fed the gate **synthetic history shaped to match what the extractor reads**, so extractor and fixture agreed with each other while neither agreed with the database. There was **no ROUND-TRIP test** — post → read back → extract → gate — asserting that **the poster's actual output is readable by the consumer**. A gate that never fires and a gate with nothing to do are indistinguishable from a green suite. **Mandatory in ·3c: the round-trip test**, and treat "both sides of a contract tested against the same fixture" as a test smell wherever it appears.
    - **★ DECIDED (2026-08-09) — FIX VIA FOLLOW-UP CHECKED UPDATE, NOT AN RPC CHANGE.** Stamp `{kind, gross, pay_date, period}` with a **`checkedRowUpdate` issued after `post_journal_entry` returns** — the pattern the payment path already uses (**App.jsx:6087 precedent**), so **·3c stays MIGRATION-FREE** (ledger unchanged, through `061`). Plus **one-off backfill SQL for the 14 existing Gusto JEs** so the norm has history from day one, and the copy fix above.
    - **RPC CONSOLIDATION IS A SEPARATE, DELIBERATE ITEM — not a bug-fix rider.** Making `import_metadata` a first-class `post_journal_entry` parameter is the right end state (it removes the two-write window and the whole class of "the RPC silently dropped my key"), but it changes the single canonical write path for **every** caller, and that does not belong bolted onto a payroll fix. **Recorded as future work; NOT scheduled — but no longer unanchored: DESIGNATED ROADMAP §2 Bugs `O95`** ("not scheduled" is the opposite of a home; the 2026-08-11 sweep flagged it).
    - **★★ TESTING — THE UNIT TEST CANNOT REACH THE BUG, SO THE DRIVE SCRIPT MUST.** The round-trip test asserts the **client contract** (post → stamp update issued → extractor reads it back). That is necessary and still **insufficient**: it runs on the client side of a boundary this bug lived on the far side of. **So the next drive's script gains a LIVE BOUNDARY CHECK — after the first August register posts, SQL-verify `import_metadata IS NOT NULL` on the new JE. DESIGNATED: ROADMAP §0 TIER 1 #12.** Unit tests cannot cross the DB boundary, and that is *precisely* how this escaped a green suite for a whole release.
  - **(ii) STATEMENT PERIOD DERIVED FROM TRANSACTION SPAN, NOT THE STATED PERIOD.** July parsed as **07-01 → 07-27** (last txn) against a **stated period end of 07-31**. Harmless in July because the tie still proved, but the span is an *inference* where the statement carries the *fact*. **Fix: prefer the STATED period; fall back to the txn span only when the statement doesn't carry one.**
  - **(iii) "TRANSACTIONS MATCHED: 0" ON A COMPLETE 21-TRANSACTION AUTO-PATH MONTH.** Literally true — the auto path books rather than "matches", so the matcher's counter is zero — and it **reads as failure** on a month that went perfectly. Same true-but-reads-false class as the C198·2b queue line and the ·3b re-upload toast. **Copy fix**; the count is not wrong, the label is.
  - **(v) THE f1 ATTESTATION SWEEP CONSERVATIVELY SKIPPED 3 JULY DUPLICATE CARDS.** `anomalySubjectPeriod` is **blind to f3's new date-pair fingerprints** (`dup:<vendor>:<cents>:<dateA>+<dateB>` — its regex looks for a trailing `:YYYY-MM`), **and** detection-time `invoice_ids` **don't resolve after a reload**, so neither the ref path nor the fingerprint path could place those three. They were **skipped, not mis-attested** — the sweep failed safe. **Fix: parse full dates out of the fingerprint as a second fallback.** *(Note the shape: ·3b(f3) re-keyed fingerprints and ·3b(f1) consumed them, in the same commit, and the two halves disagreed about the format. Same-commit coupling is exactly where a contract goes unchecked.)*
  - **(iv) CALIBRATION EVIDENCE ONLY (→ the calibration session, NOT ·3c).** **Bluebonnet excepted for the SEVENTH straight month at ~78 confidence with the correct mapping already pre-selected** — the system knows the answer and asks anyway, seven times. **Lone Star: four months, four different verdicts.** Both are specimens for Tier 1 #7, not code changes here.
  - **Also recorded:** the **f3 resurrection wall was observed live — ~18 cards re-opened under their new content keys, one-time and exactly as designed** (the ·3b commit predicted this; the queue count stayed right because the same reconcile pass resolved the old rows). **Post-sign-off anomaly counts: 16 attested · 35 auto-resolved · 21 dismissed · 3 open** — the 3 open are the (v) trio; **leave them**, the fixed sweep or August's sign-off will take them.
  - **★ NEW REQUIREMENT FROM RUBRIC Q5 — AN INVOICE-VOLUME DRIVE (→ ROADMAP §0 TIER 1 #12).** *"Close, but I want to see a lot more invoices go through first."* The fixture program to date is **statement-centric** — seven drives, seven bank statements — but **Act 1 for a real operator is invoice- and receipt-heavy**. The engine is verified strong on a path that is not the path a new client walks in on. **Deliverable: a realistic month of vendor invoices, receipts and bills through the UNIVERSAL DROP at volume**, exercising extraction, categorization, the AP flow and the document library. **GATE: before the demo-readiness conversation concludes.** *(Note what Q4's caveat implies alongside this: "No, but July was deliberately simple" means the clean-path result is real but under-tested — volume and mess are the same missing axis.)*
  - **Migrations ledger: UNCHANGED — applied & verified through `061`** (see §6). *(Caveat: if ·3c's (i) fix goes the `post_journal_entry` route, that adds one — decide first.)*
  - **★★ Operator rubric:** **1 — No · 2 — YES** — *"first payroll we've recorded for this company" was untrue (12 priors); this is finding (i)'s user-facing face and the **first rubric-Q2 yes in seven drives**, root-caused same night* **· 3 — No · 4 — No**, with the caveat that **July was deliberately simple** **· 5 — "Close, but I want to see a lot more invoices go through first."**
    - **★ WHY THE Q2 YES MATTERS MORE THAN THE INERT GATE.** Seven drives without one, and it did not come from a wrong number — the ledger was perfect and the month auto-reconciled. It came from the system **asserting a fact about the world that was false**. That is a worse failure than a missed feature, and it is the one the rubric is built to catch.
    - **★★ THE DURABLE LESSON — absence of evidence was rendered as evidence of absence.** The gate found an empty history and reported *"This is the first payroll we've recorded for this company."* The true statement was *"we couldn't find any prior payroll"* — which is a statement about the QUERY, not about the company. **Fixing the extractor does NOT fix this**: any future extraction failure re-tells the same lie with the same confidence. **·3c must change the copy too** — an empty result set may only ever be reported as an empty result set. *(Generalize it: every "this is the first / there are none / nothing found" string in the product is a claim about the world derived from a query returning zero rows, and each one is this bug waiting for its own inert path. **The CLASS is DESIGNATED: ROADMAP §2 Bugs `O98`** — payroll's instance shipped in ·3c, the rest were listed as findings, and a list is not a home.)*
- **★ O88 — THE CALIBRATION SPEC EXISTS (2026-08-16). `docs/CALIBRATION_SPEC_O88.md`, tracked in git as of this commit. DESIGNATED: ROADMAP §2 `O102`.** Four places in this section have pointed at "the calibration session" as the home for work — **(iv)** Bluebonnet's seven identical exceptions and Lone Star's four-verdict flap · **(m)** dismissal-with-reason as a learning signal · **(h)** Reconcile collapsing to exceptions-only · the **outstanding-chain footnote** — and until now that home was a phrase, not an artifact. It is now a document with a sign-off line.
  - **★ STATUS: SIGNED 2026-08-16 by Alex (CPA) — this IS the Tier 1 commit spec.** Line 92 reads `- [x]`, verified on disk before it was recorded here; the diff against the tracked copy was **exactly one character**, so nothing else in the document moved. Its own line 94 makes the signature the trigger, and the signature is now given.
    - **RECORDED AS ITS OWN COMMIT, NOT AMENDED INTO THE TRACKING COMMIT.** The unsigned-then-signed sequence is TRUE, and it is the more useful record: `513db72` correctly describes its own moment — a spec tracked in git while awaiting signature — and amending would rewrite that into a spec that was always signed, erasing an attestation as a dated event with an actor. This product keeps `period_signoffs` rows for exactly that reason; the spec's signature earns the same treatment.
    - **ONE FALSE START, WORTH KEEPING.** The signature was reported as given before it had reached disk (an unsaved editor buffer — `git diff` empty, mtime unchanged since before the tracking commit). Recording it then would have made the history assert an attestation the artifact contradicted, and the only way to reconcile them would have been for the assistant to tick the box. It was not ticked. **This spec supersedes the booking authority of seven verified drives; it does not start on an assumption, including a friendly one.**
  - **WHAT IT SUPERSEDES.** Descriptor-legibility confidence as a booking authority — the exact defect **(iv)** and the O85 specimen at line 255 recorded: a **never-seen** vendor auto-booked **$850 at 88** on name plausibility while a **twice-taught** vendor scored **78 and excepted**. Confidence survives as a display value only.
  - **NUMBERING — `O88` IS A DRIVE NAME, NOT AN ID.** §11 drives run O83–O87 and ROADMAP §2 ids run O4–O103 **in the same visual space**, and they already collide (`O87` is simultaneously the July drive and the `fiscalYearStart` UTC bug). The calibration work is tracked as **`O102`**; "O88" names the session and the spec file. **Do not read an `O##` in §11 as a ROADMAP row without checking.** *(Flagged the same week the `O90`/`O91` collision was cleaned up — the id-space check in §6 catches minting collisions, not this one.)*
  - **FIRST FINDING, BEFORE ANY CODE — THERE IS NO SUSPENSE ACCOUNT.** Rule 2 books every STRANGER line to **Uncategorized Expense**, and the chart has exactly one fallback: `7100 Miscellaneous Expense`. It **cannot be borrowed** — Tier 1 #7's acceptance test makes "Miscellaneous fallback on a recognizable vendor" a **hard fail**, so reusing it would delete the test's meaning. New account `7150` + `system_role 'uncategorized_expense'`, migration `063`.
  - **SEQUENCING DECISION — SHADOW MODE BEFORE THE SWITCH.** The ladder gets built and computes its verdict **alongside** confidence, recording what it *would* have booked, for at least one full drive before it books anything. Reason, stated once: **C198·3a shipped a gate that could never fire and a green suite could not tell.** A calibration rebuild that changes booking authority on seven months of verified behaviour, without first diffing itself against that behaviour on real statements, is the same bet at ten times the stake.
- **★★ O87 → C198·3c — SHIPPED, MERGED & LIVE (2026-08-11). All five findings closed; the batch grew two review-driven fixes, a corrected backfill and a workflow rule.** Merged to `main`, HEAD **`1f82dd9`** — three commits: **`d9b8638`** (the batch) · **`390ffae`** (D1+D3) · **`1f82dd9`** (the review bounce). **1,810 tests / 98 files green · `npm run build` clean · MIGRATIONS UNCHANGED, still applied & verified through `061`** — the follow-up-checked-update route held exactly as decided on 2026-08-09, so no RPC change and no new migration.
  - **(i) THE GATE CAN FIRE.** `payrollImportMetadata()` (pure) is stamped onto the posted entry by a `checkedRowUpdate` **after** `post_journal_entry` returns and **before** `loadAllData`, so the reload carries it. A failed stamp is audited **and** said out loud in the outcome line — the entry is correct either way; only the norm history is lost, and the next register then honestly reports that it *couldn't find* a prior run. **`depreciation.js` was genuinely inert on the identical mechanism and got the identical stamp** (D3 also made its schedule-flag write checked, §9) — its "the GL is the real guard" comment had been describing an intention, not the code, since the RPC has always dropped the key.
  - **★ THE BLAST-RADIUS CLAIM WAS HALF RIGHT, AND THE HALF THAT WAS WRONG MATTERED.** `matchPayrollBankLine` **is** structurally inert — but it is **not** what handled the June/July Gusto lines. `markAlreadyBooked` (date|amount|direction) dedup-matched them against the register's **Cr Cash leg**, and `planStatementPipeline` partitions `already_booked` out **first**, so those lines never reached `planPayrollBankLines`. Nothing needed fixing there. *(And "auto-matched at conf 100" was almost certainly the deterministic A/P clear at **99** — no path in the repo assigns a bank line a literal 100.)* **The real hazard was the opposite of the reported one:** stamping `kind:'payroll'` WAKES every reader of that key, and `flattenJournalEntries` copies one entry's `import_metadata` onto **every leg**, so the matcher's old `?? i.amount` fallback would have offered it four magnitudes to match — and a match **SUPPRESSES** the line. A Gusto tax-remittance ACH would have been swallowed as "already booked". Hence `net` in the stamp (a deliberate deviation from the decided shape) **and** the matcher now refusing that fallback outright. **Un-inerting a reader is only safe if you also give it the field it was written to read.**
  - **★★ THE REVIEW BOUNCE — the lesson of the batch.** The report claimed the backfill shape *"provably cannot start suppressing bank lines — pinned by a test."* Review restored the `?? i.amount` fallback, ran **`tests/payroll.test.js`** (the matcher suite), and watched it stay **green**. Both sides were slightly wrong and the correction is the point: **the pin did exist and did fail under mutation — but it lived in `payrollGateRoundTrip.test.js`, where nobody trying to falsify the matcher would ever run it.** Every `registerRun()` fixture in the matcher suite carries `net`, so the refusal branch was unreachable from all of them. Fixed by pinning it **where the matcher is tested**, with the real leg magnitudes (gross 4,000 · net 3,150 · employer 306 → legs 4,000 / 306 / 3,150 / 1,156, cross-checked against the REAL `buildPayrollEntry` so the fixture can't drift), and the mutation was **run, not asserted**: fallback restored → four assertions red → source restored → green. The guard comment now **names its own falsification test** and records that check.
    - **★★ LESSON — PROOFS LIVE WHERE A FALSIFIER WOULD LOOK FOR THEM.** A guard's test is not filed by which commit created it; it is filed by **where someone doubting the guard will go**. A proof in the wrong file is indistinguishable from no proof at all — it does not fail when the thing it protects is broken *by anyone who isn't already holding the map*. Sibling to the ·3a lesson (both sides of a contract tested against the same fixture): that one was a test that couldn't fail, this one is a test that couldn't be **found**. Both read as coverage.
  - **(ii) STATED PERIOD — SHIPPED, DEPLOYED, NOT YET PROVEN.** `statementPeriod` prefers the stated period **per side**, span as fallback; `period_end` added to both bank-parse profiles (it was never asked for, which is exactly why the side July got wrong was the side nobody read). Hardened past the original ask after review: well-shaped non-dates (`2026-13-45`) and **inverted** pairs fall back to the span (the span gave `min ≤ max` for free; one stated side can break it, and an inverted period silently kills `reconciliationCoversPeriod`), and the persist path now uses the validated start — `deriveStatementOpening` took it verbatim into a `date` column, where a rejected insert took statement **and** line persistence down behind a `console.warn`.
  - **(iii)** The completed-reconciliation detail no longer scores a match run that never happened; session-matched months keep their real count, and the auto-path number is **counted** off the books for that record's **own** bank account (no all-cash-codes fallback — that would have summed across accounts and double-counted cash-to-cash transfers on the one card whose job is to be exact) or renders an em dash.
  - **(v) + (D1)** `anomalySubjectPeriod` reads full dates out of f3 fingerprint tails, deliberately **not** gated on "no refs" — the live trio *had* refs that no longer resolved, which is why the old fallback was never reached. **D1 then carried the same fix to `anomalyTouchesPeriod`**, whose only consumer is the HIGH-blocks-sign-off gate: `duplicate_payment` is severity HIGH with a date-pair key, so the same dead-refs condition made the blocker silently **under-count**. The sweep failing leaves a note open; the blocker failing signs a month off over an unresolved HIGH. One fingerprint parser now feeds both, with a test pinning that they agree — two independent parsers of the same key would have been the (v) bug with the sides swapped. `rapid:` is excluded from SUBJECT and included in TOUCHES, and the asymmetry is deliberate: its key holds the window *start*, so retiring on it closes a note early (unsafe) while blocking on it blocks a month that genuinely holds the window's first charge (safe).
- **★ O108 — MIGRATION CHAIN vs LIVE: DIAGNOSED 2026-08-17, DOWNGRADED P1 → P2, AND ONE CLAIM RETRACTED. ▶ HOLD; no fix proposed.**
  - **★★ RETRACTION — CLAIM #2 WAS FALSE, AND THE CAUSE MATTERS MORE THAN THE CLAIM.** I recorded that `src/lib/constants.js` and the live chart were different COA generations — `6300` Marketing vs Repairs, `cogs` vs `cost_of_goods_sold`, `rent_occupancy` vs `rent`, Common Stock vs Owner Equity. **It is not true. `constants.js` matches live on every one of them** (`:28` `common_stock` · `:34` `product_revenue` · `:40` `cogs` · `:48` `rent_occupancy` · `:51` Repairs `6250` · `:52` Marketing `6300`).
    - **CAUSE:** I diffed **my own broken draft of `063`** against `044`, then reported the `063` side as "the client". That draft was built from `009` plus reconstruction — so what I actually measured was **`009` vs `044`**, and I labelled it `constants.js` vs live. A false attribution stacked on the original copy error, and published as **P1**.
    - **KEPT, NOT DELETED, ON PURPOSE.** A ledger that quietly loses its own false claims cannot be trusted about its true ones. The retraction is the record: this finding was wrong for two days, it drove a diagnosis session, and the same discipline that caught the bad migration — *diff it, do not trust the file* — is what caught the bad finding, one layer up.
    - **★★ AND THE CONVERSE RULE, RECORDED 2026-08-26 AFTER IT WAS BROKEN: A RETRACTION MUST BE SCOPED AS PRECISELY AS THE CLAIM IT WITHDRAWS.** An over-broad retraction **reads as diligence while deleting a correct finding** — it wears the costume of rigour and does the opposite. **Cost already paid, on `O113`:** I claimed both rate-limit walls sit at 20 files, then retracted it on discovering that the upload bucket had not bound in the observed run. But *"upload did not bind in that run"* and *"upload would not bind if `AI_LIMIT` were raised"* are different statements, and I used the first to withdraw the second. **The original claim was a counterfactual and was TRUE; it survived only because the operator restated it independently.** Before withdrawing a claim, state what it actually asserted — observed fact, mechanism, or counterfactual — and withdraw only that. A claim killed by evidence about a different claim is a claim lost to carelessness, and the ledger is no more trustworthy for it than one padded with false ones.
  - **★★ ORIGIN ESTABLISHED 2026-08-17 — `6520`/`6530` CAME FROM A HUMAN CORRECTION, NOT MACHINE INVENTION.** The live evidence: Jan 28 Toast booked at conf **85** with reasoning arguing for **6500 Technology & Software**, and Jan 31 bank fee at conf **70**, hedging **8000 vs 7100**, flagged *needs review*. Both now sit in accounts (`6520`, `6530`) their own stored reasoning contradicts. **`persistRecode` explains it exactly**: it UPDATES `journal_entry_lines.account_id` in place (App.jsx ~1290) and creates the target account first if absent (~1263) — so the line MOVES while the entry keeps its original `ai_reasoning`. The stale reasoning is the signature of an in-place recode. Then `learnFromCorrection` stored the mapping, and from **Feb 25 onward the AI proposes `6520` directly at conf 99** — the learned-mapping stamp. **The system worked as designed at every step; the defect is that creating two permanent accounts on a client's chart left no trace anywhere.**
    - **ONE CORRECTION TO THAT READING.** The deleted lines at `6500` (0 live / 1 deleted) and `7100` (0 live / 2 deleted) are **NOT** the recoded originals. An in-place recode moves the line and leaves the source account with **zero** lines, not a deleted one — so those three deleted lines are separate voided entries, unrelated to the correction story. Worth stating because the coincidence is seductive: `7100` having zero live lines is genuinely good news (Tier 1 #7's Miscellaneous hard-fail has no live instances) and it is not evidence for the recode narrative.
    - **DECIDED 2026-08-17 — `6520`/`6530` ARE BLESSED AS CANONICAL**, roles `merchant_processing_fees` and `bank_service_charges`, into both the seed and `constants.js` (→ `068`). They describe real, recurring costs for a restaurant client and were chosen by a CPA; the defect was never the accounts, it was that nothing recorded their creation.
- **★★ C201 — THE SHADOW EXECUTOR + migration `072` (2026-08-25). Both ▶ HOLD/unwired; shadow mode is OFF.**
  - **★★ "IT CANNOT BOOK" IS A PROPERTY OF THE FILE, NOT A PROMISE ABOUT IT.** `src/lib/shadowRun.js` performs **no I/O**: no client, no `fetch`, no `logAudit`. It imports exactly five pure modules — a test asserts the import list **by exact equality**, so a sixth import fails the suite — and every booking primitive is asserted **absent by name** (`persistJournalEntry`, `persistMultiLineEntry`, `post_journal_entry`, `bookToDb`, `buildJournalEntry`, `buildAccountInsert`, `ensureAccount`, `markBillPaid`, `checkedRowUpdate`). Amendment A §0 requires shadow mode to book nothing, and *"we were careful"* is not a mechanism.
  - **★ NO PHANTOM IS STRUCTURALLY POSSIBLE.** §4.1(1) makes one phantom an automatic fail; `proposeAccount` returns `null` for STRANGER **unconditionally**, so the executor cannot emit one. Pinned by a test that runs three unknown vendors and asserts all park.
  - **★ THE DIRECTORY RESOLVES A ROLE — AND A ROLE THIS COMPANY LACKS PARKS, IT DOES NOT FALL BACK.** Reaching `useAccounts`' `DEFAULT_BY_ROLE` (O108 finding 4) would return a plausible account for a role the company does not have, and `ensureAccount` would then **materialise it** — a shadow run creating accounts on eleven companies while claiming to book nothing. `directory_role_absent_from_chart` is its own park basis.
  - **EVERY PARK IS ATTRIBUTED.** `parkBasis` counts `stranger` / `no_attested_mapping` / `directory_role_absent_from_chart` / `no_directory_mapping`. Amendment A §5 requires the report to say WHICH vendors parked and why; a park rate without reasons is a number nobody can act on.
  - **MIGRATION `072`** — durable shadow records. `proposed_account_id` **NULL means PARKED** (sentinel-free, so the FK stays real and "declined to name an account" can never be confused with "named one that does not exist"); `run_id` makes §4.1(3) run-to-run drift a `group by` rather than a memory; `excluded_reason` is a **column**, because a row that is not there cannot be counted; **no jsonb blob** — every scored field is a real column, and `O95` is the standing proof of what a jsonb write boundary does to fields it does not recognise. A CHECK enforces **SCORED xor EXCLUDED**, and **no UPDATE policy exists**: editing an observation would make run-to-run drift indistinguishable from a correction. Re-run with a new `run_id` instead.
  - **TWO DESCRIPTOR COLUMNS (operator addition).** `descriptor_display` is what a human sees; `resolver_input` is the string **actually fed to identity resolution** — the RAW half for bank lines, the vendor field for READ sources. Storing only one makes a disagreement untraceable: you cannot tell whether the resolver was wrong or whether it was handed the wrong string.
  - **OWED VERIFICATIONS, tied to their blockers rather than blocking their migrations:** `066`'s VERIFY (d) — the cross-tenant write attempted as a NON-ADMIN and watched to fail — and `069`'s behavioural check both need the non-platform-admin account **ROADMAP TIER 1 #8** owes. `066` is not blocked on it; the verification is recorded as outstanding.
- **★★★ O88 — THE AUGUST DRIVE, ACTS 1–4 (2026-08-26). DRIVE STOPPED AT ACT 4. Three debts settled, four findings, two P0. Findings recorded; NO FIXES PROPOSED at the operator's instruction.**
  - **✅ THE O87 DEBT IS SETTLED — the payroll gate fired, and its stamp landed.** Both registers auto-posted with `import_metadata` present: `kind` / `gross` / `net` / `pay_date` / `register_import_id`. **This is the check unit tests structurally could not make**, and it is exactly how ·3a shipped inert for a release. The gate that had never once been able to fire, fired twice.
  - **✅ FIX (ii) VERIFIED LIVE.** `period_end = 2026-08-31` against a last transaction of `08-28`. Deployed 2026-08-17 and unverifiable until a month with a gap in it; a silent model failure would have looked identical to the old behaviour, and did not occur.
  - **✅ RE-UPLOAD SUPERSEDE HELD.** Statement 1 `superseded` with 17 booked; statement 2 active with 19 `already_booked`, 5 excepted; **anomalies emitted once, not twice.** Cash ties exactly: `54,880.41 + 23,482.65 − 11,555.90 = 66,807.16`.
  - **✅ THE ABSORBER HELD.** `account_materialized` = **0 rows** across 15 processed invoices, including vendors with no matching account. The O108 instrumentation ran on real volume and stayed silent because there was nothing to say.
  - **★★★ AND THE FINDING THAT MATTERS MOST — SPECIMEN 3 REPRODUCED, EIGHT MONTHS LATER, AT THE SAME CONFIDENCE.** `ALAMO FIRE & SAFETY LLC` — **never seen, no history, no census, no directory entry** — **auto-booked $425 to 6250 Repairs & Maintenance at confidence 88**, reasoning *"fire suppression inspections… facility compliance/maintenance costs."* No human was asked. **That is Culinary Edge again**: same shape, same score, same silent booking, eight months after the incident that founded the familiarity axis. Meanwhile **Bluebonnet — attested eight months running — excepted at ~78 for the eighth time.** **THE SYSTEM TRUSTS A STRANGER MORE THAN A VENDOR IT HAS BEEN TAUGHT EIGHT TIMES, AND THE TWO NUMBERS ARE 88 AND 78.** Recorded as **the C203 justification**: this is not an argument that the ladder would be nicer, it is a demonstration that the current authority is inverted, on live data, twice, with the numbers written down.
  - **★ P0-1 — THE RATE LIMIT BLOCKS ANY PAYING CLIENT. DESIGNATED `O113`.** 21 of 36 invoices died at HTTP 429. **The throttle is OURS** (`AI_LIMIT = 60`/user/hour, `ai-proxy/index.ts:17`, via `bump_rate_limit`, migration `021`), not upstream. **Three calls per file confirmed** — `classify-document` → `extract-invoices-batch` → `code-invoices-batch` — and **the "batch" profiles batch WITHIN a document, not ACROSS files**, while the queue processes one file at a time. The observed break at ~15 files implies a **fourth call the structural read does not account for; not pinned, and recorded as unpinned rather than guessed at.** A real restaurant is 40–80 invoices/month and onboarding wants 2–3 months, so **a new client's first action exceeds the limit by 3–10×.**
  - **★ P0-2 — THE DUPLICATE DETECTOR IS DETECTING THE INVOICE-TO-PAYMENT LIFECYCLE. DESIGNATED `O114`.** `findDuplicate` keys on normalised vendor + amount within 1% in a date window — and an invoice and the bank line paying it are **by construction** the same vendor and amount days apart. **The detector is right that they are one economic event and wrong about which kind.** The bank line already booked the expense; the invoice books it again, so **only the card stands between this and a double-counted P&L — and the natural answer, "New charge — book it", is the one that corrupts the books.** Wrong-by-default on the most common shape in small-business accounting. Bluebonnet ×4 flagged against last month's identical $145 delivery: same-vendor-same-amount is the NORMAL case for a weekly fixed-fee vendor. **P2 is downstream of this:** the deliberate Hill Country ±18 transposition was never tested because **there is no invoice-to-payment matching to mismatch on.**
  - **★ P1-1 — THE SUMMARY CONTRADICTS THE BOOKED ACCOUNT. DESIGNATED `O115`.** Queue says Lone Star *"as a client meal"*; ledger says 5000 COGS. `plainCategoryPhrase` (`clarify.js:165`) **re-derives a category from the entry's text** rather than reading the account already on the row — an independent guess about an answer sitting three feet away. Q2 world-claim, in the owner's seat.
  - **★ P1-2 — `Alamo Ice & Beverage` (CO2 tanks, bagged ice) booked to `7100` MISCELLANEOUS.** ROADMAP §0 TIER 1 #7's acceptance test names this exactly: *"Miscellaneous fallback on a recognizable vendor is a hard fail."* **First live instance.** Folded into #7 rather than given its own id.
  - **MAPPINGS THAT WERE RIGHT: 4 of 5** (5000 COGS ×3, 6250 ×1). Recorded because a finding list is not a verdict, and the categoriser is not uniformly wrong — it is wrong in a specific, structural way that `O102` names.
- **★★★ O88 — THE AUGUST DRIVE, ACT 7: THE PAYROLL GATE DISCRIMINATES (2026-08-26). 4 FOR 4, AND EACH REFUSAL NAMED ITS OWN CONDITION.**
  - **★★ TWO PASSES AND TWO REFUSALS ON *DIFFERENT* CONDITIONS — the first evidence the gate DISCRIMINATES rather than merely GATES.** An inert gate and a working gate both look like "nothing bad posted"; that is exactly how ·3a shipped. What separates them is a refusal that names a condition **and a different refusal that names a different one.**
    - **Register 04 (footing bait) REFUSED on condition 2** — verbatim: *"The register doesn't foot: gross $4,000.00 less withholdings $850.00 is $3,150.00, but it states net pay of $3,200.00."*
    - **Register 05 (out of norms) REFUSED on condition 5** — verbatim: *"Gross pay of $12,000.00 is well outside this company's usual $4,000.00 a run."*
    - **AND THE ATTRIBUTION IS PINNED, NOT ASSUMED:** 08/31 against a period end of 08/28 sits **inside the 7-day grace by design**, so the second refusal could only have been about norms. The condition was identified by elimination on the live artifact, not read off the copy.
    - **POST-CHECK:** `source='payroll'` and `entry_date >= 2026-08-01` returns **exactly two rows**, 08/14 and 08/28, both gross 4,000. **Nothing bad posted.**
  - **★★ THE COPY EXEMPLAR — BOTH CARDS ARE PURE QUERY-CLAIMS, IN THE HIGHEST-STAKES COPY IN THE PRODUCT.** They state the arithmetic and what the document says, and **draw no conclusion about why**. No *"may be fraudulent"*, no *"appears incorrect"*, no theory of the operator's intent. **RECORDED AS THE REFERENCE STANDARD for `O115`'s fix and for flag copy generally** (Q9 doctrine, `O98`): the machine reports what it computed and what it read, and the human supplies the meaning. Quote these two strings when writing any new flag.
  - **★★★ AND THE ONE WORTH THE MOST — THE WITHHELD ENTRY BALANCES AND IS STILL WRONG. THIS IS WHY CONDITION 2 EXISTS.** Register 04's refused entry **totals 4,306 on both sides**: it plugged Payroll Taxes Payable to **1,106** to absorb the 50 the register does not foot. **A DOUBLE-ENTRY SYSTEM WILL HAPPILY BALANCE A HALLUCINATED NUMBER.** Dr = Cr is a check on the ENTRY BUILDER, not on the DOCUMENT — the plug line is exactly where a balancing system hides a source error, and `tests/gaapInvariants.test.js` asserting (a) Dr = Cr would have passed on this entry all day. **FOOTING MUST BE CHECKED AGAINST THE DOCUMENT, SEPARATELY FROM BALANCING AGAINST ITSELF**, and no amount of double-entry rigour substitutes. Generalise it: **any entry with a plug line can absorb a bad input and look perfect.**
- **★★ O113 — THE FOURTH AI CALL DOES NOT EXIST. DIAGNOSIS CLOSED ON LIVE COUNTERS, 2026-08-26. A PRIOR CLAIM OF MINE IS RETRACTED.**
  - **THE QUERY (operator-authorised, read-only, Management API):** `rate_limit` for the drive user. **Drive hour `2026-08-25 23:00Z`: `ai` = 81 against a limit of 60 · `upload` = 18 against a limit of 20.** (Also `2026-08-26 04:00Z` ai=2, `2026-08-25 03:00Z` ai=9.)
  - **★ THE AI BUCKET BLEW; THE UPLOAD BUCKET NEVER TRIPPED.** 81 > 60, and 18 < 20. So the throttle that stopped the drive is unambiguously the **60/hr AI limit**, and the upload limit was never in play.
  - **★★★ THE RETRACTION WAS ITSELF WRONG, AND IS HEREBY WITHDRAWN. "BOTH WALLS SIT AT 20 FILES" IS TRUE.** On 2026-08-26 I retracted my own claim that both limits bind at 20 files and that raising `AI_LIMIT` alone would move the wall to the upload bucket. **The operator restated the claim independently the same day, and they are right.** Per file the pipeline spends **3 AI bumps and 1 upload bump**, so `60 ÷ 3 = 20` and `20 ÷ 1 = 20` — **the two limits are coincidentally the SAME constraint for the invoice path**, and raising one alone relocates the wall without moving it.
    - **WHAT WAS ACTUALLY FALSE was narrower than what I retracted.** The true statement is about the OBSERVED RUN: because the proxy bumps `ai` and returns 429 **before** reaching the upload bump (`ai-proxy/index.ts:41-49`), a call rejected on the AI check never touches the upload counter — so in the 81/18 event the upload bucket **was not in play**, and it could not have been. That is why it read 18 and not 20. **But "upload did not bind in that run" does NOT imply "upload would not bind if AI were raised", and I used the first to invalidate the second.** The original claim was a COUNTERFACTUAL and it survives: lift `AI_LIMIT` and the 21st file's classify call reaches the upload bump and is refused there.
    - **★ THE LESSON, AND IT CUTS BOTH WAYS.** §11 already records that *a ledger that quietly loses its own false claims cannot be trusted about its true ones*. **The same discipline forbids losing a TRUE one** — and an over-broad retraction is exactly that: it reads as diligence while deleting a correct finding. **A retraction must be scoped as precisely as the claim it withdraws.** Mine was not, and the operator's own reading is what caught it. The live counters corroborate the surviving claim rather than refuting it: **`upload` at 18 was TWO FILES from its own wall, independently.**

  - **★ THE ARITHMETIC CLOSES COMPLETELY — every number fits, and prior spend is now BOUNDED, not guessed.** With P prior AI calls in the hour, the classify call for file *n* is AI call `P + 3(n−1) + 1`, and the upload counter is bumped only when that classify PASSES. `upload = 18` therefore requires `18 ≤ (62 − P)/3 < 19`, i.e. **P ∈ {6, 7, 8}** — which lands exactly on the independently-derived floor of "at least 6 non-invoice calls" below. Acts 1–3 ran in the same clock hour and `handleBankFile` costs **2 calls** per statement. **Nothing is left unexplained, and no fourth call exists.**
  - **★ NO FOURTH CALL. THREE PER INVOICE FILE, PROVABLY** — `classify-document` (`App.jsx:3406`, the only upload-tagged site along with the spreadsheet path at `:4230`), `extract-invoices-batch` (`:3901`), `code-invoices-batch` (`:3950`). **The deficit was never a missing call; it was a SHARED BUCKET.**
  - **★★ AND THE COUNTERS PROVE THE SHARING ARITHMETICALLY, not by inference.** `upload` = 18 means **18 classify calls passed the AI check**. Those 18 files can account for at most 18 + (18 × 2) = **54** of the 60 successful AI calls. **So AT LEAST 6 of the 60 were NOT invoice-pipeline calls** — and that is a floor, since any file that died mid-pipeline consumed fewer than 3. Acts 1–3 ran in the same clock hour on the same user: `handleBankFile` is **2 calls** (parse + categorize) per statement, and the drive put through two statements plus payroll. **6 is exactly that shape.** The 60/hr limit is therefore not "20 invoices" — it is **"20 invoices minus whatever else you did this hour"**, which is not a number any user can predict.
  - **★ A REJECTED CALL STILL CONSUMES BUDGET.** `bump_rate_limit` (`021:36-41`) increments and returns the new count; the proxy checks `> AI_LIMIT` **after** bumping. The live evidence is the number itself: **81 means 21 calls were charged for doing nothing**, and the operator reported **21 invoices failed**. **Retrying digs the hole deeper**, and nothing in the UI says so.
  - **THE WINDOW IS A FIXED CLOCK HOUR** (`date_trunc('hour', now())`), not rolling — so the wall's duration is arbitrary: hit it at :05 and you wait 55 minutes; at :55, five. The 04:00 bucket showing `ai`=2 is the reset, visible in the data.
  - **★★ `O113` IS THREE SEPARATE DEFECTS, NOT ONE NUMBER (operator, 2026-08-26). Split so each can be fixed and closed on its own.**
    1. **`O113a` — REJECTED CALLS CONSUME BUDGET. A CORRECTNESS BUG IN THE LIMITER ITSELF, INDEPENDENT OF EVERY LIMIT VALUE. FIX FIRST.** `bump_rate_limit` increments and the proxy checks after, so a refusal is charged for. **`ai = 81` against a ceiling of 60 IS THAT BUG, VISIBLE IN PRODUCTION DATA** — 21 calls billed for doing nothing, against 21 invoices that failed. **It makes retrying ACTIVELY HARMFUL**, and nothing in the UI says so, so the user's most natural response to a 429 is the one that extends it.
    1b. **★★ AND THE DEEPER DEFECT IS UNPREDICTABILITY, NOT THE NUMBER (operator, 2026-08-26).** The AI limit is not *"20 invoices"* — it is **"20 invoices minus whatever else you did this hour"**, and **no user can compute that.** The same action succeeds or fails depending on invisible prior spend from a different feature. **Fixing the number does not fix this; only making the budget REASONABLE-ABOUT does** — either per-operation accounting, so a document costs a knowable amount, or a **visible remaining count**, so the ceiling is something a person can steer by. **No number proposed**; the shape is the requirement.
    2. **`O113b` — BOTH WALLS SIT AT 20 FILES.** 3 AI bumps and 1 upload bump per file → `60/3 = 20` and `20/1 = 20`. **Any limit change must move BOTH or NEITHER**; moving one alone relocates the wall to the identical file count and changes nothing a user could observe.
    3. **`O113c` — THE WINDOW IS A FIXED CLOCK HOUR, NOT ROLLING** (`date_trunc('hour', now())`). **The penalty for hitting the wall is therefore arbitrary: 55 minutes at :05, five minutes at :55** — the same mistake costs an order of magnitude more or less depending only on when it happened. The 04:00 bucket reading `ai = 2` is that reset, visible in the data.
  - **▶ HOLD ON `AI_LIMIT` — DO NOT TOUCH IT until `O113a` is fixed and the per-invoice call count is settled (operator, 2026-08-26).**

- **★ O113 PART (b) — THE PER-INVOICE CALL COUNT. PROPOSAL ONLY, NOTHING BUILT (2026-08-26).**
  - **★★ THE FINDING THAT REFRAMES IT: THE TWO BUCKETS CONSTRAIN DIFFERENT AXES, AND ONLY ONE OF THEM IS ABOUT CALLS.** The **AI** bucket scales with **calls per file** — consolidation moves it. The **UPLOAD** bucket scales with **FILES**, at 1 bump per file no matter how few calls each takes — **consolidation cannot move it at all.** So *"reduce the call count"* raises the ceiling only until it hits 20 files and stops. **A real restaurant is 40–80 invoices a month and onboarding wants 2–3 months, i.e. 120–240 files — so at `UPLOAD_LIMIT = 20/hr` a new client's onboarding takes SIX TO TWELVE HOURS of wall-clock waiting no matter how efficient the pipeline becomes.** The upload limit, not the AI limit, is the onboarding ceiling.
  - **★ CALLS 1 AND 2 SEND THE SAME DOCUMENT; CALL 3 SENDS NO DOCUMENT AT ALL.** `classify-document` (`max_tokens: 20`, one word) and `extract-invoices-batch` (`max_tokens: 4000`) are handed **the same bytes**, and **classification is a strict subset of what extraction must already determine** — a model that reads an invoice necessarily knows it is one. `code-invoices-batch` is different in kind: it takes **text plus the company's chart**, not the document.
    - **PROPOSAL 1 — MERGE 1 INTO 2.** One profile returning `{ doc_type, invoices: [...] }`; a non-invoice doc_type returns an empty array and routes exactly as today. **Saves one call per file AND one re-upload of the document bytes** (cost and latency, not just quota). Risk: the classifier's cheapness is a feature — 20 tokens to reject a bank statement, versus a 4,000-token extraction budget spent discovering the same thing. **Mitigation: keep the merged prompt's cheap path cheap by making doc_type the FIRST field emitted**, so a non-invoice terminates early.
    - **PROPOSAL 2 — DON'T MERGE 3; DELETE IT FOR VENDORS WE ALREADY KNOW.** Merging extraction and coding puts two jobs in one prompt and makes each less separately testable — the ·3a lesson. **The better move is that `O102`/`C203` make call 3 unnecessary for KNOWN and UNIVERSAL vendors by construction:** the ladder's whole claim is that a familiar vendor's account comes from attested history or the curated directory, **not from a model**. `App.jsx:4005` already skips the AI's answer when a `vendor_rule` exists — the mapping is read, not asked for. **So the calibration work is also the throttle fix**, and on a fixture where most invoices come from recurring vendors it removes most of the third call. **This is the one to prefer: it deletes work rather than compressing it.**
    - **PROPOSAL 3 — CACHE ON THE CONTENT HASH WE ALREADY COMPUTE.** C193 hashes document bytes (SHA-256, unique per company) to dedup the library. **Identical bytes re-uploaded currently re-extract from scratch.** Reusing the prior extraction costs nothing new to build the key for. Bounded value — it helps re-uploads, which the August drive did twice, not first-time volume.
    - **PROPOSAL 4 — BATCH ACROSS FILES, NOT WITHIN THEM.** The "batch" profiles batch invoices *inside* one document while the queue processes one file at a time, **so the axis that actually matters is not batched at all.** Multiple documents can share one Anthropic message. **Costed honestly: this is the only proposal that moves the UPLOAD bucket**, since N documents in one call is one upload bump — but it is also the riskiest (one malformed document degrades a whole batch, and `max_tokens: 4000` caps the batch size), and it changes what a per-file error means.
  - **★★ DECIDED 2026-08-26 — PROPOSAL 2 IS PRIMARY, AND IT IS SEQUENCED WITH `C203`, NOT AS SEPARATE WORK.** *Operator: deleting call 3 for KNOWN and UNIVERSAL vendors is **not compression, it is the ladder's claim made operational** — a familiar vendor's account comes from attested history, not from a model.* **RECORD IT EXPLICITLY: `C203` IS ALSO THE THROUGHPUT FIX. THE CALIBRATION WORK PAYS OFF TWICE** — once as booking authority (`O102`), once as call volume (`O113`). That is not a coincidence: a system that has to ask a model what it already knows is paying for its own forgetfulness, in both currencies.
  - **PROPOSAL 1 (merge classify into extract) — REJECTED FOR NOW, on my own risk note.** *Operator: the classifier's cheapness is the feature.* **20 tokens to reject a bank statement beats a 4,000-token extraction discovering the same thing, and merging trades a cheap guard for an expensive one.** ▶ REVISIT ONLY IF the AI bucket is still binding after proposal 2 lands.
  - **PROPOSAL 3 (content-hash cache) — ACCEPTED.** Cheap, bounded, and the drive proved re-upload is a real path (it re-uploaded twice). Reuses C193's existing SHA-256.
  - **PROPOSAL 4 (batch across files) — ▶ HOLD, and it needs its OWN failure analysis.** It is the only thing that moves the upload bucket, which makes it matter more than the others combined — **and one malformed document degrading a whole batch is exactly the failure mode this project keeps finding** (·3a's inert gate, C195(7)'s empty input, the QBO swallow). `max_tokens: 4000` caps the batch size anyway. **Design separately, later.**
  - **NO RECOMMENDATION ON `AI_LIMIT`/`UPLOAD_LIMIT` VALUES**, deliberately. Both are ours to choose, and choosing them before `O113a` is fixed would set a ceiling measured in a currency that still charges for refusals.
- **★★ O113a — THE LIMITER NO LONGER CHARGES FOR ITS OWN REFUSALS. MIGRATION `074` + `ai-proxy`, BOTH ▶ HOLD (2026-08-26).**
  - **`consume_rate_limit` READS, DECIDES, AND ONLY THEN CHARGES**, inside one plpgsql function and therefore one transaction — so "decide, then charge" is atomic without a second round trip, and `for update` serialises concurrent callers so two requests cannot both observe 59 and both be allowed. **`021`'s `bump_rate_limit` is DELIBERATELY KEPT, NOT DROPPED:** the edge function and the database deploy separately, so the old code must keep working until the new function ships and a rollback must not hit a missing RPC.
  - **▶ ORDERING IS LOAD-BEARING: APPLY `074` *BEFORE* DEPLOYING `ai-proxy`.** The reverse order calls a function that does not exist and fails **every** AI request — a total outage of the AI path, not a degradation.
  - **★ A SECOND INSTANCE OF THE SAME BUG, IN THE SAME FUNCTION, FOUND WHILE FIXING THE FIRST.** An upload-tagged call bumped `ai`, passed, then was refused by `upload` — **leaving the `ai` charge in place for a call that never ran.** Charging one bucket for a refusal issued by another is the identical defect wearing a different hat, which is why the new RPC takes an ARRAY and is **all-or-nothing across buckets**. VERIFY block (c) proves it.
  - **★★ THE VERIFY BLOCK MAKES THE LIMITER REFUSE SOMETHING AND SHOWS THE COUNTER DID NOT MOVE** — `after_refused = 10, NOT 11`, in a rolled-back transaction. **A limiter nobody has watched refuse *without charging* is a fix on paper**, the standard `071` was held to.
  - **THE PROXY NOW FAILS CLOSED (503) IF THE LIMITER CANNOT ANSWER.** Guessing permissively is how a limiter becomes decorative under exactly the load it exists for.
  - **THE 429 NOW SAYS WHICH BUDGET RAN OUT AND WHEN IT RESETS**, and the remaining budget is returned on SUCCESS too (`x-ratelimit-remaining-*`) because the limiter computed it to make its decision anyway. **This is a step toward the budget being reasonable-about, NOT the fix for it** — that needs a surface, and none is built here.
  - **BOUNDARY, STATED: this changes the limiter, not the limits.** `AI_LIMIT` and `UPLOAD_LIMIT` are untouched at 60 and 20, per the operator's instruction, and `O113b`/`O113c` remain open.
- **★★ O97 — ACCEPT-AND-QUEUE. `O118` MERGED IN; THE UPLOAD CEILING AND THE DURABLE QUEUE ARE ONE ITEM (2026-08-26). ▶ HOLD — DESIGN SESSION, NO CODE.**
  - **★★ THE DEFECT IS NOT THE CEILING'S HEIGHT — IT IS THAT THE CEILING IS ENFORCED SYNCHRONOUSLY WHILE A HUMAN WATCHES.** *Operator: 200 files in, 20 succeed, 180 turn red, and the owner concludes the product is broken.* **Onboarding is inherently a BATCH JOB — nobody needs January's invoices categorised in ninety seconds — and we are failing it in the foreground.** Raising the number would move the failure without changing its shape; the owner would still be watching.
  - **★ THE MERGE IS TECHNICAL, NOT ORGANISATIONAL: THE DURABLE QUEUE IS THE PREREQUISITE.** Established in code today — `fileStoreRef.current[id] = f` (`App.jsx:3743`) puts the **bytes** in an in-memory React ref · `logIntake` (`:3748`) durably records the **arrival** · `classifyFile` (`:3806`) makes the **first AI call** · `storeDocument` (`:3877`) reaches Storage **only afterwards**. **So the arrival is durable, the bytes are not, and the AI work starts before anything is persisted.** Acceptance and processing are **one synchronous act**, and accept-and-queue is **impossible** until the durable byte-write moves ahead of the AI work. That is precisely `O97`'s original defect, which is why this is one item and not two. *(`O118` is kept as a row pointing here rather than deleted — a merged id that vanishes reads as a lost item to the next sweep.)*
  - **PROMOTED FROM P2/UNSCHEDULED TO P0-FOR-ONBOARDING.** *Operator: the drive turned it from unscheduled into the thing between us and a first client.* The live orphan of 2026-08-06 20:11:42 was the same bug costing one file; onboarding is the same bug costing a customer's first impression.
  - **THE FIVE DESIGN QUESTIONS, RECORDED WITH THE ITEM AND DELIBERATELY NOT ANSWERED HERE:** (1) accept-and-queue — *partially answered above: not today, the byte-write must move first* · (2) what the owner sees while a queue drains, and the honest copy when it is six hours — **Q9 applies, and a progress bar that cannot state a finish time is a world-claim** · (3) failure mid-queue — stop, skip-and-report, or retry, noting `O113a` made retry safe only as of this week · (4) is per-user-per-clock-hour the right unit at all, given an onboarding and a monthly close share one budget · (5) **what is actually being protected — answered below, and it was the right question to ask first.**
  - **★★ (5) ANSWERED: THE PURPOSE IS STATED ONCE IN THE ENTIRE REPO, AS A SLASH.** §7: *"limits prevent abuse/runaway cost"*. Migration `021`'s header and `ai-proxy`'s header both state **what** the limits are and **never why**. **Three candidate threats, and the unit fits exactly one** — cost wants **tokens**, not requests (a 20-token classify and a 4,000-token extraction cost the same against the budget, a 200× miscalibration); upstream wants **org-scope and per-minute** (ten users at 59/hour can spike one minute while all are "under"); **abuse wants per-actor-per-hour, which is what we have.** **So the mechanism is right for abuse and wrong for the other two — and it is currently refusing a legitimate first-time onboarding, which is none of the three.** Recorded as the §9 sibling rule: **name the threat, the way you name the reader.** **And the design consequence follows directly: if the threat is abuse, a legitimate bulk workload needs A DIFFERENT DOOR, NOT A BIGGER NUMBER.**
- **★★ O114 RE-DRIVE PREP — THREE FINDINGS FROM READING THE LIVE DATA INSTEAD OF THE FIXTURE (2026-08-27). One is a real defect, one is a correction to my own record.**
  - **★★ `ROMA CHEESE + DAIRY CO` — THE `+` IS NOT NORMALISED, SO ONE VENDOR HOLDS TWO KEYS.** August carries **two** Roma bank lines: `ACH DEBIT - ROMA CHEESE & DAIRY CO` (08-04, 551.20) and **`ACH DEBIT - ROMA CHEESE + DAIRY CO` (08-19, 538.75)**. `normalizeDescriptor` maps `&` → `and` and **leaves `+` as a token**, so the keys are `roma cheese and dairy` and `roma cheese + dairy` — `identityRelation` returns **NONE**. Same vendor, same rail, same month, two identities. **DESIGNATED `O119`.** *(And note the shape: this is exactly the C200 corpus problem again — real descriptor variance that the fixtures did not contain. The fixture had one Roma string; production has two.)*
  - **★★ MY ALAMO FIRE TEST FIXTURE WAS WRONG, AND THE FIX I JUSTIFIED WITH IT ADDRESSES NOTHING IN THE REAL DATA.** `tests/invoicePayment.test.js` uses `"ACH DEBIT - ALAMO FIRE SAFETY LLC"`. **The real descriptor is `ACH DEBIT - ALAMO FIRE & SAFETY LLC`** — it HAS the ampersand, so both sides normalise to `alamo fire and safety` and relate **EXACT** with no help needed. I recorded the `and`-dropping in `compareKey` as *"found by this feature's own suite on a real specimen"*. **It was found on a string I invented.** The rule stands on its own merits (a bank that drops an ampersand is a realistic variant, and the guard is comparison-only with the anti-merge cases pinned either way) — **but it fixes no case present in this data, and it does NOT fix the `+` case either** (`[roma,cheese,dairy]` vs `[roma,cheese,+,dairy]` is not a prefix). **Recorded as a correction because "found on a real specimen" was the claim that made it credible, and it was false.** Same lesson as C201's assumed-vs-real invoice strings, repeated inside two days.
  - **★ THE OPERATOR'S STATE SUMMARY WAS OFF, AND THE QUERY IS WHY WE KNOW.** Reported as *"19 lines booked, 12 of 36 invoices booked, 10 unanswered clarification cards"*. Live: **17 bank lines**, **5 booked invoices**, and **ZERO open anomalies** (24 dismissed, 52 resolved). The clarification queue is React state, so the "10 unanswered cards" did not survive the session — there is nothing to clear. **Only ONE of the 5 booked invoices is a specimen** (Hill Country 08-06, 468.50). *Not a criticism — a demonstration of the standing rule that estimates about production data are claims about a query nobody ran.*
- **★ O120 / O121 — WHICH CARDS EARN AN INTERRUPTION, AND A TRUST HEADER THAT CANNOT SEE THEM (2026-08-27). ▶ HOLD, no design.**
  - **★★ THE TENSION IS THE ITEM.** Clarification cards sit below the fold and can be scrolled past — **10 sat unanswered in the August drive while the header said *"your books are correct and up to date"***. But the obvious fix fights the product: **the stated principle is BOOK EVERYTHING AND BATCH THE JUDGMENT TO CLOSE**, and a modal per card is its opposite — it stops the world per item and **turns a queue you can zip through into a gauntlet**. So the question is not *"modal or not"*, it is **WHICH CARDS EARN AN INTERRUPTION.**
  - **CANDIDATE SPLIT, recorded as a candidate:** a card **whose wrong answer books silently wrong** may interrupt; anything **recoverable** stays in the batch. **The `O114` lifecycle card is the archetype of the first** — *"different purchase"* mints a phantom payable, *"same purchase"* suppresses a real charge, and **the more dangerous of the two leaves nothing on any screen.** A low-confidence category question is the archetype of the second: wrong is visible, and a recode undoes it. *(Note this reuses the same discriminator as the `O114` option ordering — VISIBLE-vs-SILENT failure. That is now the third place that distinction has decided a design, which is a sign it is load-bearing rather than local.)*
  - **★★ AND THE HALF THAT IS NOT UX AT ALL — `O121`, established in code.** `ownerTrustState` (`ownerTrust.js:35`) accepts `controlTotals`, `openConfidenceFlags`, `intakeRows`, `unknownDocs`, `reviewedThrough`, `bankMatch`, `openHighAnomalies` — **and no clarification-queue input.** It can therefore return `all_clear` and *"your books are correct and up to date"* **while N documents sit unbooked awaiting an answer.** The one indirect path — an unanswered card leaves its intake row `held_for_review`, and `intakeRows` IS an input — **only reaches the header through the completeness net's staleness window, so a green header beside FRESH unanswered cards is structurally possible.** Whether the header is right is **incidental, not enforced.**
    - **THIS IS THE C194 / `O98` FAMILY, NOT A UX PREFERENCE:** a false green on the surface whose whole job is to be trustworthy, and a world-claim derived from a query that never asked the question. **The function was hardened against exactly this twice already** — its own comments name the O83 false *"Nothing wrong"* and the bank-overdue false green — **and the clarification queue is simply the net nobody wired in.** The operator's instinct that this is the cheaper fix is right, **and it is also the one that is required independently**: the header should stop claiming correctness while cards are open whatever is decided about interrupting.
- **★★★ O122 — THE CARD RATE IS A SYSTEM-LEVEL ACCEPTANCE CRITERION, NOT A FEATURE (2026-08-27). THE PROMISE IMPLIES A NUMBER NOTHING CURRENTLY MEASURES.**
  - **★★ THE GAP, AND IT IS THE MOST IMPORTANT THING ON THIS PAGE: "ALL BEHAVIOURS CORRECT" AND "THE PRODUCT IS USABLE" ARE DIFFERENT CLAIMS, AND ONLY THE FIRST HAS EVER BEEN TESTED.** Every drive, every suite, every acceptance doc in this repo scores INDIVIDUAL behaviours. **We can therefore end with every behaviour correct and a screen carrying nine cards — which is not a usable product.** The promise is *"drop anything here — your AI controller handles the rest"*, and that sentence implies a **CARD RATE**. Nothing measures it. `tests/` cannot: a unit test asserts one card is right, never that there are few enough of them.
  - **★★ THE TARGET IS NOT ZERO — IT IS THE CONTROLLER'S OWN QUESTION RATE.** *(Operator, verbatim: **"the target card rate is the controller's own question rate, not zero."**)* A controller who asks nothing is rubber-stamping; one who asks nine questions about a 60-document month is drowning the client. **So the test for any INDIVIDUAL card is: WOULD A COMPETENT CONTROLLER HAVE ASKED THIS, OF THIS CLIENT, THIS MONTH?**
  - **★★★ AND THE RULE THAT DOES THE WORK — *(verbatim)*: "A CARD THE USER SEES EVERY MONTH IS A BUG WEARING A QUESTION MARK."** Formally: **any card that can recur monthly for the SAME VENDOR on the SAME QUESTION is a design defect BY DEFINITION** — not a tuning preference, not a P3, a defect. **The rule has teeth, and the proof is that it RETROACTIVELY RECLASSIFIES TWO KNOWN ITEMS:**
    - **`O117`** (weekly fixed-fee vendor flagged duplicate every week) stops being a P1 annoyance and becomes **definitionally a defect**.
    - **The `C203` justification itself** — Bluebonnet excepted at ~78 for **eight consecutive months on the same question** — was recorded as evidence that confidence is miscalibrated. **Under this rule it is simply a bug, and was one from month two.** *(That reframing is worth more than the calibration argument: it needed no theory of confidence at all.)*
  - **THE THREE CATEGORIES — every existing and future card type gets tagged, and they are NOT the same thing stacked on one screen:**

    | # | category | expected rate | nature |
    |---|---|---|---|
    | **1** | **BUG** — *"we recorded more than one payment of $462.85"* when there was exactly one. **No controller would ever say that.** | **ZERO** | a fix, **not a design question** |
    | **2** | **ONE-TIME TEACHING** — Franklin Ave's alias; Bluebonnet's two-month graduation under Amendment B. A controller asks these **once, on day one, and never again.** | **onboarding cost, NOT monthly cost** | **each must be askable ONCE and then never re-asked** — if it can re-ask, it is category 1 |
    | **3** | **GENUINE JUDGMENT** — Hill Country's invoice at 468.50 against a payment of 486.50. **A real bookkeeper stops on that.** | **forever, and RARE** | the residue that proves the system is thinking |

  - **STARTING TAXONOMY, applied to what actually exists today** *(this is the tagging, not a promise to tag later)*: `lifecycle/amount_differs` **3** · `lifecycle/identity_differs` **2** (closes with `O111`) · `lifecycle/multiple_candidates` **3** genuinely, **1** when produced by the flatten-dedupe defect · `duplicate_payment` **1** on lifecycle pairs (`O114`) and **1** on weekly vendors (`O117`), **3** only for a true double-pay · `gl` low-confidence category **3**, shrinking toward **2** as vendors reach KNOWN · `gaap` capitalise-vs-expense **3** · `direction` revenue-vs-expense **2** (the company's own identity settles it — O75) · `large_transaction`/`round_number` **3**, but **1** while they fire on payroll (O85(c)) · `missing_recurring` **1** while it measures against `now()` instead of period context (O86) · `category_spike`/`vendor_spike`/`rapid_sequential` **3**.
  - **STEADY-STATE TARGET — established client, all vendors KNOWN, aliases learned, no bugs: UNDER 5% OF DOCUMENTS.** For a 60-document month that is **0–3 cards**. *(Operator's working number, recorded as a working number.)* **Onboarding gets its OWN, HIGHER, SEPARATE number** — conflating them would either make onboarding look broken or make steady state look fine.
  - **★ MEASURE IT ON EVERY FUTURE DRIVE, SPLIT BY CATEGORY.** Not a total — **a split**, because the whole question is **whether category 2 actually shrinks month over month rather than being assumed to.** A falling total with a flat category 2 means the teaching is not sticking, and that is invisible in an aggregate. **This is now a standing drive deliverable.**
- **★★★ O114 RE-DRIVE §1 FAILURE — ROOT-CAUSED 2026-08-27. ONE MISNAMED PARAMETER, AND A CARD THAT LIED ABOUT WHY. BOTH MINE.**
  - **★★ THE ROOT CAUSE: `checkedRowUpdate` takes `patch`. The O114 attach passed `values`.** So `patch` arrived `undefined`, `.update(undefined)` did nothing, `recordFailure` fired, and **every attach failed** — `import_metadata` is `NULL` on all three targets and there are **zero `invoice_attached` audit rows**. **13 of 14 checked-write call sites in the repo had it right; mine was the only outlier.**
  - **★★★ AND THE FAILURE PATH REPORTED A FALSE CAUSE, WHICH IS THE WORSE HALF.** I had written the fallback to ask instead of half-attaching — correct — but gave it `reason: MULTIPLE_CANDIDATES`, which renders as ***"We recorded more than one payment of that amount."*** **That is a statement about the LEDGER, and it was false: there was exactly one payment.** All three cards said it, identically, and **the drive became undiagnosable from its own output.** It cost a full session: the operator hypothesised duplicate legs from `journal_entry_lines`, then duplicate JEs from the supersede path; I shipped an entry-level dedupe; both of us were chasing a candidate-counting bug **that did not exist**, because the card invented a cause.
    - **THE RULE THIS EARNS: A CARD MAY REPORT THAT WE FAILED. IT MAY NOT INVENT A REASON IN THE LEDGER.** `ASK_REASON.RECORD_FAILED` now exists precisely so the honest sentence has somewhere to live — *"…matches a payment we already recorded on X. We couldn't save the link between them, so we haven't recorded anything."* **Q9 says an empty result may only be reported as an empty result; this is its twin — OUR failure may only be reported as OUR failure.** Reusing a neighbouring reason code because it was already there is how a diagnostic becomes a decoy.
  - **★ WHY NOTHING CAUGHT IT, AND THE GUARD THAT NOW DOES.** `tests/invoicePayment.test.js` proves `planInvoiceArrival` returns ATTACH; **nothing asserted the CALLER then successfully attaches.** That is the ·3a gap exactly — both sides of a contract tested against the same fixture, with the boundary between them untested. **Reproducing the real ledger through the real function returned ATTACH for all three specimens**, which is what proved the defect was in the wiring rather than the logic. New guard in `tests/checkedWrite.test.js` (where a falsifier of the CALLING CONVENTION would look, not in this feature's suite): **every `checkedRowUpdate`/`checkedIdsUpdate` call site in `src/` must name its payload `patch`.** Mutation-verified — restore `values:` → red; restore → green. It also had to learn two things about itself: **ES6 shorthand `patch,` is valid** and must pass, and **the helper's own definition is not a call site** — a guard that flagged the contract it protects.
  - **AND THE OPERATOR'S TWO HYPOTHESES WERE BOTH WRONG AND BOTH WORTH HAVING.** *Duplicate legs:* refuted — `flattenJournalEntries` emits ONE row per ≤2-line entry; the two-row result came from SQL joining `journal_entry_lines`, a property of the query. *Duplicate JEs from the supersede path:* refuted — one live entry each for Toast and Alamo Fire. **But the first hypothesis exposed a REAL latent defect in the >2-line branch, which is now fixed and pinned.** A wrong hypothesis that surfaces a real bug is a good turn; the thing that wasted the session was my card, not their theory.
- **★★★ THE VOID PATH POSTED THREE REVERSALS AGAINST ONE INVOICE (2026-08-27). ROOT CAUSE: `O95` — THE `p_meta` DROP — CLAIMING ITS THIRD VICTIM, AND THE FIRST ONE THAT FAILS *OPEN*.**
  - **ONE BUG, THREE SYMPTOMS.** `reverseJournalEntry` posts with `p_meta: { kind: "reversal", reverses: origId }`. **`post_journal_entry` cherry-picks six named scalars and never writes `import_metadata` at all** — confirmed live: **all three reversals carry `import_metadata = NULL`.** Net effect on Hill Country: one 468.50 debit against **three** 468.50 credits = **−937.00**. *(Timing: 03:24:20, 13:05:29, 13:05:49 — the last two twenty seconds apart. Textbook "nothing happened, click again".)*
  - **★★ DEFECT 1 — VOID IS NOT IDEMPOTENT, AND THE GUARDS THAT WOULD MAKE IT SO ARE STRUCTURALLY INERT.** CR-17 built **two**: `alreadyReversed` (`ledger.js:200`) reads `r.import_metadata.reverses`, and a DB probe (`App.jsx:2394`) filters `.eq("import_metadata->>reverses", …)`. **Both key on the field the RPC discards, so both are permanently false.** The guards are well-written, deliberate, and have never once fired.
    - **★★★ AND THIS IS THE FIRST `p_meta` VICTIM THAT FAILS OPEN.** Payroll failed CLOSED (asked a human — a missed feature). Depreciation failed CLOSED (posted nothing). **This one CREATES WRONG ENTRIES, one per click, with no ceiling.** The class just graduated from "features that silently do nothing" to "a control that silently does damage".
    - **★★ THE CODE COMMENT ASSERTS THE OPPOSITE, AND CITES THE FIX THAT WAS NEVER APPLIED HERE.** `App.jsx:2407-2409`: *"Post the reversal WITH its link metadata in the SAME atomic RPC (p_meta persists to import_metadata — the depreciation guard relies on the same contract). No separate, swallowable update: the idempotency marker is written iff the entry is posted."* **Every clause of that is false**, and it was written as the JUSTIFICATION for not using a follow-up checked update — the exact remedy ·3c applied to payroll and depreciation. **·3c's D2 sweep listed `tax_amount` and `signedPeriod`'s `original_date` as `O95` victims and MISSED the reversal marker.** An incomplete audit of `import_metadata` readers, and the miss is recorded here rather than quietly corrected.
  - **★★ DEFECT 2 — THE UI CANNOT DISTINGUISH REVERSED FROM LIVE, AND THE FEATURE THAT WOULD FIX IT WAS ALREADY DEFERRED *AND* WOULD ALSO HAVE BEEN INERT.** The original staying visible is **BY DESIGN** (§12 #14 — reverse, never delete; the comment at `App.jsx:2374-2379` says so explicitly and is right to). **But nothing marks it.** §12 already defers precisely this: *"Reversal display marker — show the original struck-through / 'reversed on DATE' when a live reversal links to it (display annotation only via `import_metadata.reverses`)"* — **which keys on the same never-written field.** So the one mechanism that would have stopped the repeat-clicking was unbuilt, and had it been built it would have shipped inert. **Three-way interlock: the marker is missing, the guard is inert, and the same dropped key causes both.**
  - **CORRECTION TO THE REPORT, ON THE EVIDENCE.** The toast was reported as saying *"deleted"*. The VOID path's toast says **"Reversed … tap Undo to restore"** and its confirm modal says *"It stays in the audit trail and posts a reversing entry"* — **that copy is accurate.** The audit log shows **three `entry_reversed` and ZERO `invoice_deleted`**, so the void path was used all three times and delete was never invoked. **The substance of the finding stands** — the action reported success and nothing visibly changed — but the copy is not where it went wrong.
  - **★ DEFECT 3, FOUND WHILE LOOKING FOR DEFECT 2 AND REAL INDEPENDENTLY: `softDeleteInvoices` CLAIMS SUCCESS UNCONDITIONALLY.** It shows *"Deleted {label} — tap Undo to restore"* **without ever checking `allIds.length`**, while `softDeleteJournalEntry` returns `[]` on a signed-period block or a DB error — **and the function's own comment (`App.jsx:2370`) says callers gate "✓ done" on a non-empty result (O78). Its own toast does not.** Not what happened here (August is unsigned; the guard never fired) but the same false-success class as C194, one surface over.
- **★★ O125 — THE DISPLAY LAYER HAS ITS OWN VENDOR IDENTITY, AND IT IS A THIRD IMPLEMENTATION OF THE SAME CONTRACT (2026-08-27, from the InvoicesView cleanup screen).**
  - **ONE ROOT CAUSE: `flattenJournalEntries` (`ledger.js:31`) derives the vendor as `description.split(" – ")[0]` — a raw string split on an EN-dash.** That is an identity decision, made in the display layer, by punctuation.
  - **THREE SYMPTOMS, ALL ON THE SCREEN THAT IS THE ONLY ROUTE TO DELETE:** *(1)* **GUSTO RENDERS AS FIFTEEN SEPARATE VENDORS** — payroll descriptions use an **EM-dash** before the period (`Gusto Payroll — 2026-08-15`), so the en-dash split misses and **the pay period becomes part of the vendor name**. *(2)* **A TRAILING PERIOD SPLITS A VENDOR IN TWO** — `Hill Country Milling Co` and `Hill Country Milling Co.` are separate chips, likewise Roma. *(3)* **REVERSALS GET THEIR OWN VENDOR** (`REVERSAL: Hill Country Milling Co.`) instead of filing under the entry they reverse.
  - **★★ `entityKeyFor` ALREADY HANDLES ALL THREE CORRECTLY** — it normalises trailing periods, and the per-source strategy (C201) exists precisely because the em-dash case bit us once already. **This is the SAME defect C201 found in the payroll descriptor split, resurfacing in the UI because the UI never used the resolver.**
  - **★★★ AND THAT MAKES THREE IMPLEMENTATIONS OF VENDOR IDENTITY IN ONE CODEBASE:** *(a)* `entityKeyFor`/`identityForEntry` — the canonical one, anti-merge-tested · *(b)* `autoMatchBankLines`' `normalizeName` + **two-way substring containment** — already recorded as owed after O114 §2.1 · *(c)* **this string split.** **The operator's framing is the right one: this is exactly what `065` was released for — the vendor backfill *"cannot be SQL"* because it would have been a second implementation of the identity contract.** We declined to write one in SQL and shipped two others in JS. **The fix is not to improve the split; it is to delete it and call the resolver**, and reversals should file under the reversed entry's vendor (which needs `import_metadata.reverses`, i.e. `O123`'s fix, so the two are coupled).
- **★★ O126 — DELETE BELONGS ON THE DETAIL PANEL, AND VOID MUST BE VISIBLY TERMINAL. ONE FUNNEL FROM BOTH ENDS (2026-08-27).**
  - **(A) WIRE `softDeleteInvoice` TO `TransactionDetailPanel`**, same confirm treatment as the Books-list control. That panel is **the surface a user reaches by clicking an entry they intend to deal with**, and today it offers **Mark as Paid / Full entry / Void** — with Void the only destructive option and the one that compounds `O123`. **The safe action sitting four steps deep behind *"View all invoices for X →"* is not a discoverability nitpick: it is what produced three reversals.**
  - **(B) VOID MUST BE IDEMPOTENT *AND* VISIBLY TERMINAL.** Fixing the `O95` marker so the two existing guards actually fire is **necessary and not sufficient**: an already-reversed entry must **show as reversed in the transactions list**, and its Void control must be **DISABLED, not merely refusing on click**. A guard that rejects on click still teaches the user that clicking is how you find out — and the click is free only because we already lost the marker.
  - **THEY ARE ONE FUNNEL: the safe action is hidden and the dangerous one is unmarked, so the user is routed to the destructive path and given no signal to stop.** Both are instances of the §9 rule this session earned — **an action whose effect is invisible will be repeated** — and so is `O124(c)`.
- **★★★ THE O114 RE-DRIVE, SCORED (2026-08-27). §1 PASSES · §2 FAILS · THE ANTI-VACUITY CHECK EARNED ITS ENTIRE EXISTENCE.**
  - **✅ §1 PASSES, VERIFIED IN THE DATABASE NOT THE UI.** Roma (551.20), Toast (462.85) and Alamo Fire (425.00) attached **silently, no card**, and all three carry `invoice_attached=true` with an `attached_invoice_id`. **Franklin Ave raised the identity card and did NOT attach** — the outcome §1 marks as a failure if inverted. **Hill Country raised the amounts-differ card stating "$18.00 more than the invoice" and named no cause.** The order-independence bug is closed on the specimens it was written for.
  - **★★★ §2 FAILS, AND IT FAILED IN THE EXACT WORDS THE CRITERION USED.** §2 was written as: *"IF BLUEBONNET'S CARDS VANISH, SOMETHING OVER-MATCHED — the single most likely way for a wrong implementation to LOOK BETTER than a right one."* **Four were required. Three remain.** Invoice `BLS-88412` (08-03) attached to the payment of **07-27** — **seven days earlier, exactly on the `WINDOW_BEFORE_DAYS` boundary.**
    - **THE EVIDENCE: Bluebonnet pays WEEKLY at exactly $145.00** (07/06, 07/13, 07/20, 07/27). So the candidate set held **FOUR entries identical in vendor and amount to the cent**, and **only the date window separated them** — leaving exactly one, which then satisfied *"one certain candidate → ATTACH"*. **THE CERTAINTY WAS AN ARTEFACT OF THE WINDOW, NOT OF THE EVIDENCE:** at ±14 days it would have asked; at ±3 it would have booked a payable. **A threshold decided it, not the documents.**
    - **AND IT FAILED SILENT — the direction the whole design exists to avoid.** The 08/03 delivery's expense is **suppressed**, the 07/27 payment is claimed by an invoice that is not its own, and **nothing on any screen says so.** → **`O127`, P0.**
    - **★★ IT IS `O117` WEARING THE `O114` COSTUME.** One root property: **for a flat-fee recurring vendor, amount and identity carry NO information**, so the decision rests entirely on a date window. O117's version emits a noisy card; **this version silently deletes a real charge.** The two items were split on 2026-08-26 as different failure modes of the duplicate detector — **they are now confirmed as one property surfacing in two subsystems**, and `O127`'s fix should be designed with `O117`, using the cadence and amount band `vendor_state` already holds.
  - **★★ THE METHOD POINT, AND IT IS THE ONE TO KEEP: A PRE-REGISTERED CRITERION CAUGHT A DEFECT THE RESULT LOOKED TOO GOOD TO CONTAIN.** Four attaches against a `>= 3` threshold reads as a clean beat, and it was reported that way. **§2 existed only because it was written BEFORE the data, naming a specific vendor and a specific direction of surprise.** Without it, the over-match would have been recorded as a bonus — **and the missing card is easier to miss than a wrong one, because absence has no pixel.** *(This is the §9 invisible-action rule at the level of TEST DESIGN: an assertion about something that must still be there is the only kind that catches a silent deletion.)*
  - **CARD RATE (`O122`) — 9 documents, 5 cards: category 1 = 3 (Bluebonnet duplicates, `O117`) · category 2 = 1 (Franklin Ave identity → `O111`) · category 3 = 1 (Hill Country's $18 gap).** Steady state for this set is **1 card** once `O117`, `O111` and `O127` land. **First drive scored under `O122`, and the split immediately paid: three of five cards are defects, which a total of "5 cards" would have hidden.**
  - **`O128` — THE SUCCESSFUL ATTACH ANNOUNCES ITSELF AS A NULL RESULT:** the queue reads **"✓ 0 invoices booked · $0.00 total"**. Technically true and read as *nothing happened*, at the moment the best possible outcome occurred. **The most valuable behaviour this feature has, described as a zero.**
- **★★ O127 SEQUENCED WITH O117, AND THE PROPERTY STATED (2026-08-27). ▶ HOLD — do not tune the threshold.**
  - **THE PROPERTY, in the operator's words and worth keeping exactly: FOR A FLAT-FEE RECURRING VENDOR, AMOUNT AND IDENTITY CARRY NO INFORMATION.** Everything the matcher normally reasons from is **constant**, so **any decision it reaches is being made by whatever variable is left — which today is a date window nobody chose for this purpose.** Bluebonnet: four payments, one vendor, $145.00 to the cent, weekly.
  - **★ SO THE FIX IS NOT A BETTER WINDOW. ▶ EXPLICIT CONSTRAINT: DO NOT TUNE THE THRESHOLD.** Widening it produces more ambiguity, narrowing it produces more payables, and **neither adds a bit of information** — the two documents remain indistinguishable on every axis the matcher reads. **Tuning would convert a diagnosable failure into a quieter one.**
  - **THE VENDOR CLASS NEEDS A DIFFERENT DISCRIMINATOR ENTIRELY, or must always ask.** Candidates, not a design: **invoice number** (`BLS-88412` is unique per delivery and we already store it) · **a delivery/service date on the document**, distinct from the invoice date · **sequence** (the Nth invoice matches the Nth payment) · or **detect the class and ALWAYS ASK**, which is honest and costs one card per delivery. **`vendor_state` already holds the amount band and the cadence needed to RECOGNISE the class** — that recognition is the shared half, and it is why this is one design with `O117` rather than two.
  - **WHY ONE ITEM AND NOT TWO: two thresholds where one rule belongs is how this gets worse.** `O117` and `O127` are the same property surfacing in the detector and in the matcher; designed apart they would grow two independent notions of "recurring", and the next symptom would get a third.
- **★★ O97 STEP 2 — THE DRAIN, PLANNING HALF (2026-08-28). NO MIGRATION, AND THE REASON IS EVIDENCE.**
  - **★ THE MIGRATION QUESTION, ANSWERED FROM THE LIVE TABLE: NO COLUMNS NEEDED.** `document_intake` already carries every piece of retry state — **`document_id`** says the bytes are durable (the gate step 1 opens), **`status`** is the state machine, **`updated_at`** is BOTH the lease and the spacing gate, **`received_at`** is the age. **And `document_intake_company_status_idx` is already exactly the drain's selection path**, so not even an index.
    - **`attempts` — CONSIDERED AND DEFERRED, with the trigger recorded rather than skipped silently.** It buys a nicer *report* (*"we tried 9 times"* vs *"we retried for 36 hours"*), not a correct *decision*; both statements are true and only one costs a column, and it is additive with a default whenever the reporting proves inadequate.
    - **`next_attempt_at` — REJECTED OUTRIGHT.** The thing we wait for is an AI budget that **resets on a clock hour**, so a fixed interval derived from that IS the schedule; a per-row time column would store the same number redundantly.
    - **★★ AND THE DEEPER REASON THERE IS NO COUNTER: TRANSIENT-vs-PERMANENT COMES FROM THE ERROR, NOT FROM A COUNT.** A 429 is transient however many times it happens; an unreadable file is permanent on the first try. A counter would only bound repeated *transient* failure — which the age box already does.
  - **★★★ AND THE NUMBER THAT MATTERS MORE THAN THE SCHEMA: `document_id` IS NULL ON ALL 150 LIVE ROWS.** Every one predates step 1, so **the drain's real input is EMPTY today** and stays empty until documents arrive under the new ordering. Two consequences, both recorded:
    1. **THE 21 `failed` ROWS FROM THE AUGUST DRIVE ARE PERMANENTLY UNRESUMABLE.** Those are the rate-limited invoices; their bytes only ever lived in `fileStoreRef`. **No drain can recover them — they need re-uploading** — and because `failed` is non-terminal the completeness net will nag about them forever. **Step 1 is forward-looking only, and that must not be discovered later as a surprise.**
    2. **THE SUITE IS WRITTEN AGAINST A POPULATION THAT DOES NOT EXIST YET, WHICH IS THE C195(7) TRAP EXACTLY.** So the assertions are **positive** — a specific pick count, a specific census — and one pins that **150 unresumable rows report as UNRESUMABLE, not as "all caught up"**. `planDrain` returns `drainable` and `unresumable` as first-class fields for that reason: **when `drainable` is 0 the honest report is "nothing is resumable", NOT "nothing to do".**
  - **DESIGN CHOICES THAT EARNED A COMMENT:** give-up is evaluated **before** pick, or an old row gets retried once more on its way out and the hold reason is written against an attempt that already failed *(mutation-tested — removing it turns 2 red)* · the box is **36 hours, generous on purpose**, because 240 documents at 20/hour is twelve hours of *legitimate* waiting and a tight box would abandon a healthy onboarding — **erring long costs a slow queue, erring short costs a lost file** · **oldest-first**, because newest-first starves exactly the documents someone has already waited on · an **unrecognised error is PERMANENT**, the conservative direction, since a document held with a stated reason is visible while one retried forever is not.
  - **THE COPY IS DERIVED FROM THE PLAN (§9).** `drainProgressCopy` reads the census: the finish time is **documents remaining ÷ our own hourly rate — arithmetic, not an estimate** — and when the rate is unknown (`O113b`'s shared bucket) it **states the counts and omits the time.** A test pins that it never says *email*, and never emits a spinner in place of a number it does not have.
- **★★ C202 — THE UNIVERSAL DIRECTORY (2026-08-25). Migration `066` + `src/lib/vendorDirectory.js`, both ▶ HOLD/unwired.** Built before shadow mode on the operator's sequencing call, and the reasoning is worth keeping: with Amendment B withholding every backfill tier and no directory or census, **every vendor would propose UNCATEGORIZED and shadow mode would park everything** — a green-looking log with no content, which is the ·3a failure in a new costume, and precisely what Amendment A's AMBIGUOUS band exists to stop reading as a pass.
  - **★ TOAST IS RESOLVED BY RECOGNITION, as designed.** All three real descriptors (`…MERCHANT FEES JAN` / `FEB` / `TOAST MERCHANT FEES APRIL`) now hit one entry → `merchant_processing_fees`. Identity resolution still cannot merge them and still must not learn to; the directory answers a question string surgery could not.
  - **★ CULINARY EDGE STILL PARKS** — no directory entry, so STRANGER → Uncategorized. The counter-case is pinned by a test alongside the positive one, because a directory that recognised Culinary Edge would have re-created Specimen 3 at global scope.
  - **★★ MY OWN SEED SHIPPED TWO OVER-MATCHES, CAUGHT BY THE ANTI-MERGE PROBE AND NOT BY READING IT.** (1) `square inc` as a PREFIX pattern **degraded to `square`** — normalising a pattern strips legal suffixes — and swallowed `SQUARE DANCE HALL`. (2) `sysco` as a PREFIX swallowed `SYSCO FUEL`, **a pair `vendorIdentity.test.js` already asserts must never merge**. Both are Q4's one-way door re-entering through the directory, at GLOBAL scope where one bad row misroutes every tenant. Fixed by making **EXACT the default and PREFIX opt-in per row** — Toast is the only prefix entry, and it earns it because its descriptors carry a month name. Both kept as named regression tests.
  - **`directoryConflicts()` — curation hygiene, run over the seed by a test.** Two entries claiming one descriptor is worse than no entry at all: the second parks and flags, the first books to whichever row was inserted first. The detector is itself checked against a rigged conflicting entry so it cannot pass vacuously.
  - **RLS IS THE INVERSE OF EVERY OTHER TABLE AND NEEDED ITS OWN REASONING.** No `company_id`: SELECT for any authenticated user (the list contains no tenant data), **INSERT/UPDATE/DELETE for `is_platform_admin()` only**. This is **the one genuinely cross-tenant write in the product** — a tenant who could add a row could redirect a vendor's default mapping for every other tenant — so `066`'s VERIFY (d) asks for the write to be **attempted as a non-admin and watched to fail**, rather than trusting the policy text. *(That probe needs the non-platform-admin account ROADMAP TIER 1 #8 still owes.)*
  - **TWO CATEGORIES DELIBERATELY NOT SEEDED, each with the reason in the file.** **Utilities** — there is essentially no national utility; Austin Municipal serves Austin, and a local vendor belongs to the per-company ALIAS (`O111`), not a global asset. **Delivery platforms** — commission vs marketing vs revenue-contra is genuinely contested, and a contested default in a global asset is plausibility scoring with a human's name on it. Both were in the spec's seed sketch; excluded with reasons rather than seeded thin to match it.
  - **A THIRD SOURCE-GUARD FALSE POSITIVE, and now a shared fix.** Two guards tripped on their own explanatory COMMENTS — `vendorTier`'s "explicit transitions", and the spec line *"no fuzzy-match scores"* quoted in `vendorDirectory`'s header. Rather than write a third cleverer regex, the test file now strips comment lines before scanning. **A guard that fails for a reason it does not mean is a guard nobody will trust.**
- **★★ C201 — AMENDMENT B IMPLEMENTED; THE PREVIEW NOW GRADUATES NOTHING, AS SIGNED (2026-08-25).** `12 entities · 0 graduating · 7 withheld by Amendment B · attestationMix {explicit: 0, implicit: 59}`.
  - **The seven withheld each say WHY, and it is not "too few observations" — that would be false.** Bluebonnet (23 obs), Roma (8), Lone Star (5), Franklin Ave rent (5), Austin Municipal (5), Hill Country (4) and **Culinary Edge (2)** all met every Q1 condition and were held back solely for want of an explicit attestation. `withheldByAmendmentB` is in the **report shape**, so a preview of nothing-but-STRANGERs can never be mistaken for a data problem or for a judgement about those vendors.
  - **THE BAR LIVES IN THE BACKFILL, NOT THE STATE MACHINE** (Amendment B §3) — putting it in `vendorTier.js` would silently raise the LIVE rule too, and the asymmetry is the whole point: live records the distinction as it happens, history cannot. A test asserts `vendorTier.js` never reads attestation strength. *(That guard first matched the WORD "explicit" and tripped on the comment "explicit transitions" — retightened to name the mechanism, because a test that fails for a reason it does not mean is a test nobody will trust.)*
  - **ONLY THE TIER IS WITHHELD (§5).** `observation_count`, `distinct_months`, the amount band and first/last-seen are all still seeded — pinned by a test. The vendors re-earn KNOWN over two live months.
  - **The payroll bank-line exclusion now shows in the numbers:** 4 Gusto bank lines excluded as `payroll_bank_line`, so the `gusto payroll` entity is gone and 13 entities became 12. The 3,150-NET half of the net-vs-gross mismatch no longer exists as a vendor.
  - **SIX PRE-AMENDMENT TESTS FAILED AND WERE CORRECT TO.** They asserted graduation on implicit-only observations, which the signed amendment now forbids. Updated by giving each a real explicit observation rather than by weakening the assertion — except the one whose PREMISE changed ("both kinds count toward the clock"), which was reworded: both still count toward the CLOCK, and the TIER is a separate gate.
- **★ MIGRATION `071` — the `vendor_state` month-key CHECK, its own file on the operator's instruction.** The format was guarded in JS (`isYm`) and not in the schema, and the failure mode was silent rather than loud: `monthsBetween` returns null on a malformed key, `applyDormancy` reads that null and declines to decay, and a vendor that should have dropped to DECLARED **quietly stays KNOWN on stale pattern data** — a tier wrong because a string was wrong, with nothing on screen to say so. Its VERIFY block makes the constraint **refuse something** inside a rolled-back transaction: a constraint nobody has watched reject anything is a constraint on paper. ▶ HOLD.
- **★★ C201 — MERGE TEST RE-RUN ON REAL STRINGS (2026-08-25). 4 of 5 merge; Franklin Ave splits, knowingly accepted.** The assumed invoice strings were replaced with the real ones from the re-pull, so this is evidence rather than inference.
  - **✅ MERGE across all three doors** — Roma Cheese & Dairy (bank + invoice + manual), Lone Star, Austin Municipal, Hill Country. Every one lands on a single key.
  - **✗ FRANKLIN AVE SPLITS — and the shape is sharper than before: invoice and manual AGREE WITH EACH OTHER (`franklin ave properties`), the BANK alone dissents (`franklin ave properties rent`).** Two of three doors already concur; the odd one out is the rail that carries a PURPOSE SUFFIX. **Knowingly accepted, pinned by a test** so the alias feature has a failing case to fix and so nobody "repairs" it by widening the strip — which would also eat "Lone Star Restaurant **Supply**". **→ `O111`, per-company alias, sequenced with C202.**
  - **The two operator checks, both verified rather than assumed.** (1) A trailing `Co.` on the invoice side **normalises away** — `Roma Cheese & Dairy Co.` and `ACH DEBIT - ROMA CHEESE & DAIRY CO` reach the same key. (2) The split targets the **FIRST** separator only and READ takes everything left of it: `Austin Municipal Utilities` survives a right half containing both an em-dash and an **unspaced** en-dash (`12/05/2025–01/05/2026`), and `Hill Country Milling Co.` survives `freight — bread flour`. Only a **spaced** en-dash separates.
  - **★ PAYROLL BANK-LINE EXCLUSION ADDED, reusing `isPayrollBankLine`.** And an ordering defect it exposed: checking the predicate BEFORE the source exclusion labelled payroll **register** entries as `payroll_bank_line`. Reordered so a register is `source_payroll` and a net-pay debit is `payroll_bank_line` — **the counters now say what actually happened**, which is the whole point of counting exclusions by name. Caught by a test that had asserted the more specific counter.
- **★★ C201 — PER-SOURCE STRATEGY IMPLEMENTED (2026-08-25). PREVIEW RE-RUN. `065` STAYS BLOCKED — one vendor still splits across the two doors.**
  - **★★ THE MERGE TEST — 6 of 7 merge, FRANKLIN AVE PROPERTIES SPLITS.** The question the operator set as the gate: does a vendor arriving by the bank door (RESOLVE) land on the same key as the same vendor arriving by the invoice door (READ)? Computed on both strings, since the merge is a pure function of them:

    | vendor | bank RESOLVE key | invoice READ key | |
    |---|---|---|---|
    | Roma Cheese & Dairy | `roma cheese and dairy` | `roma cheese and dairy` | ✅ |
    | Lone Star Restaurant Supply | `lone star restaurant supply` | `lone star restaurant supply` | ✅ |
    | Hill Country Milling | `hill country milling` | `hill country milling` | ✅ |
    | Austin Municipal Utilities | `austin municipal utilities` | `austin municipal utilities` | ✅ |
    | Bluebonnet Linen Service | `bluebonnet linen service` | `bluebonnet linen service` | ✅ |
    | Culinary Edge Consulting | `culinary edge consulting` | `culinary edge consulting` | ✅ |
    | **Franklin Ave Properties** | **`franklin ave properties rent`** | **`franklin ave properties`** | **✗ SPLIT** |

    **CAUSE: the bank descriptor carries a PURPOSE SUFFIX the vendor name does not.** `ACH DEBIT - FRANKLIN AVE PROPERTIES LP RENT` — the trailing `RENT` describes what the payment was FOR, not who it was TO. Rail-stripping removes transport noise; it has no rule for purpose words, and **it should not acquire one by widening** — "RENT", "FEES", "SERVICES" are exactly the words that would also eat a real vendor name ("Lone Star Restaurant **Supply**"). This is the same shape as the Toast month-name split and probably wants the same answer: **recognition (directory / alias) rather than string surgery.** ▶ HOLD for the operator; **`065` does not proceed** — one vendor, two keys, is a merge failure by the stated gate.
    - *Caveat on the evidence: the bank-side strings are real (from the extraction); the invoice-side strings are ASSUMED, since no `universal_upload` rows were pasted. If the invoice vendor field literally reads "Franklin Ave Properties LP Rent" the split disappears — but any field that omits the purpose word splits.*
  - **★ A GAP IN "PAYROLL EXCLUDE" THAT THE RE-RUN FOUND: payroll arrives on the BANK rail too.** Excluding `source='payroll'` removes the register entries, but January and February's Gusto lines came in as `source='bank_import'` — the net-pay debit on the bank statement — so a `gusto payroll` entity survives at **3,150 (NET)** and graduates then decays. That is precisely the half of the net-vs-gross mismatch the exclusion was meant to prevent. **PROPOSED, NOT IMPLEMENTED:** also exclude bank lines matching the EXISTING `isPayrollBankLine` (`payroll.js`) — verified to match `GUSTO PAYROLL 011526` and to leave `ROMA CHEESE`/`MONTHLY SERVICE FEE` alone. Reusing that predicate keeps the two modules agreeing instead of growing a second definition of "this is payroll".
  - **IMPLEMENTED:** `identityForEntry({ description, source })` in `vendorIdentity.js` — the strategy table lives with identity, not with the backfill, because live shadow mode needs it too. `bank_import` RESOLVE · `universal_upload`/`manual`/`recurring`/`qbo_import` READ · `payroll`/`opening_balance`/`ar_invoice`/`api` EXCLUDE, **each excluded reason counted by name**. An **unrecognised source is EXCLUDED and counted, never guessed** — a wrong key merges two vendors and launders one's mapping onto the other's charges, a missing key books to suspense and flags, and those two errors are not symmetric. `identity_source: 'resolved' | 'read'` is carried per observation and surfaced as `identityMix` **in the report shape**, per the operator's instruction that a PROCEED resting on READ identities must not be able to hide.
  - **Preview unchanged on the current data** (13 entities, 7 graduating, `identityMix {resolved: 63, read: 0}`) because every extracted row is `bank_import`. The strategy's effect will only show once invoice-path rows are in the sample — which is itself the reason the merge test above was computed on strings rather than waiting for them.
- **★★ C201 — IDENTITY RESOLUTION NEEDS A PER-SOURCE STRATEGY (2026-08-25). The one-rule split is CONFIRMED BROKEN on two of four sources; `065` is blocked until this settles.**
  - **★ THE DEFECT, REPRODUCED IN CODE.** Payroll descriptions read `Gusto Payroll — 2026-02-28 – 2026-03-13`: an **EM-dash** after the vendor, an **EN-dash** between the period dates. `rawDescriptorOf` splits on the first `" – "` (en-dash) and therefore splits at the DATE RANGE — `rawDescriptorOf(...)` → `"2026-03-13"`, `entityKeyFor("2026-03-13")` → `null`. **Every payroll line resolves to nothing.** *(One correction to the report: it lands in `skipped.no_identity`, not `skipped.ambiguous_descriptor` — the split SUCCEEDS and yields a date. It is counted, so not silent, but it is indistinguishable from a genuine trace-number row.)* It has never fired in anger only because the backfill query filtered `source='bank_import'`; the unfiltered re-pull would have hit it immediately.
  - **★★ ROOT CAUSE — THERE ARE TWO DESCRIPTION-CONSTRUCTION PATHS, AND NEITHER KNOWS ABOUT THE OTHER.** `persistJournalEntry` (App.jsx:1386) builds `` `${invoice.vendor} – ${invoice.description}` `` — en-dash, vendor on the LEFT. `persistMultiLineEntry` (App.jsx:1482) takes `entry.description` **verbatim**, so payroll keeps whatever `payrollEntryForImport` made. And `payments.js:70` returns `` `Payment – ${vendor}` `` — a description that **already contains the separator**, which is why manual entries come out as `Vendor – Payment – Vendor` with three parts. The descriptor is a DISPLAY STRING assembled from structured data that already existed, and identity resolution has been trying to reverse-engineer it. **That is the actual defect; the em-dash is a symptom.**
  - **★ THE DECISIVE REFRAME: only ONE source has a descriptor problem at all.** Rail-stripping exists because bank descriptors are noisy. The other three sources carry a vendor as STRUCTURED DATA and never had variants to resolve — so for them the open-book objection does not apply, because there is nothing to resolve. Reading a field is not cheating on a test the field was never sitting for.

    | source | description shape | vendor identity | strategy |
    |---|---|---|---|
    | `bank_import` | `Vendor – RAW BANK TEXT` | RIGHT half, genuinely noisy | **RESOLVE** (rail-strip + normalize) |
    | `payroll` | `Gusto Payroll — start – end` | none on the right; it is not a vendor mapping at all | **EXCLUDE** |
    | `universal_upload` | `Vendor. – PRODUCT DESCRIPTION` | LEFT half; right is a product | **READ** (normalize only) |
    | `manual` | `Vendor – Payment – Vendor` | LEFT half; right is a synthetic phrase | **READ** (normalize only) |

  - **★ PAYROLL SHOULD BE EXCLUDED FROM `vendor_state` ENTIRELY, not parsed.** A payroll register books to salaries / payroll tax / cash / taxes-payable by STRUCTURE, not by a vendor→account judgement — there is no mapping for the ladder to learn. **Amendment A §2 already excludes payroll register entries from shadow scoring**, so excluding them here makes the backfill agree with the criterion instead of quietly disagreeing with it.
  - **★★ AND THE GUSTO "BAND EVENT" IS A UNITS MISMATCH, NOT A SPEND CHANGE — this is why excluding payroll matters.** Gusto reads **3,150.00** via `bank_import` in Jan/Feb and **4,000.00 + 306.00** via `payroll` from March. Those are not the same quantity: **3,150 is NET pay** (gross 4,000 less 850 withholdings) and **4,000 is GROSS**. A unified entity would flag a 1.27× band event and the flag would be **wrong** — the spend did not change, the measurement did. A vendor whose amounts arrive from two different paths measuring two different things cannot have a meaningful band, which is a second, independent reason payroll does not belong in `vendor_state`.
  - **DORMANCY — one artefact, two real.** GUSTO was a query artefact, confirmed: it continues biweekly March–July under `source='payroll'`, hidden by the `bank_import` filter. **HARTLINE and the BANK MONTHLY SERVICE FEE are REAL** — a query across all three names returns nothing matching them after 2026-03-01. They genuinely stop in February, so **dormancy-on-arrival for those two is correct behaviour**, and the decay is the state machine working rather than a bug.
  - **▶ HOLD — `065`/the backfill does not proceed until the per-source strategy is settled.** A backfill built on a rule that fails on two of four sources would seed exactly the wrong entity keys, and a state machine seeded with wrong keys is inert on arrival and green.
- **★★ C201 — THE GRADUATION PREVIEW RAN (2026-08-24). 70 rows · 7 deleted excluded · 63 observations · 13 entities · 7 would graduate.** Four findings, and the first is the one the whole calibration exists for.
  - **★★ CULINARY EDGE GRADUATES TO KNOWN — the specimen that founded the familiarity axis, promoted by the migration built to prevent it.** Two auto-bookings of $850.00 (04-14, 05-14), two signed months, agreeing mapping, identical amounts: it clears Q1 exactly. Specimen 3 is the never-seen vendor auto-booked at confidence 88 on name plausibility into the WRONG LEDGER CLASS — the program's only wrong-class entry. It would have arrived as a clean row in a green preview. **→ AMENDMENT B** (`docs/CALIBRATION_SPEC_O88_AMENDMENT_B.md`, drafted, unsigned): backfill graduation additionally requires ≥1 EXPLICITLY attested observation. Live Q1 unchanged.
    - **★ AND THE AMENDMENT DOES NOT DISCRIMINATE — it defaults. Say so.** `exception_resolved` is **null on all 63 live rows** and `recoded` cannot be derived at all, because `persistRecode` updates `account_id` IN PLACE and leaves no marker (O108). So **zero explicit observations exist** and the rule graduates **0 of 13**. It is adopted as a rule because it stays correct if the data ever becomes discriminating; it must never be REPORTED as a test it cannot perform. Every vendor starts STRANGER and re-earns KNOWN over two live months — Bluebonnet costs 2 cards instead of 7.
  - **★ TOAST SHOULD GRADUATE AND CANNOT — the exact inverse of Culinary Edge.** Three observations, three distinct months, ONE agreeing account (`a3edd514`) — a textbook KNOWN vendor. It splits into **three STRANGERs** because the MONTH NAME is embedded in the descriptor: `…MERCHANT FEES JAN` / `…FEB` / `TOAST MERCHANT FEES APRIL` normalise to three different keys. **DECIDED 2026-08-24: leave it split. NO month-name stripping** — word-stripping is precisely the anti-merge risk the narrow-strip design exists to avoid. **Toast is instead the motivating case for the UNIVERSAL tier**: a national payment processor belongs in the curated directory with a canonical mapping, which resolves identity by RECOGNITION rather than string surgery. **Carried into C202's directory seed as a named test case.** If it still splits once the directory lands, a tested month-name rule gets revisited then.
    - **BAND NOTE, corrected by computation rather than intuition.** A band built from all three amounts (214.36 / 228.91 / 487.20) flags NONE of them — the outlier inflates σ enough to swallow itself (mean 310.16, ±2σ → 3.16–617.15). But the band that would actually exist when April arrives is built from Jan+Feb only: mean 221.64, σ 10.29, band 201.06–242.21 — and **April's 487.20 lands OUTSIDE at 2.2× the pattern.** The band is built from attested history AT THE TIME, never retrospectively, and that is what makes it work. Worth watching in shadow.
  - **★ IDENTITY RESOLUTION WAS EXERCISED AFTER ALL — and the earlier claim that the corpus is flat needs correcting.** GUSTO collapsed FOUR distinct raw strings (`GUSTO PAYROLL 011526/013026/021326/022726`) into one entity — the trailing-numeric tail strip working exactly as designed. So the resolver was tested on real data, **passed on Gusto and failed on Toast**. Lone Star is the constant one; the corpus is not uniformly flat. *(The C200 corpus guard still stands: the four Lone Star VARIANTS remain absent, so Specimen 2's flapping cause is still unrepresented.)*
  - **★ THE RIGHT-HALF RULE EARNED ITSELF ON REAL DATA.** The bank fee arrived under two DIFFERENT resolved names — `Bank` and `Bank (Monthly Service Fee)` — with identical raw text (`MONTHLY SERVICE FEE`). Resolving from the full string would have split one vendor into two. The operator's finding, vindicated on the first run.
  - **DORMANCY DECAY IS PROBABLY A QUERY ARTEFACT, NOT A FINDING — not acted on.** Gusto, Hartline and the bank fee graduate then immediately decay (unseen since 2026-02, `asOf` 2026-08). The extraction filtered `source='bank_import' AND category='Expenses'`, so payroll booked through the REGISTER path and anything hitting a non-expense account is invisible. Gusto vanishing after February is payroll moving paths, not a restaurant that stopped paying staff; a monthly bank fee ending in February is likewise implausible. **A re-pull without those two filters is owed before the planner is touched.**
- **★★ C201 — TWO FINDINGS FROM THE BACKFILL EXTRACTION (2026-08-23), both about the EVIDENCE rather than the code.**
  - **★ THE OPEN-BOOK PROBLEM.** `journal_entries.description` on a bank line is **not** the raw bank descriptor — it is `"Resolved Vendor Name – RAW BANK TEXT"`, en-dash separated (`ledger.js:31` splits the same way for display). Resolving identity from the FULL string would have graded the resolver against a string **containing its own answer**: every variant resolves perfectly because the resolved name is sitting in the input, and the backfill reports flawless grouping while proving nothing about the function under test. An open-book exam scored as closed-book. **Caught by the operator before the planner ran.** Fixed: `rawDescriptorOf` takes the RIGHT half only, and a row with no separator is **excluded and counted** (`skipped.ambiguous_descriptor`) rather than silently scored on the contaminated string — falling back to the whole string is the contaminated case in disguise. Two tests pin it from both sides: same raw text under DIFFERENT resolved names must group as ONE vendor; one resolved name over two different raw texts must stay TWO.
  - **★★ THE FIXTURE PROGRAM CANNOT EXERCISE IDENTITY RESOLUTION.** Lone Star's raw bank text is **byte-identical across all four months** (`ACH DEBIT - LONE STAR RESTAURANT SUPPLY`). **Specimen 2's flapping cause — descriptor variance — is not represented in the drive data at all.** The four variants the spec names as the seed corpus were never in the ledger; they were in the statements, or in the reading of them. **Normalisation has no work to do on a constant string, so a clean backfill preview over this data is an artefact of the fixture, not evidence about the resolver.** `planVendorBackfill` now reports `variance.identityResolutionUnexercised` so a green result can never be read as a tested one, and the C200 corpus guard records that the fixtures cannot supply what it has been waiting for. **OWED: the real statement strings, or explicitly-labelled synthetic variants.**
  - **JANUARY DELETED TWINS — re-import residue, correctly excluded.** Every January line has a deleted twin (same descriptor, amount and date; one soft-deleted, one not). The planner drops deleted lines before anything else and counts them; a test holds it. Without that, every January vendor would have **doubled its observation count** — and since Q1 needs two observations, **a one-month vendor would have graduated on a single month's re-import.** Q1's distinct-months rule would have caught it anyway, which is a second reason that rule is stored as `text[]` rather than a count.
- **★ O110 — DIAGNOSIS CLOSED 2026-08-23. Origin EXTERNAL and unexplained; every in-app door ruled out.** The audit log for the window returned **zero rows** (run with full UUIDs). Every in-app materialisation path writes an audit event, so silence is dispositive — combined with the two refutations (legacy COA generation: `008` seeds the same modern chart; QBO import: `matchAccount` and the mapping UI can only yield codes from OUR chart), the 36 accounts came from **outside the application**: direct SQL, a fixture script not in this repo, or a pre-audit tool version. The March timestamps line up with fixture-company setup. **No further archaeology** — the remediation is the same whichever it was.
  - **★ SEVEN MATERIALISATION SITES, NOT FIVE — and the count kept rising because each guard was scoped to where we had already looked.** Five in `App.jsx`, plus **`QBOImportView.jsx`** (a hand-rolled insert, invisible to a guard that only knew `buildAccountInsert`) and **`addCustomAccount`** (audited as `coa_added`). **The CI guard I wrote on 2026-08-17 read only `App.jsx` and reported "all five sites covered" — true, and useless.** A test scoped to where you already looked will always tell you that you have looked everywhere. It now scans all of `src/` and separately forbids the raw-insert SHAPE, because the sixth site was invisible by construction rather than by oversight.
  - **★ (a) FIXED 2026-08-23 — the QBO import was silently broken, and the silence was the defect.** `QBOImportView.jsx` inserted an `account_type` column that does not exist, inside a bare `try {} catch {}`. The insert threw, the throw was eaten, `acctId[code]` stayed undefined, and the downstream guard `if (!pId || !oId) { failedN++; continue; }` dropped every row needing that account — reported to the operator as a bare "N failed" with no cause anywhere. **A silent NON-booking, not a wrong booking**: it fails safe and opaquely, which is exactly the combination that survives seven drives unnoticed. Now: shared shape, error surfaced to the operator by name, creation audited.
  - **★ (b) ▶ HOLD — RESTORING THE FINGERPRINT NEEDS A DECISION, and it is not a small one.** `system_role IS NULL` meant "invented at runtime"; 36 unexplained rows retire that meaning, and the detector was the durable outcome of O108. Three mechanisms, costed:
    1. **`accounts.origin` column** (`seed|runtime|external|import`), migration + backfill. Cleanest semantically — the detector becomes `system_role IS NULL AND origin <> 'external'` and stays honest as new doors appear. Costs a column on a core table and a decision about what existing rows are.
    2. **Role-bless the 36.** Cheapest to write, **and probably wrong**: they are a FOREIGN chart, so `5300 Utilities` and `6200 Utilities` would both want `utilities` on the same company — and `byRole` is a plain object, so one would silently shadow the other. **That is a new silent-wrong-account bug introduced to fix a detector.** Not recommended without a uniqueness constraint that does not currently exist.
    3. **Quarantine — `active = false` on the 36**, if they carry no live lines. Restores the detector by removing the noise rather than relabelling it, and they sit on fixture companies rather than Franklin Ave. **Needs a query first:** do any of the 36 carry live journal lines? If yes, this option is off.
    - **RECOMMENDATION: (1), and (3) only after the line-count query.** (2) trades a diagnostic for a booking hazard.
    - **★ DECIDED 2026-08-23 — OPTION 1 ALONE (→ migration `070`, ▶ HOLD). Quarantine is DEAD.** The line-count query returned **`5300 Utilities` = 1 live line and `6450 Vehicle Expense` = 1 live line** (`6210 Internet` = 0). **Two accounts from a chart nobody in this codebase recognises are carrying real, undeleted journal lines, and because their `system_role` is NULL they are invisible to every `getAccountByRole` lookup in the app.** They sit on fixture companies, not Franklin Ave. `070` LABELS them and deliberately does not MOVE them — reclassifying a booked transaction is an accounting decision, not a schema one.
    - **`070`'s default is `'runtime'`, not `'seed'`, and that is the load-bearing detail.** An INSERT that forgets to say where it came from is, by definition, one of the seven materialisation doors; defaulting to `'seed'` would let the next unaudited door label itself legitimate. The new detector is `system_role IS NULL AND origin <> 'external'`, and it stays honest as doors appear rather than degrading again.
  - **★ FIVE MATERIALISATION SITES, NOT FOUR — the fifth was found by the CI guard, not by reading.** `persistJournalEntry` · `persistMultiLineEntry` · `ensureAccountIdForCode` · `persistRecode` · **`resolveAccountId`** (App.jsx ~2506), which is reached from the **AI action path** (`add_rule` / `set_contact_rule`) — so a model-proposed GL code can mint a permanent account on a client's chart. The test asserting "all sites log" failed on a count of 4 and produced the fifth; that is the guard earning its place on the day it was written.
  - **THE SURVIVING FINDINGS (all four evidenced; none currently producing a wrong number).**
    1. **`044` was never applied and could not be** — it inserts `account_type`, absent from `public.accounts`. The repo's highest-numbered definition of `seed_company_accounts` has never run, so §6's rebuild claim is **false for functions**.
    2. **`anon` holds EXECUTE on a SECURITY DEFINER function.** Live `proacl` is `{postgres=X,anon=X,authenticated=X,service_role=X}`; `009`/`044` both revoke from public and grant only `authenticated`. Not exploitable — `is_company_member` is the first statement and fails for anon — **but the guard is doing the work the grant was supposed to do.**
    3. **`opening_balance_equity` (`3400`) is absent from the live seed.** New companies get no OBE account; it is created on demand at first opening-balance post (`App.jsx:1581`). Works by accident.
    4. **THE ABSORBER, NAMED (this is the durable one).** `useAccounts.js:60-63` resolves a missing role to `DEFAULT_BY_ROLE` — the client's hardcoded chart — returning a plausible account object with a code, **silently, with no warning and no null**. `App.jsx:1448-1458 ensureAccount` then **INSERTS that code** into the company's chart if absent. So a role gap does not fail; it **materialises into a real account row**. Today it absorbs exactly one thing (finding 3). Its significance is prospective: **it would hide the next real divergence exactly as completely**, and for seven attested months nobody knew it was there.
  - **WHAT THE DIAGNOSIS ACTUALLY MEASURED.** 30 distinct literal role lookups across `src/`, **0 missing from live**; 13 dynamically-produced role strings, **0 missing** (the lone apparent hit, `"income"`, is `clarify.js:166` returning a plain-language phrase, not a role); 11 hardcoded `|| "NNNN"` code fallbacks, **all currently agreeing with live**. `constants.js` holds 57 roles to the live seed's 56 — the difference is exactly `opening_balance_equity`.
  - **CALIBRATION IS UNAFFECTED, and the reason is structural.** `vendor_state.attested_account_id` is a per-company uuid FK to `accounts.id` — never a role, never a code. A chart divergence could not have corrupted it even had one existed. `7150` is seeded and backfilled on all 11 companies (verified 2026-08-17), so STRANGER suspense resolves identically everywhere.
  - **THE ONE THING THAT COULD REOPEN THIS.** The whole comparison above is client vs the **SEED FUNCTION**, which describes what NEW companies get and says nothing about what existing ones hold. If older companies carry `cost_of_goods_sold`/`rent`/`owner_equity` rows, the divergence is real **for them** and `getAccountByRole` has been falling back to `constants.js` on every lookup. Query (4) of the diagnosis checks it; **until it is run, this item is diagnosed, not closed.**
  1. **`044` was never applied and could not have been.** It inserts an `account_type` column that does not exist on `public.accounts`. The repo's HIGHEST-numbered definition of `seed_company_accounts` has never run.
  2. **`src/lib/constants.js` and the live chart are different generations.** Live: Common Stock · Product Revenue · role `cogs` · role `rent_occupancy` · **`6300` = Marketing & Advertising**. Client: Owner Equity · Sales Revenue · role `cost_of_goods_sold` · role `rent` · **`6300` = Repairs & Maintenance**. So `rc("cost_of_goods_sold")` and `getAccountByRole("rent")` resolve against roles the database does not have, and `6300` means two different things depending on which side you ask.
  3. **The live ACL is not the one the repo asserts.** `009`/`044` both do `revoke all from public; grant execute to authenticated`, yet live `proacl` is `{postgres=X,anon=X,authenticated=X,service_role=X}` — `anon` holds EXECUTE on a SECURITY DEFINER function. **Not currently exploitable**: the function's first statement is `is_company_member(p_company_id)`, which is false for an unauthenticated caller, so it raises. But the guard is what saves it, not the grant — and the repo believes the grant was removed.
  - **WHY THIS IS BIGGER THAN `O35`.** `O35` is "older companies were seeded by different COA versions" — a data-shape problem. This is the migration chain and the client both disagreeing with production, which makes §6's "empty DB → `000` → `001…036` reproduces the live schema" claim **false for functions**, and makes every future `create or replace` a coin-flip unless it is copied from `pg_get_functiondef` first (the rule now recorded in `063`'s header, learned by writing that file wrong).
  - **▶ HOLD — WHICH CHART IS CORRECT IS AN ACCOUNTING DECISION, NOT A MERGE.** Repointing `6300`, or renaming `cogs`↔`cost_of_goods_sold`, moves where real transactions land. Operator decides; nothing is reconciled until then.
  - **★ BACKFILL APPLIED 2026-08-11 — 10 ROWS STAMPED, NOT THE ESTIMATED 14. The estimate was counting a mixed population.** `stamped_payroll_runs = 10`, count-verified. **Franklin Ave's January and February payrolls predate the register path entirely**: they came in as `source=bank_import` with **en-dash** ACH descriptions and were booked as **net-lines**, not as registers — so the `description like 'Gusto Payroll — %'` (em-dash) filter never matched them, and **that exclusion is correct, not a miss.** Stamping them was considered and **deliberately declined on two independent grounds**: `gross: 4000` would be **false data** on a row whose amount is net and whose gross was never recorded, and `kind:'payroll'` would **wake `matchPayrollBankLine` on rows never shaped for it**. *(Note how the two ·3c fixes interlock: even had they been stamped gross-only, the null-net refusal would have kept them from suppressing anything. Belt and braces, arrived at from opposite directions.)* **Norm history is 10 runs at 4,000 gross** — comfortably enough for condition 5, so nothing is pending on the shortfall. **The "14" was never verified against the database; it was an estimate carried forward through the spec and repeated in the ·3c report without a caveat. Estimates about production data are claims about a query nobody ran.**
  - **★ `ai-proxy` REDEPLOYED 2026-08-11 — v15 → v16 ACTIVE (`2026-08-11 03:42:27` UTC), both assets uploaded.** The working command (and why the bare form fails) is recorded in **§6 → Edge-function deploys**. **The bare `supabase functions deploy ai-proxy` failed**: no `config.toml`, and `.temp/linked-project.json` is not what the CLI keys on — an assumption stated confidently in a report and falsified one command later.
    - **★ (ii) IS DEPLOYED-BUT-UNVERIFIED, AND THAT IS A REAL STATE, NOT A FORMALITY.** A version bump proves the bundle shipped; it proves nothing about whether the model returns `period_end` or whether it survives to `bank_statements.period_end`. **A silent model failure looks EXACTLY like the old behaviour** — the client falls back to the transaction span, which is what it always did. **The August drive owns the proof:** drop a statement whose header states a period end beyond its last transaction and confirm the **stated** date persists. Until then (ii) is shipped, not closed. **DESIGNATED: ROADMAP §0 TIER 1 #12.**
  - **★ DESIGNATED, NOT FIXED — carried out of the ·3c review with anchors, per the orphan rule.**
    - **D2 — the `p_meta` drop is WIDER than payroll and depreciation. DESIGNATED: ROADMAP §2 Bugs `O95`.** *(Filed `O95`, not `O90`: the 2026-08-11 sweep caught `O90`/`O91` ALREADY IN USE by the trust-panel and batch-upload rows — the ids were minted off the end of ONE table instead of scanning all of §2, and this section was already using "O90" with two meanings. Renumbered `O95`/`O96`.)* `App.jsx` puts `tax_amount` into `p_meta` and `controlTotals.js` reads it back, so **the O59 sales-tax cross-foot control total is inert by the identical mechanism**. Likewise `signedPeriod.js` builds `original_date` / `rebooked_from_signed_period` that `persistJournalEntry` never forwards, so the "original date kept on file" promise is not persisted. This is the class the RPC-consolidation item exists to end; do **not** bolt more per-caller stamps on without deciding that first.
    - **D4 — (ii) moved a comparand, expected and fails-safe. DESIGNATED: ROADMAP §0 TIER 1 #12 (drive watch-item).** `stated_ending_balance` is still derived from the **last transaction's** running balance while `booksBalance` is now `asOf` the wider stated `period_end`. A book entry dated after the last transaction but inside the stated period will now break the tie → `attention` instead of auto-reconcile. That is **more** correct (such an entry genuinely hasn't cleared) but it can regress the C198·2 auto-reconcile headline until outstanding-items handling absorbs it. **Watch it on August.**
    - **D5 — one-time transition, self-clearing. DESIGNATED: ROADMAP §0 TIER 1 #12 (drive watch-item).** `reevaluateStatement`'s coverage test requires `st.period_end <= r.period_end`; a re-upload with a widened stated end against a reconciliation created **before** ·3c flips `covered` false. Same shape as the ·3b f3 resurrection wall — expected, one-time, and **no re-sweep path was built** deliberately.
    - **D6 — dead code. DESIGNATED: ROADMAP §2 Bugs `O96`.** `runDepreciationThrough` / `planDepreciationRun` are destructured in `DashboardView.jsx` but absent from `erpCtx`, so they resolve to `undefined`. `autoPostDepreciation` is the sole live poster — which is what kept D3's exposure to a single sequence.
  - **Migrations ledger: UNCHANGED — applied & verified through `061`.** The 2026-08-09 caveat (*"if ·3c's (i) fix goes the `post_journal_entry` route, that adds one"*) is **resolved: it did not.**

- **PRODUCT IDEAS (post-launch; recorded from the O85 drive — both now carried on ROADMAP §0 TIER 3).**
  - **PAYROLL AUTO-POST** *(SUPERSEDED — promoted to C198-adjacent by O86 finding (b); kept for the rationale)*. A payroll register dropped on the home zone should **auto-post its standard entry** (with an undo window) rather than parking on a confirm card. The debit/credit confirmation card is a **CPA-only** surface once the IA collapses (North Star Phase 2) — the owner should see an outcome, not a journal entry to approve. Pairs with the payroll PDF-ingestion gap (§11 O84 finding 3).
  - **★ OWNER NUDGE CHANNEL (differentiator-class; Tier 3).** Route exceptions and clarifying questions to the owner **where they already are** — Slack / SMS / email — e.g. *"What was this $155 charge at X yesterday?"* — and let the **answer teach the vendor mapping** (`learnFromCorrection`, so the reply is worth something permanently). The exception ROUTING already exists (C186's queue); this is purely a **delivery-channel** feature on top of it. Turns the trust layer from a screen the client must visit into a conversation that finds them, and it feeds the same learning loop as the teach-in/entity-profiles work.

- **★ SECURITY & DOCUMENT-HANDLING AUDIT — client peace-of-mind + tenant integrity. ✅ TIER 1 COMPLETE (2026-08-09, ROADMAP §0 TIER 1 #11); TIER 2 UNBLOCKED; the SECURITY LADDER (s1–s6) is the live work.** *(Opened 2026-08-08: the **first candidate third-party user attempted signup that day**. Until now every company in the database has been the operator's own; the tenant wall has never had to hold against a stranger, and "it has never been tested by an adversary" is not the same claim as "it holds.")* Two deliverables, strictly ordered — **the story is written from the audit's findings, never ahead of them.**

  - **★ TIER 1 — RLS / TENANT-WALL AUDIT (ROADMAP §0 TIER 1, launch-blocking). MUST COMPLETE BEFORE ANY REAL THIRD-PARTY ACCOUNT HOLDS REAL DATA.** A **read-only verification session, architect-driven via SQL** — no code changes expected, and none welcome mid-audit. Four checks:
    1. **Every table carrying `company_id` has RLS enabled AND policies that actually enforce it.** Enumerate from `pg_policies` / `pg_tables` and **join against every table with a `company_id` column** — the deliverable is the **set difference**: any company-scoped table with no policy, or with RLS off, is a finding. **This has happened before, twice**: `accounts` shipped company-scoped with no RLS at all (caught by the OWASP pass, fixed in `023` — see §3), and `048_rls_exposed_tables.sql` exists because a later sweep found more. §8's "new tenant table → migration with RLS + the four policies" is a convention, and a convention is not an enforcement mechanism.
    2. **Storage.** Buckets **private** (the `documents` bucket holds bank statements and receipts); object paths **company-scoped**; download URLs **signed and expiring**, never permanent public URLs. A private bucket fronted by a permanent URL is a public bucket with extra steps.
    3. **Key hygiene.** The anon key being public is safe **ONLY IF (1) passes** — it is not independently safe, it is safe *because* RLS is the boundary (§3). Verify **no service-role key is shipped client-side** (grep the built bundle, not just the source — `dist/` is what ships) and that `ai-proxy` remains the only holder of `ANTHROPIC_API_KEY`.
    4. **Cross-tenant probe — the actual proof.** Confirm a session scoped to one company **cannot read another's rows or files**. The **five-company census** recorded above (`3a704760` Franklin Ave Pizza, `c787582f` Test Corp ONE, `24d5e576` Northwind Studio, `3a4a6b3c` Shadow Test Co, `66fa0cc2`) makes this **concretely testable today** — real distinct tenants, known row counts, no fixture to build first.
    **Any gap found = an immediate fix commit BEFORE onboarding anyone.** ⚠️ **Run the audit OUTSIDE the app.** The Security dashboard's `security_check` RPC is the application grading its own homework, and it is `SECURITY DEFINER` — a passing self-report is exactly what a missing policy would also produce. Query `pg_policies` directly. *(Same lesson as C195(7): a guard whose input is always empty is indistinguishable from a clean queue — assert the mechanism FIRES, don't accept "nothing outstanding".)* Note also the **deliberate platform-admin bypass** (§3 Option A): `is_company_member` returns true for `PLATFORM_ADMIN_EMAILS`, so the audit must probe as a **normal user of one tenant**, never as the operator — the operator's own account cannot fail this test. *(Same trap that blocked the DB-layer sign-off proof below.)*

  - **TIER 2 — THE CLIENT-FACING SECURITY STORY (ROADMAP §0 TIER 2; write AFTER Tier 1 verifies, never before).** The truthful two-sentence answer to **"where does my bank statement go?"** — transit encryption, at-rest encryption, access scoping — **plus the part that answer usually omits: the AI pipeline is a data path.** Per §5 the document's text/base64 travels client → Supabase Edge Function (`ai-proxy`) → `api.anthropic.com`, so the honest answer names **Anthropic as a sub-processor** and must state **retention terms, whether we are on a zero-retention tier, and whether client data trains models — with the vendor's answer IN WRITING**, not inferred from a docs page. Three more, each currently unanswerable:
    - **Deletion/retention honesty.** The architecture is **soft delete** (§7: `deleted_at`/`deleted_by`, deliberately, for recoverability and audit trail). So **"delete" does not mean purged** — and a retention promise must be **defined AND implemented** before it is quoted to anyone. Quoting a purge we don't perform is the same class of defect as the O86 surfaces that reported work they hadn't done, with legal consequences instead of a lost afternoon.
    - **Audit-log completeness — VIEWED, not just CHANGED.** `logAudit`/`logAI` record **mutations** (§8). **Reads are not logged at all**, so *"who has looked at my books?"* has no answer today. Sharpest edge: **Support Mode**. Platform-admin *actions* are attributed (`Platform Admin - <email>`), but platform-admin *views* are not recorded anywhere — and read access to their books by someone at the vendor is precisely what a client would most want logged. Scope a read-audit for at minimum support-mode access before making any access-transparency claim.
    - **SOC 2 — noted as a company milestone, NOT scheduled.** Recorded so it stops being re-raised as though it were a task; it is a program with an auditor and a calendar, and nothing above depends on it.

  - **★★ AUDIT COMPLETE — TIER 1 CLOSED (2026-08-08/09). THE WALLS HOLD, AND ONE OF THEM DIDN'T.** *(Supersedes the Tier 1 spec above, which stays as the record of what was checked.)*
    - **RLS coverage — VERIFIED from live `pg_policies`.** **All 33 tables carrying `company_id` have RLS enabled AND policies enforcing it.** The set difference the spec asked for came back **empty** — no company-scoped table is unprotected.
    - **Storage — VERIFIED.** Bucket **private**, object paths **folder-scoped by company**, membership-checked policies on **all four commands**.
    - **★ FINDING (real, and the reason the audit was worth running) — the journal family carried TWO OR-combined permissive policy generations.** Postgres ORs permissive policies, so **the loosest wins** and every role gate and guard in the older generation had been a **dead letter** since migration `001` landed beside it — a `viewer` had full write access to the general ledger. **Second defect in the same place:** the older `UPDATE` policies had `USING` but **no `WITH CHECK`**, so the post-update row was never re-validated and **a member could reassign a row to another tenant's `company_id`** — a hole that was *masked* by the very broad policies the dedup removes, so a naive dedup would have opened it.
    - **FIXED — `061_je_policy_dedup.sql` APPLIED and verified live (C199 `d612964`).** A single generation governs each of the six ledger tables, **the stricter one survives**, **`WITH CHECK` on all six updates**, and writes route through **`is_company_writer()` (SECURITY DEFINER)** — the role gate **OR** platform admin, preserving Support Mode rather than collaterally revoking it.
    - **★ INVARIANT ESTABLISHED — no ledger table carries two permissive policies for one command.** This is the durable rule, not the one-time cleanup: it is checkable in a single query (group `pg_policies` by `tablename, cmd` having `count(*) > 1`) and **every future policy migration must leave it true**. *(Corollary worth internalising: in RLS, adding a policy can only ever LOOSEN a table. A strict policy beside a broad one is not a restriction — it is a comment.)*
    - **Known remaining duplicates — deliberately excluded, not missed.** `audit_log` / `subscriptions` (old SELECTs are **strict subsets** of the surviving ones → dropping them is a pure no-op cleanup); `unknown_documents` (**a real behaviour change** — its older generation is role-gated, so deduping would start rejecting uploads by a `viewer` → **queued as `062`**); `users` (the own-OR-teammates pair is **intentional**, not a generation collision).
      - **DECLINED 2026-08-11 — `audit_log` / `subscriptions` duplicate SELECTs stay. No row, no work.** Reason: they are a **provable no-op** — the old policies are strict SUBSETS of the surviving ones, so they can neither widen access nor change a result; the §11 invariant they nominally offend ("no ledger table carries two permissive policies for one command") is about POLICY GENERATIONS DIVERGING, and these do not diverge. A migration to drop them would be pure churn against a live database for zero behavioural gain. **Recorded as a decision so the orphan sweep stops re-discovering them as unanchored work** — declining is an answer; silence is what makes something an orphan.
    - **★ LIVE CROSS-TENANT PROBE — PASSED (2026-08-09).** A **non-platform-admin** account (admin of one test company only) **sees only its own company**: every surface renders empty, **no cross-tenant rows anywhere**. This is the proof the spec insisted on — run as a normal user of one tenant, not as the operator, whose platform-admin bypass means their account *cannot* fail the test. **Timing matters: the probe PREDATES any real data exposure** — the first real third-party account (**Basement Ballin Studios**, owner `maxabdella6@`) now exists, and the wall was proven before it held anyone else's books.

  - **★ THE SECURITY LADDER (tracked, IN ORDER — each rung gates a growth stage). ★ ALL SIX ANCHORED 2026-08-11** — the sweep found every rung carrying **no roadmap row at all**: six live gates parked inside Tier 1 #11, an item marked ✅ DONE. **s1 → ROADMAP §0 TIER 1 #11a · s2 → #11b · s3 → #11c · s4 → TIER 2 (probe-as-script) · s5 → §2 Security `O100` · s6 → §2 Security `O101`.** Note what s2 holds: checks **(b)** signed/expiring document URLs and **(c)** key hygiene from the #11 audit, never separately reported as verified — so #11 was marked DONE with two of its four checks living on as an unanchored rung. The audit proved the database boundary. These are the rungs above it; **each is a gate, not a wish**, and the gate names the stage it blocks.
    - **(s1) 2FA sweep on operator accounts — Supabase, GitHub, Vercel, the app itself. GATE: THIS WEEK.** First for a reason: `is_platform_admin()` makes **operator credentials the master key to every tenant** (§3 Option A). Every wall the audit just verified is downstream of one password. The strongest RLS in the world is a rounding error next to a phished operator login.
    - **(s2) Service-key grep + document-URL check** — verify **no service-role key ships in the client bundle** (grep `dist/`, not just `src/`), and confirm document URLs are **signed and expiring**, never permanent. **GATE: before the warm channel widens.**
    - **(s3) AI-vendor data path IN WRITING** — which extraction provider sees statement contents, **retention terms**, **training-use terms**. **GATE: before the warm channel widens.** This is the **one gap in the client-facing story** below until it closes.
    - **(s4) Probe-as-script** — the cross-tenant probe becomes an **automated test in the suite**, not a memory of an afternoon. **GATE: next code session touching tests.** *(The C195(7) lesson applied to security: assert the mechanism FIRES. A probe nobody re-runs and a wall that quietly fell look identical from the outside.)*
    - **(s5) External fixed-price RLS / security review** — a second set of eyes on our own audit. **GATE: before the warm channel widens past the first operators.** Stated plainly: **we reviewed a thing we built**, and that limitation doesn't go away because the result was good.
    - **(s6) Third-party penetration test. GATE: before strangers at scale.** **SOC 2 remains a company milestone, unscheduled** (a program with an auditor and a calendar; nothing above depends on it).

  - **★ THE CLIENT-FACING STORY IS NOW WRITABLE — from verified facts ONLY (Tier 2, unblocked 2026-08-09).** What may be said today, because each clause has evidence behind it: **database-layer tenant isolation verified table-by-table (33/33) and probed live with a non-privileged account**; **documents held in private, company-scoped, encrypted storage**; **ledger writes role-gated**; and — the most credible claim of the four — **found-and-fixed, with the evidence attached (`061`)**. A security story that admits a real finding and shows the fix reads as true; one that claims perfection reads as marketing. **HARD RULE: the claims must never exceed that verified list.** The AI-vendor data path (s3) is **the single open gap** — until its answer is in writing, the story must **stay silent on what the extraction provider retains**, and must not imply otherwise by omission.

- **Backlog — sign-off DB-layer gating needs a non-platform-admin acceptance test (fold into team-invites; not built). DESIGNATED: ROADMAP §0 TIER 1 #8.** *(O87 note: the s4 probe-as-script rung now needs the same non-platform-admin account, so the two share a prerequisite.)* O83 role-gating verification flipped the operator's Franklin Ave role `admin → viewer` (DB CHECK rejects `'member'`; `'viewer'` is the valid non-privileged role) and confirmed **live** that the **UI layer** correctly blocks a non-reviewer from sign-off (`ReviewView` `canSignOff = isReviewer = canAttestPeriod('viewer') = false` → no sign-off card/button/month-picker/override). BUT the **DB-layer** rejection could **not** be proven with the operator's account: `is_company_reviewer(cid)` (migration 051) is `is_platform_admin() OR role IN ('admin','accountant')`, and the operator's email is the platform admin — so the policy would have **permitted** the write regardless of company role. Full end-to-end DB-layer proof (that `period_signoffs` INSERT is rejected for a non-reviewer) requires a **non-platform-admin test user**, which needs team invites. Fold in as an acceptance test when invites ship. *(Related finding surfaced same session: the app's `isMember = role === "member"` can never be true — the `company_users` role CHECK allows only `owner/admin/accountant/viewer`, never `'member'`; the app's "member" concept and the DB's `'viewer'` are out of sync. Reconcile the naming as part of the roles/invites work.)*

- **Backlog — Trust panel "Documents" queue line reads as an unearned/pending check (not built; UX-honesty finding). DESIGNATED: ROADMAP §0 TIER 2 "Activity-feed jargon scrub + trust-panel Documents queue glyph" — shares that line with the Home Activity feed item below.** The owner `TrustPanel` (`src/components/views/TrustPanel.jsx`) mixes two line semantics on one checklist: **Reviewed** and **Nothing wrong** are EARNED states (green `✓` only when verified — the O90 no-vacuous-pass rule), while **Documents** (`lines.captured`, sourced from `ownerTrust.js`) is a QUEUE — when empty it renders the `info`/neutral state whose glyph is a hollow gray dot `◦` (see the `Line` component: `glyph = state === "ok" ? "✓" : state === "attention" ? "•" : "◦"`). The **semantics are correct** — an empty queue must NOT get a celebratory green check (that would be exactly the vacuous-pass class we've been eliminating) — but **visually**, a gray dot sitting next to green checks reads as "pending/unfinished," and the O83 operator himself found it confusing. **Target:** differentiate QUEUE lines from STATE lines visually so "empty queue" doesn't resemble "unearned check" — e.g. a subtle inbox/tray icon instead of the dot, or restructure Documents to render as a **count chip** ("0 waiting") rather than a checklist row. When documents ARE waiting it should **escalate visually** (amber + count). **Keep the earned-state lines (Reviewed / Nothing wrong) exactly as they are** — do not touch the green-only-when-verified gate; this is a presentation change to the queue line only.

- **Backlog — Vendors tab: system/deposit entries leak in as vendors + blanket 1099 tagging is wrong (O83 finding, CPA-liability class). DESIGNATED: the LEAK → ROADMAP §0 TIER 2 ("vendors-tab leak: `vendorSummary` needs a source/side filter" — flagged demo-lethal); the 1099 half → TIER 1 #10 (minimal default-OFF) and TIER 3 (full derived eligibility).** Two defects on the Vendors surface (`VendorsView.jsx`, fed by `vendorSummary` in `App.jsx` + the `contacts` table). **(1) Non-vendor rows leak into the vendor list.** `vendorSummary` (`App.jsx` ~5586) keys a map on `inv.vendor` over ALL flattened ledger rows with **no filter on `source` or entry side** — so the system **opening-balance** entry shows up as a vendor named "Opening balances as of 2026-01-01" (Edit button and all), and a **revenue-deposit** line spawned a zero-transaction "Toast POS" vendor shell (deposits correctly aren't vendor payments, yet the description created a vendor). Some also arrive as real `contacts` rows via `createOrUpdateContact` in the booking path. **Target:** system/source entries (`opening_balance`, adjustments, and any non-A/P leg) must NEVER derive or create a vendor — filter `vendorSummary` to expense/A-P-side rows with a real counterparty, exclude system `source`s, and stop customer-side/deposit lines from spawning contacts; retro-filter the ones already created. **(2) Blanket "+1099" badge is wrong and creates CPA filing liability.** The badge renders off `v.is1099` for `fromContact` vendors (`VendorsView.jsx:49,233`) and is effectively defaulted-on rather than derived — on Franklin Ave nearly every vendor carried it, including **goods** vendors (Roma, Lone Star, Hill Country, Kitchen Supply — food/equipment purchases are **not** 1099-NEC reportable) and a utility. Only rent (Franklin Ave Properties, if non-corp) plausibly qualifies. Wrong flags → **wrong 1099 filings under the CPA's name at year-end.** **Target:** 1099 eligibility must be **derived** (services vs goods by GL category/`system_role`, payee **entity type** (corps exempt), and the **$600 `IRS_1099_THRESHOLD`**), or at absolute minimum **default-OFF with explicit CPA opt-in per vendor**. **Pairs with the Taxes/1099s Admin flow** whenever that gets built. *(Amounts on the tab verified correct — Lone Star $2,339.33 = both invoices; totals matched the answer key. This is a classification/listing defect, not a math defect.)*

- **Backlog — Document library misses bank statements + weak metadata (O83 finding, sign-off-trust class). DESIGNATED: ROADMAP §0 TIER 2 doc-library line, now widened to (a) route ALL intake paths through `storeDocument` [bank path done in C185] + (b) derived type + (c) document date.** Three defects on the Documents library (`storeDocument` in `App.jsx:530`; `docsFilterType`; `DocumentPreviewModal`/docs view). **(a) Not every intake path stores its file.** `storeDocument` is called ONLY from the universal-upload path (`App.jsx:3691` invoice, `3947` contract); **`handleBankFile` (Bank Import) and the Reconcile flow parse the statement but never call `storeDocument`.** So Franklin Ave's January bank statement — uploaded **4×** across Bank Import + Reconcile — is **absent from the library**, despite the header promising bank statements are stored. This is also why the 20 bank-line entries render **"No source document attached"**: their source PDF was never retained. **For the sign-off trust story this is load-bearing — the primary document behind a reconciled, attested period must exist in the system.** **Target:** route EVERY intake path (bank import, reconcile, payroll, QBO — not just universal upload) through `storeDocument`, linked to the entries/reconciliation it produced. *(Ties to the §11 North Star + `bank_account_id`-linkage backlog: persisting the parsed statement is the same missing-linkage workstream.)* **(b) Type is defaulted, not derived.** The universal path hardcodes `storeDocument(..., "invoice", ...)` (`App.jsx:3691`), so a utility bill and a register receipt both file as **"Invoice"**, and the filter chips imply an invoice-only taxonomy. **Target:** derive `document_type` from the same AI extraction that already classifies the doc (invoice / bill / receipt / bank statement / contract …) and broaden the filter chips. **(c) Only the upload date is first-class.** Cards show `uploaded_at` (e.g. "Jul 20") but not the document's OWN date, which we **already parse** during extraction. **Target:** capture and surface the document date as first-class metadata (sort/filter by it) so the library is usable for finding "the January statement" rather than "the thing I uploaded on the 20th."

- **Backlog — pattern-level duplicate suppression for legitimate recurring same-amount vendors (predicted at C178 review, confirmed live O83 Feb; not built).** C181 tightened the duplicate_payment window to 7 days so biweekly payroll / monthly insurance no longer false-fire. But a genuinely **weekly** same-amount vendor (e.g. Bluebonnet Linen at 7-day spacing) still trips the window every week, and each month's new charges carry **new fingerprints**, so the "duplicate" flags **re-fire monthly** and never converge — the same recurring vendor keeps generating HIGH anomalies the CPA must dismiss again and again. The real fix is **pattern-level suppression**: recognize a vendor as legitimately recurring at a cadence (the `recurring_transactions` machinery + `detectRecurringPatterns` in `insights.js` already model this) and **exclude its on-cadence same-amount charges from duplicate detection entirely** (a charge that matches the vendor's known recurring amount+cadence is expected, not a double-pay). This belongs with the recurring-transactions machinery, not the duplicate detector's date window. Until then, weekly-service duplicates are dismissable but noisy.

- **Backlog — opening-balance discrepancy as a persisted anomaly (deferred from C179; O83 follow-up to C178).** C179 fixed the opening-discrepancy comparand (GL cash at period start, not the opening row). Wiring the discrepancy as a **persisted anomaly** (C178 lifecycle) was deferred because it doesn't fit the `runAnomalyDetection` model: that reconcile AUTO-RESOLVES by re-running a pure function over the **ledger**, but an opening discrepancy needs the **statement's stated opening** (not in the ledger), so it can't be re-derived on a later scan. Wiring it would require a **bespoke imperative insert** at detection time (in `handleBankFile`) with its own fingerprint + a **custom auto-resolve** condition (resolves when the opening is corrected / the reconciliation is completed, not when a ledger scan stops seeing it). Today it surfaces as `openingDiscrepancyFlag` (React state) + an audit row + a notification — durable enough, just not in the `anomalies` table. **Target:** if opening discrepancies should live in the unified anomaly queue/trust-gate, add a non-detection anomaly source (imperative insert + explicit resolve on opening-correction) — a small dedicated lifecycle, not a `runAnomalyDetection` type.

- **Backlog — anomaly reviewer-comment feature (client adds context WITHOUT a dismiss verb; not built; O83 follow-up to C178).** C178 made anomalies persisted records with dismissal **permanently reviewer-gated** (dismissing judges a condition acceptable — a review act; client-side dismissal would be self-attestation one anomaly at a time). A client may still legitimately want to give the reviewer **context** on a flagged item ("that Sysco double-charge was a deposit + a bill, not a duplicate"). **Target:** a lightweight **comment/note** thread on an `anomalies` row (owner may ADD context; only the reviewer DISMISSES) — a comment is not a clear action and never changes `status`. This preserves separation of duties (the owner-facing panel already shows the honest view; see `ownerTrust.js` "your accountant is taking a look") while letting the client inform the judgment. Small: a `anomaly_comments` child table (or an `entity_refs`-style jsonb thread) + owner-visible input in the trust panel + reviewer-visible display in the Review Unusual-Activity section. Do NOT add a client dismiss verb.

- **Backlog — Review-queue anomaly cards lack inspection affordances (O83 Feb; fix when touching the queue next).** Asymmetry: the OWNER's home trust panel gives each anomaly **"View transaction →"** and **"Review →"** links (`DashboardView.jsx`), but the CPA **Review** screen — where dismiss-with-reason actually happens (the reviewer-only judgment, C178) — offers **only Dismiss**. So the reviewer must judge a possible duplicate/recurring charge **without seeing the underlying entries** — exactly backwards (the person making the call has the least context). **Target:** the anomaly cards in the Review Unusual-Activity section (`ReviewView.jsx`) should link to the involved transactions — **`entity_refs` are already on the row** (the linked journal-entry ids), so it's a lookup + navigate, mirroring the owner panel's `openTxn`. **Ideally show the pair INLINE** (the two entries' dates + amounts side by side) so the duplicate/recurring judgment is one glance rather than a navigate-away. Pairs naturally with the reviewer-comment feature above (both touch the same Review anomaly card). Small; do it the next time the anomaly queue is edited.

- **Backlog — evidence-attached anomaly resolution (operator idea, O83 Feb; extends C178 dismiss-with-reason).** Extend the reviewer's dismiss flow so a dismissal MAY carry **supporting document(s)** — a vendor agreement, the second (distinct) invoice, correspondence — attached as proof the flagged charge is legitimate. Store via the **document library** (`storeDocument`) and link from the `anomalies` row (`evidence_doc_ids` jsonb, mirroring `entity_refs`), surfaced in the dismissal history so a future auditor sees *why* it was cleared, not just that it was. **Reason stays REQUIRED (floor); evidence is OPTIONAL but first-class (ceiling)** — never hard-require support: legitimate dismissals often have none (verbal confirmations, obvious re-reads), and forcing an upload would push reviewers to attach junk. Consider a **soft prompt** (suggest, don't block) to attach evidence above a **$ threshold**, where an unsupported clear is higher-risk. **Related auto-resolve mechanic:** an uploaded doc that BOOKS and gives the flagged charges **distinct backing** (e.g. two same-amount charges turn out to be two separate invoices) dissolves the anomaly's *condition* → the C178 reconcile **auto-resolves** it on the next scan, no dismissal needed — the cleanest outcome, and it flows through the existing lifecycle for free. **Pairs with** the owner-comment feature (client uploads the support, reviewer decides) and the Review-card inspection-affordances item above — all three touch the same Review anomaly card and the same `anomalies`-row-plus-jsonb-links shape, so build them together.

- **✅ SHIPPED (C198·3b(e), `30cf680`) — burn/runway revenue-blind, false runway alarm for profitable companies.** *(Was ROADMAP §0 TIER 1 #9, folded into C198; a profitable month now reads the same figure as COVERAGE and carries no alarm. Original finding kept below for the record.)* *(orig: not built; honesty-violation class).** `businessHealth` (`reports.js`) computes "Monthly burn" via `computeBurnRate`/`burnRateDetail`, which is the trailing-3-month average of **GROSS EXPENSES only** (`plLegs(i, glIsExpense)`), ignoring revenue entirely; it then feeds that into `computeRunway(cash, burn)`. O83: Franklin Ave — a **profitable** January (+$3,174.33 net income, cash grew) — displayed "Monthly burn $13,142.15 · Runway ~1 mo · **Needs attention**" and self-contradicting copy "You're profitable **with ~1 month of runway**." A false "you're almost out of money" is the **same honesty violation as the false-greens we've been eliminating all week — in the scary direction** (arguably worse: it frightens a healthy client). **Target:** burn = **net cash change** (or net income as a proxy), period-normalized correctly. A profitable / cash-positive run rate → **no runway warning at all**, and a positive frame ("You added $3,174 in January"); runway warnings reserved for genuinely **cash-negative** run rates, in plain language. **Blast-radius caveat:** `computeBurnRate` is the SINGLE shared burn figure across the dashboard card, the runway drill, and the AI financial snapshot (deliberately, so they never drift). Redefining "burn" as net-of-revenue must flow through all three consistently — don't fork a second definition — and re-check `businessHealth`'s `concerns`/`tone` gating (the `runway < 6` / `burnUpPct` branches) and the KPI strip's Burn Multiple, which already handles the profitable case correctly (`netBurn <= 0 → "0.0× (profitable)"`) and is the model to mirror.

- **Backlog — reconcile should not re-upload an already-parsed statement (bank-linkage workstream; ROADMAP §0 TIER 2 "ReconView sources persisted statement lines instead of re-parsing" — C185 made this possible).** The Reconcile tab makes the user re-upload + re-parse the SAME statement Bank Import already read — wasteful and fragile (it's how the O83 BUG-1 parse regression surfaced: two independent parse call-sites). Root: imported statement lines aren't persisted anywhere queryable — there's no `bank_account_id` linkage on booked entries and no stored statement-line table, so the reconcile flow can't source them. Target: persist parsed statement lines against a `bank_account_id` (+ statement period/balance) at import, and have Reconcile source them from storage instead of re-parsing. This is the **third symptom of the missing bank-account linkage** — after (1) the decoupled Settings `current_balance` (booked statement txns don't update it) and (2) the placeholder-detection residual (a real account looks like the seed) — all resolved cleanly once entries carry a `bank_account_id`. See O83 Issue 3 (deferred).

- **Backlog — Reconcile's Bank Ending Balance field should PREFILL from the parsed statement (O83 Feb; North Star adjacent, cheap interim). PROMOTED onto C198 by O86 finding (l) — and now sourced from the PERSISTED statement (`bank_statements.stated_ending_balance`, C185) rather than a re-parse.** The parse **already extracts `endingBalance`** (the opening-balance adoption flow uses it — `deriveStatementOpening` / the proposal path), but the reconcile completion bar leaves the **bank ending balance field blank** and makes the user **retype a number the app just read**. O83 live-drive: the operator **stalled here expecting it to be automatic**, and manual retyping invites **transposition errors** against the verified-balance guard (`statementBalanceVerified` / `canCompleteReconciliation`) — a fat-fingered ending balance either false-blocks a clean reconciliation or (worse) "verifies" a wrong number. **Fix:** prefill the field from the parsed `endingBalance`, clearly **editable** and labeled **"from your statement"** — the user **confirms rather than transcribes**. Keep the verified-balance guard as-is (a confirmed prefill is still a real, verified balance). **Full automation** of this step (statement-derived ending balance → auto-completed verified reconciliation, no field at all) belongs to the **★ North Star pipeline**; the prefill is the cheap interim that removes the stall + the transcription-error class now. Lives in `ReconView.jsx` (the completion bar's `statementBalance` input) — source the value the same way the opening-proposal flow already does.

- **Backlog — business-type-driven chart of accounts (HIGH, pre-launch; not built). DESIGNATED: ROADMAP §0 TIER 1 #6.** `CompanySetup` asks for business type but `seed_company_accounts` seeds an **identical generic SaaS-flavored COA** regardless. O83: Franklin Ave Pizza (type Restaurant) got Subscription Revenue, Technology & Software (SaaS), ROU Asset Amortization, ASC 842 lease accounts — and **lacked** Merchant/Processing Fees, Bank Service Charges, and any food-cost structure. This causes real miscategorization *because the AI categorizer is constrained to the chart it's given*: the Toast merchant fee (bank memo literally "MERCHANT FEES JAN") booked to **6500 SaaS** because no correct account existed, and the bank service fee oscillated **8000 Interest ↔ 7100 Miscellaneous** across runs for the same reason. **Target:** per-business-type COA templates chosen at company creation (drive off the business type already collected). The **restaurant** template must include COGS-Food, Merchant/Processing Fees, Bank Charges and OMIT SaaS/lease-accounting accounts. Industry-appropriate seeding removes a whole class of miscategorization at the root (upstream of the categorizer, the clarification floor, and the control totals). Lives in `seed_company_accounts` (RPC) + `DEFAULT_CHART_OF_ACCOUNTS`/role map (`src/lib/constants.js`); keep the `system_role` scheme (§4) so downstream role lookups still resolve per template.

- **Backlog — onboarding vendor teach-in → ENTITY PROFILES (ROADMAP §0 TIER 3; sequenced AFTER business-type COA seeding (Tier 1 #6); supercharged by QBO import).** New-client cold start currently relies on **reactive** learning (a human recodes → a vendor→account mapping is written, `learnFromCorrection`/`clientProfile.js`), so the FIRST real month books against cold guesses and only gets smarter after mistakes. **Front-load it instead:** at onboarding, ingest **2–3 months of historical bank statements + any invoice pile** (or a **QBO export — the richest source**, years of confirmed vendor→account history), derive the **vendor universe** (name, typical amounts, cadence, and a proposed account mapping against the **business-type COA** above), and present the reviewing CPA a **single bulk confirmation table**. Each confirmation writes a **`human_correction`-grade vendor mapping BEFORE the first live month runs**, so the pipeline's first real statement books against **confirmed priors** instead of cold guesses (turns C186's cold start warm). **Design constraint:** teaching must be **derived from artifacts the client already has — never homework** (no "upload an example invoice per vendor"). **CPA-as-teacher is the intended onboarding motion** (fits the CPA referral channel — the reviewer confirms the mappings once, up front). **Future extension:** per-vendor **document exemplars** for parse robustness — only if parse accuracy proves to need it. Pairs with the business-type COA (the mapping target) and QBO import (the richest teach-in source). **Generalization (same architecture, later phases):** vendor teach-in is the FIRST instance of **ENTITY PROFILES** — per-client priors on any recurring entity (**vendors, customers, payroll, rent/loan obligations**), each holding **document exemplars**, **typical money-movement patterns** (amount, cadence, channel), and **confirmed accounting treatment**. **Customers are the second target:** A/R deposit matching (mangled descriptors, batch deposits, short-pays) benefits most from knowing how each customer typically pays. Teach-in **populates** profiles from historical artifacts at onboarding; the live learning layer **updates** them from corrections thereafter. **Build as ONE profile system, not per-type features.**

- **Backlog — AI confidence calibration + reasoning-consistency guard (not built; ties the clarification-flow work + Cardinal Principle). DESIGNATED: ROADMAP §0 TIER 1 #7.** O83: the Lone Star bill booked to **6400 Travel & Entertainment at 92% confidence** while its OWN stored reasoning argued the items are "direct product costs properly classified as **Cost of Goods Sold**" — an internally contradictory rationale that sailed past the 70% clarification floor. **Cheap guard:** when the reasoning text's recommended account disagrees with the *booked* account, CAP confidence below the clarification floor (`AI_CONFIDENCE_ASK_FLOOR`) so it routes to the plain-language question flow instead of auto-booking (a self-contradiction is exactly a "needs a human" signal — cf. §5 action sandbox, `confidenceFlag.js`/`autoBookDecision`). Two smaller finds bundled here: **(a)** owner-visible AI **reasoning leaks jargon** ("50% deductible under current tax law", raw GL codes) — reasoning *shown to owners* needs the same Cardinal-Principle scrub as clarification copy (`containsOwnerJargon`), while the CPA-facing `reasoning` field may stay technical (§9 exemptions); **(b)** the payment-side entry renders the literal placeholder **"Loaded from database"** in its AI Reasoning panel (from `flattenJournalEntries`' default `reasoning`) — a display bug: suppress/replace the placeholder for entries that have no real AI rationale (payments, clearings, opening balances).

- **Backlog — Home Activity feed renders GL jargon on the owner surface (Cardinal Principle; O83). DESIGNATED: ROADMAP §0 TIER 2 "Activity-feed jargon scrub + trust-panel Documents queue glyph" — paired with the trust-panel item above. (Distinct from Tier 1 #7's owner-jargon scrub, which covers AI reasoning copy, not audit detail rendered into the feed.)** The Home "Activity" feed (`DashboardView.jsx:788`) renders `auditLog` rows verbatim as `a.detail || a.action` — and `markBillPaid` (`App.jsx:5283-5285`) bakes GL notation into that detail: *"paid Franklin Ave Properties LP · $2400.00 via ACH / Bank Transfer · **GL Dr AP/Cr Cash posted**."* Everything through "Bank Transfer" is good plain language; the `· GL Dr AP/Cr Cash posted` tail (from `glStr`, and the AR twin `Dr Cash/Cr AR`) is bookkeeping jargon that belongs in the **audit trail, not the client feed** — a Cardinal-Principle violation on an owner surface (same class as the owner-jargon find above). **Target:** keep the GL notation in `audit_log` (ideally in the structured `after_state`, where the `payment_entry_id` already lives, or a separate technical field — NOT the human `detail` the feed shows), and render the owner Activity line without it. Cleanest is to **stop appending `glStr` to the `logAudit` detail string** and instead scrub owner-facing activity lines (a `containsOwnerJargon`-style strip on the feed), so the audit detail stays complete for the CPA/audit view while the client sees only plain language. Small; audit-trail completeness must not regress. *(Broader: the Activity feed rendering raw audit detail is a general leak vector — any audit detail with GL/debit-credit wording will surface on Home; a feed-level scrub covers the class, not just this one line.)*

- **★ Cold-start categorization is the top-of-funnel first impression — the two items above JOINTLY own it (elevated after the first live demo).** In the **first live demo on an unconfigured test entity**, a clearly-identifiable **utility invoice booked to Miscellaneous in front of a prospect** — the single worst cold-start failure (a document any human reads at a glance, mis-booked, with no smart question asked). This is the intersection of the two workstreams above: the **business-type COA** (an unconfigured/generic entity may lack the right expense account, so the categorizer has nowhere correct to put it) and the **confidence/reasoning guard** (a low-confidence or account-less booking should ASK a plain-language question, never silently fall back to Miscellaneous). Together they own the promise: **"the first document a new signup uploads books correctly OR asks a smart question"** — never a silent wrong bucket. Treat that sentence as the joint acceptance test for both items; a Miscellaneous fallback on a recognizable vendor/category is a hard fail. **Separately — the DEMO experience may deserve its own pass:** a **pre-seeded showcase company** (representative COA + realistic booked history + a clean sample document) so sales demos never run on a cold-start entity at all. That's a distinct workstream from fixing cold-start itself (which still must be fixed for real first-time signups) — a demo shouldn't be the thing that surfaces cold-start gaps, and cold-start shouldn't be demoed raw.

- **Normalized reconciliations model is dead schema. DESIGNATED: ROADMAP §0 TIER 3 "normalized reconciliations model".** `reconciliations` was created (`005`) in a normalized shape (`bank_account_id`, `statement_date`, `statement_ending_balance`) with a child `reconciliation_items` table. The app never adopted it — ReconView and the bank-upload flow read/write a **denormalized** record instead (`account_id`, `statement_balance`, `books_balance`, `difference`, and jsonb arrays of matched/unmatched transactions). Migration `035` added those denormalized columns and relaxed the three unused NOT-NULLs so the insert succeeds. The normalized columns and `reconciliation_items` remain but are unused. **Future consideration (not now):** migrate reconciliation records to the proper normalized relational model (`reconciliations` header + `reconciliation_items` lines, FK to `journal_entry_lines`) for audit-grade record-keeping. This is a deliberate, separate project — the denormalized shape is the working source of truth today.
- **`ap_invoices` — RESOLVED (dropped in migration `045_drop_ap_invoices.sql`, applied 2026-06-26; C78).** It was a fully-structured parallel AP-invoice model that zero app code referenced (payables are booked to `journal_entries`); the table and the dead `ap_aging` view were confirmed empty/unreferenced and dropped. *(This entry previously read "left in place" — stale, corrected by the orphan audit.)*
- **Schema is now reproducible from the repo (resolved).** Previously the migrations could not rebuild from empty — ~21 core tables predate `001` with no `create table` DDL, and `001` assumes they exist. Fixed by `000_baseline_schema.sql` (a schema-only `pg_dump` of live): **empty DB → `000` → `001…036` reproduces the live schema.** Caveats: `000` is for a fresh database only (bare `CREATE TABLE`; do not run on production); it targets a fresh **Supabase** project (relies on the `auth`/`storage`/`extensions` schemas and `auth.uid()`); and a trigger on `auth.users` that syncs `public.users` lives outside `public` and is not captured by the dump.
  - **DECLINED 2026-08-11 — the `auth.users` trigger gap stays documented, not fixed. No row, no work.** Reason: it is a **provable no-op for the stated purpose** — `000` exists to rebuild a DEVELOPMENT database from empty, and a fresh Supabase project recreates that trigger through normal signup wiring; the gap has never once been hit, because nobody has rebuilt production from `000` and nobody should (`000` is fresh-database-only by design). Capturing cross-schema objects would mean owning a `pg_dump` of `auth`, which is Supabase's schema to change under us. **The honest caveat above IS the deliverable.** *(Noted while here: the sentence says `000` → `001…036`, while the migrations ledger runs through `061` — the rebuild claim is scoped to the baseline chain, not to current live.)*
- **Function divergence from missing migrations (resolved by `036`).** Five SECURITY DEFINER functions — `is_company_member` (the platform-admin/Support-Mode bypass), `post_journal_entry`, `seed_company_accounts`, `security_check`, `list_company_members` — were last modified live by the un-committed `016/017/020/025`, so the committed chain holds older bodies. On a rebuild, the older `create or replace` would clobber the live version. `036_reassert_baseline_functions.sql` re-asserts the live bodies verbatim (a no-op on production), so a rebuild terminates exactly at live. **Lesson:** absence of a migration file does not imply absence live (e.g. `notifications` came from an un-committed `024`); verify against `information_schema`, never infer from the repo alone.

## 12. GAAP correctness as tested code

Every economic event's journal entry must be a **pure `buildXEntry()` in a lib module with a unit test asserting exact Dr/Cr/amount, routed through the handler — never built inline in a view or by the AI.** `tests/gaapInvariants.test.js` is the living spec/guardrail: over a fixture of every event it asserts (a) Dr = Cr, (b) Assets = Liabilities + Equity + NetIncome, (c) payments/collections/opening/reversals leave net income unchanged, (d) only 4xxx/5–8xxx move net income. The AI may **reason and explain** with GAAP knowledge but **only executes whitelisted actions** (`aiCapabilities.js`); it never constructs raw entries.

**The 17-event inventory** (status tracked in the guardrail/remediation): 1 book bill, 2 pay bill (✅ Step 1), 3 direct-to-cash, 4 issue invoice, 5 collect (✅), 6 opening balances, 7 bank/cash opening, 8 depreciation, 9 prepaid capitalize+amortize, 10 accrued liabilities, 11 deferred-revenue receipt+recognition, 12 lease (ASC 842), 13 payroll, 14 void→reversing entry, 15 reversal, 16 sales tax, **17 year-end closing** (Dr each Revenue / Cr each Expense / plug net to Retained Earnings 3100; zeroes the period's P&L; new year starts at zero).

**Fiscal-year / Retained-Earnings model (built — derived soft close, Option A):** `reports.js` `fiscalYearSplit(invoices, {asOf, fiscalYearEnd, cutoffDate})` (+ pure `fiscalYearStart`) splits all-time net income into prior-fiscal-years' closed net (→ **beginning Retained Earnings**, always shown as a distinct line) and the current FY's net (→ **Net Income (current period)**), keyed off `companies.fiscal_year_end` and floored at `cutoff_date`. The Balance Sheet shows the two as separate lines summing to Total RE; `priorNet + currentNet === all-time net`, so every total and the balance check are unchanged — only the RE split. This fixes the pre-fix bug where a prior-year (e.g. 2024) loss was mislabeled "current period" and the Balance-Sheet net income diverged from the Income Statement from a client's 2nd fiscal year on. Guardrail: `gaapInvariants.test.js` "fiscal-year RE split" asserts prior+current=all-time, the year-boundary case (prior loss → beginning RE, not current), FYE handling, and cutoff flooring. No posted closing entries required (that's the deferred hard close, #17).

### Governing principles (conversion & cash)

- **Clean cutoff date ("Day One").** Each company has one `companies.cutoff_date`. The entire starting position is **one balanced opening journal entry** as of the cutoff — the prior trial balance (assets, liabilities, already-accumulated retained earnings), plugging the residual to **Opening Balance Equity (3400)**. **No transaction may be dated before the cutoff**; pre-cutoff activity is represented solely by opening balances. This makes the retained-earnings double-count structurally impossible. Enforcement is **hybrid**: `persistJournalEntry` hard-rejects pre-cutoff bookings with a redirect ("record it by adjusting your opening balances"); imports skip pre-cutoff rows; posting opening balances is hard-blocked if live pre-cutoff transactions exist (`preCutoffActivity`); legacy companies with no cutoff get no enforcement (warn-only). The cutoff is editable until the opening entry is posted, then **locked**. Opening balances post through the canonical path (`buildOpeningBalanceEntry` → `post_journal_entry`, `source='opening_balance'`) **and** persist to the `opening_balances` table (`journal_entry_id`, `posted=true`); `loadAllData` reads them back so they survive refresh. Editing = reverse/replace (soft-delete the prior opening JE + rows, repost) — never duplicate.
- **Bank-as-source-of-truth.** A bank account's `current_balance` IS the opening balance of that bank's cash GL account. It flows through the **same** opening entry (Dr that bank's cash GL / Cr OBE), keyed by GL account; bank-linked cash accounts are shown **read-only** in the opening-balances grid (valued from the bank), so no GL account is opened twice. A cash balance is never a free-floating field invisible to the GL.
- **The QBO-import model is the same** (item 43): trial balance at the cutoff as the opening entry, then live transactions forward — not bulk pre-cutoff history. Keep them consistent.
- **Cash on hand is GL-derived (single source).** Every displayed cash figure (dashboard card, runway, health score, KPIs, monthly report, AI snapshot) derives from `reports.js` `glCashOnHand(invoices, cashCodes, {asOf})` = Σ `glAccountBalance` over the cash/cash-equivalent accounts (roles `cash`+`savings` + bank-linked GL codes), computed once in `ERP` as `glCash`. So dashboard cash === Balance Sheet cash line === `glAccountBalance(cash)` by construction. `computeCashPosition` and the ad-hoc runway-drill formula were removed. The **bank statement balance** (`bank_accounts.current_balance`) is kept but only as the reconciliation target (what the bank says) — never the displayed cash on hand (what the books say); a difference is a reconciliation item.
- **Parallel P&L derivations are lock-tested, not rewritten (cluster #4).** `computeRevenue/Expenses/NetIncome` and `trialBalance` already re-derive from the ledger (sum of entries), so they were left as-is rather than rewired through `glAccountBalance` (lowest-severity, highest-blast-radius). `reconciliation.test.js` "cluster #4 lock" asserts they EQUAL the `glAccountBalance` derivation (revenue = Σ over 4xxx, expenses = Σ over 5–8xxx, net income, and trial-balance per account), so any future drift between the two derivation paths fails CI. (The previously-known nuance — revenue/expense booked on an entry's *offset* leg would diverge — is now **resolved** by the Phase 0 flatten fix below: the P&L line is always the primary `gl_code`, so `computeRevenue`/`Expenses` and `glAccountBalance` agree even for `Dr A/R · Cr Revenue` and `Dr Deferred Rev · Cr Revenue` entries.)
- **AR mirrors AP (cluster #2, built).** AR Outstanding (Dashboard, ArView, AR aging total) reads `glAccountBalance(accounts_receivable)` — the same single GL source as AP. Collecting an invoice posts `Dr Cash / Cr AR` through the canonical `markBillPaid(side:"ar")`/`buildPaymentEntry`; the flag-only/local-only collection paths were fixed (SendInvoiceView "mark paid", bank auto-match, and `applyMatch` full matches all route through it; partial matches stay flag-only pending a partial-clearing feature). `planArBackfill` + `040_ar_collection_backfill_oneshot.sql` exist and are tested (symmetric to AP `039`) but are a **no-op on current data** (dry-run showed zero candidates) — kept for a future real-client conversion.
- **AP / GL-balance single source (AP Step 2, built).** `reports.js` `glAccountBalance(code, invoices, {asOf})` is THE canonical GL balance of any account (sum of its journal-entry-line movements, normal-balance signed; primary leg + offset leg for simple entries; multi-line `_` rows primary-only). The Balance Sheet (`getBal`), the Payables "Outstanding" total, and the Dashboard AP total all read it, so they reconcile by construction. "Amount owed" = the GL balance of Accounts Payable (not a `payment_status`-flag sum); the flag still drives the per-bill paid/unpaid LISTS for display. NOTE: the AR/AP **aging** report and the AI `computeAP` remain payment_status/due_date views (aging needs them); they fully converge with the GL only after **AP Step 3 (historical backfill)** posts the missing Dr-AP/Cr-Cash entries for bills marked paid before Step 1. So post-Step-2 the GL-based surfaces MATCH each other but aren't yet CORRECT until Step 3.

- **Depreciation (#8, Phase A, built).** Capitalizing a purchase now creates a real `fixed_assets` master + a generated `depreciation_schedule` (migration `041`), replacing the dead `needs_depreciation` flag (which never produced an entry). Capture: the GAAP clarification's capitalize option carries an **AI-suggested useful life** (`suggestUsefulLifeMonths` — buildings 39yr, vehicles/tech 5yr, furniture/equipment 7yr, default 5yr), overridable in the confirm step along with **salvage (default $0)** and **in-service date (defaults to purchase date)**. `createFixedAssetWithSchedule` inserts the asset + pending monthly rows from `buildDepreciationSchedule` (straight-line; last month absorbs rounding so Σ === cost − salvage exactly). **"Run depreciation through DATE"** (ReportsView control → `runDepreciationThrough`) posts every pending row due on/before the date as `Dr 6900 / Cr 1510` through the canonical multi-line path, stamps it `posted` + links the JE, and **auto-flips the asset to `fully_depreciated`** once its last pending row posts (`planDepreciationRun` decides due-rows + flips, purely). Idempotent (only pending rows). Lib `src/lib/depreciation.js`; pure builders + the run-planner are unit-tested (`tests/depreciation.test.js`: suggestion, schedule sums-to-base, periodic posting, fully-depreciated flip) and `buildDepreciationEntry` is wired into the gaapInvariants guardrail (#8). Deferred variants (declining-balance/units-of-production/MACRS) noted below.
- **Canonical multi-line write path (Phase 0, built).** Multi-line economic events (deferred-revenue recognition #11b, prepaid amortization #9, lease commencement #12, and going forward payroll #13 + sales-tax invoices #16) post as **ONE balanced journal entry with N lines** through `buildJournalEntry({lines})` (`src/lib/journalEntries.js`, pure + tested) → `persistMultiLineEntry` (`App.jsx`, resolves codes→ids and calls `post_journal_entry` once). This **replaces the per-line expansion** in `postContractEntry`/`postAllContractEntries` that posted each line as its own 2-line JE — an active **double-count** bug (a 2-line recognition entry posted revenue on both a primary and an offset leg → 2× revenue). Contract posting now builds one entry per contract JE, refuses unbalanced entries, and reloads to reflect the single posted entry. Paired fix: `flattenJournalEntries` now picks the **P&L line as primary `gl_code`** for simple 2-line entries (was first-debit), so revenue/expense never strands on the offset leg — this is what makes `computeRevenue`/`Expenses` agree with `glAccountBalance` (and repairs the latent post-reload bug where `Dr A/R · Cr Revenue` flattened with A/R as primary). Lock: `tests/multiLineEntry.test.js` runs the **real** `flattenJournalEntries`→derivation path and asserts on a deferred-revenue fixture that `computeRevenue === glAccountBalance('4200') === 500` (the agreement) and that posting once yields 1× not 2× (no double-count); `tests/journalEntries.test.js` covers the builder (balance, N-line, zero-line drop, cent rounding, unbalanced→refused).

**Standard variants deliberately deferred (industry-standard common case built first; full variants are known, not silently missing):**
- **Sales tax (#16):** built = single blended rate on outgoing AR invoices, `Dr A/R / Cr Revenue / Cr Sales Tax Payable (2350)` (sales tax is a liability, never revenue). Deferred = **multi-jurisdiction / per-line tax rates** and the **universal-upload sales path**.
- **Depreciation (#8):** built = **straight-line** monthly schedule, `Dr Depreciation Expense (6900) / Cr Accumulated Depreciation (1510)`. Deferred = **declining-balance, units-of-production, and MACRS** methods (and any mid-year convention).
- **Payroll (#13):** built = standard multi-line `Dr Salaries (gross) / Dr Payroll Tax Expense (employer) / Cr Cash (net pay) / Cr Payroll Taxes Payable (withholdings + employer)`, net pay → Cash (assumes already disbursed). Deferred = the **accrue-then-pay two-step** (accrue net+taxes to a payroll-liability, then relieve on disbursement).

**Deferred / specified-but-not-built:** **hard close** (actually posting #17 closing entries + period locking) — build when period-locking becomes a real need; pairs with reconciliation locking. The soft-close derivation is the correctness fix; hard-close is the future locking feature. **Reversal display marker** — show the original entry struck-through / "reversed on DATE" when a live reversal links to it (display annotation only via `import_metadata.reverses`; does not affect calculations). Small follow-up to #14. **"Redo opening setup" flow** — a deliberate, guarded action that reverses the posted opening entry and unlocks the cutoff, for the rare wrongly-configured-cutoff case; default stays locked. Follow-up to #6/#7.

**Remediation order:** #14 void→reversal → #6/#7 opening + bank (with the cutoff model that prevents RE double-count) → fiscal-year RE split + #17 + multi-year guardrail assertions → AP Step 2 (canonical `glAccountBalance`) + Step 3 (backfill) → **Phase 0 canonical multi-line write path (built)** → **Phase A missing events** (#8 depreciation straight-line ✅ built, #11 deferred-revenue receipt, #16 sales tax single-rate) → **Phase B shape-extract inline builders** (#4 AR issue, #9 prepaid, #13 payroll net-to-Cash incl. fixing payroll's never-persisted `setInvoices`-only bug, #11b recognition / #12 lease rerouted through the multi-line path). Each event = pure `buildXEntry()` + unit test asserting exact Dr/Cr, validated against `gaapInvariants`, routed through the handler.
