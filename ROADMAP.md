# ROADMAP.md — Shadow CFO, single source of truth

This is **the** authoritative roadmap. When the user says "add to the roadmap," append it here
(Section 2), don't just acknowledge it in chat. Items have stable ids (`C#` completed, `O#` open);
ids are never reused. Keep the two sections separate. Mark an item DONE only when it's genuinely in
the codebase (builder/function exists, tests pass, migration applied/committed).

- **Last updated:** 2026-06-27
- **Test suite:** 468 passing (`npm test`, 36 files). **Build:** clean (`npm run build`).
- **Migrations:** `000`–`045` applied (numbering non-contiguous; `045_drop_ap_invoices.sql` applied 2026-06-26 — dropped the orphaned `ap_invoices` table + dead `ap_aging` view, confirmed 0 rows). `046_company_aliases.sql` written (optional — adds `companies.aliases` for O75 self-identity; name-based direction works without it).
- **Evidence** column points at the commit / lib file / migration / test that proves the item.

---

## SECTION 1 — COMPLETED (verified in code)

### Accounting correctness engine (the GAAP/"one source of truth" program)

| id | item | evidence |
|----|------|----------|
| C1 | Reconciliation canonical calculation layer + reconciliation test suite (all surfaces unified) | `fcbd7de`, `reports.js`, `tests/reconciliation.test.js` |
| C2 | Schema-drift audit; missing payment/approval columns reconciled | `66e07ed`,`ef4ee19`, migrations `032`,`034`,`035` |
| C3 | DB baseline + reproducible-from-empty (`000` baseline, `036` function re-assert) | `699cfcb`,`93ab5c7`, `000_*`,`036_*` |
| C4 | Approval-status uuid write + account auto-creation column fixes (writeShapes) | `ef4ee19`, `src/lib/writeShapes.js`, `tests/schemaContract.test.js` |
| C5 | Post-booking visibility invariant + alerting | `9ddd9d3`, migration `030`, `tests/freshBooking.test.js` |
| C6 | Mark-as-Paid persistence: canonical payment state + write verification + id resolution | `7d3e4f3`,`6d3522f`, `tests/paymentState.test.js` |
| C7 | GAAP guardrail (`gaapInvariants`) — living spec over every economic event | `868e31f`, `tests/gaapInvariants.test.js` |
| C8 | Payment-posting integrity (Step 1): paying a bill posts a balanced GL entry | `183c4e4`, `src/lib/payments.js`, `tests/payments.test.js` |
| C9 | #14 Void → real GAAP reversing entry; AI reverse double-negation fixed | `fefd399`, `src/lib/journalEntries.js` (`buildReversalLines`) |
| C10 | #6/#7 Opening + bank balances on the clean-cutoff model; OBE (3400) | `e8022f0`,`c72a18e`, migrations `037`,`038`, `src/lib/openingBalances.js`, `tests/openingBalances.test.js` |
| C11 | Cutoff enforcement up front in booking handlers + optimistic-add rollback | `9a9747c` |
| C12 | Fiscal-year Retained-Earnings split (derived soft close, Option A) | `4d91359`, `reports.js` (`fiscalYearSplit`/`fiscalYearStart`) |
| C13 | AP Step 2: canonical `glAccountBalance` — Balance Sheet AP + Outstanding read one GL source | `2efb0ab`, `reports.js` (`glAccountBalance`) |
| C14 | AP Step 3: historical backfill (tested planner + idempotent one-shot) | `da46d72`,`3180fa2`, `src/lib/apBackfill.js`, migration `039`, `tests/apBackfill.test.js` |
| C15 | Single-source cluster #1: cash on hand derives from the GL | `314e7c7`, `reports.js` (`glCashOnHand`) |
| C16 | Single-source cluster #2: AR collection posts to GL + AR Outstanding GL-derived; AR backfill | `8400090`, migration `040` (no-op on current data) |
| C17 | Single-source cluster #3: AR/AP aging totals reconcile with the canonical predicates | `reports.js` (`agingReport`,`arApTotals`) |
| C18 | Single-source cluster #4: parallel P&L derivations lock-tested vs `glAccountBalance` | `11447ce`, `tests/reconciliation.test.js` (cluster-#4 lock) |
| C19 | Bank-import match→book three-bug fix (stable id, `isArMatch`, no silent flag) + integration test | `d6627b3`, `src/lib/bankMatch.js`, `tests/bankMatch.test.js` |
| C20 | Phase 0: canonical multi-line write path (`buildJournalEntry`→`persistMultiLineEntry`) | `f7db564`, `src/lib/journalEntries.js`, `tests/journalEntries.test.js` |
| C21 | Deferred-revenue double-count fix + flatten P&L-primary (computeRevenue==glAccountBalance) | `f7db564`, `src/lib/ledger.js`, `tests/multiLineEntry.test.js` |
| C22 | #11b recognition / #12 lease rerouted through the multi-line path (no per-line expansion) | `f7db564` (`postContractEntry`/`postAllContractEntries`) |
| C23 | #8 Depreciation: `fixed_assets` + `depreciation_schedule`, run-through-date, fully-depreciated flip | `2b027dd`, migration `041`, `src/lib/depreciation.js`, `tests/depreciation.test.js` |
| C24 | Depreciation silent-failure fix: atomic capitalize-or-compensate | `1ee4d72` (`createFixedAssetWithSchedule`/compensation) |
| C25 | Attach-depreciation-to-existing-asset (reusable backfill action, idempotent) | `44368be` (`attachDepreciationToExistingAsset`) |
| C26 | #11 Deferred-revenue receipt: Dr Cash / Cr Deferred Revenue (2300) | `fd9bb9a`, `src/lib/revenueEntries.js`, `tests/revenueEntries.test.js` |
| C27 | #4 AR-issue builder (Dr A/R / Cr Revenue) extracted as `buildArInvoiceEntry` | `0276897`, `src/lib/revenueEntries.js` |
| C28 | #16 Sales tax Step 1: 3-line Dr A/R / Cr Revenue / Cr Sales Tax Payable (2350), per-invoice rate | `0276897`, `tests/revenueEntries.test.js` |
| C29 | Taxed-invoice collection + aging on the FULL incl-tax A/R (no stranded tax) | `96c324a`, `tests/salesTaxAr.test.js` |
| C30 | #16 Sales tax Step 2: saved company default rate, pre-fills Send Invoice | `ac3e624`, migration `042` |
| C31 | Send Invoice null-draft crash fix (complete-draft factory + safe functional updates) | `9c9facb`, `src/lib/invoiceDraft.js`, `tests/invoiceDraft.test.js` |

### Platform, AI, reporting & product foundation (pre-existing, verified)

| id | item | evidence |
|----|------|----------|
| C32 | UI design system (Mercury/Linear/Stripe-inspired) + polish passes | `081c47a`,`1aadae2` |
| C33 | Universal upload → AI extract/code → balanced journal entries | `App.jsx` upload pipeline |
| C34 | Conversational clarification flow + free-text natural-language booking | `0d4503b`,`a63b86b` |
| C35 | AI function-calling (live DB tools) | `dca8ecf`, `src/lib/aiTools.js`, `tests/aiTools.test.js` |
| C36 | AI inline charts/CSV/summaries, smart duplicate detection, recurring suggestions | `c63cec1` |
| C37 | Adaptive client AI profile + world-class CFO prompt + action sandbox | `9a8af7a`, migration `022`, `src/lib/aiCapabilities.js` |
| C38 | Reports: P&L, Balance Sheet, Cash Flow, AR/AP aging, trial balance, KPIs, health score + drill-downs | `ee3d2bd`,`0376999`, `reports.js`, `tests/reports.test.js` |
| C39 | Tax Center: estimates, deadlines, deduction tracker, 1099s | `src/lib/tax.js`, migrations `011`,`007`, `tests/tax.test.js` |
| C40 | Bank import + reconciliation + exact-balance match | `c5c17e1`, migrations `005`,`026`,`035` |
| C41 | Contracts / ASC 842 leases (Day-1 + monthly schedules) | `App.jsx` contract flow |
| C42 | QuickBooks Online import (mapping, dup detection, undo) | `0769ab7`, migration `028`, `src/lib/qboParser.js`, `tests/qboParser.test.js` |
| C43 | Automatic monthly reporting + immutable archive + executive summary | `784765b`, migration `029`, `tests/monthlyReport.test.js` |
| C44 | Multi-user team invites (owner/admin/member roles) | `f5cf088`, migration `027` |
| C45 | Multi-tenancy via Postgres RLS (`is_company_member`) + `accounts` RLS fix | migrations `001`,`023` |
| C46 | Data safety: soft delete, audit log, undo toast, bulk-delete protection, export-all | `366f407` |
| C47 | Platform admin panel + Support Mode (Option A bypass) | `467a2ec` |
| C48 | Security verification dashboard (`security_check`) | `ce0e276`, migration `018` |
| C49 | Per-user rate limiting (60 AI/hr, 20 uploads/hr) | `1117cf9`, migration `021`, `tests/ratelimit.test.js` |
| C50 | Anomaly detection, onboarding flow, in-app notifications | `5093b23`, migration `033` |
| C51 | Upload log (per-file processing status) | `6a7290a`, migration `019` |
| C52 | OWASP Top-10 security audit pass + invoice-print XSS hardening | `6260178` |
| C53 | Terms of Service + Privacy Policy pages | `ecd3b80` |
| C54 | Sentry error-monitoring integration (code wired; DSN config is O-side) | `2fd4113`, `src/lib/sentry.js` |
| C55 | Payroll import UI + AI parse (books to ledger) — exists but NOT GAAP-correct/persisted; see O1 | `App.jsx` PayrollView |
| C56 | #14 Void persistence — void posts a **DB-persisted** reversing entry (`post_journal_entry`), idempotent via `import_metadata.reverses`; Undo soft-deletes the reversal. NOT local-only (was O15; rechecked 2026-06-21) | `fefd399`, `reverseJournalEntry`/`voidInvoiceWithUndo` in `App.jsx` |
| C57 | #13 Payroll — deterministic `buildPayrollEntry` (Dr Salaries / Dr Payroll Tax Exp / Cr Cash(net) / Cr Payroll Taxes Payable), role-resolved; fixes the never-persisted bug (PayrollView now posts via `persistMultiLineEntry`); COA role-reconciliation (mig `044`) + new `2101` Payroll Taxes Payable; preview renders the real entry | `src/lib/payroll.js`, `tests/payroll.test.js`, migration `044`, gaapInvariants #13, PayrollView |
| C79 | **UI overhaul — "Midnight + Gold" design system + rename to "Shadow."** Full token system (single source of truth in `src/styles.css` + `src/lib/theme.js` accessors): midnight canvas / dark-slate surfaces / gold accent reserved for money·CTAs·emphasis, bright semantic green/warm-red, high-contrast text; Fraunces (display) + Space Grotesk (UI) + DM Mono (figures) via Google Fonts. Core components (`src/components/ui/`): StatCard (signature — eyebrow + oversized **count-up** gold figure via `useCountUp`, easeOutExpo), Button, Card, Badge. **Swept all 41 views/components** off hardcoded light hex onto the tokens — property-aware so the light→bright inversion stays readable (solid accent → bright fill + dark `--sc-on-accent` ink; tints → soft tokens; SVG attribute hexes → literal dark values). Motion: count-up hero, staggered card rise, page/route transitions (`sc-rise`), card hover-lift, button micro-interactions + CTA sheen, bar-chart grow-in (`sc-bar`); all `prefers-reduced-motion`-guarded. Rename "Shadow CFO" → "Shadow" everywhere (0 left). Visual/styling + rename only — no logic/data changes; 447 tests still pass. **Follow-ups (user walkthrough):** Send-Invoice print/preview is a white-paper context that may want to stay light; vendor-avatar ink contrast on light hues; lib-returned semantic colors left as-is | `fd77d53`+`<this>`, `src/styles.css`, `src/lib/theme.js`, `src/lib/useCountUp.js`, `src/components/ui/*`, all `src/components/**` + `index.html` |
| C83 | Shakedown BUG: **sales tax lumped into revenue on uploaded invoices** (Riverside $1,200 + $84 tax @ 7% booked Dr A/R 1,284 / Cr Revenue 1,284 — overstating income, omitting the liability). **Diagnosis:** both layers — extraction had no structured tax field, AND the upload booking path had no split logic (`buildArInvoiceEntry`, the tested 3-line builder, was wired only into Send-Invoice). **Fix:** extraction now pulls `subtotal`/`tax_amount`; `persistJournalEntry` books a revenue invoice carrying tax as the 3-line split — **Dr A/R (total) / Cr Revenue (subtotal) / Cr Sales Tax Payable (2350, tax)** — via the same `buildArInvoiceEntry`, so the upload path matches Send-Invoice. Falls back to the clean 2-line when there's no tax. Clean invoices (Acme/Meridian, no tax) unaffected. **Follow-up (messy-PDF robustness):** when extraction is uncertain about a tax line, ask "does this invoice include sales tax?" rather than silently lumping — noted, not built. | App.jsx (`persistJournalEntry` split + extraction `tax_amount`), `src/lib/revenueEntries.js` (`buildArInvoiceEntry`), `tests/revenueEntries.test.js` (Riverside case) |
| C82 | O75 **Document direction from company self-identity + clarification correction UX.** **Direction:** new pure `src/lib/docDirection.js` (`companyIdentityNames`/`classifyDocDirection`) anchors invoice direction on the company's legal name + aliases — issued BY us → revenue/AR, addressed TO us → expense/AP, neither/both (with identity set) → ambiguous→ask, no identity → fall back to AI (unchanged). Invoice extraction now also pulls `issuer`/`recipient` + is told who "us" is; a DETERMINISTIC override (not the AI's guess) sets `type` post-extraction. Identity-confirmed revenue books straight through (the "your own invoices just work" case); ambiguous routes to a direction-first clarification. Fixes the shakedown bug (Northwind's own invoices booked as expenses). Company **aliases/DBA** field added to Settings (guarded persist; migration `046` for durability — name alone works without it). **Correction UX:** `buildGaapClarification` no longer fires on revenue (root of the Meridian prepaid-on-revenue lock-in); every clarification card now shows a persistent **"Wrong type? · This is revenue / This is an expense"** override that re-routes to type-correct questions (confirm type → then sub-questions, never on a wrong premise). | `src/lib/docDirection.js`, `tests/docDirection.test.js`, App.jsx (extraction + direction override + decision branches), ClarificationFlow.jsx, SettingsView/writeShapes (`aliases`), `046_company_aliases.sql` |
| C81 | Shakedown BUG: **active company didn't persist on refresh** (reset to the first company — a data-integrity hazard: a multi-company user could enter/upload against the WRONG company's books unnoticed). Now the active company is **persisted per user** (`localStorage` `cfai_lastCompany_<uid>`, set on every switch) and **restored on load** via pure `pickActiveCompany(cos, lastId)` — validated against the user's accepted list, falls back to the first if access changed. (DB/profile store would add cross-device — noted as a future enhancement.) | `src/lib/companies.js` (`pickActiveCompany`), `tests/companies.test.js`, App.jsx (loadCompanies + persist effect) |
| C80 | Shakedown BUG: **Opening Balances auto-plug to Opening Balance Equity** (onboarding blocker). The user could enter opening cash but **Post was gated on the grid balancing**, forcing a non-accountant to hand-enter the offsetting equity. Now the user enters only known asset/liability (and optional equity) day-one balances; the app **auto-plugs the residual to OBE (3400)** so it always balances — the View shows the computed "Opening Balance Equity · auto-balanced" line, the "✓ Auto-balanced — $X to Opening Balance Equity" indicator, and Post requires only a cutoff + an entry (no manual equity). OBE seeded in the default COA (`3400`, role `opening_balance_equity`) so it renders on the Balance Sheet; `ensureAccountIdForCode` already auto-creates it for existing companies. The post path (`buildOpeningBalanceEntry`) already plugged OBE — this removed the View gate. | `src/lib/constants.js` (OBE 3400), `src/components/views/OpeningBalancesView.jsx`, `tests/openingBalances.test.js` (onboarding plug), `src/lib/openingBalances.js` |
| C78 | O19 Drop orphaned `ap_invoices` table — confirmed zero `src/` references (only its own DDL + the dead `ap_aging` view read it; aging is client-side in `reports.js`); applied the self-guarded migration `045` (verified 0 rows, dropped the `ap_aging` view then the table). Resolves the CLAUDE.md §11 dead-schema note for this table | `045_drop_ap_invoices.sql` (applied 2026-06-26), CLAUDE.md §11 |
| C77 | O10 Depreciation month-end auto-prompt — a dashboard **nudge** surfaces unposted depreciation months that are due (pure `depreciationDue(scheduleRows, asOf)` counts pending rows dated on/before today; `loadDepreciationDue` refreshes the count on company load + after a run). Never auto-posts — shows *"N months due through DATE"* with a one-click **Run depreciation now** (`runDepreciationThrough(today)`). Degrades to zero pre-migration-041 | `src/lib/depreciation.js` (`depreciationDue`), `tests/depreciation.test.js`, App.jsx (`loadDepreciationDue`/`depreciationDueInfo`), DashboardView |
| C76 | O55 File-detect AI-classifier extension — the AI doc classifier now recognizes **payroll** and **qbo** (was invoice/bank/contract/unknown only): `classifyFile`'s prompt offers them and `classifyDocReply` maps them; a PDF/image payroll register or QuickBooks export now **routes to its importer** (`processUploadItem` non-spreadsheet path) instead of falling to unknown. Plus deterministic **`.xlsx`/`.xls` binary sniffing** in `detectFileType` (lazy `xlsx` → first-sheet header rows → `detectFromText`), so binary spreadsheets column-score like CSVs | `src/lib/fileDetect.js` (`classifyDocReply`/`detectFileType`), `tests/fileDetect.test.js`, App.jsx (`classifyFile`/`processUploadItem`) |
| C75 | O44 Ambiguous-document handling — an uploaded doc the type-classifier can't confidently place now routes to **`unknown` → held for review** instead of being force-guessed as an invoice. Root cause: `classifyFile`'s fallback was `return "invoice"`. New pure `classifyDocReply(text)` recognizes invoice/receipt/bill + bank/card statement + contract positively and defaults everything unrecognized/unsure to `unknown` (held, not booked). The invoice path already routed low-confidence/AI-question items to the clarification queue; the unknown branch holds the doc in `unknownDocs` (visible review), no auto-book. Ties O60 (held, never silently mishandled) | `src/lib/fileDetect.js` (`classifyDocReply`), `tests/fileDetect.test.js`, App.jsx (`classifyFile`) |
| C74 | O54 Support-mode boundary bugs — **(a)** entering Support Mode now clears this session's upload/preview state (`uploadedFile`/`uploadQueue`/`fileStoreRef`/`docsPreview`) and **blocks entry while an upload is still processing**, so the admin's own in-flight upload can't land in the client's context; **(b)** Exit now reliably returns to the admin's OWN company — pure `enterSupportState`/`exitSupportState` preserve the REAL admin company even through nested support entry (was: `adminCompany: currentCompany` captured a *client* when entering support from within support), and the return target is read directly instead of via a side-effect inside a `setState` updater | `src/lib/supportMode.js`, `tests/supportMode.test.js`, App.jsx (`enterSupport`/`exitSupport`) |
| C73 | O62 Company logo persistence — the logo now persists as a **base64 data URL in the existing `companies.logo_path` text column** (no Storage bucket, no new column, no migration, no user setup). `buildCompanyUpdate` writes `logo_path: logoBase64`; `mapCompanyRow` reads it back into `companySettings.logoBase64` → survives refresh; SettingsView caps upload size (≤750 KB, image-only) to keep the row sane. A Storage-bucket move is a clean future optimization if logos get large | `src/lib/writeShapes.js` (`buildCompanyUpdate`/`mapCompanyRow`), `tests/schemaContract.test.js`, SettingsView |
| C72 | O8 Reversal display marker — a GAAP reversal (#14) posts a separate offsetting entry (`import_metadata.reverses = origId`) while the original stays live; the original now shows **"↩ Reversed · DATE"** and strike-through (display-only, no accounting change). `flattenJournalEntries` now carries `import_metadata`; pure `reversalIndex`/`reversalFor` map original→reversal; BooksView renders the marker | `src/lib/ledger.js` (`reversalIndex`/`reversalFor`), `tests/ledger.test.js`, BooksView |
| C71 | O3 #10 Accrued liabilities — dedicated pure `buildAccruedLiabilityEntry` (Dr Expense / Cr Accrued Liabilities 2100), role-resolved, mirrors the other event builders; wired into gaapInvariants #10 (replaced the literal). **Builder-only** (no handler/UI yet — a "record an accrual" action is a follow-up); feeds the O7 coverage audit | `src/lib/accruedLiabilities.js`, `tests/accruedLiabilities.test.js`, gaapInvariants #10 |
| C70 | O70 Reports date range auto-defaults to the current period on open — pure `currentPeriodRange(period,{today,fiscalYearEnd,cutoffDate})` ("to" always today, "from" = fiscal-year start respecting non-calendar FYE + cutoff); ReportsView mount effect applies fiscal-YTD-through-today on open instead of a stale sessionStorage date. Ties O20 | `src/lib/reports.js` (`currentPeriodRange`), `tests/reports.test.js`, ReportsView |
| C69 | O69 Bank/card import must not claim success without persisting (trust-critical; **ties O60** completeness). Four defects from the cards shakedown, fixed B→A→D→C: **(B)** `applyMatch` now verifies every `markBillPaid` committed via `allClearingsPosted([...])` and bails (left in review + error toast) if any didn't — never records "AP Cleared / payment posted ✓" on a write that didn't happen (was: false success with **zero journal entries** — a silent-disappearance instance); **(A)** `bookBankTransactions` direct-books every line through `bookToDb`/`post_journal_entry` (was `setInvoices`-only → vanished on refresh); **(D)** offset threaded from the import account (card→`Cr 2200`/bank→`Cr 1000`) via `buildBankLineEntry` (was hardcoded `rc("cash")`); **(C)** `shouldRunApMatching(account)` — credit_card → skip AP-matching + direct-book Dr Expense / Cr 2200; bank → `planBankImport` split (clears post-only + standalone persisted, no double-count). Verified live (AWS Dr 6500/Cr 2200 $89; Office Depot Dr 6600/Cr 2200 $142.50). Deferred (explicit opt-in): "paid a vendor bill by card" (Dr A/P / Cr 2200) needs a non-cash offset in `buildPaymentEntry`/`markBillPaid` | `98c0598`, `src/lib/bankMatch.js` (`allClearingsPosted`/`shouldRunApMatching`/`buildBankLineEntry`/`planBankImport`), `tests/bankMatch.test.js`, App.jsx (`bookBankTransactions`/`applyMatch`), BankView |
| C68 | O63 Inline "+ Add credit card account" at the point of need — the import account-picker listed only existing `bank_accounts`, so a new user with a card statement dead-ended at Settings. `createBankAccountInline` inserts the source with the role-resolved offset GL via shared `glCodeForAccountType` (credit_card→2200 / loan→2500 / bank→1000) and auto-selects it; BankView "+ Add account…" inline form; SettingsView reuses the same helper. (Auto-open-prefilled-on-detection deferred — the deterministic detector can't tell card vs bank from CSV content, so the form defaults type to `credit_card`; a filename/issuer heuristic could refine later) | `96faef9`, `src/lib/bankAccounts.js`, `tests/bankAccounts.test.js`, App.jsx (`createBankAccountInline`), BankView, SettingsView |
| C67 | Universal-upload `offsetCode` crash → route statements to Bank Import — dropping a bank/card CSV on the main universal zone threw `ReferenceError: offsetCode is not defined` (`processUploadItem` booked inline with no account binding). Now any file resolving to `bank_statement` (spreadsheet or PDF) is pre-loaded via `pendingImportFile` and routed to Bank Import, where the account-picker (C63) sets the offset — never booked inline (which would crash, or silently default to Cash and re-break O57 for cards). New pure `planUniversalSpreadsheetRoute` is the single routing decision | `029eb4c`, `src/lib/fileDetect.js` (`planUniversalSpreadsheetRoute`), `tests/fileDetect.test.js`, App.jsx (`processUploadItem`), BankView |
| C66 | O58 Processing-toast "View" button was a no-op — now navigates Home and scrolls to the upload zone (`#universal-upload-zone`) | `b81149c`, App.jsx |
| C65 | O53 Duplicate-alert routing — clicking a "possible duplicate" anomaly alert routed to Home instead of the transaction; the anomaly notification now encodes `txn:<id>` and `openNotification` deep-links to the flagged entry in the detail panel | `b81149c`, App.jsx (`openNotification`) |
| C64 | O52 Setup-flow buttons audit — the onboarding accountant "coming soon" notice vanished on reload; now persists via `accountantDismissed`/`dismissAccountantStep`. Bank/opening setup steps verified routing correctly (`settings#bank-accounts-section` / `opening-balances#opening-balances-section`) | `b81149c`, App.jsx (`accountantDismissed`/`dismissAccountantStep`), onboarding/setup |
| C63 | O57 Credit-card offset binding — bank import bound to a selected account (picker in BankView, defaults to first); direct bookings offset to that account's GL (`Cr 2200` card / `Cr 1000` bank) via `buildBankLineEntry({offsetCode})`; dismiss path carries the offset (C60 interaction); recon record uses the account name; type→credit_card auto-defaults gl_code to 2200 | `src/lib/bankMatch.js`, `tests/bankMatch.test.js`, App.jsx (`handleBankFile`/`dismissMatch`), BankView, SettingsView |
| C62 | O12 By-Vendor report excludes revenue/customers — `computeVendorTotals` classified by GL class (expense only), not by counterparty-name presence, so a revenue invoice with a customer name no longer appears in Expenses-by-Vendor; vendor drill filters `glIsExpense` too; helper gains a `side:'revenue'` option for a future by-customer view | `reports.js` (`computeVendorTotals`), `tests/reports.test.js`, ReportsView |
| C61 | O13 Company settings persist to DB — `SettingsView.save()` now writes ALL identity/accounting fields (name, address, tax id, fiscal-year-end, currency, default accounts, business type, sales-tax rate) to `companies` via `buildCompanyUpdate`, not just sales_tax_rate; `loadAllData` reads them back via `mapCompanyRow` → survives refresh. No migration (all columns existed). Logo excluded (see O62) | `src/lib/writeShapes.js` (`buildCompanyUpdate`/`mapCompanyRow`), `tests/schemaContract.test.js`, SettingsView, App.jsx |
| C60 | Bank deposit/dismiss correctness — direct-book direction by type (deposits post Dr Cash / Cr Revenue, was inverted Dr Revenue / Cr Cash); `dismissMatch` now books the line directly instead of stranding it (income never silently lost); reconciliation record status maps to CHECK-allowed `open` (was `needs_review`, which failed the insert) | `src/lib/bankMatch.js` (`buildBankLineEntry`/`reconRecordStatus`), `tests/bankMatch.test.js`, `App.jsx` |
| C59 | O37 Smart file-routing / misroute protection — deterministic `detectFileType` (header/column sniff: bank/payroll/invoice/qbo/unknown); bank/payroll/contract importers warn + offer to route on a confident mismatch (never silently mis-process); universal path sniffs spreadsheets and routes payroll/QBO instead of assuming bank. Incident (payroll CSV → 9 wrong bank entries) can't recur | `src/lib/fileDetect.js`, `tests/fileDetect.test.js`, `App.jsx` (`guardImport`/`routeFileToType`), PayrollView |
| C58 | #9 Prepaid — `buildPrepaidCapitalizeEntry` (Dr 1300 / Cr A/P) + `buildPrepaidAmortizeEntry` (Dr expense / Cr 1300) + `buildPrepaidSchedule` (monthly, last month absorbs rounding → Σ === capitalized, no stranded residual). `bookPrepaid` rerouted off inline/`bookToDb` → builders + `persistMultiLineEntry`. Role-resolved (`prepaid_expenses`). Generate-upfront model kept (no schedule table); contract amortize path already correct post-Phase-0 | `src/lib/prepaid.js`, `tests/prepaid.test.js`, gaapInvariants #9/#9b, `bookPrepaid` |

---

## SECTION 2 — OPEN / NOT STARTED

Priority: **P1** now/next · **P2** soon · **P3** later/launch · **P4** deferred-by-design.

### ▶ Suggested execution order

Ordered index into the items below (ids are stable; the category tables that follow are the source of truth for each item's detail). `→` = do in this sequence.

- **PHASE 1 — Perfect the common paths** (95% of client usage; do first):
  **O37** (file-misroute protection) → **O12** (vendor report bug) → **O13** (settings persist) → **O38** (high-frequency path hardening) → **O41** (clean-company shakedown).
- **PHASE 2 — Trust & integrity** (the "books you can trust" promise):
  **O60** (document-completeness guarantee; subsumes O42) → **O49** (AI-accuracy verification) → **O7** (coverage audit) → **O21** (RLS audit) → **O16** (GL cash integrity) → **O50** (CPA-review-efficiency).
- **PHASE 3 — Common-path features clients want soon:**
  **O11** (invoice due-dates) → **O45** (bulk mark-paid) → **O46** (batch payments) → **O51** (scoped deletion) → **O3** (accrued liabilities) → **O6** + **O40** (remittance events).
- **PHASE 4 — UX & polish:**
  **O20** (reports redesign) → **O39** (progressive disclosure) → **O14** (render-test harness) → **O43**, **O44**, **O52**, **O53**, **O54** (smaller fixes).
- **PHASE 5 — Launch gates** (near ship):
  **O22** (rebuild test) → **O25** (codebase review) → **O23**/**O24** (DPA/SOC2) → **O26** (Sentry DSN) → **O30** (Stripe) → **O31** (custom domain). Plus opportunistic: **O8**, **O9**, **O10**, **O17**, **O18**, **O19**, **O35**, **O36**, **O4**+**O5** (hard close + period-locking — pair; not in the original phase spec, parked here).
- **PHASE 6 — Deferred / post-launch** (P4 + big features):
  **O32**, **O33**, **O34** (variant deferrals) → **O27**, **O28**, **O29** (integrated payments, receipt submission, Slack) → **O47**, **O48** (volume/scale, chatbot-config).

### ★ North star (vision-tier)

**O56 — "Invisible controller" experience.** The client interacts via an **INBOX** (email and/or Slack) where they forward everything — receipts, invoices, statements — which feeds directly into the app and **becomes the permanent document record**. A **chatbot** (in Slack *and* in-app) answers questions and generates reports on request ("send me my P&L", "how much on travel this quarter?"). Behind the scenes the AI controller does the bookkeeping and the **CPA reviews**. Goal: the client forwards documents and asks questions; their finances feel **handled by a person, not operated by them via software**. The app UI exists for those who want it but isn't required for the core experience. **P3 / vision-tier** — the organizing goal the near-term work serves.

Existing items that ladder into it:
- **O29** (Slack/SMS bot) — the chat interface layer (Slack + in-app).
- **O28** (expense/receipt submission) — the forwarding/inbox **input**.
- **Source-document attachment** (being designed now) — the inbox auto-becomes the **audit trail**; forwarded receipts auto-match to transactions.
- **O49** (AI-accuracy verification) + **CPA review** (**O50**) — the **trust layer** that MUST be solid before the experience can be hands-off.
- **O38** (high-frequency path hardening) — the bank-flow + categorization the controller runs behind the scenes.

**Sequencing:** this is a LATER build that sits on top of a **bulletproof core**. Hands-off only works when the behind-the-scenes result is trustworthy (accurate categorization + review). Build the core first (engine done; **O38**/**O49** in progress), *then* this delivery layer.

### ★ PRINCIPLE — Per-tenant learning (the app gets smarter within each client's own instance)

**The app should learn and get smarter WITHIN each customer's own instance over time**, accumulating per-client context so it requires **progressively less user input**. Learning is **PER-TENANT** — scoped to that client's own data, **never shared across clients** — which is both more personal *and* the correct data-isolation / privacy posture (the same boundary RLS enforces; see CLAUDE.md §3). **Core rule: never make the user correct the same thing twice.** Every correction is a **teaching signal** the app remembers and **auto-applies for that client going forward** (always with an override). This is the **mechanism behind the "invisible controller" vision (O56)**: less input over time → eventually hands-off. These instances **compound** — each one makes the app feel more handled and ladders into O56.

**Substrate that already exists (extend, don't duplicate):** the per-company **`client_ai_profile`** table (learned `common_vendors` / `spending_patterns` / `custom_rules` / `ai_notes` — CLAUDE.md §5/§7), the **`vendor_rules`** rules engine (vendor → `gl_code`, applied via the AI `add_rule` action), **recurring suggestions (C36)**, and the **`contacts`** table (vendor/customer `type`). The instances below mostly **extend these** rather than start from zero.

**▷ Direction note — import flow has too much manual confirming today (feedback 2026-06-24; for O38 + the learning work, NOT a now-fix).** User feedback from the shakedown: the current import flow asks for too much hand-holding — manual account-selecting and per-item match/categorization confirming. Correctness issues come first, but this is exactly the early friction the learning layer is meant to **dissolve** — the manual-now → hands-off-later arc of the invisible-controller vision (O56). Target end state: **(1)** the import account-picker **pre-selects via learned statement fingerprints (O64)** so the user rarely picks after the first import per account; **(2)** high-confidence categorizations/matches **auto-apply with override** (lean on the O67/O65 teaching signals + match confidence) instead of requiring per-item confirmation; **(3)** confirm-steps shrink to **exceptions / low-confidence only**. Pairs with **O38** (high-frequency path) and **O49** (the accuracy/trust layer that must be solid before auto-apply widens). Captured as direction, not an immediate task.

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O64 | **Statement format-fingerprint → account auto-detection** — fingerprint a bank/card statement's format (header layout, columns, institution markers) and remember which account it was imported to, so after the first time the import account auto-selects and the user rarely re-picks it per statement | not started | **P2** | **extends C63/O57** (the import account-picker — this removes the manual pick); new fingerprint store keyed per-tenant |
| O65 | **Vendor → GL category memory** — once a vendor is categorized/recoded, auto-apply that category to future transactions from the same vendor (with override) | not started | **P2** | **extends the `vendor_rules` rules engine** + `client_ai_profile.common_vendors`; pairs with O67 (a recode should *write* the rule) |
| O66 | **Recurring-transaction learning** — recognize this client's regular rent, payroll cadence, and recurring vendors automatically | not started | **P2** | **extends recurring suggestions (C36)** + `client_ai_profile.spending_patterns`; deepen detection, don't rebuild |
| O67 | **Categorization-correction learning** — when the AI's guess is corrected (a recode), that correction **sticks** and improves future guesses for this client | not started | **P2** | the teaching-signal core ("never correct twice"); writes through to O65's vendor memory + `client_ai_profile`; pairs with O49 (accuracy) |
| O68 | **Counterparty learning** — learn who this client's customers vs. vendors are, so the app stops asking | not started | **P2** | **extends `contacts`** (vendor/customer `type`) + `client_ai_profile`; feeds AR-vs-AP routing |

### Accounting events & correctness

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| ~~O1~~ | #13 Payroll — GAAP-correct multi-line builder + never-persisted fix | → C57 | — | done 2026-06-21 (migration `044`) |
| ~~O2~~ | #9 Prepaid — shape-extract builders + route off inline `bookPrepaid` | → C58 | — | done 2026-06-21 |
| O36 | Unify amortization schedules (prepaid + depreciation + lease) under one model — prepaid/contracts generate-upfront (posted future-dated JEs) while depreciation uses a pending-rows table + run-on-demand. Consolidate to one mechanism for consistency | **assessed → DEFER (don't build now)** | P3 | **Recommendation (2026-06-26):** both models are correct + independently unit-tested (C23/C58); this is pure consistency with real regression blast-radius against tested booking paths. The only practical asymmetry (depreciation needs a manual run while prepaid auto-posts future months) is now mitigated by **C77/O10** (the depreciation-due nudge). **Skip until there's a concrete trigger** — e.g. a 3rd amortizing event type, or the divergence causes an actual bug. Net: churn for no correctness gain today |
| ~~O3~~ | #10 Accrued liabilities — discrete builder + test | → C71 | — | done 2026-06-26 (`buildAccruedLiabilityEntry`, wired into gaapInvariants #10). **Builder-only** — a "record an accrual" handler/UI is a follow-up if needed |
| O4 | #17 Hard close — post year-end closing entries (Dr Rev/Cr Exp → Retained Earnings 3100) + period locking | not started | P3 | soft-close (C12) is the correctness fix; pairs with O5 |
| O5 | Reconciliation period-locking | not started | P3 | pairs with O4 |
| O6 | Sales-tax remittance event — Dr Sales Tax Payable (2350) / Cr Cash when remitting to the state | not started | P2 | completes the #16 lifecycle (2350 currently only accrues) |
| O40 | Payroll-tax remittance event — Dr Payroll Taxes Payable (2101) / Cr Cash when collected payroll taxes are paid to the government; clears the liability | not started | P3 | companion to O6; closes the #13 lifecycle (2101 currently only accrues) |
| O7 | **Economic-event coverage audit** (NOT the individual-builder tests, which are done). **Part 1 — COMPLETENESS:** systematically enumerate the full economic-event space by walking the chart of accounts (what events touch each account?) and confirm each is either handled by a tested builder or explicitly scoped out. The 17 events we have arose ad-hoc during building; this surfaces what's silently missing — e.g. owner contributions / draws (3300), loan proceeds & repayment (2500/2600), interest income (4300) / interest expense (8000), asset disposal & gain/loss (8200), bad-debt write-off (7000/1250), refunds & credit memos, inter-account transfers. **Part 2 — INTERACTION / END-TO-END:** the bugs this project keeps hitting (deferred-rev double-count, deposit-direction inversion, dismiss-stranding) live in the WIRING and in event INTERACTIONS, not the isolated builders — so verify events through the full handler/UI path and in combination on a real set of books. **O41 (clean-company shakedown) is the practical execution of Part 2**; O7's Part-1 checklist + O41's live run together satisfy the coverage goal. | not started | P2 | depends on O1✅/O2✅ (builders done); **Part 2 ≡ O41**; informs O38 |
| ~~O8~~ | Reversal display marker — show original struck-through / "reversed on DATE" | → C72 | — | done 2026-06-26 (`reversalIndex` + BooksView "↩ Reversed · DATE" + strike-through; `import_metadata` now carried through the flatten) |
| O9 | "Redo opening setup" flow — guarded reverse of the posted opening entry + unlock cutoff | not started | P3 | follow-up to C10 |
| ~~O10~~ | Depreciation month-end auto-prompt — surface unposted months due | → C77 | — | done 2026-06-26 (dashboard nudge via `depreciationDue`; one-click run, never auto-posts) |
| O11 | Invoice due-date end-to-end (set, surface in aging/collections, reminders) | not started | P2 | |
| O72 | **Payroll-from-bank books wrong (net pay ≠ salary expense).** Surfaced by the Shadow-Test-Co shakedown: a bank line "PAYROLL JANE SMITH NET" direct-booked **Dr 6000 Salaries 4,401 / Cr Cash 4,401** — treating NET pay as the full salary expense. Payroll is a multi-line event (Dr GROSS salaries / Dr payroll-tax expense / Cr cash NET / Cr payroll taxes payable, #13/C57); the gross↔withholding split lives in the payroll **register**, not the bank line. A bank net-pay line therefore **understates salary expense and omits the tax liability**, and would **double-count** if the register is later uploaded. **Fix approach:** detect payroll-type bank lines (vendor/description heuristic, e.g. "payroll"/"net pay"/"paychex"/"gusto"; reuse the `fileDetect`/classifier signals) and **route them to MATCH a register-sourced payroll entry** (the `buildPayrollEntry` multi-line JE) rather than direct-booking salary — i.e. the bank disbursement CLEARS the payroll liability/cash, it doesn't create the expense. If no register entry exists yet, hold for review / prompt to upload the register rather than booking a wrong standalone salary. | not started | **P1** | shakedown #2; ties C57 (payroll builder), O73 (bank-match-don't-direct-book), O60 (held not mis-booked) |
| O73 | **Bank-first import has no AR/AP matching — invoices/bills must be bookable first, then bank lines MATCH & clear them.** Shakedown: on a bank-only upload, an Acme deposit 4,500 and Riverside 1,284 booked **Dr Cash / Cr Revenue** (should clear **AR** from their invoices) and a Pixel payment 1,800 booked **Dr Professional Services / Cr Cash** (should clear **AP** from its bill). Correct *given no invoices/bills were uploaded yet* — but it reveals the **core workflow ordering**: a deposit that pays an open invoice should **clear A/R** (Dr Cash / Cr A/R), not re-recognize revenue; a payment of an open bill should **clear A/P** (Dr A/P / Cr Cash), not re-expense. The matching engine + `planBankImport` + `markBillPaid` clearing path already exist (C19/C60/C69) — this is about the **product flow**: make issuing invoices / entering bills a first-class step, and have bank imports preferentially **match open items before direct-booking**. Without invoices present they direct-book (today's behavior), which is why the shakedown looked right but isn't the intended steady state. **This is central to the product being correct** (revenue/expense recognized once, at invoice/bill time; cash movement just settles it). | not started | **P1** | shakedown #3; the core matching workflow; builds on C19/C60/C69 matching; pairs with O38 (high-frequency path), O11 (invoices), O72 |

### Bugs & technical debt

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| ~~O12~~ | Vendor report shows customers (mixed AR customers into vendor list) | → C62 | — | done 2026-06-21 (classify by GL class, expenses only) |
| ~~O13~~ | Company settings fields don't persist to DB | → C61 | — | done 2026-06-21 (no migration; via buildCompanyUpdate/mapCompanyRow) |
| ~~O62~~ | Company logo persistence | → C73 | — | done 2026-06-26 (chose **base64 in `logo_path` text column** — no bucket/migration/user-setup; round-trips via buildCompanyUpdate/mapCompanyRow; size-capped. Storage-bucket move is a future optimization if needed) |
| O14 | Component render-test harness (jsdom + ERP-context mock) — unit tests can't mount views; the Send Invoice crash (C31) slipped because only pure seams were tested | not started | P2 | adds a real regression layer for view logic |
| ~~O15~~ | `voidInvoiceWithUndo` persistence — **rechecked: already done → C56** (DB-persisted reversing entry, not local-only) | → C56 | — | resolved 2026-06-21 |
| O16 | GL cash integrity — audit every cash figure still derives from `glCashOnHand` (no stored-balance leak) | **✓ verified clean (2026-06-26)** | P2 | **Audit result:** every displayed/used cash figure derives from `glCash`/`glCashOnHand` — Dashboard cash tile + runway, health score, KPIs, AI snapshot, monthly report, AI tools all pass `glCash`. The `cashBalance` state (Σ bank `current_balance`) is **vestigial — read by no display/calc** (set by `cashFromBanks`, the bank-statement balance kept only as a recon target). No leak. Minor optional cleanup: remove the vestigial `cashBalance` state |
| O17 | Payment atomicity — replace compensation-based payment posting with a single `pay_journal_entry` RPC (atomic flag + GL) | not started | P3 | hardening of C8 |
| O18 | Normalized reconciliations model (header + `reconciliation_items`, FK to JE lines) — denormalized shape is working today | not started | P3 | CLAUDE.md §11; deliberate separate project |
| ~~O19~~ | Drop orphaned `ap_invoices` table | → C78 | — | done 2026-06-26 (migration `045` applied — confirmed 0 rows, dropped `ap_aging` view + table) |
| O35 | **COA normalization (Tier-2)** — companies were seeded by different COA versions over time (e.g. `5101`/`6400` Payroll Tax Expense, `2101` Payroll Taxes Payable with NULL roles); migration `009` set roles by v2 code only, so v1-era accounts have NULL roles / vestigial codes. Audit every company for missing/NULL/variant roles and reconcile **all** roles (not just payroll). Set roles (safe); renumber codes only with a JE-line re-point plan (unsafe otherwise). Driven by a live per-company audit. | not started | **P2** | surfaced by payroll (C57/mig `044`, which fixed payroll roles only); affects any role-resolved feature for legacy companies |
| ~~O37~~ | Smart file-routing / misroute protection | → C59 | — | done 2026-06-21 (deterministic CSV sniff) |
| ~~O55~~ | File-detect AI-classifier extension — payroll/qbo recognition + `.xlsx` sniffing | → C76 | — | done 2026-06-26 (classifier knows payroll/qbo & routes them; xlsx column-scored via lazy `xlsx`) |
| ~~O57~~ | Credit-card offset binding | → C63 | — | done 2026-06-21 (account picker + offset by account) |
| ~~O63~~ | Inline "+ Add credit card account" at the point of need (first-use blocker) | → C68 | — | done 2026-06-24 (`96faef9`). Auto-open-prefilled-on-detection deferred (detector can't tell card vs bank from CSV content; form defaults type to `credit_card`) |
| ~~O58~~ | Processing-toast "View" button is a no-op | → C66 | — | done 2026-06-21 (navigates home + scrolls to upload zone `#universal-upload-zone`) |
| ~~O52~~ | Setup-flow buttons audit — onboarding/setup buttons behave inconsistently | → C64 | — | done 2026-06-21 (accountant "coming soon" notice now persists via `accountantDismissed`, no longer vanishes; bank/opening steps verified routing to `settings#bank-accounts-section` / `opening-balances#opening-balances-section` — already correct) |
| ~~O53~~ | Duplicate-alert routing — clicking a "possible duplicate" alert routes to Home instead of the transaction | → C65 | — | done 2026-06-21 (anomaly notification encodes `txn:<id>`; openNotification deep-links to the flagged entry in the detail panel) |
| ~~O54~~ | Support-mode bugs — (a) own upload leaks into client view; (b) exit stuck on wrong company | → C74 | — | done 2026-06-26 (clear transient upload state + block entry while uploading; pure enter/exit state preserves the real admin company through nesting) |
| O76 | **UI reactivity / display-sync — fix as a CLASS, not per-screen.** Recurring root issue: the UI reads data on load but doesn't consistently re-sync when the underlying data changes, so stale views persist until a hard refresh. **Data integrity is fine throughout — these are display-only.** Known instances (Nth of the same pattern): **(a)** soft-deleted journal entries keep showing in the ledger/list until refresh (the delete persists to DB; the list doesn't re-read after the mutation); **(b)** company rename doesn't update the header/wordmark live; **(c)** active company/tab not persisting on refresh (the *persistence* half fixed in **C81**, but it's the same family — state vs. source-of-truth sync). **Fix as a class:** a consistent re-fetch/invalidate-on-mutation discipline (every data-changing action refreshes or invalidates the affected reads — most paths already have `loadAllData()`; the gap is the mutations that mutate local state without re-syncing), rather than patching each screen. Audit the mutation handlers (delete/void/rename/recode/switch) for a missing re-read. | not started | **P2** | recurring class; display-sync only (no data-integrity risk); instances: soft-delete list, header rename, (active-company persistence → C81); pairs with O14 (render-test harness would catch regressions) |

### AI quality & data trust

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O42 | **Invoice-disappearance safeguard** — standing guarantee that anything an uploaded doc creates can never silently vanish (broader than C5's post-booking invariant; ongoing assurance, raised repeatedly). **Subsumed by O60** as the specific case of the general document-completeness guarantee — build via O60, not in isolation | → O60 | **P1** | the specific instance; O60 is the general guarantee |
| O60 | **DOCUMENT COMPLETENESS GUARANTEE (foundational).** The cardinal accounting risk is a **silently missing transaction** — a document uploaded and never recorded anywhere, no trace, nothing unbalances, no error fires; the books look fine but are incomplete and no one can tell. This must be **structurally impossible**. **Principle — conservation of documents:** every uploaded file gets a tracked DB row with a status the instant it arrives, BEFORE any processing, and can only resolve to one of **three terminal states, never a fourth** — **(1) Recorded** (became a journal entry, linked + traceable), **(2) Held / needs-review** (couldn't auto-process / errored / ambiguous → sits in a VISIBLE queue, not lost), **(3) Rejected** (explicitly not-a-transaction — duplicate/junk/personal — with a reason). "Silently disappeared" is not a possible outcome: every failure / unexpected type / mid-process error / unrecognized format routes to state 2, never to nothing. **Required pieces:** (a) a document-intake row created on upload **before** processing, with status (documents table exists — add a lifecycle `status` if absent); (b) every processing path (bank, payroll, invoice, contract, universal, AND the error / unexpected-type paths) must move the doc to a terminal state, including a **catch-all** so an unhandled type lands in review, not void; (c) a **completeness-reconciliation check** — query any document NOT in a terminal state (or stuck "processing" past a threshold) and surface them (the standing guarantee that nothing fell through); (d) a **"documents received vs. resolved" count** the user/CPA can see (population-completeness). Subsumes **O42** (invoice-disappearance) as the general case; foundation of the trust layer with **O49**. The dismissed-deposit bug (**C60**) was an instance of this class — fixed, but this GUARANTEE prevents the next unknown one. **Architecture: see the "◆ O60 — Document Completeness architecture" design note below** (two populations / two nets; phased intake — enforce on existing upload paths NOW, inbox later; completeness dashboard). | not started | **P1** | **foundational**; subsumes O42; pairs with O49 (AI-accuracy); extends C5/C51 (intake/visibility); C60 was an instance; design note ↓ |
| ~~O75~~ | Document direction from company self-identity (revenue/AR vs expense/AP) + clarification correction UX | → C82 | — | done 2026-06-27. Direction anchored on legal name + aliases (`docDirection.js`); deterministic override in the pipeline; persistent "Wrong type?" override on every clarification; gaap no longer fires on revenue. Migration `046` (aliases) optional — name-based works without it |
| O74 | **Source-document storage + retrieval (the audit-trail backbone).** Every uploaded source document (invoice/receipt/bank statement/contract/payroll register) must be **stored durably** and **linked to the journal entry / transaction it produced**, then **retrievable from that transaction** ("view the original document" on the entry, and a per-company document library). This is what lets the books *prove themselves* — a signing CPA (and the client) can click any entry and see the paper behind it. **Substrate exists:** the `documents` table (`storage_path` in the `documents` Storage bucket, `linked_invoice_id`, `document_type`) + `storeDocument` + `relinkDocsForInvoice` (re-links on the durable `db_entry_id`) + the upload-time attach in `bookToDb`; DocsView/TransactionDetailPanel already render some of this. **Gaps to close:** guarantee a stored doc for EVERY ingest path (bank/payroll/qbo/contract/universal, not just single-invoice), the link survives booking + refresh on every path, retrieval is present on every transaction surface (detail panel, books row, reports drill), and an orphan check (entry without a source doc / doc without an entry). Pairs tightly with **O60** (completeness: the doc population) — O60 tracks that every doc resolves; O74 keeps the doc itself attached + viewable. Ladders into the **invisible-controller** vision (O56): the forwarded inbox auto-becomes the permanent, timestamped audit trail. | not started | **P1** | audit-trail backbone; extends `documents`/`storeDocument`/`relinkDocsForInvoice`; pairs with O60 (completeness) + O56 (inbox→audit trail) |
| ~~O69~~ | `bookBankTransactions` reported success without persisting (false-completeness + card-vs-AP gap); four defects B(false-success)/A(persistence)/D(offset)/C(card≠AP) | → C69 | — | done 2026-06-24 (`98c0598`), verified live. **Deferred (explicit opt-in):** "paid a vendor bill by card" (Dr A/P / Cr 2200) needs a non-cash offset in `buildPaymentEntry`/`markBillPaid` + a per-line UI action |
| ~~O44~~ | Ambiguous-document handling — route unclear docs to review instead of guessing | → C75 | — | done 2026-06-26 (`classifyDocReply` → `unknown`/held; invoice path already routed low-confidence to clarification). Ties O60 |
| O49 | **AI-accuracy verification layer** — a dedicated way to catch AI mistakes: bulletproof reconciliations + completeness checks confirming the AI coded things correctly and nothing is missing (broader than `gaapInvariants`; about catching AI categorization/extraction errors). Core to "books you can trust" | not started | **P1** | complements C7 (GAAP invariants) and the single-source clusters |
| O51 | Scoped deletion via chatbot — "delete the Adobe transaction" must NOT delete ALL Adobe-associated records; scope deletes precisely (single targeted entry, confirm before bulk) | not started | P2 | safety; extends the AI action sandbox (C37) bulk-delete cap |

#### ◆ O60 — Document Completeness architecture (design)

**Two populations, two nets — both required for airtight completeness.** The guarantee must hold **NOW** on existing upload paths; the inbox is a future enhancement, not a prerequisite.

**Population 1 — Documents received** (what the client *sends*):
- Every document gets an **intake row with status the moment it enters, before processing** — regardless of channel (upload now; inbox/email/Slack later).
- **Terminal states only:** `recorded` / `held-for-review` / `rejected-with-reason`. A **catch-all** routes unhandled / unexpected / errored files to `held-for-review`, **never void** — no silent disappearance is possible.
- **Received-vs-resolved reconciliation:** `count(entered) == recorded + held + rejected`, at all times. Any doc not in a terminal state (or stuck `processing` past a threshold) is surfaced.

**Population 2 — Transactions that happened** (the bank/card statement as **ground truth**):
- Every bank-statement line must resolve to a journal entry **or** an explicit categorization. This catches **real transactions for which NO document was ever sent**.
- **Bank-line completeness check:** every imported line accounted for; any unresolved line surfaced.
- The bank statement is the **ultimate population control** — the independent record of what actually touched the money.

**Intake — phased:**
- **NOW:** enforce the Population-1 guarantee on the **existing upload paths** (intake row on upload, terminal-state enforcement, reconciliation check, catch-all for unexpected types). Don't gate completeness on new intake mechanisms.
- **LATER (vision — see O56):** an **inbox (email/Slack) as primary intake**. Its completeness value: the received-count lives **outside the app** (the mail server counts it) → an **independent control total** to reconcile against, plus an **immutable, timestamped audit trail**. A better front door for Population 1, layered on once it exists.

**The Completeness Dashboard:** surface **both** reconciliations to the CPA/client — *"X documents received, all accounted for (Y recorded, Z in review, W rejected)"* and *"all N bank lines resolved."* This is the demonstrable assertion that lets the signing CPA say **"I have everything," with proof**.

**Buildable now:** intake-row + terminal-state + reconciliation core (Population 1) and the bank-line completeness check (Population 2). **Future:** the inbox front door. Population 2 also ties to **O7** (event coverage) and is exercised at scale by **O59**'s control-sheet check.

### Bulk operations & scale

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O45 | Mass-edit / bulk mark-paid — marking payments paid one-by-one is tedious; need bulk selection + bulk status/payment-method edit | not started | P2 | routes through `markBillPaid` (C6/C8) per selected id |
| O46 | Batch-payment matching — match a single lump/batch payment against multiple bills/invoices | not started | P2 | extends the matching engine (C19); partial/multi-clear |
| O47 | Volume/scale check — confirm the app handles large uploads (50+ invoices) and high transaction volume without breaking | not started | P2 | perf + correctness under load |

### Reports & UX

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O20 | Reports page redesign + report date semantics (range/asOf consistency across reports). **Sub-note — fiscal-year-boundary audit (P2, recurring weak spot):** different surfaces apply different period boundaries to "current year" — two instances found so far: P&L-vs-BS period difference, and the Net Income tile-vs-drill-in (the drill summed all-time → pulled prior-period expenses; fixed by scoping to the tile's FY range). Systematically audit that EVERY current-period figure (dashboard tiles, drill-ins, all reports) derives its boundary from ONE shared helper (`fiscalYearStart`/`fiscalYearSplit`), so "current period" means the same thing everywhere. O59's full-year sim will stress period boundaries hard. | not started | P2 | recurring class; lock candidates beyond the per-fix unit tests |
| O39 | **Progressive disclosure** — surface advanced accounting UI (lease/ASC 842, deferred revenue, depreciation, multi-line payroll) only when a client actually uses those features; hidden by default to reduce clutter for simple expense-first clients | not started | P2 | pairs with O20 (Reports redesign) |
| O50 | CPA-review-efficiency design — how the reviewing CPA reviews most efficiently: health-score vs spot-check vs detailed review; what gets surfaced/summarized for sign-off | not started | P2 | pairs with O49 (AI-accuracy layer) |
| ~~O70~~ | Reports date range should auto-default to the current date on open | → C70 | — | done 2026-06-26 (`currentPeriodRange` + ReportsView mount effect → fiscal-YTD-through-today; "to" always today). Ties O20 |
| O71 | **Auto-derive opening balance from the first bank statement (simple/new businesses) — two onboarding paths.** The opening balance is the FULL day-one position (cash + AR + AP + other assets + equity), so it can't be fully derived from a bank statement alone — **but** for a NEW business with only a checking account, opening cash = the statement's starting balance, balanced to equity (OBE 3400), IS the whole picture. **Simple path:** detect that case and **offer one-click "set opening cash from your first bank statement"** (no manual entry) → true dump-and-go onboarding. **Established path:** when there's existing AR/AP/assets (not knowable from the documents — it lives in their prior books), still prompt for the **guided full opening-balance-sheet entry**. Routes through the existing cutoff/opening-entry model (`buildOpeningBalanceEntry` → OBE) — visual/flow, no new accounting. Ladders into the invisible-controller vision (O56) — less manual input to get started. | not started | P2 | onboarding/UX; builds on C10 (opening-balance/cutoff model) + the bank-import account binding (C63); pairs with O38/O60 |

### Security, compliance & launch-readiness

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O21 | Full RLS policy audit — reconcile all committed policies vs live (~135) per table | not started | **P1** | tenant-isolation assurance |
| O22 | Live rebuild test + auth-trigger capture — prove `000`→`042` rebuilds to live; the `auth.users`→`public.users` sync trigger lives outside `public` and isn't in `000` | not started | P2 | CLAUDE.md §11 caveat |
| O23 | Data-processing / AI disclosure — DPA, subprocessor list, training opt-out, prompt-injection policy | not started | P3 | |
| O24 | GITC / SOC 2 readiness | not started | P3 | |
| O25 | Pre-launch industry-standard full codebase review | not started | P3 | gate before launch |
| O26 | Sentry DSN setup (integration C54 exists; production DSN + env config). **Note (currently false UX):** the error boundary shows *"Our team has been notified"* but with NO DSN configured, errors are NOT actually sent anywhere — the message is a lie today; the browser console is the only error record. When doing O26 (create Sentry account + project → get DSN → add to env config), **confirm errors actually flow to the Sentry dashboard** so the "notified" message becomes true. | not started | P2 | error-boundary message is misleading until DSN is wired |

### Pre-ship milestones

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O38 | **High-frequency path hardening** — dedicated correctness + polish pass on the paths an expense-first client uses daily: expense capture (receipt/bill upload → AI categorization → correct expense account, with easy correction when miscategorized), bank feed (import/match/categorize), revenue capture, and clean P&L/expense reports for the simple case. ~95% of real usage; about perfecting common flows, not adding event types (lease/deferred-rev/depreciation/payroll are done but lower-frequency) | not started | **P1** | pre-ship; pairs with O37, O20/O39. **See the "▷ Direction note — import flow has too much manual confirming" under the Per-tenant learning principle** (feedback: too much manual account-picking + per-item confirming today → pre-select via fingerprints O64, auto-apply high-confidence with override, confirm only exceptions; the manual→hands-off arc) |
| O41 | **Clean end-to-end shakedown** — build a fresh company from scratch with current code and run a full normal cycle (set cutoff → opening balances → book/pay bills → issue/collect invoices → run depreciation → pull financial statements), confirming everything ties with no manual SQL or cleanup. Proves the product works whole and surfaces common-path rough edges in context (vs. accumulated mess in Test 1). **This IS the execution of O7 Part 2** (event interaction / end-to-end on a real set of books). | not started | **P1** | pre-ship milestone; depends on O37/O38 fixes landing; **satisfies O7 Part 2** |
| O59 | **FULL-YEAR / FULL-SCALE SIMULATION — the proof-of-readiness gate (capstone).** Generate a realistic full year of activity at volume (hundreds–thousands of transactions across EVERY event type: bills, payments, AR invoices + collections, bank deposits, expenses, payroll runs, depreciation, prepaid amortization, deferred revenue, sales tax, leases, voids, refunds, transfers, owner draws, …), run it all end-to-end through a fresh company, then verify EVERYTHING holds with zero glitches: **(a) reports** — every report ties out and is internally consistent (P&L, Balance Sheet balances, Cash Flow, Trial Balance balances, AR/AP aging, KPIs, health score, monthly reports, by-vendor, by-category); **(b) cash** agrees across every surface (dashboard, BS, trial balance, raw ledger) — derived == raw everywhere; **(c) time-based logic** across the full year (fiscal-year close / RE split, depreciation month-over-month, deferred-rev recognition across periods, aging actually ages); **(d) performance/scale** at volume (no choking at 500+/1000+ txns; reports stay fast; categorization doesn't break; no pagination/timeout issues — this is the **live test of O47**); **(e) every feature/option** exercised (matching, reconciliation, recode, bulk ops, tax center, audit log, void/undo, exports, support mode); **(f) audit trail** complete and every transaction traceable. **THE final gate before onboarding any real client.** **VERIFICATION SPEC (books prove themselves — no eyeballing every txn; control figures + invariants computed INDEPENDENTLY of the app):** the generator emits a **CONTROL SHEET** as it creates the data — every expected total by simple arithmetic as it goes (total revenue; expense by category; payroll gross/withholdings/employer; AR billed/collected/outstanding; total AP; ending cash = Σ inflows − Σ outflows; count of each event type) = the answer key, derived independently of the GL machinery. Verification in **3 layers:** **Layer 1 — INVARIANTS** (always hold, data-independent; automate as pass/fail assertions over the seeded company): total debits == total credits across all `journal_entry_lines`; Balance Sheet Assets == Liabilities + Equity; trial balance nets to zero; **derived == raw for EVERY account** (`glAccountBalance(acct)` == Σ raw `journal_entry_lines` — the universal single-source check that catches masked inversions like the deposit bug); cash agrees across all surfaces (dashboard == BS == trial balance == raw ledger). **Layer 2 — CONTROL TOTALS** (app reports == the generator's control sheet): P&L revenue == generated; expense by category == generated; payroll totals == generated; AR outstanding == billed − collected; ending cash == generator's computed cash; etc. — independent computation arriving at the same number = strong proof. **Layer 3 — CPA SPOT-CHECK** (reasonableness/judgment): does the P&L/BS/cash-flow look like a believable real business; anything that jumps out. **Sub-tasks (build):** the seed/generator producing a plausible year of a small business's books, the control-sheet emitter, and the Layer-1/2 automated assertions — turning "looks right across thousands of txns" into "provably right." | not started | **P1** | pre-ship CAPSTONE. Relationship: **O41** is the small/early shakedown (basic cycle, run soon); **O59** is the full/comprehensive volume sim (last gate, after Phase 1–2 fixes land). Also the at-scale execution of **O7 Part 2**; live test of **O47** |

### Product features

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O27 | Integrated vendor payments (actually pay bills, not just record) | not started | P3 | |
| O28 | Expense / receipt submission (employee capture flow) | not started | P3 | |
| O29 | Slack / SMS bot interface | not started | P3 | |
| O30 | Stripe billing (subscription/usage) | not started | P3 | |
| O31 | Custom domain support | not started | P3 | |
| O43 | Split invoices — split one invoice/bill into multiple line items or allocations (e.g. across GL accounts / projects) | not started | P2 | multi-line booking already supported via C20 |
| O48 | Chatbot-configurable features — explore letting users customize/configure features via the chat interface | not started | P3 | feasibility discussion first |

### Standard-variant deferrals (common case built; full variants known, not silently missing)

| id | item | status | pri | deps/notes |
|----|------|--------|-----|-----------|
| O32 | Sales tax: multi-jurisdiction / per-line rates + universal-upload sales path | deferred | P4 | extends C28 |
| O33 | Depreciation: declining-balance, units-of-production, MACRS (+ mid-year conventions) | deferred | P4 | extends C23; `method` column already gated |
| O34 | Payroll: accrue-then-pay two-step (accrue to liability, relieve on disbursement) | deferred | P4 | extends O1 |

---

### Maintenance protocol
- New work the user asks to "add to the roadmap" → append as the next `O#` (never reuse ids).
- When an `O#` ships, move it to Section 1 as the next `C#`, fill the evidence column, and strike its `O#` line (leave a one-line "→ C#" pointer so ids stay stable).
- Update the header (date, test/migration counts) whenever Section 1 changes.
