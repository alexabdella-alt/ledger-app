# Migration verification narratives (moved out of CLAUDE.md §6 on 2026-09-15)

What each applied migration's checks returned, verbatim as recorded at the time — newest first. The applied ledger itself stays on CLAUDE.md §6.

> **★★★ `087` APPLIED & VERIFIED LIVE 2026-09-03 — A DAILY CAP BESIDE THE HOURLY ONE, AND THE NUMBERS NOW FIT REAL WORK.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`087`. **Five checks, five passes.**
>   · **(a)** both signatures present — the 3-arg form KEPT so the migration goes live before the proxy deploy and the product runs correctly in the gap (`086`'s discipline; `074` could not). **(b)** service role only, asked of `has_function_privilege` rather than a regex over `proacl`.
>   · **★★★ (c) IS THE POINT OF THE MIGRATION AND THE NUMBER PROVES IT: *"refused by the DAY while the HOUR was empty (0 of 60 used); resets in 240 minutes"*.** The probe seeds five calls at 20/15/10/5/2 hours — **all outside the hour, all inside the day** — so **240 minutes is only reachable if those rows survived the prune.** `086` deletes anything older than its window, and its window was an hour: **a daily cap on hourly retention can never exceed the hourly count, so it would exist, read correctly, and never once fire.** Retention now follows the WIDEST window, and the check was written specifically so a wrong prune reads ALLOWED rather than passing quietly.
>   · **★★ (d) `O113a` EXTENDED, NOT ASSUMED: *"refused by the day, and NOTHING was charged (3 → 3)"*.** A refusal issued by the DAY must not charge the HOUR, or retrying digs the hole deeper in a window that was fine.
>   · **★★ (e) IS THE `079` DIRECTION AND IT IS THE ONE A LAZIER VERIFICATION OMITS: *"allowed, and remaining reports the min across windows (hour 299, day 1189 → 299)"*.** For a guard, *"it fails to block"* is the obvious failure and the unlikely one; **"it blocks work that should pass" is the dangerous one**, because it breaks ordinary use and surfaces only when someone tries to do their job.
>   · **★★★ WHY THE NUMBERS MOVED, AND IT IS NOT GENEROSITY: WITH ONE WINDOW THERE WAS NO BOUND ON TOTAL DAMAGE.** A compromised account ran 60/hour forever — 1,440 calls a day, indefinitely, nothing ever stopping it — while a legitimate first upload was refused four minutes in (**measured: 20 of 35 documents, Red River drive, 04:39**). The instrument barely inconvenienced a patient attacker and completely blocked an impatient customer. **The hour bounds the BURST RATE; the day bounds TOTAL DAMAGE.** 300/hour is 100 documents (3 calls each, **measured not assumed**), so a monthly close never meets a limit and an onboarding finishes in ~2 hours rather than 12; the daily caps hold a stolen account to ~400 documents.
>   · **★ AND IT WAS ONLY SAFE TO RAISE BECAUSE THE DRAIN WORKS.** A bigger allowance is defensible only if a refusal is SURVIVABLE — until `O133` was fixed it was not, which is exactly why the repo held this change behind it.
>
> **★★★ `086` APPLIED & VERIFIED LIVE 2026-08-31 — THE RATE LIMIT IS A ROLLING HOUR, AND THE RESET TIME IS REAL.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`086`. **Five checks, five passes.**
>   · **(a)** table present, index present, **RLS on with 0 policies** — service role only, the same shape as `rate_limit` (`021`). **(b)** *"counts a trailing window; no clock-hour truncation remains"*.
>   · **★★ (c) IS `O113a` PRESERVED, WATCHED: *"refused at the limit, and the refusal was NOT charged (3 → 3)"*.** That number is the whole of `O113a` — `ai = 81` against a ceiling of 60 was that bug visible in production data, and it made retrying actively harmful. A limiter rewrite that quietly lost it would have been a regression nobody could see.
>   · **★★★ (d) IS THE POINT OF THE MIGRATION, AND THE NUMBER SAYS IT: *"capacity returns in 2 minute(s), not at the top of the hour"*.** Under the clock hour the same moment could only ever have said 55. **And the reason that matters is the onboarding queue, not fairness** — a drain under a clock hour processes a burst at `:00` and then sits idle for 59 minutes; under a rolling window it moves continuously.
>   · **★★ (e) ALL-OR-NOTHING ACROSS BUCKETS, ALSO PRESERVED: *"refused by upload, and NO ai charge was left behind"*.** Charging one bucket for a refusal issued by another is `O113a` wearing a different hat, and it was found the first time only because someone looked.
>   · **✅ `ai-proxy` v17 → v18 DEPLOYED 2026-09-01 01:52:31Z, both assets, ACTIVE — so BOTH halves are live.** The rolling window was already working in the database; the deploy is what makes the 429 **message** quote the real reset time instead of the clock hour. **The version bump proves the bundle shipped and nothing about the message under load** — the first real 429 is its test, and it will read minutes rather than "up to 59".
>   · **★ AND THE DEPLOY ORDER BEING NON-LOAD-BEARING WAS TESTED BY EVENTS, NOT JUST BY DESIGN:** the migration went live first and the product ran correctly on the old proxy for the gap between them, because the old code simply ignored the new field. `074` could not have done that.
>   · **(superseded — kept for the sequence) ▶ THE HALF THAT WAS NOT LIVE YET: the `ai-proxy` deploy.** The rolling window itself is in the DATABASE and is working now. The 429 **message** still computes its reset from the clock hour until the proxy ships — so behaviour is fixed, and the sentence describing it is not. **Deploy order was deliberately made non-load-bearing** (unlike `074`), which is exactly why this half can wait without breaking anything.
>
> **★★★ `085` APPLIED & VERIFIED LIVE 2026-08-30 — A SOLO OWNER CAN SIGN THEIR OWN BOOKS, AND THE ROW CANNOT LIE ABOUT IT.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`085`. Five checks; four passed first time, the fifth needed rewriting before it meant anything.
>   · **(a)** column present, **7 sign-offs, 0 flagged** — correct: every one predates this and was genuinely reviewer-written. **(b)** both predicates installed.
>   · **★★ (c) + (d) ARE ONLY A RESULT AS A PAIR.** (c) *"PASS - a solo owner signed their own books, with self_attested recorded"* · (d) *"PASS - refused: an owner cannot record their sign-off as a reviewer's"*. **Without (d), (c) is equally consistent with having recorded an accountant's review that never happened** — which is the one lie `period_signoffs` must never be able to tell.
>   · **★★★ (e) CAME BACK INCONCLUSIVE AND THAT IS NOT A PASS.** No company has both an owner and a reviewer, so the separation direction was **unproven** — and it is the dangerous direction here: **if `company_has_reviewer` always returned false, (c) and (d) would BOTH have passed exactly as they did while every owner in the system could self-attest.** Rewritten as **(e2)**, which SEEDS the accountant inside the transaction it rolls back (the fix `081`'s check (c) needed) and **refuses to pass vacuously** — if the seed does not land, or the predicate cannot see it, it reports INCONCLUSIVE rather than PASS. **Live: *"PASS - refused: the moment a reviewer exists, the owner can no longer self-attest"*.**
>   · **★ AND THE REASON (e) HAD NOTHING TO TEST IS ITSELF A FINDING: `053` PROMOTED THE OWNER TO ADMIN ON EVERY COMPANY THAT LACKED A REVIEWER**, so older companies hold admins and no owners while anything created since holds an owner and nobody else. **The separation-of-duties path has therefore never been exercised on live data by two real people** — worth doing for real when an accountant is first brought onto a company.
>
> **★★★ `079` APPLIED & VERIFIED LIVE 2026-08-29 — `078` WAS BLOCKING A CHANGE THAT MOVES NO MONEY, AND ITS OWN VERIFICATION MISSED IT.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`079`. Three checks, three passes.
>   · **`078` gave the ENTRIES table column-level discrimination** (only `entry_date`/`status`/`deleted_at` protected, so a payment can still be recorded against last quarter's invoice) **and gave the LINES table none** — so it refused ANY line update in a signed month, including `persistChatRetagProject` setting `journal_entry_lines.project`. **A signature attests to the NUMBERS; it does not freeze how work is filed for reporting.**
>   · **★★ AND THIS IS PRECISELY THE FAILURE `078`'s CHECK (5) WAS WRITTEN TO CATCH.** That check exists because for a guard *"it blocks too much"* is the dangerous failure — it breaks ordinary work and surfaces only when someone tries to do their job. It proved an open month still accepts ENTRIES. **It never tested a metadata-only LINE update in a signed month, so the regression walked straight past the verification designed for it.** A negative-case check is only as good as the axis it varies.
>   · **VERIFIED AS A PAIR, WHICH IS THE POINT:** (b) a project retag in a signed month is now ALLOWED · (c) an amount change is still REFUSED. Without (c), (b) passing is equally consistent with *"we exempted metadata"* and *"we switched the guard off"*.
>   · **★ FOUND BY APPLYING `078`'s OWN RECORDED RULE**, not by a failure report — *when adding a refusal, grep every caller of the thing it now refuses.* The same sweep found both user-facing **Undo** buttons doing unchecked writes inside `console.warn` catches, so a refused undo left the toast dismissed, the entry in the books, and the person believing it was gone. `078` made that reachable rather than merely possible. **An undo that fails silently is the worst button in the product** — the repeat is another Void, which is how one invoice was reversed three times (`O123`).
>
> **★★★ `078` HAD A SAME-DAY INTERACTION NOBODY'S TESTS COULD HAVE CAUGHT (C235).** The trigger correctly refuses to soft-delete an entry in a signed month — and `undoImport` (QuickBooks) did that delete **unchecked**, then marked the batch `undone` and reported `batch.imported_count` entries removed. So an import spanning an attested period would have said *"47 entries removed ✓"*, flipped the batch to undone, and left all 47 live — **with no way to retry, because the batch no longer looked undoable.** A guard added in the morning made a latent silent-success bug dangerous by the afternoon. **`078`'s own tests prove the trigger refuses; the QBO tests predate it; only reading one file against the other found this.** When adding a REFUSAL, grep every caller of the thing it now refuses.
>
> **★★★ `078` APPLIED & VERIFIED LIVE 2026-08-29 — THE DATABASE NOW REFUSES TO CHANGE A SIGNED-OFF MONTH.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`078`. Five checks, five passes.
>   · (2) both triggers installed. (3) **all seven signed periods (`2026-01`…`2026-07`) read back correctly** through `period_is_signed` — the helper agrees with the sign-off rows one by one, not in aggregate. (4) **WATCHED TO REFUSE**, with the message a person would actually get: *"January 2026 has been signed off by your accountant, so its figures cannot be changed. Reopen that month first if a correction is genuinely needed."*
>   · **★★ (5) IS THE CHECK THAT MATTERED, AND IT IS THE ONE A LAZIER VERIFICATION WOULD HAVE OMITTED: an OPEN month still accepts entries.** For a guard, "it doesn't block" is the obvious failure and the *unlikely* one; **"it blocks too much" is the dangerous one**, because it breaks ordinary bookkeeping and surfaces only when someone tries to do their job. Proving the negative case is not optional for anything that refuses.
>   · **★ THE TRIGGER IS DELIBERATELY NARROWER THAN "BLOCK EVERY WRITE", AND A REAL FLOW IS WHY.** Marking an old bill paid is legitimate — the PAYMENT is dated today — but it stamps `payment_status` on a row inside the signed month. A blanket block would have refused that, and the first symptom would have been a CPA unable to record a payment against last quarter's invoice. So the guard protects what a signature is ABOUT: **existence** (no entry appears, vanishes, or is hard-deleted), **the numbers** (no line added, changed or removed — which also blocks a recode), and **the date** (no entry moved into or out of a signed month). Amount-free metadata stays writable.
>   · **`opening_balance` IS EXEMPT, matching `signedPeriodForDate` exactly** — the opening entry is dated at the cutoff, which may sit inside an attested month, and the redo-opening path reverses and replaces it. **A database that disagreed with the client here would be worse than either rule alone.**
>   · **`period_is_signed` is SECURITY DEFINER on purpose:** `period_signoffs` carries RLS, and **a guard whose input can be made invisible by a policy is a guard that can be defeated by not being able to see the thing it checks.**
>
> **★★ `076` + `077` APPLIED & VERIFIED LIVE 2026-08-29. SIX CHECKS, SIX PASSES, WITH THE OUTPUT.** Ledger now: through `061`, plus `063`, `064`, `066`, `068`–`075`, **`076`, `077`**. Applied in real time with the operator, verification pasted back per check.
>   · **`076` — the invite role vocabulary.** (a) `column_default = 'viewer'::text` → PASS. (b) `unacceptable_pending = 0` → PASS. (c) **the constraint was WATCHED TO REFUSE**: `ERROR: P0001: VERDICT: PASS - the constraint refused role=member`. That error IS the result — the `raise exception` both aborts the probe transaction and surfaces the verdict, because `raise notice` does not reliably reach the Supabase editor.
>     - **★ ONE PRECISION ON (b), BECAUSE THE PASS IS WEAKER THAN IT LOOKS: `0` CONFIRMS THE END STATE, NOT THAT A CONVERSION HAPPENED.** It is equally consistent with "there were broken pending invites and step (b) fixed them" and with "there were none to fix". The check verifies the property we care about either way, and **the pre-state was not captured** — a count taken before the UPDATE would have distinguished them and was not run. Recorded so nobody later reads this as evidence that rows were repaired.
>   · **`077` — `documents.document_date`.** (a) `date / YES / null` → PASS. (b) **91 documents, 0 carrying a date** → PASS, additive as designed: no row was given a date it did not have, because a guessed document date is exactly the substitution the column exists to avoid. (c) index present → PASS.
>   · **★★ AND `076` WAS FIXED BEFORE IT WAS PASTED, NOT AFTER.** The first draft used a plain `add constraint … check`, which **validates every existing row** — `'member'` was the invite form's DEFAULT, so accepted invites carrying it almost certainly exist, and the ALTER would have failed and rolled back the whole transaction in front of the operator. `NOT VALID` is also the semantically right answer: step (b) rewrites only PENDING rows because **an accepted invite is history**, and a constraint that can only be adopted by rewriting history is asking for the wrong thing.
>
> **★★ `074` + `075` APPLIED & VERIFIED, `ai-proxy` v16 → v17 ACTIVE (2026-08-26/27). O113a IS LIVE.** Ledger now: through `061`, plus `063`, `064`, `066`, `068`, `069`, `070`, `071`, `072`, `073`, **`074`, `075`**. Applied in the required order — **migration before deploy**, because the reverse calls a function that does not exist and fails every AI request.
>   · **`074` — VERIFIED BEHAVIOURALLY, WITH THE NUMBER THAT DECIDES IT.** Block (b) `4_after_refused = 10` — *"the fix: refused and NOT charged"*. **Under `021` that reads 11, and the difference IS the fix.** Block (c) `2_ai_after = 0` — *"ai NOT charged for a call upload refused"*, closing the second instance of the same bug (a call refused by `upload` used to leave an `ai` charge behind). **The limiter was watched to refuse WITHOUT charging**, which is the standard `071` set and the only thing that separates this from a fix on paper.
>   · **`075` — APPLIED & VERIFIED 2026-08-27. `anon_exec=false · authd_exec=false · svc_exec=true` → PASS.** Proved with **`has_function_privilege`, which is Postgres's OWN resolver** — the code path the executor consults — rather than a regex over `proacl`, which silently misses a grant arriving through PUBLIC or role inheritance. **One remaining shade of doubt, recorded not glossed:** this is the executor's DECISION, not its REFUSAL — we still never watched an `anon` call raise, and `075` being applied means the failing state can no longer be observed. *(The earlier partial record, kept because the sequence is the useful part:)*
>   · **`075` — APPLIED, AND VERIFIED ONLY IN PART. SAY SO.** Catalog read confirms **`bump_rate_limit` acl went `{postgres,anon,authenticated,service_role}` → `{postgres=X/postgres,service_role=X/postgres}`** — `anon` and `authenticated` revoked, `service_role` preserved so the proxy and `074`'s rollback path still work. `consume_rate_limit` matches. **That is VERIFY (a) only. Blocks (b) and (c) WERE NOT RUN** — the operator's paste came through as an unfilled placeholder, and nothing is recorded from it. **So the ACL change is evidenced and the BEHAVIOURAL refusal is not:** we never watched `anon` actually be refused, and the chance to observe the hole in its failing state is now gone, because `075` is applied. *(Recorded as owed, not as done — a before/after on `proacl` is a strong catalog fact and it is still not the same claim as "the call raises for an unauthenticated caller".)*
>   · **`ai-proxy` v17** (`2026-08-27 03:08:05Z`, both assets). **This proves the bundle shipped and NOTHING about the new limiter path under load** — the first real drive is its test. The old `bump_rate_limit` is now called by nothing; it survives only as `074`'s rollback path.
>
> **★ `091` WRITTEN 2026-09-15, ▶ HOLD — NOT APPLIED (O139, the line-account company guard). Run its probe (A) first; it is expected to say FAIL.**
>
> **★ MIGRATIONS APPLIED — the live state.** **Applied & verified live: through `061`, PLUS `063`** (`060` on 2026-08-08, `061` on 2026-08-09, **`063` on 2026-08-17**; all verified via catalog queries, not an HTTP 201). **Pending: `062`** (unknown_documents policy dedup — queued, unwritten).
>
> **★ MIGRATION PASS 2026-08-25 — `066`, `071`, `072`, `073` ALL APPLIED & VERIFIED.** Ledger now: **through `061`, plus `063`, `064`, `066`, `068`, `069`, `070`, `071`, `072`, `073`.** *(`062`, `065`, `067` unused — `062` still owed; `065` released, the vendor backfill cannot be SQL; `067` freed when the flag queue moved to `072`'s shape.)*
>   · **`066`** — 11 directory rows, **1 prefix / 10 exact**, RLS on, 4 policies correct (SELECT on `auth.uid() is not null`; INSERT/UPDATE/DELETE on `is_platform_admin()`). **VERIFY (b) came back a FINDING, not a pass → `O112`.**
>   · **`071`** — constraint present and **tighter than specced**: it validates months `01`–`12`, not merely two digits.
>   · **`072`** — table created, all 16 columns including both `descriptor_display` and `resolver_input`.
>   · **`073`** — origin now **external 36 · runtime 6 · seed 530**, exactly as predicted. What remains `runtime` is only genuine materialisation: `2101` × 3 (the payroll path) and Franklin Ave's `3400`/`6520`/`6530`. **`origin` is now precisely right, and the O108 detector means what it says again.**
>   · *A correction worth keeping: `073` was challenged as moving 22 rows. It moves **33** — it has TWO update clauses, and the reconstructed preview implemented only the ≥5 batch rule. The proposed generalisation (same-code-across-many-companies-same-day) would have been actively WRONG: that is precisely the `2101` signature, the one population established as genuinely runtime.*
>
> **`070` APPLIED 2026-08-25 — RECORDED AS *APPLIED AND UNDERSTOOD*, NOT AS CLEAN.** `accounts.origin` exists; the O108 detector (`system_role is null and origin <> 'external'`) returns **0 rows — restored**. Distribution: **external 36 (4 companies) · runtime 39 (11) · seed 497 (11)**. But `runtime 39` is **not** the "handful" the file predicted, and the diagnostic showed the `created_at` window mislabelled **35 of the 39**:
>   · **`7150` × 11 (2026-08-17)** — `063`'s OWN BACKFILL. Seed by definition, mislabelled by construction.
>   · **22 codes × 1 company (c787582f, 2026-06-07)** — a seed batch. **Mechanism corrected from the first reading:** these are NOT "the chart grew" — all 22 are in the `000`-era seed function already, so the chart had them. **That company was behind the seed and `seed_company_accounts` was re-run for it.** Same conclusion, different cause, and the cause is the useful part: re-running the seed is a thing that happens here.
>   · **`2101` × 3 (2026-06-21)** — **GENUINELY RUNTIME.** The payroll path needed Payroll Taxes Payable, the company lacked it, `ensureAccount` created it. One row per company, idiosyncratic: exactly what the label should mean.
>   · **`3400`/`6520`/`6530` × Franklin Ave (2026-07-22)** — the O108 materialisations. **Staying `runtime`, against the operator's leaning**, because `origin` records HOW A ROW GOT HERE and that stays true after `068` blessed them. Marking them `seed` would erase the only durable evidence in the database that the materialisation path ever fired on a real client's chart. "Runtime-created and now canonical" is exactly `origin='runtime' AND system_role IS NOT NULL`.
>   **CORRECTION OWED: migration `073`** relabels the 33 mislabelled rows by SHAPE — *seed operations are batches, materialisations are singletons* (≥5 accounts for one company on one day) — rather than by naming dates that would be wrong next time. Until it applies, `origin` is **directionally right and precisely wrong**, and any query over it must say so.
>
> **`064` APPLIED 2026-08-23.** Ledger now: applied & verified through `061`, plus `063`, `064`, `068`, `069`, `070`. *(`062` still owed. `065` RELEASED — see §11: the vendor backfill cannot be SQL. `066`/`067` still reserved for the directory and the flag queue; `070` written, ▶ HOLD.)*
>
> **`064` verification, 2026-08-23** — 15 columns ✓ · RLS on, 3 policies, no DELETE policy ✓ · CHECKs carry the Q1/Q3 rules (`tier` four-valued; `demotion_reason` limited to `mapping_correction`/`dormancy`, with amount behaviour correctly ABSENT) ✓ · 0 rows ✓. **Two operator questions answered: (a) the unique key is a unique INDEX (`vendor_state_company_entity_idx`), not a table constraint, which is why it did not appear in `pg_constraint` — confirm with `pg_indexes`; it still backs `ON CONFLICT (company_id, entity_key)`. (b) `first_seen`/`last_seen`/`demoted_at` are TEXT 'YYYY-MM' deliberately** — Postgres has no month type, `date` would force a spurious day, and `monthsBetween` is pure string/integer arithmetic with no `Date` and no timezone, which is exactly the O86 UTC-date-key class avoided by construction. **GAP FLAGGED: the format is guarded in JS (`isYm`) and NOT in the schema** — a `check (first_seen is null or first_seen ~ '^\d{4}-\d{2}$')` on the three columns would close it, and is proposed rather than slipped in.
>
> **`068` + `069` APPLIED 2026-08-17.** Ledger now: applied & verified through `061`, plus `063`, `068`, `069`.
>
> **`068` verification** — (a) roles set on all three codes ✓ · (c) all six seed checks true ✓ · (d) Franklin Ave lines untouched (`6520`=3, `6530`=2) ✓ · **(a) surprise: `3400` exists on TWO companies, not one** — a second company's on-demand OBE materialisation, now correctly roled by the backfill; benign, and the identity of that company is still owed. **(b) FAILED ITS EXPECTATION AND THAT IS THE VALUABLE PART: expected 0 role-less accounts, returned 36** — none on Franklin Ave, all on three other companies, in a foreign numbering scheme. **DESIGNATED `O110`.**
>
> **`069` verification — PARTIAL, and it is NOT closed.** (a) `anon` revoked, `authenticated` kept ✓ · (b) body intact ✓ · **(c) NOT RUN.** The behavioural check — create a company end to end and confirm it seeds — is the only one that can prove the revoke didn't break onboarding, and a catalog read cannot stand in for it. **`069` is applied but NOT fully verified; the open item is recorded on ROADMAP `O108`.** *(The file's expected count said 56, from before `068`; corrected to 59 — a stale expected value turns a check into a rubber stamp, and 56 would now read as a pass while meaning `068` did not take.)*
>
> **`063` APPLIED OUT OF ORDER, deliberately.** `062` is queued-but-unwritten and is not a prerequisite of `063`; the chain is monotonic, not contiguous (§6), and waiting on an unwritten file would have blocked calibration for no gain. **`062` remains owed.**
>
> **`063` verification, 2026-08-17** — (a) **11 companies / 11 with 7150 / 11 with the role** · (b) all five true: gained `uncategorized_expense`, kept `lease_liability_current`, `marketing_advertising`, `common_stock`, and no `account_type` · (c) **0 lines booked to 7150** (C200 wires no caller, as intended) · (d) ACL `{postgres=X,anon=X,authenticated=X,service_role=X}` — preserved by create-or-replace, **and see `O108` finding 2: `anon` holds EXECUTE, which the repo believes was revoked.**
>
> **RESERVED — `063`–`067` are the O88 CALIBRATION chain (2026-08-16), in this order:** `063` Uncategorized Expense (COA seed + backfill) · `064` `vendor_state` · `065` `vendor_state` historical backfill · `066` `universal_vendor_directory` · `067` `calibration_flags`. **Applied in commit order, not written all at once.** Do not mint `063+` for anything else without renumbering the chain. *(`062` stays reserved for the queued `unknown_documents` policy dedup.)*
>
> **RULE: every migration application gets its date recorded on this line, in the same docs commit that closes the session.** The repo records migration FILES; nothing in it records their APPLICATION, and there is no `supabase_migrations.schema_migrations` table to ask (see below — deliberately). So a file's presence proves only that it was written. Without this line every fresh session re-derives the answer, guesses wrong, and re-raises a migration as "still pending" that went live days ago. *(Cost already paid: `060` was flagged unapplied twice after it was live.)*

