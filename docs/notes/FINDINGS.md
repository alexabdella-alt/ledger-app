# Findings — drives, `O###` diagnoses and decisions (moved out of CLAUDE.md §11 on 2026-09-15)

Closed drive narratives (O83–O88 and the Red River drives), the `O###` diagnoses, and the decisions recorded with them — verbatim, in the order they sat in §11. The OPEN items and the standing backlog stay in CLAUDE.md §11; the ROADMAP OPEN LEDGER is the index of what is still open.

- **★ O84 PART 1 — COMPLETE (2026-08-04 live drive of the persisted-statement pipeline, C185–C187; remediated by C188–C191).** ✅ **RESULT: March 2026 completed live** — 22 lines: **13 auto-booked** + **5 human-resolved** (low-confidence exceptions, surfaced only after C191) + the **Atlas outstanding clear** (no double-book) + **2 A/P clears** + **2 payroll matches**; **auto-reconciliation completed at 27,762.31 / 27,762.31 / 0.00** against an **independent answer key**; **line idempotency held across 5 statement uploads**. **Part 2 (April dirty-path fixture: forced exceptions, attention flow, CPA resolution) is the remaining O84 scope, sequenced after C192–C193.**
  **RESOLVED this drive:** payroll parse died on trailing text → **C188** (`extractFirstJson`, + a 19-site sweep list for deliberate migration, **DESIGNATED: ROADMAP §2 Bugs `O99`**) · payroll `payment_status` `ap_tie` leak → **C189** (+ the §9 cash-settled-write anti-pattern) · planner dead-zone guarantee + review-screen teardown → **C190** (exhaustiveness invariant + conditional `bankStep`) · **planner-exception id seam — THE actual live five-pending-lines bug** (exceptions carried the parse-time local id, so the DB write matched zero rows AND the lines were filtered out of the review table: invisible in the DB *and* the UI) → **C191**, mutation-tested (3 of 5 new tests proven red pre-fix).
  Original six findings:
  1. **STATEMENT/DOCUMENT DEDUP (→ C193).** Re-uploading the same statement creates a NEW `bank_statements` row + a NEW `documents` row each time (verified live: **3× Feb rows**). Line-level idempotency HELD — **zero double-books** (`markAlreadyBooked` / `bankTxnKey`) — but the `bank_statements` table and the doc library accumulate duplicates. **Fix:** a **SHA-256 content hash at `storeDocument` time, unique per company** — identical bytes link to the existing document instead of inserting; `bank_statements` dedups on **account + period + content hash**, superseding the prior row.
  2. **QUEUE COPY SHOWS THE PRE-OVERRIDE AI LABEL.** The processing queue said "booked … as a client meal" while the entry correctly booked to **5000 COGS** via the learned vendor mapping. The displayed label must be the **FINAL post-mapping account**, not the raw AI guess — an owner-facing honesty bug. Live specimen of the confidence/reasoning-consistency item (Tier 1 #7) — **fold into that work**, not a new entry.
  3. **PAYROLL INGESTION FORMAT GAP.** The Home drop zone advertises PDF, but payroll accepts only `csv/xls/xlsx/iif/txt` — a **Gusto PDF summary** (the artifact an owner actually has) is rejected. Either **parse payroll PDFs through the extraction path** or **stop advertising what we reject**. **DESIGNATED: its own ROADMAP §0 TIER 2 line** ("payroll PDF ingestion through the extraction path") — it was previously mis-filed under the doc-library line, where ingestion work would have been lost.
  4. **DOC LIBRARY DATE = UPLOAD DATE.** Cards show the **Aug upload date for a Feb statement**. Same as the existing doc-library backlog item **(c)** (derived document date) — the date must be the derived statement/invoice date; **now confirmed live**, fold into that item.
  5. **DOCS SEARCH MISSING.** The library header says "stored and searchable" with **no search input**. Minimum: **filename + type + date-range**; content search later on extraction text. Folds into the Tier 2 doc-library work.
  6. **API CREDIT EXHAUSTION HAS NO GRACEFUL MODE.** The org ran out of Anthropic credits mid-drive; extraction-dependent features fail **without a clear in-app explanation**. Launch-week: **credit/balance monitoring, auto-reload guidance, and an explicit degraded-mode banner** ("document reading is paused") instead of silent failures.

  **Back-half findings (added after the drive's second session):**
  - **(a) OPENING-BALANCE BANNER IS OUTSTANDING-BLIND.** It warns on a bank-vs-books difference (**$275 live**) that the **reconciliation chain fully explains** — the Atlas uncleared check. It should consult `priorOutstandingCandidates` (C187) and render **"explained by N uncleared item(s) ✓"** instead of an alarm. **Small, high trust-value** — the same false-alarm class as the C179 comparand fix: frightening a client with a difference the system already understands.
  - **(b) TRANSITIONAL REVIEW FLASH.** On upload the **full parsed line list renders briefly**, then the pipeline's verdict replaces it — content appears, then vanishes. Replace with a **"handling this automatically…"** state that resolves *directly* into the outcome summary (never render a list the client isn't meant to operate). **Dissolves fully at IA collapse (North Star Phase 2); cheap interim fix.**
  - **(c) ★ SILENT WRITES — the root-cause CLASS of the night (→ C192, NEXT CODE COMMIT).** Supabase updates that match **zero rows still "succeed"** (no error), and empty `catch {}` blocks swallowed **two live bugs** this drive (C189's missing stamp and C191's id seam were both invisible for exactly this reason). **Fix:** a **checked-write helper** for row-targeted mutations — assert **no error AND rows-affected ≥ 1**, with a **loud console warning + a visible failure counter** on violation; **adopt at every `bank_statement_lines` / `journal_entries` mutation site**. Plus **§9 doctrine**: ids reach DB calls **only** through `lineDbId` (grep-enforceable), and **every new write path stamps the hygiene fields** (`payment_status`, `bank_account_id`, `meta.kind`).
  - **(d) STATEMENT/DOCUMENT CONTENT-HASH DEDUP (→ C193).** The live library now holds **3× March + 3× Feb copies**. Dedup per finding 1's spec: **SHA-256 at `storeDocument`** (unique per company; identical bytes link to the existing document) and **statement supersede on account + period + hash**.

- **★ O84 PART 2 — COMPLETE (2026-08-05, the April dirty-path drive). O84 IS NOW FULLY COMPLETE — 4 attested months.** ✅ **RESULT: April 2026 driven end-to-end and SIGNED OFF.** The pipeline correctly landed **'attention'** (**no false reconciliation** — the honest outcome for a dirty month). Of the 3 designed traps: the **mystery deposit** was **excepted** (routed to a human, as designed) and the **merchant fees** were **legitimately BEATEN** — booked to **6520 at 99 confidence with coherent reasoning**, meaning **the §11 miscategorization specimen no longer reproduces**. Manual reconcile completed at **33,064.41 / 32,714.41 / 0** with **check #1043 written to `outstanding_books`** — **THE CHAIN PRODUCED ITS FIRST ITEM** (May will consume it, exercising C187's clear path on a self-generated item). **Learning layer, precisely observed:** Lone Star (taught) → **booked at 87**; Bluebonnet (taught) → **excepted at 78** — i.e. the learned mapping informed the **SUGGESTION but not the SCORE**. **Line idempotency and ledger integrity held throughout; ZERO wrong entries across the whole of O84.**

  **PART 2 FINDINGS:**
  - **★★ FALSE ATTESTATION SUCCESS (→ C194, NEXT CODE COMMIT — before anything else).** `ReconView`'s completion renders **"Your books match your bank ✓" UNGATED**: a failed completion insert is merely `console.warn`'d and **the success screen renders anyway** — **reproduced live with NO row existing**. This is the **worst class of bug in this product** (a false green on the attestation surface itself). **Trigger isolated by A/B:** entering the balance **BEFORE** resolving the outstanding item made the session autosave fail; completion then **UPDATED a nonexistent row** — a **zero-row, UNCHECKED** write (ReconView sites were **deferred** in the C192 sweep — this is that debt coming due). **C194 scope:** root-cause the ordering failure; **gate the ✓ on a VERIFIED complete row** (checked write **+ re-select**); adopt `checkedRowUpdate`/`checkedIdsUpdate` at **all** ReconView mutation sites; **regression test pinning "completion write failure → NO success state, plain-language 'nothing was locked in'".**
  - **★ CONFIDENCE PRICES DESCRIPTOR CLARITY, NOT VENDOR FAMILIARITY (Tier 1 #7 spec input — the sharpest specimen yet).** A **never-seen** vendor auto-booked **$850 at 88** on mere *name plausibility*, while a **twice-taught** vendor scored **78 and excepted**. The score is reading **descriptor legibility**, not **what we actually know about this client's vendors**. **Requirement:** **novel vendors DAMPENED below the auto-book floor; taught vendors BOOSTED above it** — confidence must price familiarity. (Pairs with the onboarding vendor teach-in / entity-profiles item: teaching is worthless if it doesn't move the score.) **MAY EVIDENCE (O85) — the score WOBBLES:** Lone Star, taught and **booked @87 in April**, was **excepted below the floor in May** — with the correct taught account already pre-selected. The mapping is stable month-to-month; the CONFIDENCE isn't. So calibration must not merely nudge taught vendors up once — it must **stabilize them above the floor across periods**, or the same vendor keeps oscillating between auto-booked and hand-resolved for no reason the client can see.
  - **→ C195 — WORKBENCH HONESTY SWEEP** (the narration layer, batched): **(1)** exception cards **not refreshed after MANUAL booking** (**5 zombies live** — the C193 supersede fix's sibling on the manual path); **(2)** reconciliation completion **doesn't retire covered statements'** attention status/cards (**1 stale card persists post-sign-off**); **(3)** **anomaly pattern suppression** — an identical 4-card Bluebonnet dismissal **two months running** is alarm fatigue (see the §11 pattern-level duplicate-suppression item); **(4)** **manual JE entry is UNREACHABLE** — `AddView` is orphaned, so booking check #1043 required **direct SQL mirroring `post_journal_entry`** (**near-Tier-1**: a CPA cannot hand-enter a transaction); **(5)** **transitional parse flash** (now raised **4×**); **(6)** booking toast reported **0/0 after booking 5**; **(7)** **intake orphan should auto-resolve** on content-hash match to an already-recorded doc (C193's hash, applied to the intake ledger); **(8)** **outstanding-check copy must pass the "knows nothing" bar.** **STANDING DIRECTIVE (record once, applies everywhere): every owner-facing surface assumes ZERO accounting knowledge** — this generalizes the Cardinal Principle from "no jargon" to "no assumed concepts."

- **★ O85 — MAY DRIVE, IN FLIGHT (→ C196, honesty follow-ups).** The May statement consumes April's self-generated outstanding item (check #1043), exercising C187's clear path end-to-end for the first time. Findings so far:
  - **(a) BANK IMPORT'S OPENING BANNER IS STILL THE ORANGE ALARM.** C195 item 8 gave **Reconcile** the calm, chain-aware copy ("explained by 1 check ✓"), but **Bank Import's** banner was not touched — so the same fully-explained gap still reads as a warning on the surface the client actually lands on first. **Fix:** adopt `openingMismatchCopy` + `priorOutstandingCandidates` there too. (Same false-alarm class as C179; the fix already exists, it just isn't wired to both surfaces.)
  - **(b) POST-PIPELINE SUMMARY TILES NARRATE ONLY THE RESIDUE.** After a 21-line statement runs, the tiles read **"Total transactions 5"** — the count of what's LEFT, not what happened. The counters must describe the WHOLE statement: **"21 transactions · 16 handled automatically · 5 need your input."** **That sentence IS the product pitch** — the one moment the client sees the machine's whole contribution — and today it's hidden behind a number that makes the product look like it did less than nothing. Surface it prominently, not as a footnote.
  - **(c) ANOMALY DETECTORS FIRE ON PAYROLL/SYSTEM ENTRIES.** The large-charge and round-amount detectors flagged a **$4,000 payroll gross** as "may need to be capitalized" — nonsense for a payroll leg. **Fix:** exempt `source=payroll` (and system kinds generally) from `large_transaction` and `round_number`. Detection should never opine on entries whose shape it doesn't own.
  - **(d) RECONCILE NEEDS "ABANDON THIS MATCH".** An accidentally-started match session left an `open` reconciliation row that required **manual SQL** to clean up — there is no in-app way to discard a session you didn't mean to start. **Fix:** an explicit, confirm-guarded abandon action that deletes/retires the open row (routed through the C192 checked writes + an audit row), so no reconciliation ever needs a DBA.

- **★ O86 — JUNE DRIVE, COMPLETE (2026-08-06; → C198). MONTH SIX SIGNED OFF.** *(Outcome: **(a)(g)(i)(j)(k)(l) SHIPPED + live-verified** — see ★ O86·2 below; **(b)(c)(d)(e)(f) — ALL SHIPPED**: (b) → C198·3a `68151c5`; (c)(d)(e)(f) → C198·3b `30cf680`. **C198 IS COMPLETE.** *(Read "(b)(c)(d)(e)(f) remain" until the 2026-08-11 sweep — this section stamped (e) and (f) shipped elsewhere while this summary still asked for them.)*)* The June drive ran on the post-C197 client seat (Home + Reports), so for the first time the findings are about what the OWNER experiences rather than what the workbench exposes. ✅ **RESULT: June 2026 reconciled and signed off** — reconciliation **46,434.91 / 46,434.91 · difference 0 · `complete` · verified · `outstanding_books` []**, the `bank_statements` row reached **`complete`**, and the month holds **exactly two Gusto entries** (no payroll duplication). **Operator rubric:** 1 — No · 2 — No · 3 — **upload flow not clean** · 4 — No · 5 — **"Not yet, but closer."** The engine and the ledger were right again; **the drive was spent fighting the INTAKE surface**, which is precisely what C198 (a) exists to remove. Findings:
  - **(a) ★★ HEADLINE — DROP-ZONE AUTO-RUN (single-account).** The Home drop zone still only **stashes** a statement for the CPA ("your accountant will add these") — account binding was deferred at C186 (the offset account can't be read off the file) and never revisited, and C197 turned that deferral into the client's whole experience. **Fix:** a company with exactly **ONE bank account auto-binds**, so a drop on Home runs the **FULL pipeline** (parse → dedupe → book → match → derive balances → reconcile) and the **trust panel narrates the outcome**. This is the **missing half of "handled, not operated"** — the North Star's clean path has existed since C186 but nothing client-facing could reach it. **Related fragility:** the pending-import stash is **in-memory** — today's Act-1 statement stash was **lost before Bank Import could consume it**. Auto-run **replaces** the stash for the single-account case; the stash must be **persisted** for multi-account companies (where a human still picks the account).
  - **(b) PAYROLL AUTO-POST — PROMOTED (Tier 3 → C198-adjacent / Tier 1-adjacent).** A CPA clicking **"Post to Ledger"** on a standard biweekly register is **operating, not reviewing** — the same class of defect as (a), one surface over. **Fix:** **standard-shape registers auto-post** with an **undo window + audit row**; the debit/credit confirm card survives **only for non-standard shapes**. (Supersedes the Tier 3 "payroll auto-post" product idea below, which was scoped as post-launch polish before the client seat made it load-bearing.)
  - **(c) `firstUnsignedMonth` SPEC FIX (Act 0).** The C196 helper picks the earliest month **with activity** that isn't signed off; the correct rule is **the month AFTER the latest sign-off, independent of activity** — a quiet month is still the month to review next, and skipping it silently walks the reviewer past a period nobody attested. **Fix + test:** a **zero-activity unsigned month** case (activity list empty, prior month signed → returns the following month).
  - **(d) OWNER-SURFACE SELF-CONTRADICTION.** The trust panel says **"Nothing needs your attention"** while an **amber "5 unusual patterns"** box sits **below it on the same screen** — the panel and the box are both telling the truth about different things and the owner reads a contradiction. **Fix:** on the **owner seat**, LOW anomalies render as **at most one muted line (or nothing at all)**; **amber/warning framing is CPA-side only**. (The C197 seat boundary is the mechanism; this is the content decision it enables.)
  - **(e) BURN/RUNWAY CONTRADICTION — PROMOTED onto C198.** *"You're profitable **with ~2 months of runway** · Needs attention"* now renders on the **demo surface** a prospect sees, which moves it from a backlog honesty bug to a launch-facing one. Resolve per the original Tier 1 #9 spec (burn = **net cash change**; profitable/cash-positive → **no runway warning**, positive framing). See the §11 burn/runway backlog item for the blast-radius caveat — one shared figure across card, drill and AI snapshot.
  - **(f) ANOMALIES AUTO-EXPIRE ON SIGN-OFF — ✅ SHIPPED (C198·3b f1, `30cf680`; migration `060`).** Open **LOW/MEDIUM** anomalies whose period gets **signed off** should **auto-resolve** — *"period attested over this note"* — instead of lingering into later months. A reviewer who attested the month has, by that act, judged its low-severity notes acceptable; carrying them forward is the alarm-fatigue class again (cf. the pattern-level duplicate-suppression item). HIGH anomalies are deliberately excluded — those should **block** sign-off, not be retired by it.
  - **(g) SPREADSHEET MIS-ROUTE (the C100 caveat, suspected live).** The universal drop path classifies **ALL spreadsheets as bank statements** (`docType = "bank_statement"` for any unrecognized spreadsheet), so a **payroll CSV dropped on Home** never reaches payroll intake. **Verify against the live path and fix**, with a `fileDetect` test pinning payroll-CSV → payroll intake. Pairs with (a): auto-run is only safe if the router is right about what it's auto-running.
  - **(h) FUTURE (note, NOT C198) — Reconcile collapses to an exceptions-only surface.** Once confidence calibration lands (Tier 1 #7) and the auto-run pipeline of (a) is carrying the clean path, **Reconcile stops being a standing tab** and becomes an **exceptions view the CPA visits only when the pipeline couldn't finish** — the same demotion C197 gave the client, applied to the reviewer once the machine earns it. *(Carried on ROADMAP §0 TIER 3.)*
  - **★★ DESIGN DECISION — THE MACHINE COMPLETES THE RECONCILIATION (Alex, 2026-08-06; shipped C198·2).** *Verbatim:* **"when the pipeline completes and the balance ties, the MACHINE completes the reconciliation. Rationale: lower tiers have reduced/no CPA-review cadence; the pipeline must function without a CPA click. Reconciliation = arithmetic (machine-verifiable). Sign-off = judgment (human, always). C194's gate applies unchanged: a reconciliation row may only be created complete when the balance verifiably ties — auto-complete converts a proven tie into a record, never manufactures one."** Implemented as `shouldAutoCompleteReconciliation` (all lines settled AND balance ties AND no completed reconciliation already covers the period) re-asserting `canCompleteReconciliation` immediately before the insert, writing through the EXISTING `completePipelineReconciliation` (C186) — no second writer, no new column: the **auto-vs-manual marker lives in the audit log** (`auto: true` + a plain-language sentence). **No tie → no row, ever** (mutation-tested).
  - **(g) VERIFIED — ALREADY FIXED (O55), NOW PINNED (C198·2).** The xlsx mis-route was **not reproducible**: `detectFileType` has sniffed binary workbooks through the xlsx lib since O55, and it was **verified live 2026-08-06 against real Gusto and bank workbooks**. What was missing was a **test holding it there** — every prior `fileDetect` test feeds TEXT, so the entire binary branch (`arrayBuffer` → `XLSX.read` → `sheet_to_json` → `detectFromText`) was unpinned. **Deliverable: `tests/xlsxRouting.test.js`** builds REAL .xlsx bytes in memory with the repo's own xlsx lib and asserts through the real async detector — payroll register (named AND neutrally-named), bank export, QBO export, junk sheet, corrupt bytes. One-line detector improvement went with it: garbage that xlsx TOLERATES (rather than throwing) now returns an explicit "no readable rows" reason instead of an unknown carrying no reason at all; routing is unchanged.
  - **(i) SECOND HALF CLOSED (C198·2) — the INTAKE row advances too.** C198·1 narrowed and fixed the `bank_statements` status advance; the **intake ledger** had the same defect — a statement whose every line was in the books stayed `held_for_review` forever, so the completeness net kept describing finished work as parked. Now the intake row advances **HELD → RECORDED with its journal-entry linkage** (generalizing C196(5)'s payroll path) exactly when all lines are settled, and stays HELD when any line is open. **No backfill — May's dangling rows stay as evidence.**
  - **(i) NARROWED — statement status only advances on the RECONCILE-COMPLETION path.** The **first-pass pipeline** path fails to advance `bank_statements` rows past `held_for_review`/`attention`; the **reconcile-completion** path DOES advance them (proved live: the **June row reached `complete`** while the **May rows still dangle**). So this is not "status is broken" — it is one missing transition on one path. **Fix scope: the first-pass path ONLY** (don't touch the completion path that demonstrably works).
  - **(j) ★★ HEADLINE BUG (June drive) — RE-UPLOAD OF A NON-COMPLETE STATEMENT SILENTLY NO-OPS.** Re-uploading a statement with an **identical content hash** onto a **non-complete** statement is swallowed by the C193 dedupe: it does **not** re-run the pipeline and does **not** re-evaluate the reconciliation. The stale **'attention'** status then **lies to Review** — the screen reports a problem the books no longer have. **Worse, the surface then demands work the system doesn't need:** the **Reconcile tab starts EMPTY** and asks for a **third physical upload plus a hand-typed ending balance**, when the system already holds the **parsed statement** (`bank_statements.stated_ending_balance`) **and every booked line**. **Required behavior:** (1) **once all statement lines are booked, reconciliation is OFFERED directly** — no re-upload, no re-parse; (2) **dedupe on a NON-COMPLETE statement RE-EVALUATES** (re-run pipeline / re-derive status) instead of no-oping. Dedupe must stay a no-op only where the statement is genuinely **finished**. *(This is the same "don't make the client re-do what we already did" class as (a), on the CPA side.)*
  - **(k) REVIEW'S STATEMENT-LEVEL EXCEPTION CARD LINKS NOWHERE USEFUL.** The card's **"Open in Bank Import"** renders an **empty screen** when the lines are already booked — the link's target is line-level, but the exception is **statement-level**. **Fix:** a target that carries the context — **Reconcile**, or a **statement detail view** — so the reviewer lands on the thing the card is actually about. *(Sibling of the §11 "Review-queue anomaly cards lack inspection affordances" item — same defect shape, different card.)*
  - **(l) RECONCILE MUST PRE-FILL THE ENDING BALANCE from `bank_statements.stated_ending_balance`.** The value is **already persisted** (C185), yet the completion bar still makes the CPA **hand-type a number the system holds**. **Fix:** prefill from the stored statement, **field stays editable** — it remains the CPA's **independent check**, not a rubber stamp. *(Promotes the §11 "Reconcile's Bank Ending Balance field should PREFILL" backlog item onto C198, and now sources it from the PERSISTED statement rather than a re-parse.)*
  - **(m) NOTE ONLY — DISMISSAL-WITH-REASON AS A LEARNING SIGNAL (spec belongs to the calibration session, Tier 1 #7; NOT C198).** When a reviewer dismisses a card with a user-confirmed reason ("recurring charge"), that context should **suppress future cards of the same shape** AND **seed a recurring-rule suggestion** — the dismissal is real human knowledge and today it evaporates. **Hard constraint: it must NEVER silently alter booking behavior** — a dismissal explains a flag, it does not re-code an entry; any booking change stays an explicit, visible action. *(Pairs with the pattern-level duplicate-suppression item and `learnFromCorrection`.)*

  **Also recorded from the June drive:**
  - **STALENESS DETECTOR USES `now()` INSTEAD OF PERIOD CONTEXT (→ C198, with the (d)/(f) anomaly work).** "Vendor hasn't charged you in **51 / 52 / 59 days**" rendered **on the same screen as that vendor's current-month charge** — the detector measures against **wall-clock**, not the period the books are in. This fires wrongly for **ANY books trailing wall-clock, i.e. every real client** (bookkeeping is always behind). **Fix:** measure staleness against the **period context** (latest booked/attested period), never `now()`.
  - **ANOMALY CARDS DOUBLE-EMITTED ON STATEMENT RE-UPLOAD (→ C198, same batch).** A re-upload took the queue from **5 → 10** cards: the C193 dedupe covers **lines**, not **anomaly emission**. **Fix:** anomaly emission must be idempotent per (fingerprint, period) across re-runs — the fingerprint machinery exists (C178), it just isn't consulted on the re-upload path.
  - **MULTI-COMPANY v1 JUNK CENSUS (quarantined; cleanup deferred → ROADMAP §0 TIER 3).** The **fixture company `3a704760` (Franklin Ave Pizza LLC) is FULLY CLEAN — zero pre-July entries**, so no O83–O86 result is contaminated. Surviving v1/demo junk lives in **`c787582f` (Test Corp ONE — 16 entries incl. personal purchases; the Dell asset + Bob's contact were retired 2026-08-06)**, **`24d5e576` (Northwind Studio — 21)**, **`3a4a6b3c` (Shadow Test Co — 12)**, **`66fa0cc2` (1)**. All of it is **quarantined by company scoping** (RLS + `company_id`), so it cannot leak into a real tenant; cleanup is **deferred, not forgotten** — pair it with the Tier 2 demo/showcase-company work so the demo entity is built clean rather than cleaned up.

- **★ O86·2 — THE C198·2 VERIFICATION DAY, COMPLETE (2026-08-08). C198·2 IS CLOSED.** Shipped across **`7c1f180` (C198·2)**, **`3a49906` (C198·2b)** and **`c2dcbbc` (C198·2c)**; **(a)(g)(i)(j)(k)(l) are done and LIVE-VERIFIED**, leaving **(b)(c)(d)(e)(f)** as the remaining C198 scope.
  - **(a) DROP-ZONE AUTO-RUN + AUTO-RECONCILE — LIVE-VERIFIED.** An owner-seat drop of the June statement ran the whole pipeline unattended and rendered **"All 21 transactions from your statement are in your books, and this month was already checked against your bank ✓"** — a **machine-verified** sentence: `alreadyReconciled` is derived from **real coverage** (`reconciliationCoversStatement`), never inferred from `ready` (which is false for two different reasons), so the clause cannot appear where the coverage fact isn't actually known. Single-bank-account companies auto-bind; 0 or 2+ still route to a human, now with a **durable** stash.
  - **(g) XLSX ROUTING — PINNED.** Not reproducible (the O55 sniff works); the gap was that every prior `fileDetect` test fed TEXT, so the binary branch was unheld. `tests/xlsxRouting.test.js` builds **real .xlsx bytes** and asserts through the real async detector, including a **neutrally-named** payroll register (content, not filename) and corrupt bytes.
  - **(i) BOTH HALVES CLOSED.** `bank_statements` advance on the first-pass path (C198·1) **and** the intake row advances **HELD → RECORDED** with its journal-entry linkage (C198·2). No backfill — May's rows stay as evidence.
  - **(j)(k)(l) — LIVE-VERIFIED.** Re-upload re-evaluates instead of no-oping; the statement-level card targets Reconcile (and renders nothing once resolved); the session opens with the ending balance already in it, editable.
  - **QUEUE-LINE HONESTY (C198·2b).** The tile said **"Done"** beside the **old stash sentence** over work already booked: the (a1) branch stamped `done` **before** awaiting `handleBankFile`, and nothing wrote the outcome back. Now: **processing → await → done-with-outcome**, a throw stamps **"Needs a look"** and never `done`, the stash sentence is **structurally unreachable** for a `to:"pipeline"` result, and an in-flight run says **"Adding these to your books…"**.
  - **★ C195(7) HAD SHIPPED INERT — REVIVED (`c2dcbbc`).** The duplicate auto-resolve **never fired once**: `reconcileIntake` built its dropped rows without `content_hash`, so `reconcileDroppedDocs`' `hashes` array was **always empty** and the whole block was unreachable. **A block that never runs looks identical to a block with nothing to do** — which is why it went unnoticed from C195 to now. Fixed by carrying `content_hash` + `document_id` through, and **live-verified clearing a 2-day-old orphan on the next Review load**, with plain-language detail. *(Lesson worth generalizing: a guard whose input is always empty is indistinguishable from a clean queue — prefer asserting the mechanism FIRES, not merely that nothing is outstanding.)*
  - **NOTE — THE UPLOAD QUEUE IS IN-MEMORY. DESIGNATED: ROADMAP §2 Bugs `O97`** *(was "pursue later, not scheduled", which anchors nothing — it carries live data-loss evidence and now carries a row).* `uploadQueue` + `fileStoreRef` are React state/ref, so a **reload or navigation between enqueue and processing wipes an arrival** before it is ever processed. **Live evidence:** the **2026-08-06 20:11:42** orphan — the operator hard-refreshed between drops, and the intake row sat at `received` while the same bytes were dropped again 23s later. Mitigations that now exist: the revived auto-resolve **self-heals the already-recorded-bytes case**, and a genuinely lost file still surfaces through the **completeness net after 30 minutes**. The real fix is a **durable queue** (persist the pending work, not just the arrival record) — **not scheduled**.
  - **CALIBRATION FOOTNOTE — auto-reconcile DECLINES months with outstanding items.** The auto path computes its difference with a **hardcoded zero outstanding**, so any month carrying an uncleared item fails to tie and correctly falls back to the **offered CPA session**. That is deliberately conservative (never assert a tie the arithmetic didn't prove). **Whether the outstanding chain should ever feed the AUTO path is a calibration-session question** — it is a judgment about how much the machine may assume, not a bug.
  - **COPY NIT (→ C198·3).** Bank Import's re-upload toast says **"21 transactions imported"** when all 21 were **deduped** — it should say we **already have** them. Same class as the queue-line fix above, one surface over.

- **★ O87 — JULY DRIVE, COMPLETE (2026-08-09; → C198·3c). MONTH SEVEN SIGNED OFF — THE MAGIC PATH'S FIRST LIVE FIRE.** ✅ **RESULT: July 2026 reconciled and signed off — `54,880.41 / 54,880.41 · difference 0 · complete · verified · outstanding_books []`, AUTO-COMPLETED by the pipeline on exception booking.** This is the **first live auto-reconcile**: the machine converted a proven tie into a record with no CPA click, exactly as the C198·2 design decision specified. **17 of 21 lines auto-booked** — including **Lone Star, which auto-booked for the first time after flapping UPWARD past the floor following two sub-floor months** (calibration evidence, see (iv)). **C198·3b's f1 made its debut: 16 notes closed as `attested`/`2026-07` at sign-off.** Findings:
  - **(i) ★★ HEADLINE — THE PAYROLL AUTO-POST GATE IS STRUCTURALLY INERT (shipped C198·3a, never able to fire).** `payrollHistoryFromLedger` selects on `import_metadata.kind === 'payroll'`; **no payroll entry has ever carried `import_metadata`**, so history is **always empty**, so **condition 5 (within-norms) fails for every company, forever**. Both July registers correctly fell to the confirm card — the gate **failed CLOSED**, which is the one mercy: an inert gate that defaults to "ask a human" is a missed feature, not a wrong posting.
    - **★ ROOT CAUSE IS ONE LAYER DEEPER THAN "the poster doesn't stamp it" — and it is NOT payroll-specific.** The poster stamps `meta` correctly and `persistMultiLineEntry` passes it as `p_meta`. **`post_journal_entry` (migration `010`) then cherry-picks SIX named scalar keys out of `p_meta`** (`ai_reasoning`, `ai_confidence`, `approval_status`, `payment_status`, `payment_method`, `due_date`) **into their own columns and never writes `import_metadata` at all.** Every other key — `kind`, `gross`, `net`, `withholdings`, `employer_taxes`, `period` — is silently discarded at the RPC boundary. Since the RPC is the **single canonical write path** (§7), **every entry posted through it has `import_metadata = NULL`**; the only writers are the payment path's follow-up `.update()` (App.jsx:6087) and the QBO direct insert. **⚠ This means the fix "stamp it on post" DOES NOT WORK if stamped into `p_meta`** — it must be either a follow-up `checkedRowUpdate` (the shape the payment path already uses, **no migration**) or a change to `post_journal_entry` (**a migration**, which would move `·3c` off "migrations unchanged"). Decide which before writing code.
    - **★ BLAST RADIUS beyond payroll — other `import_metadata.kind` readers are suspect on the same evidence.** `matchPayrollBankLine` (`payroll.js`) keys on `kind==='payroll'`, so **O72's payroll bank-line reconciliation is inert by the same mechanism** — a payroll net-pay bank line cannot match its register. `depreciation.js` reads `{kind:'depreciation', asset_id, period}` and needs the same check. **NOT affected:** `isSystemPostedEntry` (C196(4) noise exemption) also tests `source`, which the RPC *does* write, so it still works; `apBackfill`/`settlementLink` read the payment path's kinds, which are stamped by that path's own update. **Audit every `import_metadata` reader as part of ·3c** — one null column has been quietly disabling features for as long as the RPC has existed.
    - **★★ WHY THE TESTS PASSED — the C195(7) lesson, in test form.** `tests/payrollAutoPostGate.test.js` fed the gate **synthetic history shaped to match what the extractor reads**, so extractor and fixture agreed with each other while neither agreed with the database. There was **no ROUND-TRIP test** — post → read back → extract → gate — asserting that **the poster's actual output is readable by the consumer**. A gate that never fires and a gate with nothing to do are indistinguishable from a green suite. **Mandatory in ·3c: the round-trip test**, and treat "both sides of a contract tested against the same fixture" as a test smell wherever it appears.
    - **★ DECIDED (2026-08-09) — FIX VIA FOLLOW-UP CHECKED UPDATE, NOT AN RPC CHANGE.** Stamp `{kind, gross, pay_date, period}` with a **`checkedRowUpdate` issued after `post_journal_entry` returns** — the pattern the payment path already uses (**App.jsx:6087 precedent**), so **·3c stays MIGRATION-FREE** (ledger unchanged, through `061`). Plus **one-off backfill SQL for the 14 existing Gusto JEs** so the norm has history from day one, and the copy fix above.
    - **RPC CONSOLIDATION IS A SEPARATE, DELIBERATE ITEM — not a bug-fix rider.** Making `import_metadata` a first-class `post_journal_entry` parameter is the right end state (it removes the two-write window and the whole class of "the RPC silently dropped my key"), but it changes the single canonical write path for **every** caller, and that does not belong bolted onto a payroll fix. **Recorded as future work; NOT scheduled — but no longer unanchored: DESIGNATED ROADMAP §2 Bugs `O95`** ("not scheduled" is the opposite of a home; the 2026-08-11 sweep flagged it).
    - **★★ TESTING — THE UNIT TEST CANNOT REACH THE BUG, SO THE DRIVE SCRIPT MUST.** The round-trip test asserts the **client contract** (post → stamp update issued → extractor reads it back). That is necessary and still **insufficient**: it runs on the client side of a boundary this bug lived on the far side of. **So the next drive's script gains a LIVE BOUNDARY CHECK — after the first August register posts, SQL-verify `import_metadata IS NOT NULL` on the new JE. DESIGNATED: ROADMAP §0 TIER 1 #12.** Unit tests cannot cross the DB boundary, and that is *precisely* how this escaped a green suite for a whole release.
  - **(ii) STATEMENT PERIOD DERIVED FROM TRANSACTION SPAN, NOT THE STATED PERIOD.** July parsed as **07-01 → 07-27** (last txn) against a **stated period end of 07-31**. Harmless in July because the tie still proved, but the span is an *inference* where the statement carries the *fact*. **Fix: prefer the STATED period; fall back to the txn span only when the statement doesn't carry one.**
  - **(iii) "TRANSACTIONS MATCHED: 0" ON A COMPLETE 21-TRANSACTION AUTO-PATH MONTH.** Literally true — the auto path books rather than "matches", so the matcher's counter is zero — and it **reads as failure** on a month that went perfectly. Same true-but-reads-false class as the C198·2b queue line and the ·3b re-upload toast. **Copy fix**; the count is not wrong, the label is.
  - **(v) THE f1 ATTESTATION SWEEP CONSERVATIVELY SKIPPED 3 JULY DUPLICATE CARDS.** `anomalySubjectPeriod` is **blind to f3's new date-pair fingerprints** (`dup:<vendor>:<cents>:<dateA>+<dateB>` — its regex looks for a trailing `:YYYY-MM`), **and** detection-time `invoice_ids` **don't resolve after a reload**, so neither the ref path nor the fingerprint path could place those three. They were **skipped, not mis-attested** — the sweep failed safe. **Fix: parse full dates out of the fingerprint as a second fallback.** *(Note the shape: ·3b(f3) re-keyed fingerprints and ·3b(f1) consumed them, in the same commit, and the two halves disagreed about the format. Same-commit coupling is exactly where a contract goes unchecked.)*
  - **(iv) CALIBRATION EVIDENCE ONLY (→ the calibration session, NOT ·3c).** **Bluebonnet excepted for the SEVENTH straight month at ~78 confidence with the correct mapping already pre-selected** — the system knows the answer and asks anyway, seven times. **Lone Star: four months, four different verdicts.** Both are specimens for Tier 1 #7, not code changes here.
  - **Also recorded:** the **f3 resurrection wall was observed live — ~18 cards re-opened under their new content keys, one-time and exactly as designed** (the ·3b commit predicted this; the queue count stayed right because the same reconcile pass resolved the old rows). **Post-sign-off anomaly counts: 16 attested · 35 auto-resolved · 21 dismissed · 3 open** — the 3 open are the (v) trio; **leave them**, the fixed sweep or August's sign-off will take them.
  - **★ NEW REQUIREMENT FROM RUBRIC Q5 — AN INVOICE-VOLUME DRIVE (→ ROADMAP §0 TIER 1 #12).** *"Close, but I want to see a lot more invoices go through first."* The fixture program to date is **statement-centric** — seven drives, seven bank statements — but **Act 1 for a real operator is invoice- and receipt-heavy**. The engine is verified strong on a path that is not the path a new client walks in on. **Deliverable: a realistic month of vendor invoices, receipts and bills through the UNIVERSAL DROP at volume**, exercising extraction, categorization, the AP flow and the document library. **GATE: before the demo-readiness conversation concludes.** *(Note what Q4's caveat implies alongside this: "No, but July was deliberately simple" means the clean-path result is real but under-tested — volume and mess are the same missing axis.)*
  - **Migrations ledger: UNCHANGED — applied & verified through `061`** (see §6). *(Caveat: if ·3c's (i) fix goes the `post_journal_entry` route, that adds one — decide first.)*
  - **★★ Operator rubric:** **1 — No · 2 — YES** — *"first payroll we've recorded for this company" was untrue (12 priors); this is finding (i)'s user-facing face and the **first rubric-Q2 yes in seven drives**, root-caused same night* **· 3 — No · 4 — No**, with the caveat that **July was deliberately simple** **· 5 — "Close, but I want to see a lot more invoices go through first."**
    - **★ WHY THE Q2 YES MATTERS MORE THAN THE INERT GATE.** Seven drives without one, and it did not come from a wrong number — the ledger was perfect and the month auto-reconciled. It came from the system **asserting a fact about the world that was false**. That is a worse failure than a missed feature, and it is the one the rubric is built to catch.
    - **★★ THE DURABLE LESSON — absence of evidence was rendered as evidence of absence.** The gate found an empty history and reported *"This is the first payroll we've recorded for this company."* The true statement was *"we couldn't find any prior payroll"* — which is a statement about the QUERY, not about the company. **Fixing the extractor does NOT fix this**: any future extraction failure re-tells the same lie with the same confidence. **·3c must change the copy too** — an empty result set may only ever be reported as an empty result set. *(Generalize it: every "this is the first / there are none / nothing found" string in the product is a claim about the world derived from a query returning zero rows, and each one is this bug waiting for its own inert path. **The CLASS is DESIGNATED: ROADMAP §2 Bugs `O98`** — payroll's instance shipped in ·3c, the rest were listed as findings, and a list is not a home.)*
- **★ O88 — THE CALIBRATION SPEC EXISTS (2026-08-16). `docs/CALIBRATION_SPEC_O88.md`, tracked in git as of this commit. DESIGNATED: ROADMAP §2 `O102`.** Four places in this section have pointed at "the calibration session" as the home for work — **(iv)** Bluebonnet's seven identical exceptions and Lone Star's four-verdict flap · **(m)** dismissal-with-reason as a learning signal · **(h)** Reconcile collapsing to exceptions-only · the **outstanding-chain footnote** — and until now that home was a phrase, not an artifact. It is now a document with a sign-off line.
  - **★ STATUS: SIGNED 2026-08-16 by Alex (CPA) — this IS the Tier 1 commit spec.** Line 92 reads `- [x]`, verified on disk before it was recorded here; the diff against the tracked copy was **exactly one character**, so nothing else in the document moved. Its own line 94 makes the signature the trigger, and the signature is now given.
    - **RECORDED AS ITS OWN COMMIT, NOT AMENDED INTO THE TRACKING COMMIT.** The unsigned-then-signed sequence is TRUE, and it is the more useful record: `513db72` correctly describes its own moment — a spec tracked in git while awaiting signature — and amending would rewrite that into a spec that was always signed, erasing an attestation as a dated event with an actor. This product keeps `period_signoffs` rows for exactly that reason; the spec's signature earns the same treatment.
    - **ONE FALSE START, WORTH KEEPING.** The signature was reported as given before it had reached disk (an unsaved editor buffer — `git diff` empty, mtime unchanged since before the tracking commit). Recording it then would have made the history assert an attestation the artifact contradicted, and the only way to reconcile them would have been for the assistant to tick the box. It was not ticked. **This spec supersedes the booking authority of seven verified drives; it does not start on an assumption, including a friendly one.**
  - **WHAT IT SUPERSEDES.** Descriptor-legibility confidence as a booking authority — the exact defect **(iv)** and the O85 specimen at line 255 recorded: a **never-seen** vendor auto-booked **$850 at 88** on name plausibility while a **twice-taught** vendor scored **78 and excepted**. Confidence survives as a display value only.
  - **NUMBERING — `O88` IS A DRIVE NAME, NOT AN ID.** §11 drives run O83–O87 and ROADMAP §2 ids run O4–O103 **in the same visual space**, and they already collide (`O87` is simultaneously the July drive and the `fiscalYearStart` UTC bug). The calibration work is tracked as **`O102`**; "O88" names the session and the spec file. **Do not read an `O##` in §11 as a ROADMAP row without checking.** *(Flagged the same week the `O90`/`O91` collision was cleaned up — the id-space check in §6 catches minting collisions, not this one.)*
  - **FIRST FINDING, BEFORE ANY CODE — THERE IS NO SUSPENSE ACCOUNT.** Rule 2 books every STRANGER line to **Uncategorized Expense**, and the chart has exactly one fallback: `7100 Miscellaneous Expense`. It **cannot be borrowed** — Tier 1 #7's acceptance test makes "Miscellaneous fallback on a recognizable vendor" a **hard fail**, so reusing it would delete the test's meaning. New account `7150` + `system_role 'uncategorized_expense'`, migration `063`.
  - **SEQUENCING DECISION — SHADOW MODE BEFORE THE SWITCH.** The ladder gets built and computes its verdict **alongside** confidence, recording what it *would* have booked, for at least one full drive before it books anything. Reason, stated once: **C198·3a shipped a gate that could never fire and a green suite could not tell.** A calibration rebuild that changes booking authority on seven months of verified behaviour, without first diffing itself against that behaviour on real statements, is the same bet at ten times the stake.
- **★★ O87 → C198·3c — SHIPPED, MERGED & LIVE (2026-08-11). All five findings closed; the batch grew two review-driven fixes, a corrected backfill and a workflow rule.** Merged to `main`, HEAD **`1f82dd9`** — three commits: **`d9b8638`** (the batch) · **`390ffae`** (D1+D3) · **`1f82dd9`** (the review bounce). **1,810 tests / 98 files green · `npm run build` clean · MIGRATIONS UNCHANGED, still applied & verified through `061`** — the follow-up-checked-update route held exactly as decided on 2026-08-09, so no RPC change and no new migration.
  - **(i) THE GATE CAN FIRE.** `payrollImportMetadata()` (pure) is stamped onto the posted entry by a `checkedRowUpdate` **after** `post_journal_entry` returns and **before** `loadAllData`, so the reload carries it. A failed stamp is audited **and** said out loud in the outcome line — the entry is correct either way; only the norm history is lost, and the next register then honestly reports that it *couldn't find* a prior run. **`depreciation.js` was genuinely inert on the identical mechanism and got the identical stamp** (D3 also made its schedule-flag write checked, §9) — its "the GL is the real guard" comment had been describing an intention, not the code, since the RPC has always dropped the key.
  - **★ THE BLAST-RADIUS CLAIM WAS HALF RIGHT, AND THE HALF THAT WAS WRONG MATTERED.** `matchPayrollBankLine` **is** structurally inert — but it is **not** what handled the June/July Gusto lines. `markAlreadyBooked` (date|amount|direction) dedup-matched them against the register's **Cr Cash leg**, and `planStatementPipeline` partitions `already_booked` out **first**, so those lines never reached `planPayrollBankLines`. Nothing needed fixing there. *(And "auto-matched at conf 100" was almost certainly the deterministic A/P clear at **99** — no path in the repo assigns a bank line a literal 100.)* **The real hazard was the opposite of the reported one:** stamping `kind:'payroll'` WAKES every reader of that key, and `flattenJournalEntries` copies one entry's `import_metadata` onto **every leg**, so the matcher's old `?? i.amount` fallback would have offered it four magnitudes to match — and a match **SUPPRESSES** the line. A Gusto tax-remittance ACH would have been swallowed as "already booked". Hence `net` in the stamp (a deliberate deviation from the decided shape) **and** the matcher now refusing that fallback outright. **Un-inerting a reader is only safe if you also give it the field it was written to read.**
  - **★★ THE REVIEW BOUNCE — the lesson of the batch.** The report claimed the backfill shape *"provably cannot start suppressing bank lines — pinned by a test."* Review restored the `?? i.amount` fallback, ran **`tests/payroll.test.js`** (the matcher suite), and watched it stay **green**. Both sides were slightly wrong and the correction is the point: **the pin did exist and did fail under mutation — but it lived in `payrollGateRoundTrip.test.js`, where nobody trying to falsify the matcher would ever run it.** Every `registerRun()` fixture in the matcher suite carries `net`, so the refusal branch was unreachable from all of them. Fixed by pinning it **where the matcher is tested**, with the real leg magnitudes (gross 4,000 · net 3,150 · employer 306 → legs 4,000 / 306 / 3,150 / 1,156, cross-checked against the REAL `buildPayrollEntry` so the fixture can't drift), and the mutation was **run, not asserted**: fallback restored → four assertions red → source restored → green. The guard comment now **names its own falsification test** and records that check.
    - **★★ LESSON — PROOFS LIVE WHERE A FALSIFIER WOULD LOOK FOR THEM.** A guard's test is not filed by which commit created it; it is filed by **where someone doubting the guard will go**. A proof in the wrong file is indistinguishable from no proof at all — it does not fail when the thing it protects is broken *by anyone who isn't already holding the map*. Sibling to the ·3a lesson (both sides of a contract tested against the same fixture): that one was a test that couldn't fail, this one is a test that couldn't be **found**. Both read as coverage.
  - **(ii) STATED PERIOD — SHIPPED, DEPLOYED, NOT YET PROVEN.** `statementPeriod` prefers the stated period **per side**, span as fallback; `period_end` added to both bank-parse profiles (it was never asked for, which is exactly why the side July got wrong was the side nobody read). Hardened past the original ask after review: well-shaped non-dates (`2026-13-45`) and **inverted** pairs fall back to the span (the span gave `min ≤ max` for free; one stated side can break it, and an inverted period silently kills `reconciliationCoversPeriod`), and the persist path now uses the validated start — `deriveStatementOpening` took it verbatim into a `date` column, where a rejected insert took statement **and** line persistence down behind a `console.warn`.
  - **(iii)** The completed-reconciliation detail no longer scores a match run that never happened; session-matched months keep their real count, and the auto-path number is **counted** off the books for that record's **own** bank account (no all-cash-codes fallback — that would have summed across accounts and double-counted cash-to-cash transfers on the one card whose job is to be exact) or renders an em dash.
  - **(v) + (D1)** `anomalySubjectPeriod` reads full dates out of f3 fingerprint tails, deliberately **not** gated on "no refs" — the live trio *had* refs that no longer resolved, which is why the old fallback was never reached. **D1 then carried the same fix to `anomalyTouchesPeriod`**, whose only consumer is the HIGH-blocks-sign-off gate: `duplicate_payment` is severity HIGH with a date-pair key, so the same dead-refs condition made the blocker silently **under-count**. The sweep failing leaves a note open; the blocker failing signs a month off over an unresolved HIGH. One fingerprint parser now feeds both, with a test pinning that they agree — two independent parsers of the same key would have been the (v) bug with the sides swapped. `rapid:` is excluded from SUBJECT and included in TOUCHES, and the asymmetry is deliberate: its key holds the window *start*, so retiring on it closes a note early (unsafe) while blocking on it blocks a month that genuinely holds the window's first charge (safe).
- **★ O108 — MIGRATION CHAIN vs LIVE: DIAGNOSED 2026-08-17, DOWNGRADED P1 → P2, AND ONE CLAIM RETRACTED. ▶ HOLD; no fix proposed.**
  - **★★ RETRACTION — CLAIM #2 WAS FALSE, AND THE CAUSE MATTERS MORE THAN THE CLAIM.** I recorded that `src/lib/constants.js` and the live chart were different COA generations — `6300` Marketing vs Repairs, `cogs` vs `cost_of_goods_sold`, `rent_occupancy` vs `rent`, Common Stock vs Owner Equity. **It is not true. `constants.js` matches live on every one of them** (`:28` `common_stock` · `:34` `product_revenue` · `:40` `cogs` · `:48` `rent_occupancy` · `:51` Repairs `6250` · `:52` Marketing `6300`).
    - **CAUSE:** I diffed **my own broken draft of `063`** against `044`, then reported the `063` side as "the client". That draft was built from `009` plus reconstruction — so what I actually measured was **`009` vs `044`**, and I labelled it `constants.js` vs live. A false attribution stacked on the original copy error, and published as **P1**.
    - **KEPT, NOT DELETED, ON PURPOSE.** A ledger that quietly loses its own false claims cannot be trusted about its true ones. The retraction is the record: this finding was wrong for two days, it drove a diagnosis session, and the same discipline that caught the bad migration — *diff it, do not trust the file* — is what caught the bad finding, one layer up.
    - **★★ AND THE CONVERSE RULE, RECORDED 2026-08-26 AFTER IT WAS BROKEN: A RETRACTION MUST BE SCOPED AS PRECISELY AS THE CLAIM IT WITHDRAWS.** An over-broad retraction **reads as diligence while deleting a correct finding** — it wears the costume of rigour and does the opposite. **Cost already paid, on `O113`:** I claimed both rate-limit walls sit at 20 files, then retracted it on discovering that the upload bucket had not bound in the observed run. But *"upload did not bind in that run"* and *"upload would not bind if `AI_LIMIT` were raised"* are different statements, and I used the first to withdraw the second. **The original claim was a counterfactual and was TRUE; it survived only because the operator restated it independently.** Before withdrawing a claim, state what it actually asserted — observed fact, mechanism, or counterfactual — and withdraw only that. A claim killed by evidence about a different claim is a claim lost to carelessness, and the ledger is no more trustworthy for it than one padded with false ones.
  - **★★ ORIGIN ESTABLISHED 2026-08-17 — `6520`/`6530` CAME FROM A HUMAN CORRECTION, NOT MACHINE INVENTION.** The live evidence: Jan 28 Toast booked at conf **85** with reasoning arguing for **6500 Technology & Software**, and Jan 31 bank fee at conf **70**, hedging **8000 vs 7100**, flagged *needs review*. Both now sit in accounts (`6520`, `6530`) their own stored reasoning contradicts. **`persistRecode` explains it exactly**: it UPDATES `journal_entry_lines.account_id` in place (App.jsx ~1290) and creates the target account first if absent (~1263) — so the line MOVES while the entry keeps its original `ai_reasoning`. The stale reasoning is the signature of an in-place recode. Then `learnFromCorrection` stored the mapping, and from **Feb 25 onward the AI proposes `6520` directly at conf 99** — the learned-mapping stamp. **The system worked as designed at every step; the defect is that creating two permanent accounts on a client's chart left no trace anywhere.**
    - **ONE CORRECTION TO THAT READING.** The deleted lines at `6500` (0 live / 1 deleted) and `7100` (0 live / 2 deleted) are **NOT** the recoded originals. An in-place recode moves the line and leaves the source account with **zero** lines, not a deleted one — so those three deleted lines are separate voided entries, unrelated to the correction story. Worth stating because the coincidence is seductive: `7100` having zero live lines is genuinely good news (Tier 1 #7's Miscellaneous hard-fail has no live instances) and it is not evidence for the recode narrative.
    - **DECIDED 2026-08-17 — `6520`/`6530` ARE BLESSED AS CANONICAL**, roles `merchant_processing_fees` and `bank_service_charges`, into both the seed and `constants.js` (→ `068`). They describe real, recurring costs for a restaurant client and were chosen by a CPA; the defect was never the accounts, it was that nothing recorded their creation.
- **★★★ O88 — THE AUGUST DRIVE, ACTS 1–4 (2026-08-26). DRIVE STOPPED AT ACT 4. Three debts settled, four findings, two P0. Findings recorded; NO FIXES PROPOSED at the operator's instruction.**
  - **✅ THE O87 DEBT IS SETTLED — the payroll gate fired, and its stamp landed.** Both registers auto-posted with `import_metadata` present: `kind` / `gross` / `net` / `pay_date` / `register_import_id`. **This is the check unit tests structurally could not make**, and it is exactly how ·3a shipped inert for a release. The gate that had never once been able to fire, fired twice.
  - **✅ FIX (ii) VERIFIED LIVE.** `period_end = 2026-08-31` against a last transaction of `08-28`. Deployed 2026-08-17 and unverifiable until a month with a gap in it; a silent model failure would have looked identical to the old behaviour, and did not occur.
  - **✅ RE-UPLOAD SUPERSEDE HELD.** Statement 1 `superseded` with 17 booked; statement 2 active with 19 `already_booked`, 5 excepted; **anomalies emitted once, not twice.** Cash ties exactly: `54,880.41 + 23,482.65 − 11,555.90 = 66,807.16`.
  - **✅ THE ABSORBER HELD.** `account_materialized` = **0 rows** across 15 processed invoices, including vendors with no matching account. The O108 instrumentation ran on real volume and stayed silent because there was nothing to say.
  - **★★★ AND THE FINDING THAT MATTERS MOST — SPECIMEN 3 REPRODUCED, EIGHT MONTHS LATER, AT THE SAME CONFIDENCE.** `ALAMO FIRE & SAFETY LLC` — **never seen, no history, no census, no directory entry** — **auto-booked $425 to 6250 Repairs & Maintenance at confidence 88**, reasoning *"fire suppression inspections… facility compliance/maintenance costs."* No human was asked. **That is Culinary Edge again**: same shape, same score, same silent booking, eight months after the incident that founded the familiarity axis. Meanwhile **Bluebonnet — attested eight months running — excepted at ~78 for the eighth time.** **THE SYSTEM TRUSTS A STRANGER MORE THAN A VENDOR IT HAS BEEN TAUGHT EIGHT TIMES, AND THE TWO NUMBERS ARE 88 AND 78.** Recorded as **the C203 justification**: this is not an argument that the ladder would be nicer, it is a demonstration that the current authority is inverted, on live data, twice, with the numbers written down.
  - **★ P0-1 — THE RATE LIMIT BLOCKS ANY PAYING CLIENT. DESIGNATED `O113`.** 21 of 36 invoices died at HTTP 429. **The throttle is OURS** (`AI_LIMIT = 60`/user/hour, `ai-proxy/index.ts:17`, via `bump_rate_limit`, migration `021`), not upstream. **Three calls per file confirmed** — `classify-document` → `extract-invoices-batch` → `code-invoices-batch` — and **the "batch" profiles batch WITHIN a document, not ACROSS files**, while the queue processes one file at a time. The observed break at ~15 files implies a **fourth call the structural read does not account for; not pinned, and recorded as unpinned rather than guessed at.** A real restaurant is 40–80 invoices/month and onboarding wants 2–3 months, so **a new client's first action exceeds the limit by 3–10×.**
  - **★ P0-2 — THE DUPLICATE DETECTOR IS DETECTING THE INVOICE-TO-PAYMENT LIFECYCLE. DESIGNATED `O114`.** `findDuplicate` keys on normalised vendor + amount within 1% in a date window — and an invoice and the bank line paying it are **by construction** the same vendor and amount days apart. **The detector is right that they are one economic event and wrong about which kind.** The bank line already booked the expense; the invoice books it again, so **only the card stands between this and a double-counted P&L — and the natural answer, "New charge — book it", is the one that corrupts the books.** Wrong-by-default on the most common shape in small-business accounting. Bluebonnet ×4 flagged against last month's identical $145 delivery: same-vendor-same-amount is the NORMAL case for a weekly fixed-fee vendor. **P2 is downstream of this:** the deliberate Hill Country ±18 transposition was never tested because **there is no invoice-to-payment matching to mismatch on.**
  - **★ P1-1 — THE SUMMARY CONTRADICTS THE BOOKED ACCOUNT. DESIGNATED `O115`.** Queue says Lone Star *"as a client meal"*; ledger says 5000 COGS. `plainCategoryPhrase` (`clarify.js:165`) **re-derives a category from the entry's text** rather than reading the account already on the row — an independent guess about an answer sitting three feet away. Q2 world-claim, in the owner's seat.
  - **★ P1-2 — `Alamo Ice & Beverage` (CO2 tanks, bagged ice) booked to `7100` MISCELLANEOUS.** ROADMAP §0 TIER 1 #7's acceptance test names this exactly: *"Miscellaneous fallback on a recognizable vendor is a hard fail."* **First live instance.** Folded into #7 rather than given its own id.
  - **MAPPINGS THAT WERE RIGHT: 4 of 5** (5000 COGS ×3, 6250 ×1). Recorded because a finding list is not a verdict, and the categoriser is not uniformly wrong — it is wrong in a specific, structural way that `O102` names.
- **★★★ O88 — THE AUGUST DRIVE, ACT 7: THE PAYROLL GATE DISCRIMINATES (2026-08-26). 4 FOR 4, AND EACH REFUSAL NAMED ITS OWN CONDITION.**
  - **★★ TWO PASSES AND TWO REFUSALS ON *DIFFERENT* CONDITIONS — the first evidence the gate DISCRIMINATES rather than merely GATES.** An inert gate and a working gate both look like "nothing bad posted"; that is exactly how ·3a shipped. What separates them is a refusal that names a condition **and a different refusal that names a different one.**
    - **Register 04 (footing bait) REFUSED on condition 2** — verbatim: *"The register doesn't foot: gross $4,000.00 less withholdings $850.00 is $3,150.00, but it states net pay of $3,200.00."*
    - **Register 05 (out of norms) REFUSED on condition 5** — verbatim: *"Gross pay of $12,000.00 is well outside this company's usual $4,000.00 a run."*
    - **AND THE ATTRIBUTION IS PINNED, NOT ASSUMED:** 08/31 against a period end of 08/28 sits **inside the 7-day grace by design**, so the second refusal could only have been about norms. The condition was identified by elimination on the live artifact, not read off the copy.
    - **POST-CHECK:** `source='payroll'` and `entry_date >= 2026-08-01` returns **exactly two rows**, 08/14 and 08/28, both gross 4,000. **Nothing bad posted.**
  - **★★ THE COPY EXEMPLAR — BOTH CARDS ARE PURE QUERY-CLAIMS, IN THE HIGHEST-STAKES COPY IN THE PRODUCT.** They state the arithmetic and what the document says, and **draw no conclusion about why**. No *"may be fraudulent"*, no *"appears incorrect"*, no theory of the operator's intent. **RECORDED AS THE REFERENCE STANDARD for `O115`'s fix and for flag copy generally** (Q9 doctrine, `O98`): the machine reports what it computed and what it read, and the human supplies the meaning. Quote these two strings when writing any new flag.
  - **★★★ AND THE ONE WORTH THE MOST — THE WITHHELD ENTRY BALANCES AND IS STILL WRONG. THIS IS WHY CONDITION 2 EXISTS.** Register 04's refused entry **totals 4,306 on both sides**: it plugged Payroll Taxes Payable to **1,106** to absorb the 50 the register does not foot. **A DOUBLE-ENTRY SYSTEM WILL HAPPILY BALANCE A HALLUCINATED NUMBER.** Dr = Cr is a check on the ENTRY BUILDER, not on the DOCUMENT — the plug line is exactly where a balancing system hides a source error, and `tests/gaapInvariants.test.js` asserting (a) Dr = Cr would have passed on this entry all day. **FOOTING MUST BE CHECKED AGAINST THE DOCUMENT, SEPARATELY FROM BALANCING AGAINST ITSELF**, and no amount of double-entry rigour substitutes. Generalise it: **any entry with a plug line can absorb a bad input and look perfect.**
- **★★ O113 — THE FOURTH AI CALL DOES NOT EXIST. DIAGNOSIS CLOSED ON LIVE COUNTERS, 2026-08-26. A PRIOR CLAIM OF MINE IS RETRACTED.**
  - **THE QUERY (operator-authorised, read-only, Management API):** `rate_limit` for the drive user. **Drive hour `2026-08-25 23:00Z`: `ai` = 81 against a limit of 60 · `upload` = 18 against a limit of 20.** (Also `2026-08-26 04:00Z` ai=2, `2026-08-25 03:00Z` ai=9.)
  - **★ THE AI BUCKET BLEW; THE UPLOAD BUCKET NEVER TRIPPED.** 81 > 60, and 18 < 20. So the throttle that stopped the drive is unambiguously the **60/hr AI limit**, and the upload limit was never in play.
  - **★★★ THE RETRACTION WAS ITSELF WRONG, AND IS HEREBY WITHDRAWN. "BOTH WALLS SIT AT 20 FILES" IS TRUE.** On 2026-08-26 I retracted my own claim that both limits bind at 20 files and that raising `AI_LIMIT` alone would move the wall to the upload bucket. **The operator restated the claim independently the same day, and they are right.** Per file the pipeline spends **3 AI bumps and 1 upload bump**, so `60 ÷ 3 = 20` and `20 ÷ 1 = 20` — **the two limits are coincidentally the SAME constraint for the invoice path**, and raising one alone relocates the wall without moving it.
    - **WHAT WAS ACTUALLY FALSE was narrower than what I retracted.** The true statement is about the OBSERVED RUN: because the proxy bumps `ai` and returns 429 **before** reaching the upload bump (`ai-proxy/index.ts:41-49`), a call rejected on the AI check never touches the upload counter — so in the 81/18 event the upload bucket **was not in play**, and it could not have been. That is why it read 18 and not 20. **But "upload did not bind in that run" does NOT imply "upload would not bind if AI were raised", and I used the first to invalidate the second.** The original claim was a COUNTERFACTUAL and it survives: lift `AI_LIMIT` and the 21st file's classify call reaches the upload bump and is refused there.
    - **★ THE LESSON, AND IT CUTS BOTH WAYS.** §11 already records that *a ledger that quietly loses its own false claims cannot be trusted about its true ones*. **The same discipline forbids losing a TRUE one** — and an over-broad retraction is exactly that: it reads as diligence while deleting a correct finding. **A retraction must be scoped as precisely as the claim it withdraws.** Mine was not, and the operator's own reading is what caught it. The live counters corroborate the surviving claim rather than refuting it: **`upload` at 18 was TWO FILES from its own wall, independently.**

  - **★ THE ARITHMETIC CLOSES COMPLETELY — every number fits, and prior spend is now BOUNDED, not guessed.** With P prior AI calls in the hour, the classify call for file *n* is AI call `P + 3(n−1) + 1`, and the upload counter is bumped only when that classify PASSES. `upload = 18` therefore requires `18 ≤ (62 − P)/3 < 19`, i.e. **P ∈ {6, 7, 8}** — which lands exactly on the independently-derived floor of "at least 6 non-invoice calls" below. Acts 1–3 ran in the same clock hour and `handleBankFile` costs **2 calls** per statement. **Nothing is left unexplained, and no fourth call exists.**
  - **★ NO FOURTH CALL. THREE PER INVOICE FILE, PROVABLY** — `classify-document` (`App.jsx:3406`, the only upload-tagged site along with the spreadsheet path at `:4230`), `extract-invoices-batch` (`:3901`), `code-invoices-batch` (`:3950`). **The deficit was never a missing call; it was a SHARED BUCKET.**
  - **★★ AND THE COUNTERS PROVE THE SHARING ARITHMETICALLY, not by inference.** `upload` = 18 means **18 classify calls passed the AI check**. Those 18 files can account for at most 18 + (18 × 2) = **54** of the 60 successful AI calls. **So AT LEAST 6 of the 60 were NOT invoice-pipeline calls** — and that is a floor, since any file that died mid-pipeline consumed fewer than 3. Acts 1–3 ran in the same clock hour on the same user: `handleBankFile` is **2 calls** (parse + categorize) per statement, and the drive put through two statements plus payroll. **6 is exactly that shape.** The 60/hr limit is therefore not "20 invoices" — it is **"20 invoices minus whatever else you did this hour"**, which is not a number any user can predict.
  - **★ A REJECTED CALL STILL CONSUMES BUDGET.** `bump_rate_limit` (`021:36-41`) increments and returns the new count; the proxy checks `> AI_LIMIT` **after** bumping. The live evidence is the number itself: **81 means 21 calls were charged for doing nothing**, and the operator reported **21 invoices failed**. **Retrying digs the hole deeper**, and nothing in the UI says so.
  - **THE WINDOW IS A FIXED CLOCK HOUR** (`date_trunc('hour', now())`), not rolling — so the wall's duration is arbitrary: hit it at :05 and you wait 55 minutes; at :55, five. The 04:00 bucket showing `ai`=2 is the reset, visible in the data.
  - **★★ `O113` IS THREE SEPARATE DEFECTS, NOT ONE NUMBER (operator, 2026-08-26). Split so each can be fixed and closed on its own.**
    1. **`O113a` — REJECTED CALLS CONSUME BUDGET. A CORRECTNESS BUG IN THE LIMITER ITSELF, INDEPENDENT OF EVERY LIMIT VALUE. FIX FIRST.** `bump_rate_limit` increments and the proxy checks after, so a refusal is charged for. **`ai = 81` against a ceiling of 60 IS THAT BUG, VISIBLE IN PRODUCTION DATA** — 21 calls billed for doing nothing, against 21 invoices that failed. **It makes retrying ACTIVELY HARMFUL**, and nothing in the UI says so, so the user's most natural response to a 429 is the one that extends it.
    1b. **★★ AND THE DEEPER DEFECT IS UNPREDICTABILITY, NOT THE NUMBER (operator, 2026-08-26).** The AI limit is not *"20 invoices"* — it is **"20 invoices minus whatever else you did this hour"**, and **no user can compute that.** The same action succeeds or fails depending on invisible prior spend from a different feature. **Fixing the number does not fix this; only making the budget REASONABLE-ABOUT does** — either per-operation accounting, so a document costs a knowable amount, or a **visible remaining count**, so the ceiling is something a person can steer by. **No number proposed**; the shape is the requirement.
    2. **`O113b` — BOTH WALLS SIT AT 20 FILES.** 3 AI bumps and 1 upload bump per file → `60/3 = 20` and `20/1 = 20`. **Any limit change must move BOTH or NEITHER**; moving one alone relocates the wall to the identical file count and changes nothing a user could observe.
    3. **`O113c` — THE WINDOW IS A FIXED CLOCK HOUR, NOT ROLLING** (`date_trunc('hour', now())`). **The penalty for hitting the wall is therefore arbitrary: 55 minutes at :05, five minutes at :55** — the same mistake costs an order of magnitude more or less depending only on when it happened. The 04:00 bucket reading `ai = 2` is that reset, visible in the data.
  - **▶ HOLD ON `AI_LIMIT` — DO NOT TOUCH IT until `O113a` is fixed and the per-invoice call count is settled (operator, 2026-08-26).**

- **★ O113 PART (b) — THE PER-INVOICE CALL COUNT. PROPOSAL ONLY, NOTHING BUILT (2026-08-26).**
  - **★★ THE FINDING THAT REFRAMES IT: THE TWO BUCKETS CONSTRAIN DIFFERENT AXES, AND ONLY ONE OF THEM IS ABOUT CALLS.** The **AI** bucket scales with **calls per file** — consolidation moves it. The **UPLOAD** bucket scales with **FILES**, at 1 bump per file no matter how few calls each takes — **consolidation cannot move it at all.** So *"reduce the call count"* raises the ceiling only until it hits 20 files and stops. **A real restaurant is 40–80 invoices a month and onboarding wants 2–3 months, i.e. 120–240 files — so at `UPLOAD_LIMIT = 20/hr` a new client's onboarding takes SIX TO TWELVE HOURS of wall-clock waiting no matter how efficient the pipeline becomes.** The upload limit, not the AI limit, is the onboarding ceiling.
  - **★ CALLS 1 AND 2 SEND THE SAME DOCUMENT; CALL 3 SENDS NO DOCUMENT AT ALL.** `classify-document` (`max_tokens: 20`, one word) and `extract-invoices-batch` (`max_tokens: 4000`) are handed **the same bytes**, and **classification is a strict subset of what extraction must already determine** — a model that reads an invoice necessarily knows it is one. `code-invoices-batch` is different in kind: it takes **text plus the company's chart**, not the document.
    - **PROPOSAL 1 — MERGE 1 INTO 2.** One profile returning `{ doc_type, invoices: [...] }`; a non-invoice doc_type returns an empty array and routes exactly as today. **Saves one call per file AND one re-upload of the document bytes** (cost and latency, not just quota). Risk: the classifier's cheapness is a feature — 20 tokens to reject a bank statement, versus a 4,000-token extraction budget spent discovering the same thing. **Mitigation: keep the merged prompt's cheap path cheap by making doc_type the FIRST field emitted**, so a non-invoice terminates early.
    - **PROPOSAL 2 — DON'T MERGE 3; DELETE IT FOR VENDORS WE ALREADY KNOW.** Merging extraction and coding puts two jobs in one prompt and makes each less separately testable — the ·3a lesson. **The better move is that `O102`/`C203` make call 3 unnecessary for KNOWN and UNIVERSAL vendors by construction:** the ladder's whole claim is that a familiar vendor's account comes from attested history or the curated directory, **not from a model**. `App.jsx:4005` already skips the AI's answer when a `vendor_rule` exists — the mapping is read, not asked for. **So the calibration work is also the throttle fix**, and on a fixture where most invoices come from recurring vendors it removes most of the third call. **This is the one to prefer: it deletes work rather than compressing it.**
    - **PROPOSAL 3 — CACHE ON THE CONTENT HASH WE ALREADY COMPUTE.** C193 hashes document bytes (SHA-256, unique per company) to dedup the library. **Identical bytes re-uploaded currently re-extract from scratch.** Reusing the prior extraction costs nothing new to build the key for. Bounded value — it helps re-uploads, which the August drive did twice, not first-time volume.
    - **PROPOSAL 4 — BATCH ACROSS FILES, NOT WITHIN THEM.** The "batch" profiles batch invoices *inside* one document while the queue processes one file at a time, **so the axis that actually matters is not batched at all.** Multiple documents can share one Anthropic message. **Costed honestly: this is the only proposal that moves the UPLOAD bucket**, since N documents in one call is one upload bump — but it is also the riskiest (one malformed document degrades a whole batch, and `max_tokens: 4000` caps the batch size), and it changes what a per-file error means.
  - **★★ DECIDED 2026-08-26 — PROPOSAL 2 IS PRIMARY, AND IT IS SEQUENCED WITH `C203`, NOT AS SEPARATE WORK.** *Operator: deleting call 3 for KNOWN and UNIVERSAL vendors is **not compression, it is the ladder's claim made operational** — a familiar vendor's account comes from attested history, not from a model.* **RECORD IT EXPLICITLY: `C203` IS ALSO THE THROUGHPUT FIX. THE CALIBRATION WORK PAYS OFF TWICE** — once as booking authority (`O102`), once as call volume (`O113`). That is not a coincidence: a system that has to ask a model what it already knows is paying for its own forgetfulness, in both currencies.
  - **PROPOSAL 1 (merge classify into extract) — REJECTED FOR NOW, on my own risk note.** *Operator: the classifier's cheapness is the feature.* **20 tokens to reject a bank statement beats a 4,000-token extraction discovering the same thing, and merging trades a cheap guard for an expensive one.** ▶ REVISIT ONLY IF the AI bucket is still binding after proposal 2 lands.
  - **PROPOSAL 3 (content-hash cache) — ACCEPTED.** Cheap, bounded, and the drive proved re-upload is a real path (it re-uploaded twice). Reuses C193's existing SHA-256.
  - **PROPOSAL 4 (batch across files) — ▶ HOLD, and it needs its OWN failure analysis.** It is the only thing that moves the upload bucket, which makes it matter more than the others combined — **and one malformed document degrading a whole batch is exactly the failure mode this project keeps finding** (·3a's inert gate, C195(7)'s empty input, the QBO swallow). `max_tokens: 4000` caps the batch size anyway. **Design separately, later.**
  - **NO RECOMMENDATION ON `AI_LIMIT`/`UPLOAD_LIMIT` VALUES**, deliberately. Both are ours to choose, and choosing them before `O113a` is fixed would set a ceiling measured in a currency that still charges for refusals.
- **★★ O113a — THE LIMITER NO LONGER CHARGES FOR ITS OWN REFUSALS. MIGRATION `074` + `ai-proxy`, BOTH ▶ HOLD (2026-08-26).**
  - **`consume_rate_limit` READS, DECIDES, AND ONLY THEN CHARGES**, inside one plpgsql function and therefore one transaction — so "decide, then charge" is atomic without a second round trip, and `for update` serialises concurrent callers so two requests cannot both observe 59 and both be allowed. **`021`'s `bump_rate_limit` is DELIBERATELY KEPT, NOT DROPPED:** the edge function and the database deploy separately, so the old code must keep working until the new function ships and a rollback must not hit a missing RPC.
  - **▶ ORDERING IS LOAD-BEARING: APPLY `074` *BEFORE* DEPLOYING `ai-proxy`.** The reverse order calls a function that does not exist and fails **every** AI request — a total outage of the AI path, not a degradation.
  - **★ A SECOND INSTANCE OF THE SAME BUG, IN THE SAME FUNCTION, FOUND WHILE FIXING THE FIRST.** An upload-tagged call bumped `ai`, passed, then was refused by `upload` — **leaving the `ai` charge in place for a call that never ran.** Charging one bucket for a refusal issued by another is the identical defect wearing a different hat, which is why the new RPC takes an ARRAY and is **all-or-nothing across buckets**. VERIFY block (c) proves it.
  - **★★ THE VERIFY BLOCK MAKES THE LIMITER REFUSE SOMETHING AND SHOWS THE COUNTER DID NOT MOVE** — `after_refused = 10, NOT 11`, in a rolled-back transaction. **A limiter nobody has watched refuse *without charging* is a fix on paper**, the standard `071` was held to.
  - **THE PROXY NOW FAILS CLOSED (503) IF THE LIMITER CANNOT ANSWER.** Guessing permissively is how a limiter becomes decorative under exactly the load it exists for.
  - **THE 429 NOW SAYS WHICH BUDGET RAN OUT AND WHEN IT RESETS**, and the remaining budget is returned on SUCCESS too (`x-ratelimit-remaining-*`) because the limiter computed it to make its decision anyway. **This is a step toward the budget being reasonable-about, NOT the fix for it** — that needs a surface, and none is built here.
  - **BOUNDARY, STATED: this changes the limiter, not the limits.** `AI_LIMIT` and `UPLOAD_LIMIT` are untouched at 60 and 20, per the operator's instruction, and `O113b`/`O113c` remain open.
- **★★ O97 — ACCEPT-AND-QUEUE. `O118` MERGED IN; THE UPLOAD CEILING AND THE DURABLE QUEUE ARE ONE ITEM (2026-08-26). ▶ HOLD — DESIGN SESSION, NO CODE.**
  - **★★ THE DEFECT IS NOT THE CEILING'S HEIGHT — IT IS THAT THE CEILING IS ENFORCED SYNCHRONOUSLY WHILE A HUMAN WATCHES.** *Operator: 200 files in, 20 succeed, 180 turn red, and the owner concludes the product is broken.* **Onboarding is inherently a BATCH JOB — nobody needs January's invoices categorised in ninety seconds — and we are failing it in the foreground.** Raising the number would move the failure without changing its shape; the owner would still be watching.
  - **★ THE MERGE IS TECHNICAL, NOT ORGANISATIONAL: THE DURABLE QUEUE IS THE PREREQUISITE.** Established in code today — `fileStoreRef.current[id] = f` (`App.jsx:3743`) puts the **bytes** in an in-memory React ref · `logIntake` (`:3748`) durably records the **arrival** · `classifyFile` (`:3806`) makes the **first AI call** · `storeDocument` (`:3877`) reaches Storage **only afterwards**. **So the arrival is durable, the bytes are not, and the AI work starts before anything is persisted.** Acceptance and processing are **one synchronous act**, and accept-and-queue is **impossible** until the durable byte-write moves ahead of the AI work. That is precisely `O97`'s original defect, which is why this is one item and not two. *(`O118` is kept as a row pointing here rather than deleted — a merged id that vanishes reads as a lost item to the next sweep.)*
  - **PROMOTED FROM P2/UNSCHEDULED TO P0-FOR-ONBOARDING.** *Operator: the drive turned it from unscheduled into the thing between us and a first client.* The live orphan of 2026-08-06 20:11:42 was the same bug costing one file; onboarding is the same bug costing a customer's first impression.
  - **THE FIVE DESIGN QUESTIONS, RECORDED WITH THE ITEM AND DELIBERATELY NOT ANSWERED HERE:** (1) accept-and-queue — *partially answered above: not today, the byte-write must move first* · (2) what the owner sees while a queue drains, and the honest copy when it is six hours — **Q9 applies, and a progress bar that cannot state a finish time is a world-claim** · (3) failure mid-queue — stop, skip-and-report, or retry, noting `O113a` made retry safe only as of this week · (4) is per-user-per-clock-hour the right unit at all, given an onboarding and a monthly close share one budget · (5) **what is actually being protected — answered below, and it was the right question to ask first.**
  - **★★ (5) ANSWERED: THE PURPOSE IS STATED ONCE IN THE ENTIRE REPO, AS A SLASH.** §7: *"limits prevent abuse/runaway cost"*. Migration `021`'s header and `ai-proxy`'s header both state **what** the limits are and **never why**. **Three candidate threats, and the unit fits exactly one** — cost wants **tokens**, not requests (a 20-token classify and a 4,000-token extraction cost the same against the budget, a 200× miscalibration); upstream wants **org-scope and per-minute** (ten users at 59/hour can spike one minute while all are "under"); **abuse wants per-actor-per-hour, which is what we have.** **So the mechanism is right for abuse and wrong for the other two — and it is currently refusing a legitimate first-time onboarding, which is none of the three.** Recorded as the §9 sibling rule: **name the threat, the way you name the reader.** **And the design consequence follows directly: if the threat is abuse, a legitimate bulk workload needs A DIFFERENT DOOR, NOT A BIGGER NUMBER.**
- **★★ O114 RE-DRIVE PREP — THREE FINDINGS FROM READING THE LIVE DATA INSTEAD OF THE FIXTURE (2026-08-27). One is a real defect, one is a correction to my own record.**
  - **★★ `ROMA CHEESE + DAIRY CO` — THE `+` IS NOT NORMALISED, SO ONE VENDOR HOLDS TWO KEYS.** August carries **two** Roma bank lines: `ACH DEBIT - ROMA CHEESE & DAIRY CO` (08-04, 551.20) and **`ACH DEBIT - ROMA CHEESE + DAIRY CO` (08-19, 538.75)**. `normalizeDescriptor` maps `&` → `and` and **leaves `+` as a token**, so the keys are `roma cheese and dairy` and `roma cheese + dairy` — `identityRelation` returns **NONE**. Same vendor, same rail, same month, two identities. **DESIGNATED `O119`.** *(And note the shape: this is exactly the C200 corpus problem again — real descriptor variance that the fixtures did not contain. The fixture had one Roma string; production has two.)*
  - **★★ MY ALAMO FIRE TEST FIXTURE WAS WRONG, AND THE FIX I JUSTIFIED WITH IT ADDRESSES NOTHING IN THE REAL DATA.** `tests/invoicePayment.test.js` uses `"ACH DEBIT - ALAMO FIRE SAFETY LLC"`. **The real descriptor is `ACH DEBIT - ALAMO FIRE & SAFETY LLC`** — it HAS the ampersand, so both sides normalise to `alamo fire and safety` and relate **EXACT** with no help needed. I recorded the `and`-dropping in `compareKey` as *"found by this feature's own suite on a real specimen"*. **It was found on a string I invented.** The rule stands on its own merits (a bank that drops an ampersand is a realistic variant, and the guard is comparison-only with the anti-merge cases pinned either way) — **but it fixes no case present in this data, and it does NOT fix the `+` case either** (`[roma,cheese,dairy]` vs `[roma,cheese,+,dairy]` is not a prefix). **Recorded as a correction because "found on a real specimen" was the claim that made it credible, and it was false.** Same lesson as C201's assumed-vs-real invoice strings, repeated inside two days.
  - **★ THE OPERATOR'S STATE SUMMARY WAS OFF, AND THE QUERY IS WHY WE KNOW.** Reported as *"19 lines booked, 12 of 36 invoices booked, 10 unanswered clarification cards"*. Live: **17 bank lines**, **5 booked invoices**, and **ZERO open anomalies** (24 dismissed, 52 resolved). The clarification queue is React state, so the "10 unanswered cards" did not survive the session — there is nothing to clear. **Only ONE of the 5 booked invoices is a specimen** (Hill Country 08-06, 468.50). *Not a criticism — a demonstration of the standing rule that estimates about production data are claims about a query nobody ran.*
- **★ O120 / O121 — WHICH CARDS EARN AN INTERRUPTION, AND A TRUST HEADER THAT CANNOT SEE THEM (2026-08-27). ▶ HOLD, no design.**
  - **★★ THE TENSION IS THE ITEM.** Clarification cards sit below the fold and can be scrolled past — **10 sat unanswered in the August drive while the header said *"your books are correct and up to date"***. But the obvious fix fights the product: **the stated principle is BOOK EVERYTHING AND BATCH THE JUDGMENT TO CLOSE**, and a modal per card is its opposite — it stops the world per item and **turns a queue you can zip through into a gauntlet**. So the question is not *"modal or not"*, it is **WHICH CARDS EARN AN INTERRUPTION.**
  - **CANDIDATE SPLIT, recorded as a candidate:** a card **whose wrong answer books silently wrong** may interrupt; anything **recoverable** stays in the batch. **The `O114` lifecycle card is the archetype of the first** — *"different purchase"* mints a phantom payable, *"same purchase"* suppresses a real charge, and **the more dangerous of the two leaves nothing on any screen.** A low-confidence category question is the archetype of the second: wrong is visible, and a recode undoes it. *(Note this reuses the same discriminator as the `O114` option ordering — VISIBLE-vs-SILENT failure. That is now the third place that distinction has decided a design, which is a sign it is load-bearing rather than local.)*
  - **★★ AND THE HALF THAT IS NOT UX AT ALL — `O121`, established in code.** `ownerTrustState` (`ownerTrust.js:35`) accepts `controlTotals`, `openConfidenceFlags`, `intakeRows`, `unknownDocs`, `reviewedThrough`, `bankMatch`, `openHighAnomalies` — **and no clarification-queue input.** It can therefore return `all_clear` and *"your books are correct and up to date"* **while N documents sit unbooked awaiting an answer.** The one indirect path — an unanswered card leaves its intake row `held_for_review`, and `intakeRows` IS an input — **only reaches the header through the completeness net's staleness window, so a green header beside FRESH unanswered cards is structurally possible.** Whether the header is right is **incidental, not enforced.**
    - **THIS IS THE C194 / `O98` FAMILY, NOT A UX PREFERENCE:** a false green on the surface whose whole job is to be trustworthy, and a world-claim derived from a query that never asked the question. **The function was hardened against exactly this twice already** — its own comments name the O83 false *"Nothing wrong"* and the bank-overdue false green — **and the clarification queue is simply the net nobody wired in.** The operator's instinct that this is the cheaper fix is right, **and it is also the one that is required independently**: the header should stop claiming correctness while cards are open whatever is decided about interrupting.
- **★★★ O122 — THE CARD RATE IS A SYSTEM-LEVEL ACCEPTANCE CRITERION, NOT A FEATURE (2026-08-27). THE PROMISE IMPLIES A NUMBER NOTHING CURRENTLY MEASURES.**
  - **★★ THE GAP, AND IT IS THE MOST IMPORTANT THING ON THIS PAGE: "ALL BEHAVIOURS CORRECT" AND "THE PRODUCT IS USABLE" ARE DIFFERENT CLAIMS, AND ONLY THE FIRST HAS EVER BEEN TESTED.** Every drive, every suite, every acceptance doc in this repo scores INDIVIDUAL behaviours. **We can therefore end with every behaviour correct and a screen carrying nine cards — which is not a usable product.** The promise is *"drop anything here — your AI controller handles the rest"*, and that sentence implies a **CARD RATE**. Nothing measures it. `tests/` cannot: a unit test asserts one card is right, never that there are few enough of them.
  - **★★ THE TARGET IS NOT ZERO — IT IS THE CONTROLLER'S OWN QUESTION RATE.** *(Operator, verbatim: **"the target card rate is the controller's own question rate, not zero."**)* A controller who asks nothing is rubber-stamping; one who asks nine questions about a 60-document month is drowning the client. **So the test for any INDIVIDUAL card is: WOULD A COMPETENT CONTROLLER HAVE ASKED THIS, OF THIS CLIENT, THIS MONTH?**
  - **★★★ AND THE RULE THAT DOES THE WORK — *(verbatim)*: "A CARD THE USER SEES EVERY MONTH IS A BUG WEARING A QUESTION MARK."** Formally: **any card that can recur monthly for the SAME VENDOR on the SAME QUESTION is a design defect BY DEFINITION** — not a tuning preference, not a P3, a defect. **The rule has teeth, and the proof is that it RETROACTIVELY RECLASSIFIES TWO KNOWN ITEMS:**
    - **`O117`** (weekly fixed-fee vendor flagged duplicate every week) stops being a P1 annoyance and becomes **definitionally a defect**.
    - **The `C203` justification itself** — Bluebonnet excepted at ~78 for **eight consecutive months on the same question** — was recorded as evidence that confidence is miscalibrated. **Under this rule it is simply a bug, and was one from month two.** *(That reframing is worth more than the calibration argument: it needed no theory of confidence at all.)*
  - **THE THREE CATEGORIES — every existing and future card type gets tagged, and they are NOT the same thing stacked on one screen:**

    | # | category | expected rate | nature |
    |---|---|---|---|
    | **1** | **BUG** — *"we recorded more than one payment of $462.85"* when there was exactly one. **No controller would ever say that.** | **ZERO** | a fix, **not a design question** |
    | **2** | **ONE-TIME TEACHING** — Franklin Ave's alias; Bluebonnet's two-month graduation under Amendment B. A controller asks these **once, on day one, and never again.** | **onboarding cost, NOT monthly cost** | **each must be askable ONCE and then never re-asked** — if it can re-ask, it is category 1 |
    | **3** | **GENUINE JUDGMENT** — Hill Country's invoice at 468.50 against a payment of 486.50. **A real bookkeeper stops on that.** | **forever, and RARE** | the residue that proves the system is thinking |

  - **STARTING TAXONOMY, applied to what actually exists today** *(this is the tagging, not a promise to tag later)*: `lifecycle/amount_differs` **3** · `lifecycle/identity_differs` **2** (closes with `O111`) · `lifecycle/multiple_candidates` **3** genuinely, **1** when produced by the flatten-dedupe defect · `duplicate_payment` **1** on lifecycle pairs (`O114`) and **1** on weekly vendors (`O117`), **3** only for a true double-pay · `gl` low-confidence category **3**, shrinking toward **2** as vendors reach KNOWN · `gaap` capitalise-vs-expense **3** · `direction` revenue-vs-expense **2** (the company's own identity settles it — O75) · `large_transaction`/`round_number` **3**, but **1** while they fire on payroll (O85(c)) · `missing_recurring` **1** while it measures against `now()` instead of period context (O86) · `category_spike`/`vendor_spike`/`rapid_sequential` **3**.
  - **STEADY-STATE TARGET — established client, all vendors KNOWN, aliases learned, no bugs: UNDER 5% OF DOCUMENTS.** For a 60-document month that is **0–3 cards**. *(Operator's working number, recorded as a working number.)* **Onboarding gets its OWN, HIGHER, SEPARATE number** — conflating them would either make onboarding look broken or make steady state look fine.
  - **★ MEASURE IT ON EVERY FUTURE DRIVE, SPLIT BY CATEGORY.** Not a total — **a split**, because the whole question is **whether category 2 actually shrinks month over month rather than being assumed to.** A falling total with a flat category 2 means the teaching is not sticking, and that is invisible in an aggregate. **This is now a standing drive deliverable.**
- **★★★ O114 RE-DRIVE §1 FAILURE — ROOT-CAUSED 2026-08-27. ONE MISNAMED PARAMETER, AND A CARD THAT LIED ABOUT WHY. BOTH MINE.**
  - **★★ THE ROOT CAUSE: `checkedRowUpdate` takes `patch`. The O114 attach passed `values`.** So `patch` arrived `undefined`, `.update(undefined)` did nothing, `recordFailure` fired, and **every attach failed** — `import_metadata` is `NULL` on all three targets and there are **zero `invoice_attached` audit rows**. **13 of 14 checked-write call sites in the repo had it right; mine was the only outlier.**
  - **★★★ AND THE FAILURE PATH REPORTED A FALSE CAUSE, WHICH IS THE WORSE HALF.** I had written the fallback to ask instead of half-attaching — correct — but gave it `reason: MULTIPLE_CANDIDATES`, which renders as ***"We recorded more than one payment of that amount."*** **That is a statement about the LEDGER, and it was false: there was exactly one payment.** All three cards said it, identically, and **the drive became undiagnosable from its own output.** It cost a full session: the operator hypothesised duplicate legs from `journal_entry_lines`, then duplicate JEs from the supersede path; I shipped an entry-level dedupe; both of us were chasing a candidate-counting bug **that did not exist**, because the card invented a cause.
    - **THE RULE THIS EARNS: A CARD MAY REPORT THAT WE FAILED. IT MAY NOT INVENT A REASON IN THE LEDGER.** `ASK_REASON.RECORD_FAILED` now exists precisely so the honest sentence has somewhere to live — *"…matches a payment we already recorded on X. We couldn't save the link between them, so we haven't recorded anything."* **Q9 says an empty result may only be reported as an empty result; this is its twin — OUR failure may only be reported as OUR failure.** Reusing a neighbouring reason code because it was already there is how a diagnostic becomes a decoy.
  - **★ WHY NOTHING CAUGHT IT, AND THE GUARD THAT NOW DOES.** `tests/invoicePayment.test.js` proves `planInvoiceArrival` returns ATTACH; **nothing asserted the CALLER then successfully attaches.** That is the ·3a gap exactly — both sides of a contract tested against the same fixture, with the boundary between them untested. **Reproducing the real ledger through the real function returned ATTACH for all three specimens**, which is what proved the defect was in the wiring rather than the logic. New guard in `tests/checkedWrite.test.js` (where a falsifier of the CALLING CONVENTION would look, not in this feature's suite): **every `checkedRowUpdate`/`checkedIdsUpdate` call site in `src/` must name its payload `patch`.** Mutation-verified — restore `values:` → red; restore → green. It also had to learn two things about itself: **ES6 shorthand `patch,` is valid** and must pass, and **the helper's own definition is not a call site** — a guard that flagged the contract it protects.
  - **AND THE OPERATOR'S TWO HYPOTHESES WERE BOTH WRONG AND BOTH WORTH HAVING.** *Duplicate legs:* refuted — `flattenJournalEntries` emits ONE row per ≤2-line entry; the two-row result came from SQL joining `journal_entry_lines`, a property of the query. *Duplicate JEs from the supersede path:* refuted — one live entry each for Toast and Alamo Fire. **But the first hypothesis exposed a REAL latent defect in the >2-line branch, which is now fixed and pinned.** A wrong hypothesis that surfaces a real bug is a good turn; the thing that wasted the session was my card, not their theory.
- **★★★ THE VOID PATH POSTED THREE REVERSALS AGAINST ONE INVOICE (2026-08-27). ROOT CAUSE: `O95` — THE `p_meta` DROP — CLAIMING ITS THIRD VICTIM, AND THE FIRST ONE THAT FAILS *OPEN*.**
  - **ONE BUG, THREE SYMPTOMS.** `reverseJournalEntry` posts with `p_meta: { kind: "reversal", reverses: origId }`. **`post_journal_entry` cherry-picks six named scalars and never writes `import_metadata` at all** — confirmed live: **all three reversals carry `import_metadata = NULL`.** Net effect on Hill Country: one 468.50 debit against **three** 468.50 credits = **−937.00**. *(Timing: 03:24:20, 13:05:29, 13:05:49 — the last two twenty seconds apart. Textbook "nothing happened, click again".)*
  - **★★ DEFECT 1 — VOID IS NOT IDEMPOTENT, AND THE GUARDS THAT WOULD MAKE IT SO ARE STRUCTURALLY INERT.** CR-17 built **two**: `alreadyReversed` (`ledger.js:200`) reads `r.import_metadata.reverses`, and a DB probe (`App.jsx:2394`) filters `.eq("import_metadata->>reverses", …)`. **Both key on the field the RPC discards, so both are permanently false.** The guards are well-written, deliberate, and have never once fired.
    - **★★★ AND THIS IS THE FIRST `p_meta` VICTIM THAT FAILS OPEN.** Payroll failed CLOSED (asked a human — a missed feature). Depreciation failed CLOSED (posted nothing). **This one CREATES WRONG ENTRIES, one per click, with no ceiling.** The class just graduated from "features that silently do nothing" to "a control that silently does damage".
    - **★★ THE CODE COMMENT ASSERTS THE OPPOSITE, AND CITES THE FIX THAT WAS NEVER APPLIED HERE.** `App.jsx:2407-2409`: *"Post the reversal WITH its link metadata in the SAME atomic RPC (p_meta persists to import_metadata — the depreciation guard relies on the same contract). No separate, swallowable update: the idempotency marker is written iff the entry is posted."* **Every clause of that is false**, and it was written as the JUSTIFICATION for not using a follow-up checked update — the exact remedy ·3c applied to payroll and depreciation. **·3c's D2 sweep listed `tax_amount` and `signedPeriod`'s `original_date` as `O95` victims and MISSED the reversal marker.** An incomplete audit of `import_metadata` readers, and the miss is recorded here rather than quietly corrected.
  - **★★ DEFECT 2 — THE UI CANNOT DISTINGUISH REVERSED FROM LIVE, AND THE FEATURE THAT WOULD FIX IT WAS ALREADY DEFERRED *AND* WOULD ALSO HAVE BEEN INERT.** The original staying visible is **BY DESIGN** (§12 #14 — reverse, never delete; the comment at `App.jsx:2374-2379` says so explicitly and is right to). **But nothing marks it.** §12 already defers precisely this: *"Reversal display marker — show the original struck-through / 'reversed on DATE' when a live reversal links to it (display annotation only via `import_metadata.reverses`)"* — **which keys on the same never-written field.** So the one mechanism that would have stopped the repeat-clicking was unbuilt, and had it been built it would have shipped inert. **Three-way interlock: the marker is missing, the guard is inert, and the same dropped key causes both.**
  - **★★ FIXED 2026-08-29 (C207). THE STAMP, THE MARKER, AND ONE MORE INERT GUARD FOUND WHILE TESTING THE FIRST.**
    - **THE FIX IS THE FOLLOW-UP `checkedRowUpdate`** ·3c applied to payroll and depreciation and never here. **But the stamp is the ONLY guard on this path** — depreciation has `status='pending'` behind it, payroll merely loses norm history — so a failed stamp is **not a degraded feature, it is a live double-post hazard**, and it is audited **and said out loud**: *"Reversed — but we couldn't record that it's been reversed. Don't press Void on it again."*
    - **THE FALSE COMMENT IS GONE, AND A TEST KEEPS IT GONE.** `App.jsx:2407`'s claim was the JUSTIFICATION for not stamping; the suite now asserts the source no longer says *"p_meta persists to…"* or *"the marker is written iff the entry is posted"*. **The guard strips comments before scanning** — the suite QUOTES the old comment to explain it, and a guard matching its own explanation is the C202 false positive a fourth time.
    - **★ AND A SECOND INERT GUARD, FOUND BY WRITING THE TEST RATHER THAN BY READING.** `alreadyReversed` filters `!r.deleted_at` — and **`flattenJournalEntries` never emitted the field**, so that half was decorative. It worked only because `fetchLedgerEntries` filters deleted rows upstream. Any future path loading soft-deleted entries into `invoices` (a restore flow, a *"show deleted"* toggle) would have counted a soft-deleted reversal as live and **blocked a legitimate re-void.** Flatten now carries it. *(Note the shape: a guard correct only by virtue of something upstream is a guard nobody has actually tested.)*
    - **`alreadyReversed` HAD NO TEST AT ALL** before this — the function at the centre of a P0. The suite now pins the damage as a **number** (three reversals → **−937.00 exactly**), pins the bug itself (strip `import_metadata` → the guard goes quietly false **with the reversing entry sitting right there**), and lives in `reversalLifecycle.test.js` where a falsifier of the reversal would look, not in whichever file created it.
    - **`O124(a)` and `O126(B)` SHIPPED IN THE SAME COMMIT, because the marker unblocked them exactly as predicted.** Both the detail panel and the invoice list render **"Reversed · DATE"** and **DISABLE** the Void control rather than refusing on click.
    - **✅ CHECKED AGAINST THE LIVE BOOKS 2026-08-29 — NO REPAIR WAS NEEDED, AND THE CHECK PROVED ITSELF FIRST.** All three reversals (`164c88ab`, `d3fa0059`, `9a2bcb8b`) **and the original bill** (`31027276`) are already `deleted_at is not null`. Nothing from the incident is live.
      - **★★ THE EMPTY RESULT WAS SHOWN TO BE GENUINE BEFORE IT WAS BELIEVED.** The duplicate-detection query returned zero rows — which is exactly the shape this file spends a page warning about (*a query that finds nothing and a query that cannot match anything are the same output*). So the join was **verified against the real strings**: `substring(from 11)` + `split_part(' — ')` on the live reversal description reproduces the live original description **exactly**. The match works; the emptiness meant CLEAN. **This is `O98`'s own discipline applied to my own verification rather than to the product's.**
      - **★ CORRECTION TO THIS SECTION'S OWN NUMBERS: the bill was $937.00, not $468.50.** Every prior write-up here says "one 468.50 debit against three 468.50 credits = −937.00". The live rows say the invoice was **937.00** and each reversal was **937.00**. **The −937 figure happened to equal the invoice amount**, which is how a wrong number survived several retellings looking arithmetically checked. Kept rather than silently corrected — an unverified number that LOOKS derived is more dangerous than an obviously missing one.
      - **▶ OPEN QUESTION FOR THE OPERATOR, NOT A DEFECT: the ORIGINAL is deleted too.** Void is supposed to KEEP the original and post an offset (§12 #14). Here the original was removed as well, so the $937 purchase sits in no period at all — correct if the bill was a mistake, a gap in August if it was real.
    - **⚠ (SUPERSEDED — the repair was owed until the check above.) THE THREE LIVE REVERSALS ARE NOT REPAIRED
  - **CORRECTION TO THE REPORT, ON THE EVIDENCE.** The toast was reported as saying *"deleted"*. The VOID path's toast says **"Reversed … tap Undo to restore"** and its confirm modal says *"It stays in the audit trail and posts a reversing entry"* — **that copy is accurate.** The audit log shows **three `entry_reversed` and ZERO `invoice_deleted`**, so the void path was used all three times and delete was never invoked. **The substance of the finding stands** — the action reported success and nothing visibly changed — but the copy is not where it went wrong.
  - **★ DEFECT 3, FOUND WHILE LOOKING FOR DEFECT 2 AND REAL INDEPENDENTLY: `softDeleteInvoices` CLAIMS SUCCESS UNCONDITIONALLY.** It shows *"Deleted {label} — tap Undo to restore"* **without ever checking `allIds.length`**, while `softDeleteJournalEntry` returns `[]` on a signed-period block or a DB error — **and the function's own comment (`App.jsx:2370`) says callers gate "✓ done" on a non-empty result (O78). Its own toast does not.** Not what happened here (August is unsigned; the guard never fired) but the same false-success class as C194, one surface over.
- **★★ O119 FIXED 2026-08-29 (C209) — AND THE TEST FOUND THE PHANTOM THE FIX OPENED.** `normalizeName` mapped `&` to "and" and left `+` alone (it was not in the punctuation strip either), so the two August Roma descriptors keyed as different companies. **The widening is deliberately one character wide** — a wrong merge silently launders one vendor's attested mapping onto another's charges, so the anti-merge pairs (SYSCO vs SYSCO FUEL, Lone Star vs Lone Star Restaurant Supply) are asserted alongside, and **they are the reason to trust the widening rather than the widening being its own justification.**
  - **★★ THE FOLLOW-ON IS THE USEFUL PART, AND THE AUTHOR DID NOT FIND IT.** `"+"` used to normalise to `"+"`, carry no letters, and correctly resolve to nothing. Giving it letters meant a descriptor made only of joiners would have **minted an entity keyed on `"and"`** — the phantom-vendor one-way door this module's own comments describe, opened by a fix to a cosmetic-looking bug. The letters test now ignores the joining word. **Same shape as the original bare-numeric find, which was also caught by a test rather than by reading the code: every phantom this module has had was found by asserting a negative.**
- **★ O96 CLOSED 2026-08-29 (C209) — THE ITEM WAS STALE, AND THAT IS THE FINDING.** It read *"dead 'run depreciation through date' button — connect it or remove it"*. **There is no button.** It was deliberately removed from BOTH Reports and Dashboard — depreciation is deterministic so it auto-posts when due, and both files carry comments saying so. The decision was made long ago; what outlived it was an unused `planDepreciationRun` import and a `runDepreciationThrough` destructure resolving to `undefined`. **A ledger item describing a control that no longer exists is the same class as a printed census going old** — it cannot be wrong, only out of date, and nothing about it invites correction.
- **★★ THE VENDORS TAB — FIXED 2026-08-29 (C216), AND THE NUMBERS WERE WRONG TOO.** `vendorSummary` was keyed over EVERY flattened row with no filter. **All three defects reproduced by RUNNING THE OLD CODE against a fixture rather than reasoning about it:** `[{"Hill Country Milling Co.": 1649.2, count 2}, {"Opening balances as of 2026-01-01": 5000}, {"Toast POS": 1200}]`.
  - **★ THE THIRD ONE WAS NOT IN THE ORIGINAL FINDING: EVERY BILL'S PAYMENT COUNTED AS A SECOND PURCHASE.** An $824.60 bill read as **$1,649.20**. §11 had previously recorded *"Amounts on the tab verified correct… this is a classification defect, not a math defect"* — **that was wrong, and it was wrong because the amounts were checked against invoices only, on a vendor whose payments happened not to collide.**
  - **★★ ONE RULE REPLACES THREE SPECIAL CASES: SPEND IS AN EXPENSE-ACCOUNT MOVEMENT.** Revenue is `4xxx`, bill payments land on A/P, opening balances are equity/asset, transfers are asset-to-asset — all fall out for free. **A filter on ACCOUNT, not on `source`, deliberately:** the payment leg of a bill is a legitimate entry with a real vendor name on it, so what makes it not-spend is where the money LANDED.
- **★★ O125 FIXED 2026-08-29 (C210) — AND THE FIX DELETED AN IMPLEMENTATION RATHER THAN IMPROVING ONE.** `displayVendorName` / `vendorGroupKey` live in `vendorIdentity.js`, the resolver that already handled all three symptoms and that the display layer had simply never called. **THE ROOT CAUSE, STATED PROPERLY: one string was doing two jobs.** A vendor needs a NAME a human reads and a KEY the system groups by, and they have **opposite** requirements — the name keeps its capitals, punctuation and legal suffix; the key throws all three away. Deriving one from the other by a split gets both wrong. Flatten now emits `vendor` AND `vendor_key`; `vendorSummary` groups on the key and labels with the most RECENT display name (a real string the user has seen, not a normalised key and not an unpredictable vote), and the vendor filter follows the same grouping.
  - **★ THE REVERSAL SYMPTOM WAS ALSO AN ARITHMETIC BUG, NOT ONLY A LABELLING ONE.** A reversal filed under `REVERSAL: …` left the original's vendor total **overstated** with its offset hidden under a separate name. Grouped correctly the two **net to zero**, and the test SUMS them rather than merely comparing keys — a key-equality assertion would have passed on a fix that still displayed the wrong total.
  - **★ A NULL KEY RETURNS null, NEVER `""`.** Grouping every unkeyable row under a falsy default would merge unrelated vendors — the one-way door reached through a fallback rather than through a rule, which is the harder kind to notice.
- **★★ O125 (ORIGINAL FINDING) — THE DISPLAY LAYER HAD ITS OWN VENDOR IDENTITY, A THIRD IMPLEMENTATION OF THE SAME CONTRACT (2026-08-27, from the InvoicesView cleanup screen).**
  - **ONE ROOT CAUSE: `flattenJournalEntries` (`ledger.js:31`) derives the vendor as `description.split(" – ")[0]` — a raw string split on an EN-dash.** That is an identity decision, made in the display layer, by punctuation.
  - **THREE SYMPTOMS, ALL ON THE SCREEN THAT IS THE ONLY ROUTE TO DELETE:** *(1)* **GUSTO RENDERS AS FIFTEEN SEPARATE VENDORS** — payroll descriptions use an **EM-dash** before the period (`Gusto Payroll — 2026-08-15`), so the en-dash split misses and **the pay period becomes part of the vendor name**. *(2)* **A TRAILING PERIOD SPLITS A VENDOR IN TWO** — `Hill Country Milling Co` and `Hill Country Milling Co.` are separate chips, likewise Roma. *(3)* **REVERSALS GET THEIR OWN VENDOR** (`REVERSAL: Hill Country Milling Co.`) instead of filing under the entry they reverse.
  - **★★ `entityKeyFor` ALREADY HANDLES ALL THREE CORRECTLY** — it normalises trailing periods, and the per-source strategy (C201) exists precisely because the em-dash case bit us once already. **This is the SAME defect C201 found in the payroll descriptor split, resurfacing in the UI because the UI never used the resolver.**
  - **★★★ AND THAT MAKES THREE IMPLEMENTATIONS OF VENDOR IDENTITY IN ONE CODEBASE:** *(a)* `entityKeyFor`/`identityForEntry` — the canonical one, anti-merge-tested · *(b)* `autoMatchBankLines`' `normalizeName` + **two-way substring containment** — already recorded as owed after O114 §2.1 · *(c)* **this string split.** **The operator's framing is the right one: this is exactly what `065` was released for — the vendor backfill *"cannot be SQL"* because it would have been a second implementation of the identity contract.** We declined to write one in SQL and shipped two others in JS. **The fix is not to improve the split; it is to delete it and call the resolver**, and reversals should file under the reversed entry's vendor (which needs `import_metadata.reverses`, i.e. `O123`'s fix, so the two are coupled).
- **★ O129 — THE DEPRECIATION BAR LEFT REPORTS (2026-08-29, C211), AND REMOVING IT SURFACED DEAD PLUMBING WITH A BILL ATTACHED.** The bar's one control opened a form asking for a raw `journal_entries.id`, **typed by hand, on the tab where you read your statements**. **★★ AND `depreciationDueInfo` HAD BEEN DEAD FOR MONTHS AT A COST:** it fed a dashboard nudge that was removed when depreciation became automatic — but the SIGNAL behind it kept running, a Supabase query on **every company load**, writing state that one file destructured and **no file read**. O96's class with a database round-trip attached: dead plumbing that merely reads oddly costs nothing; this one billed for it forever to answer a question nobody asked. **▶ ONE DECISION OPEN:** `attachDepreciationToExistingAsset` now has no caller. Kept, removed from `erpCtx`, and NAMED as unreachable — because what O96 punished was dead plumbing that LOOKED wired.
- **★★ O130 — ONE REMOVAL CONTROL, AND THE SYSTEM PICKS THE MECHANISM (2026-08-29, C215). CLOSES O126 FROM BOTH ENDS.** *Operator: "users don't even know what reversal means. I think the most useful item is deleting."*
  - **★★ THE CHOICE WAS NEVER THE OWNER'S TO MAKE, BECAUSE ONE INPUT DECIDES IT: HAS THE MONTH BEEN SIGNED OFF?** Not signed → nobody has attested to those numbers, so removing a wrong entry is correcting a draft (soft delete; row survives, Undo restores, audit keeps it, linked payment goes with it). Signed → **you may not quietly change a month your accountant put their name to**, so the correction must be a NEW entry dated today, which is what a reversal actually is. **The system already knew which case it was in** — `signedPeriodForDate` gated every delete — and merely refused with *"reopen it first"* instead of doing the right thing.
  - **★ THE SENTENCE TRAVELS WITH THE DECISION.** `planEntryRemoval` returns the mode AND the confirmation text, so a caller cannot promise a delete and perform a correction. **That is §9 applied one layer up: describe from the DECISION, not in parallel with it.** A test asserts neither surface still says "Void" (comments stripped first — C202's lesson a fifth time) and that no accounting word appears in either sentence.
  - **★ A FOURTH INSTANCE OF "AN INVISIBLE ACTION WILL BE REPEATED", NOW CLOSED:** an entry already dealt with renders **"Removed · DATE"** and its control is **DISABLED**, not merely refusing on click.
- **★★ O126 (ORIGINAL FINDING, CLOSED BY O130) — DELETE BELONGS ON THE DETAIL PANEL, AND VOID MUST BE VISIBLY TERMINAL. ONE FUNNEL FROM BOTH ENDS (2026-08-27).**
  - **(A) WIRE `softDeleteInvoice` TO `TransactionDetailPanel`**, same confirm treatment as the Books-list control. That panel is **the surface a user reaches by clicking an entry they intend to deal with**, and today it offers **Mark as Paid / Full entry / Void** — with Void the only destructive option and the one that compounds `O123`. **The safe action sitting four steps deep behind *"View all invoices for X →"* is not a discoverability nitpick: it is what produced three reversals.**
  - **(B) VOID MUST BE IDEMPOTENT *AND* VISIBLY TERMINAL.** Fixing the `O95` marker so the two existing guards actually fire is **necessary and not sufficient**: an already-reversed entry must **show as reversed in the transactions list**, and its Void control must be **DISABLED, not merely refusing on click**. A guard that rejects on click still teaches the user that clicking is how you find out — and the click is free only because we already lost the marker.
  - **THEY ARE ONE FUNNEL: the safe action is hidden and the dangerous one is unmarked, so the user is routed to the destructive path and given no signal to stop.** Both are instances of the §9 rule this session earned — **an action whose effect is invisible will be repeated** — and so is `O124(c)`.
- **★★★ THE O114 RE-DRIVE, SCORED (2026-08-27). §1 PASSES · §2 FAILS · THE ANTI-VACUITY CHECK EARNED ITS ENTIRE EXISTENCE.**
  - **✅ §1 PASSES, VERIFIED IN THE DATABASE NOT THE UI.** Roma (551.20), Toast (462.85) and Alamo Fire (425.00) attached **silently, no card**, and all three carry `invoice_attached=true` with an `attached_invoice_id`. **Franklin Ave raised the identity card and did NOT attach** — the outcome §1 marks as a failure if inverted. **Hill Country raised the amounts-differ card stating "$18.00 more than the invoice" and named no cause.** The order-independence bug is closed on the specimens it was written for.
  - **★★★ §2 FAILS, AND IT FAILED IN THE EXACT WORDS THE CRITERION USED.** §2 was written as: *"IF BLUEBONNET'S CARDS VANISH, SOMETHING OVER-MATCHED — the single most likely way for a wrong implementation to LOOK BETTER than a right one."* **Four were required. Three remain.** Invoice `BLS-88412` (08-03) attached to the payment of **07-27** — **seven days earlier, exactly on the `WINDOW_BEFORE_DAYS` boundary.**
    - **THE EVIDENCE: Bluebonnet pays WEEKLY at exactly $145.00** (07/06, 07/13, 07/20, 07/27). So the candidate set held **FOUR entries identical in vendor and amount to the cent**, and **only the date window separated them** — leaving exactly one, which then satisfied *"one certain candidate → ATTACH"*. **THE CERTAINTY WAS AN ARTEFACT OF THE WINDOW, NOT OF THE EVIDENCE:** at ±14 days it would have asked; at ±3 it would have booked a payable. **A threshold decided it, not the documents.**
    - **AND IT FAILED SILENT — the direction the whole design exists to avoid.** The 08/03 delivery's expense is **suppressed**, the 07/27 payment is claimed by an invoice that is not its own, and **nothing on any screen says so.** → **`O127`, P0.**
    - **★★ IT IS `O117` WEARING THE `O114` COSTUME.** One root property: **for a flat-fee recurring vendor, amount and identity carry NO information**, so the decision rests entirely on a date window. O117's version emits a noisy card; **this version silently deletes a real charge.** The two items were split on 2026-08-26 as different failure modes of the duplicate detector — **they are now confirmed as one property surfacing in two subsystems**, and `O127`'s fix should be designed with `O117`, using the cadence and amount band `vendor_state` already holds.
  - **★★ THE METHOD POINT, AND IT IS THE ONE TO KEEP: A PRE-REGISTERED CRITERION CAUGHT A DEFECT THE RESULT LOOKED TOO GOOD TO CONTAIN.** Four attaches against a `>= 3` threshold reads as a clean beat, and it was reported that way. **§2 existed only because it was written BEFORE the data, naming a specific vendor and a specific direction of surprise.** Without it, the over-match would have been recorded as a bonus — **and the missing card is easier to miss than a wrong one, because absence has no pixel.** *(This is the §9 invisible-action rule at the level of TEST DESIGN: an assertion about something that must still be there is the only kind that catches a silent deletion.)*
  - **CARD RATE (`O122`) — 9 documents, 5 cards: category 1 = 3 (Bluebonnet duplicates, `O117`) · category 2 = 1 (Franklin Ave identity → `O111`) · category 3 = 1 (Hill Country's $18 gap).** Steady state for this set is **1 card** once `O117`, `O111` and `O127` land. **First drive scored under `O122`, and the split immediately paid: three of five cards are defects, which a total of "5 cards" would have hidden.**
  - **`O128` — THE SUCCESSFUL ATTACH ANNOUNCES ITSELF AS A NULL RESULT:** the queue reads **"✓ 0 invoices booked · $0.00 total"**. Technically true and read as *nothing happened*, at the moment the best possible outcome occurred. **The most valuable behaviour this feature has, described as a zero.**
- **★★★ O117 + O127 — SPEC WRITTEN AND BOTH HALVES BUILT 2026-08-29 (C218 matching, C220 detection). `docs/RECURRING_FLAT_FEE_SPEC_O117_O127.md`. NO CODE.**
  - **★★ THE BOUNDARY ARITHMETIC IS NOW ESTABLISHED FROM THE CODE, NOT RECALLED FROM THE DRIVE.** Invoice 08-03; payments 07-06/13/20/27 at $145.00; `gap = -7` for 07-27 and `gap < -WINDOW_BEFORE_DAYS` is `-7 < -7` = **false**, so it survives by one day while the other three are excluded. `certain.length === 1` → ATTACH. **At ±14 the same code asks; at ±3 it books a payable. Nothing about the two documents decided it.**
  - **★★★ THE REFRAME, AND IT IS THE WHOLE SPEC: THE PAIR IS THE WRONG UNIT.** Ask what actually goes wrong when invoice A attaches to payment 3 rather than payment 1, given all four are identical. **Within a period: nothing** — every account, every total, every control figure is the same, so the pairing is *unobservable in the books*. **Across a period boundary: the expense moves months**, which is exactly the `O127` damage. So the question the matcher was asking — *which payment does this invoice belong to* — is **unanswerable from the data AND mostly not worth answering**; the question that matters is *does this vendor's period balance*. **That is also what a bookkeeper does**: handed four identical invoices and four identical payments, nobody agonises over the pairing — they check that four is four, and what they catch is five-against-four.
  - **★ SO THE RULE IS COUNT, NOT PAIR — AND IT UNIFIES BOTH ITEMS.** The matcher attaches within the period when counts allow and raises ONE card when they don't; the detector stops firing on identical-pairs and fires instead on a count above the vendor's cadence (*"charged 6 times in August; they normally charge 4"*). **A genuine double-payment still surfaces. The weekly card that says two identical charges happened — which they are supposed to — does not.**
  - **★★ THE ANTI-VACUITY CRITERION IS PRE-REGISTERED, because suppression turning into blindness is the single most likely way a wrong implementation looks BETTER than a right one.** §3 of the acceptance criteria feeds a month with FIVE payments against four invoices and requires **exactly one** card: *if that card does not appear, the fix is worse than the bug.* Written before the data, per `O127`'s own lesson — **a missing card is harder to notice than a wrong one, because absence has no pixel.**
  - **★ RECOGNITION NEEDS NO MIGRATION AND MUST NOT DEPEND ON `vendor_state`.** Flat (`sd/mean ≤ 0.02`), frequent (>1 charge per period), established (≥4 charges over ≥2 periods) are all derivable from booked entries. `vendor_state` is live but EMPTY — Amendment B withholds every backfill tier and `C201` is on hold — so wiring recognition to it would park this behind `O102`, the largest item on the board.
  - **★ AND THE CLASS DEFINITION IS THE BLAST-RADIUS CONTROL.** Only vendors charging MORE OFTEN THAN THE PERIOD get set-reconciliation; a net-30 vendor bills once a month, has one plausible payment, and is untouched. An acceptance criterion pins that (Franklin Ave rent must behave exactly as today).
  - **★ THE OBVIOUS ANSWER IS RECORDED AS REJECTED, because it reads convincing: THE INVOICE NUMBER DOES NOT SOLVE `O127`.** `BLS-88412` identifies the *invoice*; the **payment is an ACH bank line and carries no invoice number**, so it cannot link the two. It helps `O117` and is specced there. Sequence/Nth-to-Nth is rejected outright — one missing invoice shifts every later pairing SILENTLY, trading one invisible error for a compounding one.
  - **★★ THREE DEFECTS WERE FOUND BY THE PRE-REGISTERED CRITERIA AND THE MUTATION RUN, NOT BY WRITING THE CODE — and one of them was in a test of mine:**
    1. **The set rule sat BELOW the empty-candidate early return.** For this class an empty candidate list is MEANINGFUL (every charge already claimed IS the five-against-four case), and a period with no payments fell through to the PAIR rule, which reached back into the previous month and re-opened the whole bug.
    2. **★ THE RECOGNITION BAR WAS RE-OPENING THE BUG IT GUARDS.** "≥2 distinct periods" left a vendor's SECOND month unrecognised — the boundary case, which is exactly where the bug lives. Relaxed to ≥1 **because the two errors are not symmetric**: recognising too eagerly books a VISIBLE payable, recognising too late is the SILENT cross-period attach. **The bar belongs on the side that fails loudly.**
    3. **★★ A TEST OF MINE WAS VACUOUS, AND FIXING IT EXPOSED A DESIGN FLAW.** "A flat-fee vendor charging an unusual amount keeps the ordinary rule" used a 2-day gap, so removing the guard still produced a card — a different one — and `length >= 1` could not tell them apart. Fixed to a 7-day (on-rhythm) gap, which then **failed against MEAN/σ flatness**: a weekly $145 vendor with two one-off $620 charges left the class entirely and **every week of $145 noise came back**. **Mean and σ are the wrong statistics by construction — outliers are what we are excluding and they are what those two move most.** Now MEDIAN and MAD, matching `typicalIntervalDays`' median gap. *A vendor is not un-flat because it once bought something else.*
  - **★ THE DETECTION HALF DEVIATES FROM THE SPEC'S OWN §7, DELIBERATELY AND IN WRITING.** §7 proposed a MONTHLY COUNT (*"6 times in August; they normally charge 4"*). **A weekly vendor legitimately charges five times in a month with five Mondays**, so a monthly count fires on the CALENDAR rather than on the vendor — a category-1 card, precisely what §7 exists to remove. Implemented as **half the vendor's own median interval**: same property, no calendar unit, and the cadence is evidence we already held rather than a threshold being tuned.
  - **★ AND THE RESIDUE IS NAMED RATHER THAN LEFT TO BE DISCOVERED: the PDF filed against a given payment may be the wrong week's.** Both documents are in the library under the right vendor and period, every figure is correct, and the failure is visible — where today's is not. Accepted, in writing, with the reason.
- **★★ O127 SEQUENCED WITH O117, AND THE PROPERTY STATED (2026-08-27). ▶ HOLD — do not tune the threshold.**
  - **THE PROPERTY, in the operator's words and worth keeping exactly: FOR A FLAT-FEE RECURRING VENDOR, AMOUNT AND IDENTITY CARRY NO INFORMATION.** Everything the matcher normally reasons from is **constant**, so **any decision it reaches is being made by whatever variable is left — which today is a date window nobody chose for this purpose.** Bluebonnet: four payments, one vendor, $145.00 to the cent, weekly.
  - **★ SO THE FIX IS NOT A BETTER WINDOW. ▶ EXPLICIT CONSTRAINT: DO NOT TUNE THE THRESHOLD.** Widening it produces more ambiguity, narrowing it produces more payables, and **neither adds a bit of information** — the two documents remain indistinguishable on every axis the matcher reads. **Tuning would convert a diagnosable failure into a quieter one.**
  - **THE VENDOR CLASS NEEDS A DIFFERENT DISCRIMINATOR ENTIRELY, or must always ask.** Candidates, not a design: **invoice number** (`BLS-88412` is unique per delivery and we already store it) · **a delivery/service date on the document**, distinct from the invoice date · **sequence** (the Nth invoice matches the Nth payment) · or **detect the class and ALWAYS ASK**, which is honest and costs one card per delivery. **`vendor_state` already holds the amount band and the cadence needed to RECOGNISE the class** — that recognition is the shared half, and it is why this is one design with `O117` rather than two.
  - **WHY ONE ITEM AND NOT TWO: two thresholds where one rule belongs is how this gets worse.** `O117` and `O127` are the same property surfacing in the detector and in the matcher; designed apart they would grow two independent notions of "recurring", and the next symptom would get a third.
- **★★ O97 STEP 2 — THE DRAIN, PLANNING HALF (2026-08-28). NO MIGRATION, AND THE REASON IS EVIDENCE.**
  - **★ THE MIGRATION QUESTION, ANSWERED FROM THE LIVE TABLE: NO COLUMNS NEEDED.** `document_intake` already carries every piece of retry state — **`document_id`** says the bytes are durable (the gate step 1 opens), **`status`** is the state machine, **`updated_at`** is BOTH the lease and the spacing gate, **`received_at`** is the age. **And `document_intake_company_status_idx` is already exactly the drain's selection path**, so not even an index.
    - **`attempts` — CONSIDERED AND DEFERRED, with the trigger recorded rather than skipped silently.** It buys a nicer *report* (*"we tried 9 times"* vs *"we retried for 36 hours"*), not a correct *decision*; both statements are true and only one costs a column, and it is additive with a default whenever the reporting proves inadequate.
    - **`next_attempt_at` — REJECTED OUTRIGHT.** The thing we wait for is an AI budget that **resets on a clock hour**, so a fixed interval derived from that IS the schedule; a per-row time column would store the same number redundantly.
    - **★★ AND THE DEEPER REASON THERE IS NO COUNTER: TRANSIENT-vs-PERMANENT COMES FROM THE ERROR, NOT FROM A COUNT.** A 429 is transient however many times it happens; an unreadable file is permanent on the first try. A counter would only bound repeated *transient* failure — which the age box already does.
  - **★★★ AND THE NUMBER THAT MATTERS MORE THAN THE SCHEMA: `document_id` IS NULL ON ALL 150 LIVE ROWS.** Every one predates step 1, so **the drain's real input is EMPTY today** and stays empty until documents arrive under the new ordering. Two consequences, both recorded:
    1. **THE 21 `failed` ROWS FROM THE AUGUST DRIVE ARE PERMANENTLY UNRESUMABLE.** Those are the rate-limited invoices; their bytes only ever lived in `fileStoreRef`. **No drain can recover them — they need re-uploading** — and because `failed` is non-terminal the completeness net will nag about them forever. **Step 1 is forward-looking only, and that must not be discovered later as a surprise.**
    2. **THE SUITE IS WRITTEN AGAINST A POPULATION THAT DOES NOT EXIST YET, WHICH IS THE C195(7) TRAP EXACTLY.** So the assertions are **positive** — a specific pick count, a specific census — and one pins that **150 unresumable rows report as UNRESUMABLE, not as "all caught up"**. `planDrain` returns `drainable` and `unresumable` as first-class fields for that reason: **when `drainable` is 0 the honest report is "nothing is resumable", NOT "nothing to do".**
  - **DESIGN CHOICES THAT EARNED A COMMENT:** give-up is evaluated **before** pick, or an old row gets retried once more on its way out and the hold reason is written against an attempt that already failed *(mutation-tested — removing it turns 2 red)* · the box is **36 hours, generous on purpose**, because 240 documents at 20/hour is twelve hours of *legitimate* waiting and a tight box would abandon a healthy onboarding — **erring long costs a slow queue, erring short costs a lost file** · **oldest-first**, because newest-first starves exactly the documents someone has already waited on · an **unrecognised error is PERMANENT**, the conservative direction, since a document held with a stated reason is visible while one retried forever is not.
  - **THE COPY IS DERIVED FROM THE PLAN (§9).** `drainProgressCopy` reads the census: the finish time is **documents remaining ÷ our own hourly rate — arithmetic, not an estimate** — and when the rate is unknown (`O113b`'s shared bucket) it **states the counts and omits the time.** A test pins that it never says *email*, and never emits a spinner in place of a number it does not have.
- **★★ O97 STEP 2 — THE DRAIN, EXECUTION HALF. SHIPPED 2026-08-29 (C206). STUCK WORK NOW RESUMES ITSELF.** `src/lib/intakeDrainIo.js` — thin, injected client, shaped like `shadowIo`. Reads the drainable rows, calls `planDrain`, recovers the bytes from Storage, hands each pick to an injected `enqueue`. **It owns NO second processing path** — picks go back through the same pipeline a fresh drop uses, because a second path is a second place for the two to disagree (·3a).
  - **★★ ITS WRITE SURFACE IS `document_intake`, CHECKABLE BY READING THE FILE.** A test asserts no `.insert(`/`.update(`/`.delete(` of its own (every status write goes through the verified `setIntakeStatus`), no storage `.upload(`/`.remove(`, no booking primitive, and that `.from(` names only `document_intake` and `documents`.
  - **★★ THE RETRY CARRIES THE ORIGINAL `intake_id` AND NEVER CALLS `logIntake`.** A second arrival row for one document would corrupt the population the completeness ledger exists to independently own (O60). **A retry is the same arrival, tried again.** Mutation-verified: mint a fresh id → 2 red.
  - **★ `excludeIntakeIds`** — a file still genuinely running in THIS tab whose lease expired would otherwise be picked up and processed twice. **That is the `O123` double-post shape wearing an intake row.** Mutation-verified.
  - **★ "NOTHING TO FETCH" IS NOT "THE FETCH FAILED".** No `storage_path` → HELD with a stated reason; a download blip → left alone and retried. Collapsing them abandons a file over one network error. Mutation-verified.
  - **THE CATCH NOW CLASSIFIES INSTEAD OF COUNTING.** Every error used to land in `failed` (non-terminal), so an unreadable file was retried forever **and** the completeness net nagged about it forever. Now transient → `failed` (the drain retries; the age box, not a counter, ends it) and permanent → `held_for_review`, terminal and visible with the reason.
  - **THE COPY IS WIRED TO REAL NUMBERS, OR ABSENT.** `drainProgressCopy` existed, was tested, and was read by nobody — so `readDrainCensus` counts the actual population rather than handing it an invented denominator. **An unreadable census reports NOT-OK, never zero** — zero would render *"Nothing waiting."* over a full queue. `perHour` is null on purpose (`O113b`), so the sentence omits the finish time rather than inventing one.
  - **▶ FORWARD-LOOKING ONLY, AND IT MUST NOT BE DISCOVERED LATER AS A SURPRISE.** The 21 `failed` rows from the August drive have no `document_id` — their bytes only ever lived in `fileStoreRef`. They are counted as `unresumable` and the copy says they would need uploading again, **rather than reporting the queue as caught up.**
- **★ O110 — DIAGNOSIS CLOSED 2026-08-23. Origin EXTERNAL and unexplained; every in-app door ruled out.** The audit log for the window returned **zero rows** (run with full UUIDs). Every in-app materialisation path writes an audit event, so silence is dispositive — combined with the two refutations (legacy COA generation: `008` seeds the same modern chart; QBO import: `matchAccount` and the mapping UI can only yield codes from OUR chart), the 36 accounts came from **outside the application**: direct SQL, a fixture script not in this repo, or a pre-audit tool version. The March timestamps line up with fixture-company setup. **No further archaeology** — the remediation is the same whichever it was.
  - **★ SEVEN MATERIALISATION SITES, NOT FIVE — and the count kept rising because each guard was scoped to where we had already looked.** Five in `App.jsx`, plus **`QBOImportView.jsx`** (a hand-rolled insert, invisible to a guard that only knew `buildAccountInsert`) and **`addCustomAccount`** (audited as `coa_added`). **The CI guard I wrote on 2026-08-17 read only `App.jsx` and reported "all five sites covered" — true, and useless.** A test scoped to where you already looked will always tell you that you have looked everywhere. It now scans all of `src/` and separately forbids the raw-insert SHAPE, because the sixth site was invisible by construction rather than by oversight.
  - **★ (a) FIXED 2026-08-23 — the QBO import was silently broken, and the silence was the defect.** `QBOImportView.jsx` inserted an `account_type` column that does not exist, inside a bare `try {} catch {}`. The insert threw, the throw was eaten, `acctId[code]` stayed undefined, and the downstream guard `if (!pId || !oId) { failedN++; continue; }` dropped every row needing that account — reported to the operator as a bare "N failed" with no cause anywhere. **A silent NON-booking, not a wrong booking**: it fails safe and opaquely, which is exactly the combination that survives seven drives unnoticed. Now: shared shape, error surfaced to the operator by name, creation audited.
  - **★ (b) ▶ HOLD — RESTORING THE FINGERPRINT NEEDS A DECISION, and it is not a small one.** `system_role IS NULL` meant "invented at runtime"; 36 unexplained rows retire that meaning, and the detector was the durable outcome of O108. Three mechanisms, costed:
    1. **`accounts.origin` column** (`seed|runtime|external|import`), migration + backfill. Cleanest semantically — the detector becomes `system_role IS NULL AND origin <> 'external'` and stays honest as new doors appear. Costs a column on a core table and a decision about what existing rows are.
    2. **Role-bless the 36.** Cheapest to write, **and probably wrong**: they are a FOREIGN chart, so `5300 Utilities` and `6200 Utilities` would both want `utilities` on the same company — and `byRole` is a plain object, so one would silently shadow the other. **That is a new silent-wrong-account bug introduced to fix a detector.** Not recommended without a uniqueness constraint that does not currently exist.
    3. **Quarantine — `active = false` on the 36**, if they carry no live lines. Restores the detector by removing the noise rather than relabelling it, and they sit on fixture companies rather than Franklin Ave. **Needs a query first:** do any of the 36 carry live journal lines? If yes, this option is off.
    - **RECOMMENDATION: (1), and (3) only after the line-count query.** (2) trades a diagnostic for a booking hazard.
    - **★ DECIDED 2026-08-23 — OPTION 1 ALONE (→ migration `070`, ▶ HOLD). Quarantine is DEAD.** The line-count query returned **`5300 Utilities` = 1 live line and `6450 Vehicle Expense` = 1 live line** (`6210 Internet` = 0). **Two accounts from a chart nobody in this codebase recognises are carrying real, undeleted journal lines, and because their `system_role` is NULL they are invisible to every `getAccountByRole` lookup in the app.** They sit on fixture companies, not Franklin Ave. `070` LABELS them and deliberately does not MOVE them — reclassifying a booked transaction is an accounting decision, not a schema one.
    - **`070`'s default is `'runtime'`, not `'seed'`, and that is the load-bearing detail.** An INSERT that forgets to say where it came from is, by definition, one of the seven materialisation doors; defaulting to `'seed'` would let the next unaudited door label itself legitimate. The new detector is `system_role IS NULL AND origin <> 'external'`, and it stays honest as doors appear rather than degrading again.
  - **★ FIVE MATERIALISATION SITES, NOT FOUR — the fifth was found by the CI guard, not by reading.** `persistJournalEntry` · `persistMultiLineEntry` · `ensureAccountIdForCode` · `persistRecode` · **`resolveAccountId`** (App.jsx ~2506), which is reached from the **AI action path** (`add_rule` / `set_contact_rule`) — so a model-proposed GL code can mint a permanent account on a client's chart. The test asserting "all sites log" failed on a count of 4 and produced the fifth; that is the guard earning its place on the day it was written.
  - **THE SURVIVING FINDINGS (all four evidenced; none currently producing a wrong number).**
    1. **`044` was never applied and could not be** — it inserts `account_type`, absent from `public.accounts`. The repo's highest-numbered definition of `seed_company_accounts` has never run, so §6's rebuild claim is **false for functions**.
    2. **`anon` holds EXECUTE on a SECURITY DEFINER function.** Live `proacl` is `{postgres=X,anon=X,authenticated=X,service_role=X}`; `009`/`044` both revoke from public and grant only `authenticated`. Not exploitable — `is_company_member` is the first statement and fails for anon — **but the guard is doing the work the grant was supposed to do.**
    3. **`opening_balance_equity` (`3400`) is absent from the live seed.** New companies get no OBE account; it is created on demand at first opening-balance post (`App.jsx:1581`). Works by accident.
    4. **THE ABSORBER, NAMED (this is the durable one).** `useAccounts.js:60-63` resolves a missing role to `DEFAULT_BY_ROLE` — the client's hardcoded chart — returning a plausible account object with a code, **silently, with no warning and no null**. `App.jsx:1448-1458 ensureAccount` then **INSERTS that code** into the company's chart if absent. So a role gap does not fail; it **materialises into a real account row**. Today it absorbs exactly one thing (finding 3). Its significance is prospective: **it would hide the next real divergence exactly as completely**, and for seven attested months nobody knew it was there.
  - **WHAT THE DIAGNOSIS ACTUALLY MEASURED.** 30 distinct literal role lookups across `src/`, **0 missing from live**; 13 dynamically-produced role strings, **0 missing** (the lone apparent hit, `"income"`, is `clarify.js:166` returning a plain-language phrase, not a role); 11 hardcoded `|| "NNNN"` code fallbacks, **all currently agreeing with live**. `constants.js` holds 57 roles to the live seed's 56 — the difference is exactly `opening_balance_equity`.
  - **CALIBRATION IS UNAFFECTED, and the reason is structural.** `vendor_state.attested_account_id` is a per-company uuid FK to `accounts.id` — never a role, never a code. A chart divergence could not have corrupted it even had one existed. `7150` is seeded and backfilled on all 11 companies (verified 2026-08-17), so STRANGER suspense resolves identically everywhere.
  - **THE ONE THING THAT COULD REOPEN THIS.** The whole comparison above is client vs the **SEED FUNCTION**, which describes what NEW companies get and says nothing about what existing ones hold. If older companies carry `cost_of_goods_sold`/`rent`/`owner_equity` rows, the divergence is real **for them** and `getAccountByRole` has been falling back to `constants.js` on every lookup. Query (4) of the diagnosis checks it; **until it is run, this item is diagnosed, not closed.**
  1. **`044` was never applied and could not have been.** It inserts an `account_type` column that does not exist on `public.accounts`. The repo's HIGHEST-numbered definition of `seed_company_accounts` has never run.
  2. **`src/lib/constants.js` and the live chart are different generations.** Live: Common Stock · Product Revenue · role `cogs` · role `rent_occupancy` · **`6300` = Marketing & Advertising**. Client: Owner Equity · Sales Revenue · role `cost_of_goods_sold` · role `rent` · **`6300` = Repairs & Maintenance**. So `rc("cost_of_goods_sold")` and `getAccountByRole("rent")` resolve against roles the database does not have, and `6300` means two different things depending on which side you ask.
  3. **The live ACL is not the one the repo asserts.** `009`/`044` both do `revoke all from public; grant execute to authenticated`, yet live `proacl` is `{postgres=X,anon=X,authenticated=X,service_role=X}` — `anon` holds EXECUTE on a SECURITY DEFINER function. **Not currently exploitable**: the function's first statement is `is_company_member(p_company_id)`, which is false for an unauthenticated caller, so it raises. But the guard is what saves it, not the grant — and the repo believes the grant was removed.
  - **WHY THIS IS BIGGER THAN `O35`.** `O35` is "older companies were seeded by different COA versions" — a data-shape problem. This is the migration chain and the client both disagreeing with production, which makes §6's "empty DB → `000` → `001…036` reproduces the live schema" claim **false for functions**, and makes every future `create or replace` a coin-flip unless it is copied from `pg_get_functiondef` first (the rule now recorded in `063`'s header, learned by writing that file wrong).
  - **▶ HOLD — WHICH CHART IS CORRECT IS AN ACCOUNTING DECISION, NOT A MERGE.** Repointing `6300`, or renaming `cogs`↔`cost_of_goods_sold`, moves where real transactions land. Operator decides; nothing is reconciled until then.
  - **★ BACKFILL APPLIED 2026-08-11 — 10 ROWS STAMPED, NOT THE ESTIMATED 14. The estimate was counting a mixed population.** `stamped_payroll_runs = 10`, count-verified. **Franklin Ave's January and February payrolls predate the register path entirely**: they came in as `source=bank_import` with **en-dash** ACH descriptions and were booked as **net-lines**, not as registers — so the `description like 'Gusto Payroll — %'` (em-dash) filter never matched them, and **that exclusion is correct, not a miss.** Stamping them was considered and **deliberately declined on two independent grounds**: `gross: 4000` would be **false data** on a row whose amount is net and whose gross was never recorded, and `kind:'payroll'` would **wake `matchPayrollBankLine` on rows never shaped for it**. *(Note how the two ·3c fixes interlock: even had they been stamped gross-only, the null-net refusal would have kept them from suppressing anything. Belt and braces, arrived at from opposite directions.)* **Norm history is 10 runs at 4,000 gross** — comfortably enough for condition 5, so nothing is pending on the shortfall. **The "14" was never verified against the database; it was an estimate carried forward through the spec and repeated in the ·3c report without a caveat. Estimates about production data are claims about a query nobody ran.**
  - **★ `ai-proxy` REDEPLOYED 2026-08-11 — v15 → v16 ACTIVE (`2026-08-11 03:42:27` UTC), both assets uploaded.** The working command (and why the bare form fails) is recorded in **§6 → Edge-function deploys**. **The bare `supabase functions deploy ai-proxy` failed**: no `config.toml`, and `.temp/linked-project.json` is not what the CLI keys on — an assumption stated confidently in a report and falsified one command later.
    - **★ (ii) IS DEPLOYED-BUT-UNVERIFIED, AND THAT IS A REAL STATE, NOT A FORMALITY.** A version bump proves the bundle shipped; it proves nothing about whether the model returns `period_end` or whether it survives to `bank_statements.period_end`. **A silent model failure looks EXACTLY like the old behaviour** — the client falls back to the transaction span, which is what it always did. **The August drive owns the proof:** drop a statement whose header states a period end beyond its last transaction and confirm the **stated** date persists. Until then (ii) is shipped, not closed. **DESIGNATED: ROADMAP §0 TIER 1 #12.**
  - **★ DESIGNATED, NOT FIXED — carried out of the ·3c review with anchors, per the orphan rule.**
    - **D2 — the `p_meta` drop is WIDER than payroll and depreciation. DESIGNATED: ROADMAP §2 Bugs `O95`.** *(Filed `O95`, not `O90`: the 2026-08-11 sweep caught `O90`/`O91` ALREADY IN USE by the trust-panel and batch-upload rows — the ids were minted off the end of ONE table instead of scanning all of §2, and this section was already using "O90" with two meanings. Renumbered `O95`/`O96`.)* `App.jsx` puts `tax_amount` into `p_meta` and `controlTotals.js` reads it back, so **the O59 sales-tax cross-foot control total is inert by the identical mechanism**. Likewise `signedPeriod.js` builds `original_date` / `rebooked_from_signed_period` that `persistJournalEntry` never forwards, so the "original date kept on file" promise is not persisted. This is the class the RPC-consolidation item exists to end; do **not** bolt more per-caller stamps on without deciding that first.
    - **D4 — (ii) moved a comparand, expected and fails-safe. DESIGNATED: ROADMAP §0 TIER 1 #12 (drive watch-item).** `stated_ending_balance` is still derived from the **last transaction's** running balance while `booksBalance` is now `asOf` the wider stated `period_end`. A book entry dated after the last transaction but inside the stated period will now break the tie → `attention` instead of auto-reconcile. That is **more** correct (such an entry genuinely hasn't cleared) but it can regress the C198·2 auto-reconcile headline until outstanding-items handling absorbs it. **Watch it on August.**
    - **D5 — one-time transition, self-clearing. DESIGNATED: ROADMAP §0 TIER 1 #12 (drive watch-item).** `reevaluateStatement`'s coverage test requires `st.period_end <= r.period_end`; a re-upload with a widened stated end against a reconciliation created **before** ·3c flips `covered` false. Same shape as the ·3b f3 resurrection wall — expected, one-time, and **no re-sweep path was built** deliberately.
    - **D6 — dead code. DESIGNATED: ROADMAP §2 Bugs `O96`.** `runDepreciationThrough` / `planDepreciationRun` are destructured in `DashboardView.jsx` but absent from `erpCtx`, so they resolve to `undefined`. `autoPostDepreciation` is the sole live poster — which is what kept D3's exposure to a single sequence.
  - **Migrations ledger: UNCHANGED — applied & verified through `061`.** The 2026-08-09 caveat (*"if ·3c's (i) fix goes the `post_journal_entry` route, that adds one"*) is **resolved: it did not.**

- **★★ `080` APPLIED LIVE 2026-08-30 — THE ANOMALY DISMISSAL GATE. (a)(b)(c)(e) PASS; (d) AND (f) OUTSTANDING, AND SAID SO.** Ledger: through `061`, plus `063`, `064`, `066`, `068`–`080`.
  · **(a)** evidence column + 7-column `anomaly_comments` ✓ · **(b)** RLS on, **1 SELECT / 1 INSERT / 0 mutating policies** — a comment cannot be edited or removed ✓ · **(e)** the live `with_check` reads back exactly as written ✓.
  · **★ (c) PASSED AND THE `[ran as: authenticated]` TAG IS WHAT MAKES IT MEAN ANYTHING:** *"PASS - refused: new row violates row-level security policy"*. **The probe reports the role it actually ran as because the Supabase editor runs as a superuser, which BYPASSES RLS** — the first draft of this probe ran a bare UPDATE and would have reported **`FAIL - a dismissal with NO REASON was accepted` against a perfectly correct migration.** A false FAIL on a correct guard is the mirror of a false PASS on a broken one; both come from testing something other than the thing you named.
  · **★★ AND (d) AS FIRST WRITTEN COULD PASS VACUOUSLY — FOUND BY READING IT, NOT BY RUNNING IT.** Under RLS an UPDATE whose `USING` clause hides the row touches **zero rows and raises nothing**, so *"no exception"* would have been recorded as a pass by a probe that never reached the thing it was testing. It now `get diagnostics row_count` and reports **INCONCLUSIVE on zero**, never PASS. Same shape as C195(7)'s guard whose input was always empty.
  · **★★★ (f) RETURNED `PASS - a non-reviewer (owner) was refused` — THE REVIEWER GATE IS PROVEN END TO END, AND THE TECHNIQUE MATTERS MORE THAN THE RESULT.** (d) returned PASS with `row_count = 1`, so the housekeeping was genuinely exercised.
  · **★★★ TWO OF THE THREE PASSED LIVE 2026-08-30, RUN AS `authenticated`.** **TIER 1 #8's DATABASE HALF IS NOW PROVED** — *"PASS - a non-reviewer (owner) was refused: new row violates row-level security policy for table `period_signoffs`"*, the check the operator's own login structurally could not perform. And **`066` VERIFY (d) is closed**: the one genuinely cross-tenant table in the product refused a normal member's write.
  · **★★ AND s4 CAME BACK `INCONCLUSIVE - saw 0 of their rows AND 0 of my own`, WHICH IS THE ANTI-VACUITY GUARD EARNING ITS PLACE ON ITS FIRST RUN.** The only non-platform-admin member owns a company with no entries. **Without the `mine = 0` branch it would have printed PASS and s4 would have been closed on a probe that proved nothing** — the exact failure the guard was written against, occurring immediately. The probe now SEEDS a row in the borrower's own company inside the transaction it rolls back, so `mine > 0` means *RLS let this user see a row that genuinely exists in their company* — the real counterpart of `theirs = 0`. **Re-run: `PASS - saw 1 of my own rows and 0 of theirs [ran as: authenticated]`. ALL THREE PROBES PASS.**
  · **▶ WHAT s4 STILL OWES, STATED PRECISELY: the rung asks for an AUTOMATED test, and this is a REPRODUCIBLE SCRIPT.** That is a real advance over *"the memory of an afternoon"* — it has a verdict, it refuses to pass vacuously, and anyone can re-run it — **but a script nobody runs decays exactly like a memory.** Automating it needs live credentials in CI, which is its own decision; until that is made, s4 stays open and the evidence is dated.
  · **★★★ THE UNLOCK: THREE ITEMS PARKED ON "WE NEED A NON-PLATFORM-ADMIN TEST USER" WERE NEVER BLOCKED ON THE ACCOUNT — THEY WERE BLOCKED ON THE TECHNIQUE.** Borrowing another member's uid via `request.jwt.claims` + `set_config('role','authenticated')` makes the policies evaluate for THEM, from the SQL editor, today. `supabase/verify/nonreviewer_rls_probes.sql` now carries all three: **TIER 1 #8**'s sign-off DB-layer rejection (never proved, because the operator's own login *cannot* fail it), **`066` VERIFY (d)**'s cross-tenant directory write, and **s4**'s cross-tenant read — which had been *"the memory of an afternoon"* since 2026-08-09.
  · **★★ AND THE FIRST DRAFT OF THOSE PROBES WOULD HAVE REPORTED A TYPO AS THE WALL HOLDING.** They caught `when others` and called it PASS — **and probe (2)'s insert named three columns that do not exist on `universal_vendor_directory`**, so it would have "passed" without the policy being consulted at all. Only `insufficient_privilege` (42501) counts; anything else is INCONCLUSIVE. **This is `076`'s recorded rule — any other error is reported as ITSELF — and I had to be taught it twice in one file.**
  · **★★ THE REVIEWER HALF IS PROVABLE TODAY, AND I HAD RECORDED THE OPPOSITE. `is_platform_admin()` RESOLVES THE EMAIL FROM `auth.uid()`, SO THE BYPASS IS KEYED ON WHICH USER THE PROBE BORROWS — NOT ON WHO IS SITTING AT THE EDITOR.** Borrow a member who is neither the platform admin nor admin/accountant (an OWNER of their own company qualifies — `is_company_reviewer` is admin-or-accountant only) and give the dismissal a **perfectly good reason**; the only thing left that can refuse it is the reviewer test. Probe (f). **The original claim — "the reviewer half cannot be proved from that account at all" — was true of the OPERATOR's uid and I generalised it to the session.** Recorded rather than quietly corrected: it is the same over-broad move as the `O113` retraction, one layer down.
  · **THE DISMISSAL GATE ITSELF WAS A REAL FIND, NOT A FEATURE.** `056` states in its own header that reviewer-only dismissal was UI-enforced and the DB boundary was membership. Dismissal clears a flag **and** feeds `priorDismissalFor`, which downgrades later flags for the same vendor and amount — **so a viewer could lower the guard against a repeat of the thing they cleared.** Same class as `061` and `078`.
  · **THE POLICY DISCRIMINATES BY THE RESULTING ROW (the `079` lesson, applied deliberately this time):** auto-resolve, sign-off expiry, reopen and `last_seen` bumps all run as whoever is logged in — **including an OWNER, who is not a reviewer** — so a blanket requirement would have switched off anomaly housekeeping for every owner-run company. Only a row that ends up `dismissed` needs a reviewer. **(d) is the check that proves that, which is exactly why it is not being written off as a formality while it is unrun.**

- **★★★ `O35` — AUDIT RUN LIVE 2026-08-30, AND IT CLOSES THE ITEM. THE THING `O35` WAS ABOUT DOES NOT EXIST ANY MORE.**
  - **★★★ QUERY 2 RETURNED NO ROWS AND QUERY 4 CONFIRMS IT: `roleless_ours = 0`.** Every account this application created carries a role. **`O35`'s "safe half" — v1-era accounts with NULL roles, invisible to `getAccountByRole` — is EMPTY.** The 36 role-less accounts are the `O110` foreign-chart set, exactly as predicted and already excluded by design.
  - **★★ AND THE ONE VARIANT IS HARMLESS BY CONSTRUCTION, WHICH IS THE ROLE SCHEME EARNING ITS KEEP.** `payroll_tax` sits on **`5101` on some companies and `6010` on others, across 11** — the v1/v2 split the item itself named. **Both carry the role, so `getAccountByRole('payroll_tax')` resolves on either**, and companies are separate tenants so cross-company code comparability buys nothing. **§4's whole argument was "users renumber, so never hardcode a code" — this is that argument being VINDICATED by a live audit rather than asserted.** Renumbering would re-point journal lines to fix something that is not broken.
  - **★★ THE REAL FINDING IS IN QUERY 1, AND IT IS A DIFFERENT ITEM: `opening_balance_equity` IS MISSING ON SEVEN OF ELEVEN COMPANIES.** That is `O108` finding 3 confirmed live — it is absent from the seed and reaches the chart only when someone posts an opening balance, via `ensureAccount`. **It works by accident, through the materialise-mid-flight path `C223`/`C254` exist to avoid.** Added to the universal template list on that evidence — **and then checked how far that actually reaches, which was less far than it sounded.**
  - **★★ `applyCoaTemplate` RUNS FROM EXACTLY ONE PLACE: saving the business profile.** So the template change covers a NEW company that completes onboarding, and **nothing else** — the nine existing companies would not have got it, and neither would a company that never sets a business type. **Stating that rather than letting "added to the template" imply "fixed everywhere".**
  - **MIGRATION `083` ▶ HOLD closes the existing-company half:** additive only, skipping on **the role OR the code** (a chart may hold it under a different number — the very reason the app resolves by role — or hold `3400` under a different name; either way inserting would create a duplicate `byRole` then picks between silently). Labelled **`origin = 'seed'`**, because `070` defaults to `'runtime'` deliberately and **an unlabelled backfill would arrive in the `O108` detector as accounts the system appears to have invented on its own.** Its VERIFY checks for **duplicates, not presence** — *"at least one"* is not the check when duplication is the plausible failure.
  - **★★★ APPLIED & VERIFIED LIVE 2026-08-30: (a) 11 companies, fewest 1, most 1 · (b) 0 lines on the new accounts · (d) idempotent. AND (c) RETURNED A FALSE FAIL, WHICH WAS MY CHECK BEING WRONG.** It asked whether EVERY opening-balance account is labelled `seed` — but pre-existing ones carry whatever origin they were created with, and **one is `runtime` BY A RECORDED DECISION**: `073` deliberately left Franklin Ave's that way, because *"origin records HOW A ROW GOT HERE"* and relabelling would erase the only durable evidence the materialisation path ever fired on a real client's chart. **The check asserted a property a deliberate decision violates.**
  - **★★ AND A NUMBER I STATED REPEATEDLY WAS WRONG, CORRECTED BY THE MIGRATION'S OWN OUTPUT.** I said **"seven of eleven"** companies lacked the account — in the report, in `CLAUDE.md`, in `ROADMAP.md` and in the migration's header. **`083` created NINE.** Counting the audit's rows properly: three companies missing 31 roles plus six missing three, all including `opening_balance_equity`; Northwind was missing only two and not that one. **I counted ten rows by eye and got the subset wrong** — §11's own rule, *"estimates about production data are claims about a query nobody ran"*, applied to me, on the same day I wrote a query specifically to avoid it. *(Precedent kept deliberately: `·3c`'s "14 payroll runs" was an estimate carried forward through a spec and a report until the backfill said 10.)*
  - **★★ AND VERIFY (b) TWO LINES ABOVE IT SCOPED CORRECTLY — same file, same migration, one check scoped to the rows it created and one not.** The unscoped one failed correct data. **A false FAIL on a correct migration is the mirror of a false PASS on a broken one**, and this is the second time today the same shape appeared (the `080` probes that would have reported a superuser bypass as the guard failing). Corrected to scope by `created_at`, and a test now pins that the origin check is scoped.
  - **▶ AND THE HALF IT DOES NOT CLOSE IS NAMED: `seed_company_accounts` STILL LACKS `3400`.** A company that never saves a business profile still relies on the create-on-demand door. Editing that function requires starting from `pg_get_functiondef` (§6, five definitions in the repo and the newest never applied) — **its own task with its own verification, and bundling it into a backfill is how `063` nearly dropped 17 accounts.**
  - **`merchant_processing_fees` and `bank_service_charges` missing on EIGHT of eleven** is `O112` confirmed on live data — already fixed by `C254`, and the audit is the evidence that the fix was aimed at something real.

- **★ THE `O98` READ-SIDE SWEEP — ONE REAL INSTANCE, ONE FALSE POSITIVE, AND THE DISCRIMINATOR IS THE USEFUL PART (2026-08-30). NO CODE BEYOND C250.**
  - **57 reads in `src/` destructure `{ data }` and drop `error`** — the read-side twin of the unchecked write. **Fixing all 57 would be churn:** a failed read usually yields an empty list on a screen the user can simply reload, and nothing is claimed about it.
  - **★★ IT MATTERS ONLY WHERE THE EMPTY RESULT ITSELF BECOMES A SENTENCE.** That is the whole of `O98`, stated as a test you can apply: *does anything on screen assert something about the world because this came back empty?* **`TeamView` did — "nobody is on this team" — and it is fixed (C250).** `MonthlyReportsPanel` looked identical to the sweep and **is not**: its empty state is derived from the LEDGER (`invoices`), not from the query whose error is dropped; that query only fetches stored AI narratives, and the figures recompute live either way.
  - **RECORDED AS A FALSE POSITIVE ON PURPOSE.** A sweep that reports only its hits teaches nothing about where to look next; **the discriminator is not "was the error dropped" but "is the empty result load-bearing for a claim"**, and the two cases only separate when you trace what feeds the sentence.

- **★★ `O131` — A SOLO SIGNUP CAN NEVER SIGN OFF, AND THE OLD NOTE SAID THE OPPOSITE (2026-08-30). PRODUCT DECISION, NOT A DEFECT; NO FIX PROPOSED.**
  - **ESTABLISHED IN CODE AND CONFIRMED LIVE:** `create_company` gives the creator `'owner'` · `canAttestPeriod` = `admin || accountant`, with a comment saying the owner may NOT self-attest (the O83 separation fix) · `is_company_reviewer` matches it exactly · `company_users` is UNIQUE on `(company_id, user_id)`, **so one person holds one role and an owner cannot also be an accountant** · `081` closed the last accidental route (self-promotion through the API). **Probe (1) this morning watched an owner be refused on `period_signoffs`.**
  - **★★ SO THE CONSEQUENCE IS: A SOLO CLIENT'S TRUST PANEL SAYS "AWAITING YOUR ACCOUNTANT'S SIGN-OFF" PERMANENTLY, AND NOTHING EXPLAINS WHY.** Correct for the CPA-led model this product is built around; a dead end for anyone who arrives alone.
  - **★ AND `O93`'s LEDGER TEXT IS STALE IN THE DANGEROUS DIRECTION — it describes the PRE-`053` state** (*"gated to owner+admin via `is_company_admin`… correct for the solo case, deliberately left as-is"*) and calls the separation **not started**. It shipped in `053`. **The note has been describing a hole that was closed, while the real consequence — the solo dead end — went unrecorded.** *(Tenth stale item this week, and the first whose staleness pointed at a live product gap rather than at finished work.)*
  - **DISCLOSURE `D2` REVERSED, NOT CLOSED.** It said *"you would be signing off your own books"*. The truth is the opposite and it is the more awkward sentence: *"you cannot sign off your own books at all."*

- **★★★ `081` — THE MEMBERSHIP TABLE HANDED OUT ADMIN. FOUND BY CHECKING WHETHER A LEDGER ITEM WAS STALE (2026-08-30). ▶ MIGRATION WRITTEN; PROBE FIRST.**
  - **THE ROUTE IN IS THE POINT: I WAS CHECKING `O93`** (*"the client-owner must not sign off their own books"*), **which IS stale** — it describes gating via `is_company_admin`, but `053` replaced that with `is_company_reviewer` (admin or accountant, **owner excluded**), and probe (1) confirmed live that **an owner IS refused on `period_signoffs`**. **Asking how that could be worked around is what surfaced this.**
  - **`001` GAVE `company_users` THE FOUR STANDARD POLICIES, EACH CARRYING `user_id = auth.uid()` BESIDE THE ADMIN TEST**, intended for accepting an invite. Unscoped, it says considerably more:
    1. **UPDATE — SELF-PROMOTION.** `role` is on the row a member may update. **A `viewer` can make themselves `admin`** — the entire role vocabulary C225 repaired, undone in one call. **And an OWNER can make themselves `admin`, hence a REVIEWER, hence able to sign off their own books** — reaching `O93`'s hole through legitimate admin power rather than through a missing gate.
    2. **★★ INSERT — JOINING A COMPANY YOU WERE NEVER INVITED TO.** `with check (user_id = auth.uid() or …)` **does not constrain `company_id`.** An authenticated user may insert `(company_id = <any>, user_id = self, role = 'admin', accepted_at = now())`, and **`is_company_member` asks for exactly that row** — so every tenant table opens. The only obstacle is knowing a uuid, which is **not a secret and not a boundary**.
  - **★★ WHY THE TIER 1 AUDIT DID NOT CATCH IT, AND THIS IS THE DURABLE LESSON. It asked *"is RLS enabled, and does a policy exist, for every table carrying `company_id`?"* and answered 33 of 33, correctly. IT NEVER ASKED WHAT THE POLICIES PERMIT.** A table can be fully covered and still hand out admin. **Coverage is not a property of the wall; it is a property of the checklist.** *(And `052` sits one inch away — it fixes this exact shape on `companies` and `users`, and states in its own header: "Does NOT touch … the company_users policies.")*
  - **NEITHER CLAUSE IS USED BY ANYTHING.** Every membership write goes through a SECURITY DEFINER function (`create_company`, `accept_invite`) that bypasses RLS; **`src/` never writes the table at all** — pinned by a test that walks the tree rather than by a claim.
  - **★ THE FIX IS A POLICY *AND* A TRIGGER, AND THE SPLIT IS FORCED: RLS `WITH CHECK` CANNOT SEE THE OLD ROW.** A policy can express *"the new role is admin"*; only a trigger can express *"the role CHANGED"*. Refusing self-UPDATE outright would be the `079` mistake — blocking more than we mean, surfacing when someone tries to do their job.
  - **★★ AND IT REFUSES CHANGING YOUR OWN ROLE EVEN AS AN OWNER OR ADMIN. That is `O93` itself, not belt-and-braces:** you may manage other people's roles, not your own. An admin needing their own role changed asks another admin, which is the correct amount of friction for that operation.
  - **★★★ PROBE (B) RAN LIVE 2026-08-30 AND RETURNED `FAIL - joined another company as admin and can now read 224 of its journal entries [ran as: authenticated]`. THE TENANT-ISOLATION HOLE IS DEMONSTRATED, NOT INFERRED** — one company's member reached another company's books, and the damage is stated as a COUNT rather than as a mechanism.
  - **★★ AND PROBE (A) IS A WEAKER RESULT THAN IT LOOKS — SAY SO. It returned `FAIL - a admin promoted THEMSELVES to admin`: THE SPECIMEN IT BORROWED WAS ALREADY AN ADMIN**, so the role did not change. That proves the role column is **self-writable**; it does **not** demonstrate **ESCALATION**, which is one inference further than the observation. *"The update was permitted"* and *"a viewer can become an admin"* are different claims and only the second is the finding. **(A2) added, requiring the borrowed member to be BELOW admin** so a success is a real promotion — and falling back to the OWNER case, where promotion to `admin` confers REVIEWER and is `O93` exactly. **Recorded rather than quietly re-run: this is the same over-claim shape as the `O113` retraction and the "reviewer half is unprovable" slip, both caught the same week.**
  - **★★★ (A2) RAN LIVE AND CLOSED THE GAP (A) LEFT: `FAIL - a owner promoted THEMSELVES to admin (and is now a reviewer, so can sign off their own books) [ran as: authenticated]`.** So the escalation is **observed, not inferred** — and the specimen is the OWNER case, which means **`O93`'s hole is reachable through the MEMBERSHIP table even though the SIGN-OFF gate itself is sound.** Probe (1) this morning proved the database refuses an owner on `period_signoffs`; **this proves the owner can simply stop being an owner.** *(Worth keeping as a shape: a gate can be correct and irrelevant if the ROLE it reads is writable by the person it is gating.)*
  - **★★★ `081` APPLIED & VERIFIED LIVE 2026-08-30. BOTH HOLES CLOSED, WATCHED TO REFUSE.** (A2) → *"PASS - refused: **You cannot change your own role. Ask another admin on this company to do it.**"* — the trigger's own sentence, which is what a person would actually get. (B) → *"PASS - refused: new row violates row-level security policy for table `company_users`"*. **Ledger: through `061`, plus `063`, `064`, `066`, `068`–`081`.**
  - **★★ AND (C) — THE "DID IT BLOCK TOO MUCH" CHECK — CAME BACK INCONCLUSIVE, WHICH IS NOT A PASS.** It required a company already holding an admin AND a second member; **no company has two.** So the `079` direction is **unproven**, and (A2)+(B) passing is still formally consistent with *"we broke role management entirely"*. **RE-RUN AND PASSED: *"PASS - an admin can still change someone else's role [ran as: authenticated]"* — so the guard refuses exactly the two things it names and nothing more. `081` IS FULLY VERIFIED: THREE CHECKS, THREE PASSES.** Rewritten to **create the second member itself** inside the transaction it rolls back — the seed row goes in as the superuser before the role switch, and the trigger under test fires on the UPDATE regardless. **The dependency on the data happening to be shaped right was the defect in the check, not a fact about the fix.**
  - **▶ THE PROBE RUNS FIRST AND IS EXPECTED TO SAY *FAIL*.** `supabase/verify/081_self_escalation.sql` demonstrates both holes in a rolled-back transaction before the migration is applied — **this whole finding is currently READ FROM POLICY TEXT, and this project's standard is that a claim about the database is a claim about a query somebody ran.** (B) reports **the consequence, counted** — *"can now read N of its journal entries"* — rather than the mechanism. (C) checks the `079` direction after.

- **★★★ O131 DECIDED & BUILT 2026-08-30 (C272; migration `085`) — A SOLO OWNER MAY SIGN WITH AN ACKNOWLEDGEMENT.** Operator's call. **It needed a MIGRATION, not a checkbox:** `053` gates `period_signoffs` on `is_company_reviewer` and the database means it — probe (1) watched an owner refused that morning — so a UI-only fix would have rebuilt the C225 team-invites failure exactly: the whole flow present and the last step impossible.
  - **★★ IT GRANTS THE ACTION, NOT THE ROLE, AND NOT THE SEAT.** `053` step 3 met this same case once by setting `role = 'admin'` on the owner of any reviewer-less company — right for a one-time backfill, wrong now: **permanent, so a company that later hires an accountant never gets the separation back, and un-undoable since `081` blocked self-role-changes.** `085` evaluates the condition AT WRITE TIME, so **the separation returns by itself the moment a real reviewer joins, with nobody changing a setting.**
  - **★★★ AND `canAttestPeriod` WAS DELIBERATELY NOT WIDENED, WHICH WAS THE ONE-LINE FIX AND WOULD HAVE BEEN WRONG.** `nav.js isReviewerSeat` derives the CPA cockpit from it, so widening it drops a client into the ten-tab reviewer workbench — the exact IA the North Star exists to keep them out of. `canSelfAttest` is its own predicate and a test pins that `canAttestPeriod('owner')` and `isReviewerSeat({role:'owner'})` both stay false.
  - **★★★ THE FLAG CANNOT LIE — THE POLICY DECIDES IT, NOT THE CLIENT.** A `CASE` in `with check` accepts a row marked `self_attested` ONLY from a solo owner and an unmarked row ONLY from a reviewer. **So the column is a property of how the write was permitted, not a description written alongside it** — §9's describe-from-the-record rule enforced by the database. Without it an owner could record an accountant's review that never happened, which is the one lie this table must never be able to tell.
  - **★ THE CONTROL LIVES ON THE OWNER'S HOME, BECAUSE AN OWNER CANNOT OPEN `ReviewView` AT ALL.** Putting it on the reviewer's screen would have been the third instance this year of a flow whose last step is unreachable.
  - **★ THE ACKNOWLEDGEMENT STATES THE TWO NON-OBVIOUS FACTS: nobody else has checked, AND the month locks.** The lock clause is not boilerplate — `078` makes the database refuse changes to a signed month, and someone who signs without knowing that finds out when a correction is rejected, which is the worst moment to learn it.
  - **★★ AND THE PANEL NOW SAYS WHO SIGNED.** *"Reviewed and signed off"* and *"you signed these yourself — no accountant has reviewed them"* are **different facts**, and the whole value of a sign-off is who stood behind it. Read off the ROW rather than inferred from current membership, so a company that later hires an accountant keeps telling the truth about its self-signed history. Four mutations, four reds — one of which had to be re-run because shell escaping meant the anchor never matched.

- **★★★ THE FIRST LIVE THROUGHPUT MEASUREMENT — 20 DOCUMENTS AN HOUR, AND THE ARITHMETIC CLOSES ON EXACTLY 60 (Red River drive, 2026-09-02).** 35 documents dropped at 04:35; **15 fully processed** (13 booked, 2 correctly held for a question) and **20 refused** by our own AI limiter, each carrying its real trailing-window reset (*"resets in about 56 minutes"* — `086` working on a live backlog for the first time).
  - **★★ EVERY NUMBER FITS, WITH NOTHING LEFT OVER: 15 documents × 3 calls = 45, leaving 15 of the 60.** Those 15 are 15 of the 20 failures getting their **classify** call through and being refused on **extract**; the last 5 were refused at classify and cost nothing. **15 + 5 = 20 failures · 45 + 15 = 60, the ceiling exactly.**
  - **★★★ SO THREE CALLS PER DOCUMENT IS NOW MEASURED RATHER THAN ASSUMED** — `AI_CALLS_PER_DOCUMENT = 3` was a conservative guess and is the real figure, which is what makes the "documents remaining" display honest rather than optimistic.
  - **★★★ AND `O113a` IS INDEPENDENTLY CORROBORATED BY PRODUCTION DATA FOR THE FIRST TIME.** Were refusals still charged, the 20 refusals would have driven the counter to ~80 and the arithmetic could not have landed on 60. **The limiter does not charge for work it declines** — proved in a rolled-back probe when it shipped, and now proved again by a real burst that never knew it was being watched.
  - **★ THE THROUGHPUT IS THE `O97` NUMBER, MEASURED: 20 documents/hour**, so a genuine onboarding at 120–240 documents is **six to twelve hours**. Not a defect — the design. **It is also why the drain is the whole ballgame: at that rate EVERY real client's first upload hits the wall**, and the only question is whether the queue quietly finishes or drops it. On this drive it dropped 20, for the single reason recorded in `O133`.
  - **★★ AND A DIAGNOSTIC TRAP WORTH KEEPING: `rate_limit_events` CANNOT ANSWER A RETROSPECTIVE QUESTION, BY DESIGN.** `086` prunes anything outside the window on every call — *"anything outside the window is not evidence any more"* — so a burst's events are gone within the hour. **Throughput must be captured DURING a run or derived from the outcome, never queried afterwards**; a query the next morning returns one row and looks like the limiter never fired.

- **★★ THE REVIEW-SCREEN PASS — SIX FINDINGS, 2026-09-03. Recorded, not fixed.**
  - **★★★ (1) FIXED 2026-09-03 (C305) — THE SALES-TAX CONTROL TOTAL COUNTED TAX WE *PAID* AS TAX WE *OWE*, SO IT COULD ONLY EVER FAIL ON A PURCHASE-ONLY MONTH.**
    - **★★★ AND "REVENUE-BEARING" WAS ALREADY IN THE FUNCTION'S OWN COMMENT, WITH THE HELPER DEFINED ONE LINE ABOVE AND CALLED BY NOTHING.** `glIsRevenueCode` existed, was never invoked, and the comment described the filter as though it were there — the `O123` shape exactly, where a comment asserts behaviour the code does not have. **The fix is the line somebody meant to write.**
    - **★ REVENUE-BEARING IS A PROPERTY OF THE ENTRY, NOT OF THE ROW.** A taxed sale is `Dr A/R / Cr Revenue / Cr Sales Tax Payable` and arrives as several expanded rows sharing one base id, so the revenue leg may not be the row carrying the tax figure. Collected in a first pass; a mutation that checks only the current row kills 7 tests.
    - **★★ AND THE NEGATIVE CASES CARRY THE WEIGHT, because "the purchase month ties" is equally satisfied by returning 0 always** — which would silently retire the `O59` control this check exists for. Pinned: a real sale is still counted, a real month counts only the sales side (8, not 22.80), and the mis-posted-to-revenue failure still fires. Three mutations, three reds.
    - *(original finding:)* **THE SALES-TAX CONTROL TOTAL COUNTS TAX WE *PAID* AS TAX WE *OWE*, SO IT CAN ONLY EVER FAIL ON A PURCHASE-ONLY MONTH. CONFIRMED IN CODE.** `taxChargedOnInvoices` sums `import_metadata.tax_amount` over **every** live entry with no filter on direction, and compares it to `Sales Tax Payable` — a liability for tax **we collect on OUR sales**. Tax paid to a supplier is part of the expense and is owed to nobody. Live: *"tax charged on invoices is $14.80, sales tax owed is $0.00 — off by $14.80"*, on books that are correct. **`severity: high`, so it degrades the trust panel and blocks a clean sign-off** — the `079`/C291 direction, a guard firing on good work, in the check `C241` repaired without ever questioning its population. **The fix is the SIDE: only outgoing (AR) invoices charge sales tax we owe.**
  - **★★ (2) FIXED 2026-09-06 (C306) — THE DUPLICATE CARD CONTRADICTED ITSELF AND FIRED TWICE ON ONE PAIR.**
    - **★★★ ONE CAUSE FOR BOTH SYMPTOMS: THE LOOP VISITS EVERY PAIR TWICE — ONCE FROM EACH ROW — AND THE DEDUPE KEY CARRIED `cents(i.amount)`, THE AMOUNT OF WHICHEVER ROW IT ARRIVED FROM.** `findDuplicate` matches within 1%, so a pair with two different amounts produced two keys and emitted two cards, headed **$632.30** and **$629.04** — which is also why the two headlines disagreed. The key is now symmetric over the pair (both amounts, sorted), and the same defect was in the off-rhythm branch two lines up.
    - **★★ AND THE SENTENCE ASSERTED THE AMOUNTS WERE EQUAL, DIRECTLY ABOVE THE TWO THAT WERE NOT.** The detector's tolerance is 1%, so unequal is the ORDINARY case — **the copy was describing a stricter rule than the one that fired.** It reads both rows now, and keeps the single-figure wording when they genuinely match (pinned, because "name both amounts" must not become "always print two numbers", which would read as a discrepancy where there is none).
    - **★★ A SURVIVING MUTATION CORRECTED MY OWN COMMENT AGAIN.** I wrote that the numeric comparator prevents an order-dependent key; **swapping to lexicographic `.sort()` left everything green, because ANY total order is symmetric.** The sort is load-bearing, the comparator is legibility. The test that claimed to cover it now says what it actually covers — a pair straddling a digit boundary is still one card. *(C255's rule a third time: a surviving mutation is a question, and sometimes the answer is that the line could not have mattered.)*
    - **★ THREE EXISTING TESTS WENT RED, ALL CORRECTLY, AND ONE WAS FRAGILE IN A WAY WORTH FIXING RATHER THAN REPOINTING.** `recurringVendor`'s anti-vacuity check identified WHICH rule fired by matching *"within a week"* in the body — a phrase that now appears only when the amounts DIFFER, which is a property of the figures and not of the rule. It discriminates on the TITLE now. The other two pinned the literal fingerprint; **the assertion beneath one of them runs `anomalySubjectPeriod` against the new key, so the date-tail parser feeding the HIGH-blocks-sign-off gate is PROVED to still place these — not assumed.**
    - **▶ ONE-TIME CHURN, EXPECTED: open `dup:` rows carry the old fingerprint**, so they auto-resolve and re-open under the new key on the next reconcile — the `C198·3b(f3)` resurrection wall, same shape, same reason.
    - *(original finding:)* **THE DUPLICATE CARD CONTRADICTS ITSELF, AND FIRES TWICE ON ONE PAIR.** Two HIGH cards for the same Hill Country pair, one headed *"Two charges … for $632.30"* and the other *"…for $629.04"* — while both list the SAME two rows, **$629.04 and $632.30**. The detector matches within 1%; the SENTENCE asserts the amounts are equal. Neither statement is true, and the pair is emitted twice. §9 world-claim plus a fingerprint that is not idempotent across near-amounts.
  - **★★ (3) THE A/P CONTROL TOTAL IS OFF BY EXACTLY THE HELD SABINE AMOUNT.** *"open bills $27,053.03, payables balance $31,678.03 — off by $4,625.00"*, and $4,625.00 is Sabine Kitchen Equipment, one of the eight documents held for a question. So a document awaiting an ANSWER appears to reach payables while being excluded from the open-bills universe. **Needs a query before it is called a defect** — but an exact match to the cent is not a coincidence worth ignoring.
  - **✅ (4) ALREADY CLOSED BY `C302` — CONFIRMED ON THE LIVE BOOKING, NOT ASSUMED.** Gusto booked `Dr 6000 Salaries & Wages / Cr 2000 A/P`, and `couldBeCapital` returns false for `salaries_wages`, so the large-charge detector can no longer ask whether payroll is equipment. *(The account gate fixed a case the payroll-source exemption never reached, which is why gating on the ACCOUNT was the right axis.)*
  - **★★★ (3) FIXED 2026-09-06 (C309) — AN OPEN BILL IS ONE WITH AN A/P LEG, NOT ONE WHOSE DEBIT IS AN EXPENSE.**
    - **`apUnpaid` READ `isExp(i) && !paid`, DERIVING OPENNESS FROM THE ENTRY'S P&L CLASS** — the §9 anti-pattern by name (*side is the A/R-or-A/P OFFSET code on the leg, never the type*). Live: Sabine posted `Dr 1500 Fixed Assets / Cr 2000 Accounts Payable`, correct in every respect, and **a fixed-asset debit is not an expense** — so the bill was invisible to open bills while its A/P credit sat in the GL balance and `ap_tie` diverged by exactly **$4,625.00**. **Buying equipment on terms is ordinary**, so this broke for any client who finances anything.
    - **★★ ADDITIVE, DELIBERATELY: the expense rule stays and still carries the overwhelming majority.** Only the non-expense purchase whose OFFSET is A/P is added, so nothing that counted before stops counting — **the change cannot subtract from a figure people already read**, and a test pins that ordinary bills are untouched.
    - **★★★ AND IT IS THREADED TO ALL FOUR CALLERS RATHER THAN THE ONE THAT WAS FAILING.** `controlTotals` had `codes.ap` in scope and fixing only it would have made the control total count Sabine while the AI snapshot and the monthly report did not — **§12's forked-definition warning, arrived at by taking the cheap option.** `aiTools` reaches it through `ctx.getAccountByRole`, the monthly report takes it as a parameter, and a test asserts every call site passes it.
    - **★ WITHOUT A CODE IT BEHAVES EXACTLY AS BEFORE — never a guess.** `2xxx` would sweep in a **loan-financed** purchase, which is a liability and NOT an open bill; the counter-case is pinned. §4's rule (users renumber) makes the company's own code the only honest input.
    - **★ EXPANDED MULTI-LINE ROWS ARE SKIPPED**, since flatten emits several rows that can share one offset and counting them all would double the bill. Stated as the scope limit it is: a multi-line capitalized purchase still diverges, and that is visible rather than silently doubled.
    - **★★ AND A SURVIVING MUTATION REMOVED A LINE OF MINE — the third time this week.** An explicit *"and this row is not the A/P leg itself"* guard could not change any outcome: a bill's A/P leg carries the EXPENSE code as its offset, so it never reaches that test. **It read as a check and was one only in appearance** (C255's shape). Removed; the property stays pinned by the test rather than by a second line. *(One mutation also broke the module outright and produced NO verdict — recorded because §12's rule applies: a module that fails to load is neither a pass nor a fail, and must be re-run properly rather than counted.)*
    - *(original diagnosis:)* **IT IS THE §9 ANTI-PATTERN BY NAME, AND IT BREAKS FOR ANY CLIENT WHO BUYS EQUIPMENT ON TERMS.** Sabine posted `Dr 1500 Fixed Assets / Cr 2000 Accounts Payable` — correct: the freezer is capitalized and the money is owed. **But `apUnpaid = isExp(i) && payment_status !== "paid"` derives openness from the entry's P&L class**, and a fixed-asset debit is not an expense — so the bill is invisible to the open-bills list while its A/P credit sits in the GL balance, and the control total diverges by exactly the capitalized amount. **§9 already states the fix: the side is the A/R-or-A/P OFFSET code on the entry's leg, never the entry's type.** ▶ NOT FIXED — `apUnpaid` feeds aging, the dashboard and the AI snapshot, so it is its own change with its own evidence.
  - **(4, original finding:) A PAYROLL PAYMENT FLAGGED AS POSSIBLE EQUIPMENT.** *"Large charge: $5,967.00 to Gusto … may need to be capitalized"*. `O85(c)` recorded the payroll exemption and `C302` gates on the ACCOUNT — which does not help an entry that was never booked to one.
  - **★★ (5) FIXED 2026-09-06 (C307) — THE STORED REFERENCE WAS NEVER DURABLE, SO THE ENTRY WAS ALWAYS GOING TO GO "MISSING".**
    - **★★★ DETECTION RUNS ON `invoicesRef.current`, AND RIGHT AFTER A BOOKING SESSION THOSE ARE IN-SESSION OBJECTS WHOSE `id` IS `Date.now() + Math.random()`** (`buildUploadedInvoice`). That float was persisted straight into `entity_refs`, so the pointer was **valid for minutes**: on the next load the ledger is keyed by database uuid, the float matches nothing, and the card reports *"1 of 1 linked entry can no longer be found"* about a `6100 Rent & Occupancy` row sitting in the books at exactly that amount.
    - **★★ THIS IS THE `O87(v)` MECHANISM AT ITS SOURCE, AND IT COST MORE THAN A CONFUSING LINE.** The same dead-refs condition made `anomalyTouchesPeriod` under-count, and **its only consumer is the HIGH-blocks-sign-off gate** — so a month could be attested over an unresolved HIGH. `C198·3c` taught both parsers to fall back to the fingerprint's own dates, which was the right patch for rows already written; **this fixes the refs rather than the reading of them.**
    - **★ AN ID THAT RESOLVES TO NOTHING IS KEPT, NEVER DROPPED.** Dropping shrinks `total`, so a genuinely removed entry would stop being reported — **trading a visible wrong count for an invisible one**, which is the trade this whole file exists to refuse. Pinned, and mutation-killed.
    - **★ AND THE GUARD ASSERTS THE INSERT ACTUALLY CALLS IT.** A pure helper nobody invokes is the `O95` shape; removing the call kills a test rather than passing quietly.
    - *(original finding:)* **THE RENT ANOMALY'S LINKED ENTRY CANNOT BE FOUND, THOUGH IT EXISTS.** *"1 of 1 linked entry can no longer be found"* against a live `6100 Rent & Occupancy` row of exactly that amount. The `O87(v)` class — detection-time `invoice_ids` not resolving after a reload — still live.
  - **★★★ (6) FIXED 2026-09-06 (C308) — THE COMPLETENESS NET HAS NEVER RUN, ON ANY COMPANY, AND THE COPY MADE IT LOOK MOMENTARY.**
    - **`fetchIntakeRows` SELECTED `created_at` FROM A TABLE WHOSE COLUMN IS `received_at`** (`047` declares no `created_at` at all). PostgREST errors on a select naming a column that does not exist, so it returned `ok:false` **every time, for everyone** — and `completenessChecked` has been false since the fetch was written.
    - **★★★ THE COPY WAS HONEST ABOUT THE QUERY AND WRONG ABOUT THE DURATION, WHICH IS WHY IT SURVIVED.** *"We couldn't check your documents JUST NOW"* is exactly right for a network blip and exactly wrong for a permanent error — **a permanent failure wearing transient clothing is one nobody investigates.** `O98` got the hard half right (never claim the books are fine) and the easy half hid the bug for months.
    - **★★★ AND THE SECOND LAYER WOULD HAVE OUTLIVED THE FIRST, SILENTLY.** `staleIntakeRows` ages each row from `r.received_at || r.created_at || now` — **the column it actually reads was not being fetched either.** So a fix that merely made the query legal would have left every row aging from `now`, the staleness window never firing, and the net **reporting all-clear while doing nothing** — strictly worse than the error, because an error is visible.
    - **★★ A FAILED CHECK NOW SAYS WHY.** The caller records ok/not-ok and **discards `error.message`**, so this had nowhere to be seen for as long as it existed. *(Third instance of the same shape this week: `O133`'s constraint violation, `C305`'s unused helper, and now a column name — all invisible because nothing printed the reason.)*
    - **★★ THE GUARD READS THE REAL DDL.** A column name is a contract with the schema and nothing was checking it: the build cannot see inside a string, and a unit test with a hand-made fixture supplies whatever the reader asks for — the `·3a` shape, both sides agreeing while neither matches the database. **Every `select` in the module is now parsed against `047`'s `create table`**, and the DDL parse refuses to pass vacuously. Three mutations, three reds — including the SILENT one, dropping `received_at` while keeping the query legal.
    - *(original finding:)* **THE COMPLETENESS NET DID NOT RUN.** *"We couldn't check your documents just now"*. The copy is honest (it says it could not check, not that everything is fine — `O98` observed), and the check still failed to run.
  - *(Also noted, possibly deliberate: Home says "2 things look unusual", Review lists 4. If that is the owner seat muting MEDIUM per `O86(d)`, it is correct and undocumented.)*

- **★★★ THE SECOND RED RIVER DRIVE — SIX FIXES CONFIRMED LIVE, AND THE DUPLICATE QUESTION ROOT-CAUSED (2026-09-06, C310).**
  - **✅ CONFIRMED ON LIVE DATA:** every description reads true (*"as food"*, *"as drinks"*, *"as linen and laundry"*, *"as kitchen supplies"*, *"as trash pickup"*, *"as taxes or a license"*) · **Bluebonnet → Linen & Laundry** and **Lone Star → Kitchen Supplies**, both off Travel & Entertainment and off 50% deductibility · **Guadalupe BOOKS instead of asking** whether a dumpster is a business vehicle · **no rent flag** · **6910 = Taxes & Licenses** · **one card per duplicate pair naming both amounts** · and **the completeness net ran for the first time ever**: *"No documents waiting — drop a receipt or bill here anytime."*
  - **★★★ AND THE FIRST RUN OF THIS DRIVE TESTED NOTHING, WHICH IS ITS OWN LESSON. The app was serving a build without any of the fixes**, and I nearly scored it: accuracy checks read **0 of 2** and unusual activity **1 of 4**, both of which look like the fixes working. **They were explained entirely by FEWER THINGS BEING BOOKED** — the eight questions went unanswered, so Sabine, Gusto and Hill Country never entered the books to break anything. **A metric that improves because less happened is not a result**, and the only thing that proved the build was stale was running the real function on the real shape: `plainCategoryPhrase({gl_code:"5010"})` returns `"food"` while the screen said *"a meal or travel expense"*. **★ THE CHEAP TELL IS NOW WRITTEN DOWN: check `6910`'s name on a new Restaurant company before spending twenty minutes on a drive.**
  - **★★★ THE DUPLICATE QUESTION — TWO INDEPENDENT CAUSES, AND THE OPERATOR'S HYPOTHESIS WAS ONE OF THEM.** Three of seven cards asked whether an ordinary weekly delivery was a duplicate (Corner Market Aug 8 $65.12 vs Aug 15 $65.34, and the same for Hill Country and Alamo Ice).
    - **(A) THERE ARE THREE IMPLEMENTATIONS OF "IS THIS A DUPLICATE" AND ONLY TWO WERE EVER TAUGHT.** `C220` gave the anomaly detector flat-fee suppression and `C218` gave it to the matcher; **the card raised DURING UPLOAD calls `findDuplicate` raw — vendor, amount, date window, and nothing else.** It would ask every week forever, with any amount of history. *(O125's shape exactly, one subsystem over: three implementations of one contract, taught one at a time.)*
    - **(B) AND THE OPERATOR ASKED THE RIGHT QUESTION — *"maybe this company just doesn't have enough history yet?"* — WHICH IS ALSO TRUE.** `classifyCadence` needs `MIN_OBSERVATIONS = 4`; when Corner Market's second invoice is read, one row exists. **So (A) alone fixes nothing on a cold start**, and a fix that stopped there would have looked complete and changed nothing on the drive that motivated it.
    - **★★★ THE EVIDENCE THAT SETTLES BOTH: THE DETECTOR RAISED ZERO DUPLICATE ANOMALIES FOR THOSE THREE VENDORS.** Running over the whole ledger after the batch landed, with the complete picture, the system reached the right answer **unattended**. **The card asked a person before the data that answers it had finished arriving, and then answered itself ten minutes later.** That is not a threshold to tune; it is a question asked at the wrong time.
    - **THE FIX IS BOTH HALVES.** `duplicateIsExpectedRhythm` (the rule the other two paths already had, now shared rather than copied — **the CALLER supplies the vendor's rows**, because importing a fourth normalizer is how `O125` happened) and `deferDuplicateAsk` (a bulk drop waits for the batch; **a LONE file is not a batch** and is asked immediately, since nothing further is coming). **Suppression rests on `O120`'s recorded discriminator**: a missed double-charge is two identical rows in the ledger — visible, removable, and re-raised by the detector in the same session.
    - **★ FIVE MUTATIONS, FIVE REDS, and the two that matter are the blindness directions**: suppressing regardless of rhythm, and deferring forever once the pattern exists. **An unusual amount from a recurring vendor keeps the question, and a same-amount charge OFF the vendor's cadence still surfaces** — pinned, because going blind is the way this fix fails while looking better.
  - **★ AND A FINDING I RAISED WAS WITHDRAWN ON THE OPERATOR'S CORRECTION.** I reported Review's *"All clear"* as contradicting a *"7 invoices need review"* banner; **the banner is app-wide (it shows on Admin too) and the Review body is CPA-scoped.** Different queues for different people, which is the design. **The real question underneath is whether sign-off should be blocked while documents are unanswered — and the answer is no**: the signed-period path already offers to reopen the month or book into the current one, so it is not a dead end. A warning is a preference, not a defect.

- **★★★ O139 — A JOURNAL LINE MAY POINT AT ANOTHER COMPANY'S ACCOUNT. MIGRATION `091` WRITTEN, ▶ HOLD; PROBE FIRST (2026-09-15).** `C416` closed the client-side route and asked what the database would have said: `post_journal_entry` (`010`) checks membership of `p_company_id` and that the lines balance, **and never that each `account_id` belongs to that company**; `journal_entry_lines`' RLS checks the line's own `company_id`, not the account's. So a member of B can write a line into B's books that points at A's account — from the client during a company switch (now closed), from the QuickBooks importer's direct insert, or from a hand-made call with any uuid (`081`: a uuid is not a secret and not a boundary). The effect is not a leak of A's data; it is a leg in B's ledger that B's chart cannot explain — an unknown code that drops out of every role-derived report. **The `081` shape once more: a covered table whose rule permits something no policy author intended, invisible to a coverage audit.** `091` is a BEFORE INSERT/UPDATE-of-`account_id` trigger on the lines table — **a trigger, not a change to the RPC, because §6 forbids rewriting a live function from the repo**, and a trigger covers the RPC, the importer and any future writer at once; `auth.uid() is null` passes (a migration carries its own review). `supabase/verify/091_line_account_probe.sql` runs (A) BEFORE applying — expected FAIL, the evidence — then (A) and (B) after, (B) being the `079` direction. **Nothing is applied.**
  - **★★ AND IT IS A CLASS, NOT A COLUMN: THE BASELINE CARRIES ~35 CROSS-ROW FOREIGN KEYS ON TENANT TABLES, AND EVERY ONE OF THEM IS CHECKED ONLY FOR EXISTENCE.** `ar_invoices.customer_id → contacts`, `bank_accounts.gl_account_id → accounts`, `vendor_rules.account_id/contact_id`, `recurring_transactions.debit/credit_account_id`, `opening_balances.account_id`, `bank_statement_lines.statement_id/journal_entry_id`, `documents ← journal_entries.document_id`, `journal_entry_lines.journal_entry_id` itself (a line of B under a header of A) — the RLS policy on each table asks `is_company_member(company_id)` of the ROW and nothing of what the row points at. `091` closes the one that changes the ledger's numbers. **The general fix is a decision, recorded rather than taken:** either one trigger per FK through a shared `assert_same_company(table, id, company_id)`, or — the declarative answer — a unique index on `(id, company_id)` per referenced table and **composite foreign keys `(ref_id, company_id) references ref(id, company_id)`**, which makes Postgres enforce tenancy on every reference with no function to keep honest. ~35 constraints either way; the operator's call, with a probe per FK the same shape as `091`'s.

- **★★ O82 — APPROVED THE SAME DAY (Resend · Phase A budget accepted · digest opt-in · Phase C gated); SERVER HALF WRITTEN, NOTHING APPLIED OR DEPLOYED (2026-09-14, C349).** Migrations `088` (`company_channels` — the secret inbound token, admin-only readable, with a member-facing view that omits it), `089` (`inbound_messages` — every outcome of the sender wall is a row, including refused ones), `090` (`outbound_messages` — a row BEFORE the provider; no member INSERT or UPDATE, so "sent" can never be typed in) with one-statement verify files; **`supabase/functions/_shared/mailChannel.js` holds EVERY decision** (sender wall, per-company counters that charge only when allowed, what an attachment becomes, the receipt sentence that says *saved* and never *booked*) **and is imported by the edge functions AND the client** — the one placement where both run the same rule; `receive-mail` (Svix-signed webhook, `--no-verify-jwt` correctly, stores bytes then intake rows with `source='email'`, never books, never calls the AI) and `send-mail` (JWT + membership, row-then-provider, questions/reports only to members, an invoice only about this company's invoice). **Blocked on a DOMAIN** — the app has none (`O31`); `MAIL_DOMAIN` is a secret nobody can set yet. 30 tests; the edge functions are held by source-structure guards, stated as weaker than a run. *(original:)* The operator's two asks — *forward invoices to an inbox* and *send an invoice from the app* — are one integration (one domain, one provider, one message record, two directions). **The load-bearing design call: an emailed document is SAVED on receipt and BOOKED by the existing drain when someone next opens the company** — because the pipeline lives inside `ERP` and a server-side copy of it is the `·3a` two-implementations shape that produced the inert payroll gate, the inverted sales-tax check and the dedupe that never fired. Booking without a tab open (Phase B) waits on `O89` extracting the pipeline; replies that ACT on the books (Phase C) wait on the `O81` battery, because an email body is untrusted data and `From:` is forgeable. Sender wall: token address AND a member/allowed `From:`; an unknown sender is held with zero AI calls, never dropped. Twelve criteria pre-registered with their wrong-reason passes. **Five decisions are the operator's (§10) before a line is written.**

- **★★ THE OPEN LEDGER DID NOT CARRY THE PRODUCT THESIS (2026-09-14).** The operator asked whether *"forward invoices to an inbox, talk to it by text"* was on the roadmap. It was — as `O56`/`O82`/`O80`/`O28` in Section 2's prose and tables — **and it was not on the OPEN LEDGER**, whose own header says *"if work is tracked anywhere in this file, it is on this list."* The ledger listed the outbound half (the nudge channel) and not the thing it is half of. Added, with its sequencing and the open channel decision. **The rule this earns: the ledger is checked against the thesis, not only against §11** — a curated list that omits the product's stated centre of gravity is precisely the drift the ledger was created to stop. Nine Section 2 rows that had shipped weeks ago (`O9`, `O14`, `O111`, `O116`, `O93`, `O32`–`O34` partials) still read *not started*; refreshed.

- **★ THE FOURTH DRIVE'S CRITERIA ARE PRE-REGISTERED — `docs/DRIVE4_ACCEPTANCE.md` (2026-09-12).** Twenty-two commits since the third drive, none driven live. The `O114` re-drive taught that a criterion written BEFORE the data catches the defect a good-looking result hides (`§2` of that doc caught a silent over-match the numbers read as a bonus); this does the same for `C305`–`C345`: twelve binary checks, each naming its wrong-reason pass (the stale build, the residue, the completeness net not running, Sabine answered the easy way) and what is NOT exercisable on this company (`C344` needs three signed months — the honest absence is the pass).

- **★ AND THE CENSUS IN THAT FILE HAD AN UNREACHABLE BRANCH (2026-09-14).** Part (a) selected from the candidate join, so a document with NO matching entry produced no row — its `'NO MATCH'` verdict could never print, and a run over 28 unlinked documents that found 20 linkable would have shown 20 rows and read as complete. `C195(7)` in SQL: the rows a census cannot repair are the ones it most needs to show. It starts from the unlinked set and LEFT JOINs now, so every one of the 28 is a row with a verdict.
- **★★★ `O134` — THE INVOICE PATH MARKS A DOCUMENT BOOKED AND NEVER RECORDS WHICH ENTRY IT BECAME. FOUND ON THE THIRD RED RIVER DRIVE (2026-09-10). ✅ FIXED 2026-09-10 (C311) — SEE THE FIX NOTE BELOW THE DIAGNOSIS.**
  - **★★ THE SCREEN SAID `$28.00` AND THERE IS NO MONEY ANYWHERE IN THIS CHECK.** `docs_recorded` compares **COUNTS** — intake rows marked `recorded` against those carrying a `journal_entry_ids` link — and `toAccuracyFlag` formats every check's figures through `fmtMoney` **unconditionally**, so *28 documents* rendered as *"$28.00"* on the one screen whose whole job is to name what is wrong. **35 documents less the 7 open questions is 28: the entire booked population, not a stray.**
  - **THE DEFECT UNDERNEATH.** `markIntake(item.intake_id, RECORDED, { journalEntryIds: highConfidence.map(i => i.db_entry_id).filter(Boolean), … })` (`App.jsx` ~5285) reads `db_entry_id` off the **LOCAL** invoice objects. `bookToDb` writes that field through `setInvoices(prev => prev.map(…))`, which builds **NEW** objects in state — **the captured `highConfidence` array still holds the originals, which never gain it.** So the map yields `undefined`, `.filter(Boolean)` empties it, and `setIntakeStatus`'s `if (journalEntryIds && journalEntryIds.length)` guard skips the patch entirely. **Recorded, unlinked, every invoice, every time.**
  - **★★ AND THE REAL IDS ARE SIX LINES ABOVE, RESOLVED AND THROWN AWAY:** `const jeIds = await Promise.all(bookPromises)` is computed for the document relink and only `jeIds[0]` is used. **The fix does not need new plumbing; it needs the value already in scope.**
  - **★★★ IT IS `O114`'s SHAPE — A VALUE THAT SATISFIES THE WRITE AND DEFEATS THE READ IT WAS WRITTEN FOR.** The empty array is accepted without error, the row goes **terminal**, and the control total reports the absence forever. **And it is `C307`'s mechanism a second time** — an in-session object read where a durable id was needed.
  - **★★★ THE CONSEQUENCE IS NOT COSMETIC: A FAILED CONTROL TOTAL BLOCKS SIGN-OFF (`evaluateSignOff`).** So on the invoice path — the one a real client actually walks in on — **the accuracy net fails permanently and a month can never be signed clean**, while the books themselves are correct. The `079`/`C291` direction again, a guard firing on good work, and this time unfixable from the UI.
  - **★★ WHY NOTHING CAUGHT IT, AND IT IS THE `·3a` SHAPE FOR THE FOURTH TIME.** `documentIntake.test.js` hands `setIntakeStatus` a hand-made `["je-99"]` and confirms it stores it; `controlTotals.test.js` hands the check hand-made intake rows. **Both halves of the contract tested against fixtures, the boundary between them untested** — so a caller that can only ever produce an empty array passes both suites.
  - **★ THE OTHER THREE `RECORDED` PATHS ARE FINE, WHICH IS WHY THIS SURVIVED:** payroll (~5805) and the statement path (~6066) pass real ids they **hold at the call site**. Only the invoice path reads a field written back asynchronously.
  - **▶ THE FIX IS TWO THINGS:** thread the resolved `jeIds` into the `RECORDED` transition, and **stop rendering a count as currency** — either `check()` carries a unit or `docs_recorded` gets its own sentence. **A failed stamp must say so** rather than leaving the row terminal and unlinked, since terminal-and-unlinked is precisely what the completeness net cannot tell from a lie.
  - **★★★ FIXED 2026-09-10 (C311). NINE MUTATIONS, NINE REDS, EACH CONFIRMED TO HAVE CHANGED THE FILE FIRST.**
    - **THE IDS NOW COME FROM WHAT THE WRITE RESOLVED TO.** `Promise.all(bookPromises)` was already being awaited six lines above and thrown away except for `jeIds[0]`; the RECORDED stamp reads that array now instead of mapping over the captured invoices. **No new plumbing — the value was in scope the whole time.**
    - **★★ AND AN ATTACH IS BACKED BY AN ENTRY TOO, WHICH THE ORIGINAL DIAGNOSIS DID NOT SAY.** `docs_recorded` counts a recorded row with no link as a false success — so the attach-only outcome (`O128`'s *"✓ 0 invoices booked"*, a real live result) would have kept failing the check **after** the thread-through fix, on work that was correct. The payment the invoice was filed against IS the entry standing behind it, so its durable id is linked. **Fixing only the half that was reported would have left the control total red on the invoice path for a second, different reason.**
    - **★★★ AND RECORDED IS NOW GATED ON HOLDING AN ENTRY FOR EVERY TRANSACTION.** Short of that the row goes **HELD carrying the ids that DID land**, with an audit row and a sentence naming the file and the count. **Terminal-and-unlinked is precisely what the completeness net cannot tell from a lie** — the row stops being chased and the check reports the absence forever — so the same convention an unanswered card already uses one line up applies here: incomplete is visible, not done.
    - **★★★ THE FIX NEEDED A SEAM, BECAUSE THE `·3a` SHAPE IS WHAT LET THIS SHIP.** `documentIntake.test.js` hands `setIntakeStatus` a hand-made `["je-99"]`; `controlTotals.test.js` hands the check hand-made rows. **Both halves proved against fixtures, the boundary between them untested**, so a caller that could only ever produce an empty array passed both suites forever. `recordedEntryLinks` (`src/lib/intakeEntryLinks.js`) is pure and importable **so a test can cross it**: id produced the way a booking produces one → real collector → real `setIntakeStatus` → row read back the way `fetchIntakeRows` shapes it → real `computeControlTotals`.
    - **★★★ AND THE FIXTURE HAD TO BE ABLE TO EXPRESS THE BUG, OR THE PASSING TEST PROVES NOTHING.** The stand-in for `bookToDb` replicates `setInvoices(prev => prev.map(...))` — the slot is replaced with a **NEW** object, so the captured array never gains `db_entry_id`. **A fixture that simply stamped the field onto the captured object would pass identically before and after the fix.** One test then restores the shipped expression verbatim and asserts the row goes recorded-and-unlinked and the check FAILS — **while the durable ids sit in state the whole time.**
    - **★ THE SOURCE GUARD IS SCOPED TO THE STAMPS, NOT TO THE FIELD.** Reading `db_entry_id` off the live `invoices` state is legitimate (those objects have it) and `App.jsx` does it correctly elsewhere; a blanket ban would fail for a reason it does not mean. **Every `INTAKE_STATUS.RECORDED` call site must not mention the field** — which is exactly the shipped code, and which fails on it.
    - **★★ THE COUNT-AS-CURRENCY HALF: `check()` NOW CARRIES A `unit`, DEFAULTING TO MONEY**, so the five money checks keep their exact wording and only a check that says otherwise reads differently. `docs_recorded` renders ***"1 of 28 documents has no entry behind it."*** — **the sentence is built for counts rather than the money sentence with the symbol stripped off**: *"off by 1"* is a magnitude, and what a reader needs is which population is short and by how many.
    - **★★ AND A COMMENT IN `documentIntake.faultInjection.test.js` HAD BEEN DESCRIBING THIS DEFECT AS A DESIGN CHOICE.** It read *"our own wiring can leave a recorded row with empty journal_entry_ids when db_entry_id resolves after the mark, so we deliberately do NOT flag recorded-with-no-JE here to avoid false positives."* **That wiring WAS the bug, and the comment turned it into a reason not to look** — the eleventh stale item this month, and the second whose staleness was actively load-bearing. Replaced with what the limit genuinely is.
    - **▶ ONE HONEST LIMIT: the RECORDED-vs-HELD GATE ITSELF IS PINNED BY A SOURCE GUARD, NOT BEHAVIOURALLY.** It lives in `processUploadItem`, which is not importable, and the boundary test necessarily decides the status the way the caller does. Mutation 9 (`if (true)` in place of `if (entryLinks.complete)`) kills the guard and nothing else. **Recorded as the seam's edge rather than left to be discovered.**
  - **DRIVE OBSERVATION, RECORDED WITH THE CAUTION IT NEEDS:** Review read **0 flagged · 0 unusual activity · 0 statement exceptions**, and the completeness net reported all 35 documents accounted for. The `C310` duplicate cards are genuinely gone. **But 7 documents sat unanswered, so some of those zeros are things that never entered the books** — the stale-build trap from the second drive, where a metric improves because less happened. **Answer Sabine before reading the zeros as a result.**

- **★★★ THE RED RIVER LEDGER REVIEW — 13 OF 15 PAIRINGS CORRECT, AND THE QUEUE WAS WRONG IN THE MIRROR IMAGE OF THE BOOKS (2026-09-03, C302/C303/C304).** The operator challenged the screen — *"Rio Grande produce as a meal? nahhh"* — and the challenge was right and productive even though the ACCOUNT was fine. Checked against the ledger rather than the screenshot.
  - **★★★ EVERY LINE ON THAT SCREEN WAS FALSE ABOUT THE WORLD, SO THE READER HAD NOTHING TO STEER BY.** Rio Grande, Hill Country and Sysco were booked CORRECTLY to Food Cost and called meals; Corner Market went to Kitchen Supplies and was called office supplies; Bluebonnet and Lone Star went to Travel & Entertainment and were called client meals. **A linen service is not a client meal under any reading — so all seven were wrong, and NOTHING distinguished the five that were fine underneath from the two that were genuinely broken.**
    - **★★ A CORRECTION TO MY OWN FIRST WRITE-UP, KEPT BECAUSE THE SLIP IS THE LESSON.** I recorded the queue as *"accurate exactly where the books were wrong"* — meaning the sentence MATCHED THE ACCOUNT it booked to. That is INTERNAL CONSISTENCY, and I wrote it as *honest*, which reads as TRUE. **A description can agree perfectly with the record and still be false about the world**, and calling that honesty is how a §9 fix gets marked complete while the sentence a person reads is still wrong. The operator caught it immediately: *"queue honest when it says a client meal for Bluebonnet? That isn't accurate at all is it?"*
    - **★★★ AND THE CORRECTED READING IS THE STRONGER FINDING: NOT "MISLEADING IN BOTH DIRECTIONS" BUT "UNIFORMLY WRONG, THEREFORE UNUSABLE AS A SIGNAL".** The operator's reaction — *"I think there are a bunch of wrong ones in here"* — was the CORRECT response to that screen, and it was right about the count and unable to be right about which. Separating the five from the two took a SQL query. **That is what a divergent description costs: not confusion about one line, but the loss of the only audit a client can perform.**
  - **★★ THAT IS THE COST OF THE §9 CLASS STATED PRECISELY, AND IT IS NOT "THE COPY IS SLOPPY": A DESCRIPTION THAT DIVERGES FROM THE RECORD DESTROYS THE READER'S ABILITY TO AUDIT BY READING.** Every wrong sentence spends attention on correct work, and every accidentally-right one buys cover for a real defect. The only way to tell the two apart was a SQL client.
  - **THE TRAP WAS BEATEN: Alamo Ice & Beverage → `5020 Beverage Cost`, 3 for 3.** CO2 tank exchanges and fountain syrup read like equipment and are drinks inventory; that specimen was planted from the `O88` Alamo Fire finding and did not reproduce.
  - **★★ AND THE ONE REMAINING MISS WAS A CHART GAP, NOT A CATEGORISER ERROR — Tier 1 #6's argument on a live document.** A Texas **mixed beverage gross receipts tax** is a tax on the restaurant's OWN sales and a real operating expense. The chart holds Payroll Tax, Income Tax and (real-estate template only) Property Taxes, **and nothing for anything else** — so it landed in Licenses & Permits, the nearest thing that existed. **The categorizer picked second because there was no first.** `6910` is now **"Taxes & Licenses"**, matching **IRS Schedule C line 23**, so a gross receipts tax, a franchise tax and a liquor license sit where a preparer looks for all three. Dedupe is by CODE, so existing companies keep their own name and gain no duplicate — new companies only, and a rename is theirs.

- **★★★ `O133` — THE DURABLE-FIRST INTAKE HAS NEVER ONCE STORED A FILE (found live, mid-drive, 2026-09-01; fixed C300).**
  - **`documents.document_type` IS `NOT NULL DEFAULT 'other'` UNDER A CHECK ALLOWING SEVEN VALUES, AND O97 STEP 1 PASSED THE LITERAL `"pending"`.** Every durable-first insert was rejected, the storage upload rolled back, nothing was written, and **`document_intake.document_id` — the exact field `planDrain` requires to consider a row drainable — stayed null.**
  - **★★★ THE DRAIN'S INPUT WAS EMPTY FOR A DIFFERENT REASON THAN THE ONE THIS FILE RECORDS.** §11 says the drain is *"forward-looking only"* and waits for documents to arrive under the new ordering. **They never arrived, because the write that creates them always failed** — so a note explaining an empty queue was describing the wrong cause, and looked like the situation was understood.
  - **★★ FOUR DEFECTS IN ONE FEATURE, AND THE SECOND IS THE ONE WITH A LESSON:** the placeholder violated the constraint · **the caller claimed durability from a truthy return** — `storeDocument` hands back an in-session float on failure, so `if (durableDocId)` was true either way and the intake row was stamped *"file stored — safe to close the tab"* · the repairing stamp wrote the classifier's RAW vocabulary (`"qbo"` is not an allowed value — **the same defect on the line meant to fix it**) · the dedup early-return could never correct a placeholder row.
  - **★★★ THE GUARD FOR (2) ALREADY EXISTED, WAS COMMENTED, AND WAS NOT ADOPTED.** Both bank-statement callers test the return with an inline uuid regex and say why — *"only a real uuid; the in-session fallback id isn't a documents FK"*. **The NEWEST caller, whose entire purpose is durability, is the one that skipped it.** One shared `isDurableDocId` now: three copies of a predicate is precisely the shape that lets the third site omit it.
  - **★★★ AND THE TEST THAT PINNED THIS PASSED THE WHOLE TIME. It asserted the writer emits the literal `"pending"` and never asked whether the COLUMN would accept it** — the `·3a` extractor-and-fixture shape, in a test that reads like coverage. **The doctrine it violated was already written one file over**, in `docLibrary.js`: *"a document we cannot label is still a document we must keep, and losing the file to protect a taxonomy would be the wrong trade."* That is exactly the trade that was happening on every upload.
  - **★ A SURVIVING MUTATION CORRECTED THE CODE'S OWN COMMENT.** The line described as the downgrade guard **cannot affect correctness** — the test below it already refuses that case. What it actually prevents is a no-op write on every unclassifiable document, and it is pinned for that instead. *(C255's lesson again: a surviving mutation is a question, and sometimes the answer is that the line could never have mattered.)*

- **★★ `O132` — THE OPENING BALANCE IS ASKED FOR ONE STEP BEFORE THE DOCUMENT THAT ANSWERS IT (2026-09-01). FOUND BY WALKING A NEW COMPANY END TO END, NOT BY READING CODE.**
  - **EVERY PART WORKS; ONLY THE SEQUENCE IS WRONG.** `deriveStatementOpening` recovers the opening from the first row's running balance less its amount, **prefers the PRINTED figure when the statement states one and flags disagreement rather than silently choosing**, and `shouldProposeOpening` raises a confirm card in Bank Import when no opening exists for that account. **The checklist runs opening balances at step 3 and the first upload at step 4**, so the statement that would derive the number arrives after the person has been asked to invent it.
  - **★★ AND TYPING FIRST IS NOT MERELY REDUNDANT, IT COSTS SOMETHING — which is what lifts this above an ordering nit.** `resolveAdoptedBalance` returns `mismatch` when a typed balance disagrees with the statement's ending and **deliberately leaves the typed value alone**. That refusal is CORRECT — silently adjusting someone's stated cash position is exactly the auto-adjust this product refuses everywhere else — **but it means the number they were asked to guess becomes a reconciliation question instead of a starting point.**
  - **★ THE STEP'S OWN HINT ALREADY SAYS *"We'll pull your starting cash from a bank statement"*.** So the copy knows the better path exists and the ordering points away from it — **the same shape as a guard whose input is always empty: the feature is present, correct, and structurally hard to reach.**
  - **▶ NOT FIXED. The repair is ORDER, not mechanism** — let step 3 be satisfied BY step 4, or say plainly that uploading a statement first fills it in. Recorded rather than fixed because changing onboarding's sequence is a product decision, and the machinery it would route to is already built and tested.

- **★★ O131 — A SOLO SIGNUP'S BOOKS CAN NEVER BE SIGNED OFF, AND THE SCREEN PROMISED OTHERWISE FOREVER (2026-08-30, C271). THE PRODUCT DECISION IS STILL THE OPERATOR'S; THE DISHONEST SENTENCE IS NOT.** An owner deliberately cannot attest their own books — the separation is the point when an accountant is involved, and the database enforces it. **But one person holds one role**, so a solo company has nobody who ever can, and the trust panel said *"Awaiting your accountant's sign-off"* about a person who does not exist.
  - **★★ THE FIX IS THE PART THAT IS TRUE UNDER EVERY OPTION ON THE TABLE, WHICH IS WHY IT COULD SHIP BEFORE THE DECISION.** Require an accountant, or let a solo owner attest with an acknowledgement — **under both, promising a review that cannot arrive is wrong.** The line now states the position and the ONE thing that changes it, and **deliberately does not imply the books are wrong: nobody has reviewed them is a different fact**, and conflating the two would frighten someone whose books are fine.
  - **★★ THE FAILURE DIRECTION IS THE WHOLE REASON THIS IS NOT ONE LINE. `hasAttester` DEFAULTS TRUE, AND A FAILED MEMBERSHIP QUERY KEEPS THE OLD SENTENCE.** An errored query returns no rows, which is **indistinguishable from a company with no accountant** — getting it backwards would tell an accountant-led client that nobody is reviewing their books. `O98`'s rule, applied to a membership read: an empty result may only be reported as an empty result when the query is known to have RUN. A test pins the caller's error check, not just the predicate.
  - **★ AN UNACCEPTED INVITE IS NOT A PERSON.** Counting one would put the same false promise back with an extra step — the screen would wait on an accountant who never clicked the link. Pinned.
  - **★ AND THE NEGATIVE CASE IS PINNED BESIDE IT: an accountant-led company still reads *"Awaiting your accountant's sign-off"*.** Without that, "the solo sentence changed" is equally satisfied by deleting the sentence for everyone. **What IS signed stays a fact even when no attester remains** — a company whose accountant has left has still genuinely had those months reviewed.
  - **★ A MUTATION'S `sed` FAILED SILENTLY AND ITS "PASS" WAS MEANINGLESS.** The grep count of 0 caught it; re-run properly, it killed 3 tests. **Confirm the file actually changed before believing a mutation survived** — a mutation that never applied looks exactly like a mutation the tests withstood.

- **★★ THE TWO RAILS' NAME RULES — INSTRUMENTED, NOT CHANGED (2026-08-30, C270).** The bank rail accepts a party when either normalised name CONTAINS the other; the invoice rail demands exact equality. The roadmap's instruction was explicit and right: *tighten toward exact, never loosen — and not as a rider on a bug fix, because that path has been live and correct for eight drives and changing it needs its own evidence.*
  - **★★ SO NOTHING CHANGED. `nameMatchKind` IS THE SAME PREDICATE, SPLIT SO THE KIND IS REPORTABLE** — a test asserts `kind !== NONE` agrees with the old inline expression on **every ordered pair of a 15-name corpus**, so the refactor cannot have quietly become a behaviour change. Every deterministic match now carries `name_match`, and a drive prints the census.
  - **★★★ AND THE RISK IS NARROWER THAN THE RULE READS, WHICH IS ITSELF THE FIRST PIECE OF EVIDENCE: THE NAME TEST ONLY EVER SEES CANDIDATES THAT ALREADY MATCHED THE AMOUNT TO THE CENT, ON THE SAME SIDE.** A wrong merge therefore needs three coincidences at once — two vendors whose names contain one another, an open item of the wrong one at the identical amount, and the same A/R-or-A/P side. **That is not "the loose rule is fine"; it is a precise statement of WHERE IT BITES, and it points at one population: flat-fee recurring vendors, whose amounts are constant and identical by construction (`O117`/`O127`).** If this rule has ever merged anything wrongly, that is where.
  - **★ THE CENSUS ANSWERS THE QUESTION NOBODY COULD ANSWER: IS THE LOOSE RULE DOING ANY WORK AT ALL?** A drive returning `substring: 0` makes tightening free and the decision trivial; a non-zero count turns each case into a thing to look at by hand. **Neither could be said before, which is why the decision had sat unscheduled.**
  - **`substringOnlyPairs` is the standing blast-radius census** — every pair of names the loose rule would merge and the strict one would not — and its test asserts it names the exact pairs `vendorIdentity.test.js` already forbids merging (SYSCO / SYSCO FUEL, Lone Star / Lone Star Restaurant Supply) **and that it manufactures no risk where none exists.** The normaliser is INJECTED rather than imported, so the answer is about the rule actually in use rather than a copy of it that has since drifted.

- **★★★ O97 CLOSED 2026-08-30 (C269) — THE QUEUE WAS CALLING SAFE WORK BROKEN, AND THE FLAG THAT SAID OTHERWISE WAS ALREADY ON THE ROW.** Steps 1 and 2 had landed: the bytes are stored BEFORE the first AI call, the upload catch reads `classifyAIFailure`, marks the intake `failed` so the drain retries it, stamps `transient: true`, and writes the honest sentence — *"We've hit our own hourly limit… Everything you sent is saved and will carry on automatically — there's nothing to re-send."*
  - **★★ AND THE TILE RENDERED THAT SENTENCE IN RED, UNDER A ⚠, INSIDE A RED BORDER, BESIDE A CHIP READING "Error", WITH "Clear ×" OFFERED OVER IT. FIVE SIGNALS SAYING BROKEN WRAPPED AROUND ONE SENTENCE SAYING FINE.** Nobody reads the sentence; they read the colour. **That is why correct copy did not save the August drive** — the operator's *"200 files in, 20 succeed, 180 turn red, and the owner concludes the product is broken"* was literally true with the honest text already on screen.
  - **★★ IT IS THE §9 DESCRIBE-FROM-THE-RECORD RULE ONE LAYER OUT: THE SENTENCE WAS RIGHT AND THE STYLING WAS THE PARALLEL DESCRIPTION THAT DIVERGED.** Every prior instance had a STRING computed from the intent instead of the outcome. Here five independent style expressions each re-decided what `status === "error"` meant, and **`item.transient` — the one field that distinguishes *waiting* from *broken* — was read by none of them.** Five readings is how they came to disagree; `queueItemTone` is now the single one.
  - **★ RED IS RESERVED FOR WHAT A PERSON CAN ACT ON.** A transient failure is our own budget and the drain clears it with no human involved, so it gets ⏳, muted text and *"Waiting its turn"*. **The chip deliberately does not say "Retrying"** — we are not retrying now, and inviting someone to watch for a change that arrives in forty minutes is its own small lie.
  - **★ THE NEGATIVE CASE IS PINNED BESIDE THE POSITIVE, because "waiting is not red" is equally satisfied by switching red off.** An unreadable file still reads ERROR, still shows ⚠, still says *"Needs a look"*; and **a failure carrying NO `transient` flag is treated as BROKEN, not as waiting** — the conservative direction, since a mislabelled waiting row sits there looking fine while nothing ever collects it. Four mutations, four reds, each confirmed to have changed the file first.
  - **★ AND AN EXISTING TEST WENT RED FOR THE RIGHT REASON: C198·2b PINNED THE CHIP'S LITERAL EXPRESSION IN THE VIEW, AND THE LOGIC MOVED.** Re-pointed at `queueItemChip` rather than deleted — the guarantee it protects (a pipeline throw reads *"Needs a look"*, never *"Done"*) is unchanged, and it now additionally asserts that a pipeline throw is ERROR and not WAITING. **A test that fails because its subject moved is a test doing its job; the failure mode to avoid is quietly dropping the assertion with it.**

- **★★ THE `078` CALLER SWEEP — COMPLETE 2026-08-30. FOUR FINDS, AND THE RULE PAID FOR ITSELF FOUR TIMES.** The rule (§6): *when adding a refusal, grep every caller of the thing it now refuses.* A new refusal does not merely add a behaviour — it **retroactively changes every path that used to be allowed**, and each of those paths was written against the old permission.
  - **C235 — the QuickBooks undo** announced success without checking, and counted rows stored at import time rather than rows actually removed. `078` turned that from harmless into dangerous: an import covering a signed month can no longer be undone, so it would have reported *"47 entries removed ✓"*, marked the import undone, and left all 47 in the books with no way to retry.
  - **C236 — `079`, the guard blocking a change that moves no money.** `078` gave ENTRIES column-level discrimination and LINES none, so retagging a project on a signed month's entry was refused. **This is the failure `078`'s own check (5) existed to catch, and it varied the PERIOD where it needed to vary the COLUMN.** For a guard, *"it blocks too much"* is the dangerous direction — it breaks ordinary work and only surfaces when someone tries to do their job.
  - **★ C237 — A BACKGROUND JOB THAT COULD RAISE AN INTERACTIVE DIALOG.** `persistMultiLineEntry` holds a signed-period entry by popping a confirmation — right for a person who just dropped a document into a closed month. **`autoPostDepreciation` is the only caller with nobody watching**, and it runs on every company load: a depreciation row in a signed month would have asked for a decision about something the user never did, got no answer, left the row `pending`, and repeated forever. `background: true` audits and returns; the caller counts the row as incomplete. **The flag is opt-in and a test pins that exactly ONE call site passes it** — defaulting it would have been the same bug pointed the other way, and quieter.
  - **★★ C238 — THE UNDO BUTTONS, AND THEY WERE HIDING INSIDE THE FUNCTION WE HAD JUST FIXED.** `softDeleteInvoices` was hardened yesterday (`O124(c)`) to check whether the delete landed. **The Undo handler ten lines below it wrote, ignored the result, and said "Restored ✓".** Contracts the same, in a loop nobody read. **A FAILED UNDO IS INVISIBLE BY CONSTRUCTION** — local state puts the rows back on screen either way, so success and failure render identically until the next reload takes them away, which is the worst order to learn it in. Both now write FIRST and paint after: **the screen follows the database, not the click.**
  - **★ AND A MUTATION ESCAPED THE FIRST VERSION OF ONE TEST, WHICH IS THE METHOD POINT.** Asserting `checkedIdsUpdate(` APPEARS survived leaving the call behind `if (false)`. **A checked write whose answer nobody reads is exactly the unchecked write it replaced, and a source scan cannot see reachability** — so the assertion has to pin the CONSUMPTION (`const r = await …` and `if (!r.ok)`), not the call. Sibling of the `O123` guard that was correct only by virtue of something upstream.
  - **★ A TEST THAT FAILED AGAINST CORRECT CODE, KEPT AS THE LOUD VERSION OF A QUIET FAILURE.** C237's first draft sliced `App.jsx` from `persistMultiLineEntry` to `const ensureAccount` — which appears EARLIER as a nested helper, so `indexOf` returned a position before the start and the slice was empty. Three tests failed against correct code. **The quiet version of that mistake is a slice that happens to be non-empty and contains none of what is asserted** — which passes forever. An end anchor must be searched from the start offset.

